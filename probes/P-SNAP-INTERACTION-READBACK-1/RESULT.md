# P-SNAP-INTERACTION-READBACK-1 result

**PROOF-SURVIVES / NON-CANONICAL L1 MATHEMATICS.**
The first formal audit completed with 181706 exact checks, no mismatches,
exit 0 and empty stderr. Required two-architecture reproduction is separate
reviewed-head evidence. No canonical or physical status is promoted.

## Exact negative result: operation erasure loses zero-batch identity

The existing terminal account accepts a zero DEPOSIT and retains its batch.
A passive READ appends nothing. Under the frozen projection to zero-port
amplitudes before/after and their energies, the two transitions are equal;
their batch increments are one and zero. That increment therefore cannot
be recovered from this projection.

The projection explicitly omits operation, presence, ordinal, slot address
and archive shape/length. The complete existing protocol distinguishes the
states correctly. Its consistency is not refuted. Both transitions emit
zero threshold marks, and the result does not impose a physical event at
zero input. A zero=NO_EVENT convention changes the proposed batch contract.

## Complete loader classification and the fresh-space criterion

For every finite-dimensional rational positive Gram G, a two-port orthogonal
map perfectly loading all inputs has exactly the form

```
W = [[0,B],[I,0]],   B^T G B=G.
```

Consequently its next application to (0,x) gives (Bx,0), exporting the old
record. Reusing this same complete two-port coupling is not passive reading.
A finite invariant record sector similarly cannot receive incoming
orthogonal amplitude from its complement. The proof explicitly distinguishes
these premises from mere invertibility and infinite-dimensional invariance.

With a pointwise protected old bank O, perfect loading from S into fresh F
exists exactly when there is a rational isometric embedding J:S->F. The
displayed Gram-adjoint construction is an orthogonal involution attaining
that condition. It requires dim F>=dim S, but dimension alone is insufficient:
forms <1> and <2> have no rational isometry. A Gram-isometric copy attains the
dimension bound; the proof also exhibits <1> embedded into <2,2>.

This is a conditional linear amplitude-storage result. It is not a physical
bit bound, a cost for every repeated known payload, or a no-go for larger
controllers, nonlinear storage, compressed records or additional ports.

## Positive mechanism and its exact failure after reuse

One fixed update swaps the current input and receiver cells and advances a
cyclic head. Its emitter detects BLANK-to-occupied receiver transitions.
It needs no READ/DEPOSIT opcode in that update. The complete cell type
explicitly distinguishes BLANK from PRESENT(0).

For N prepared input positions and an initially blank receiver bank, the
first N steps retain every earlier receiver value. Two equal arrivals,
including zero-valued arrivals, create records at distinct positions.
Stipulated passive state queries create no new transition emission.
This is a mathematical observation contract, not a physical nondisturbance
certificate.

The same flow empties those records during its second lap. Its 2N-th power
is identity on the complete bank state, so the third lap reissues the same
addresses. The bank is a recorder for its declared first pass, not an
indefinitely persistent archive. A separately retained emitted log would
be another state resource. Choosing larger banks for longer finite prefixes
does not give one fixed finite bank with unlimited retention.

Erasing the occupancy distinction makes a zero arrival and an idle slot
indistinguishable again, even when their cyclic head is retained. Energy
alone is also insufficient: the warm SWAP (1,-1)->(-1,1) changes signed
fields at zero net receiver-energy gain. Arrival records, transferred energy
and threshold marks cannot be silently identified.

## Disposition and remaining physical obligation

All preregistered mathematical targets survived; there is no unexpected
fired mismatch to hide or repair. The excluded inference is that the frozen
port values and weak retention requirements already supply the intended
event distinction. The positive construction supplies that distinction
only after admitting its occupancy carrier, input preparation, fresh
addresses, storage capacity and passive-query convention.

The next physical obligation is now specific: independently realize the
arrival/presence distinction and access to a fresh receiver while preserving
old records, and identify what a passive query physically does. These remain
under the existing #539 apparatus owner at **STOP-DEFINITION**. The result
does not derive that mechanism from native U, establish a universal Snap,
falsify Born marginals, or introduce a probability or dimension reading.

See [the occurrence note](../../notes/C-SNAP-OCCURRENCE-IDENTITY-N.md#6-concrete-attack-arrivals-readback-and-finite-storage).
Canon, Registry, Frontier, gates, authority, release, workflows and previous
probes are unchanged. Any canonical promotion needs a later declared fold.
