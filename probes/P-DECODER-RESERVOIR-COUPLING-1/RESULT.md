# Reservoir coupling and record accounting: result

Status: **CONFIRMED**, conditional mathematical claim
`DECODER-RESERVOIR-RECORD-ACCOUNTING`.

**PUBLIC CLAIMS UNREGISTERED / CANON UNCHANGED / PHYSICAL COMPLETION UNRESOLVED.**

## Exact result

All seven frozen audit gates passed. The complete candidate was pinned at
`550420d188a45c4929e300ca6aabcde812f4d65a` and read back publicly before
execution. The seven new source files and one inherited wave dependency
were unchanged before and after the sole initial clean Linux run. Exit was
zero, stderr empty, and EXPECTED.txt contains the exact 437-byte, 14-line
LF output. RUN.md records all hashes, byte counts and commit pins.

PROOF.md establishes the following conjunction uniformly for the chosen
finite-support rational wave and finite rational conductance contexts:

- The complete wave/incoming-port to wave/outgoing-port coupling is a
  rational bijection, including conductance 2, and conserves wave plus port
  energy with the exact local balance.
- Fresh zero incoming ports give nonnegative deposits. The wave loses
  precisely the energy stored in its signed outgoing reservoir tape.
  Accumulated heat is the weighted square summary of that same tape.
- Exact per-channel floor counts and remainders partition this heat. Every
  newly crossed lifetime ordinal is emitted once in a complete atomic batch;
  no energy is lost at a threshold or counted twice.
- Generated histories have compatible immutable prefixes, and reversing
  their signed tape reconstructs the initial wave and zero incoming slots.
  With zero initial heat, total threshold ordinals are bounded by
  `floor(E_initial/q)`. With the inherited source preparation completed
  before coupling, `2(E_wave+heat_total)=m_QDD` at every finite step.

The finite audit uses independent pointwise forward/inverse formulas on 33
cases in both directions, warm and cold energy checks, exact local balances,
threshold endpoints and multiple crossings, zero and blind controls, prefix
continuation, tape reversal, type rejection and locality checks. Its finite
coverage audits the uniform proof rather than replacing it.

## Fired falsifiers and informative boundaries

No required falsifier fired. Preregistered controls make the limits precise:

- A reservoir at the origin never absorbs an inversion-odd wave, even though
  the initial passive local energy there is positive. The perpetual result
  follows from the invariant odd subspace; three steps audit it exactly.
- At conductance 2, pair `(2 delta_origin,0)` is completely absorbed in one
  step, depositing energy 2 and emitting both threshold ordinals at q=1.
  The signed outgoing port still permits exact reversal.
- Positive subthreshold deposits can give NO_CROSSINGS; a later empty batch
  does not reissue old ordinals. A fixed threshold changes the record while
  leaving the wave and energy transfers unchanged.
- Flooring pooled heat is different from summing the fixed per-channel
  counters. The latter are the chosen ledger and obey region overlap rules.

Thus the model supplies neither universal complete absorption nor a claim
that complete absorption is impossible. No reflection-free or finite-time
detection guarantee is established.

## Choices, ownership and physical scope

The port coupling, conductance profile, threshold, channel partition, fresh
zero input slots, persistent signed tape and atomic threshold convention are
disclosed choices. The inherited source's QDD norm match was exposed during
its design. Unbounded fresh reservoir capacity is an explicit idealization,
with only finite use at each finite horizon. The energy in the tape, its heat
summary, and the threshold/remainder partition are alternative accounts of
the same store. No erasure or reset is introduced.

This is a mathematical interaction and post-state rule, beyond the previous
passive reader. It is not a physical source/detector certificate or the L4/L5
apparatus profile owned by #539. Physical coupling, environment, capacity,
calibration, pointer/reduction, occurrence, quantum post-state instruments,
apparatus-family completeness and equality remain unresolved. Threshold
ordinals are not photons, QDD LOW/HIGH outcomes, independent trials, Born
frequencies or an exactly-one-click law. COMM-SAT, Bell accounting and L6
measure do not follow. No record is an input to autonomous U.

The QDD apparatus obligations and #539 remain open; #744's photon
identification chain is unchanged. F3 in #756 remains NOT_SATISFIED and
production #742 FORBIDDEN. COINCIDENCE-RECORD-FREQUENCY is UNTESTED / STOP.
No old probe or Canon file was changed, and no public status is promoted.

## Evidence status

Local formal audit: **PASS, 7/7**. Independent GitHub **aarch64 and x86_64
replay PASS**, with exact verifier and stdout hash agreement in workflow
[33932023196](https://github.com/mathorn1973/twist-j/actions/runs/33932023196).
RUN.md records both jobs and their result commit. Independent post-result
proof/code/scope/integrity and security/license review passed. All 155 tool
tests and policy, Canon, ledger and gate-contract checks passed. The seven
new source files and the inherited transport dependency are unchanged.
The proof supplies the conditional theorem argument; registration requires
a separate earned Canon fold. Public Canon v76 remains unchanged.
