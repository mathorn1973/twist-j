# Native protected record and complete fixed-reader boundary

Status: PROSPECTIVE / PROOF-FIRST / RESULT-EXPOSED / L1 ONLY.
Formal claim lock: issue #862. Owner: A. M. Thorn.
This proof precedes the new formal audit. The same mathematical results
were exposed in incubation #861, as disclosed by PREREG.md. They are not
blind predictions and create no public Canon status by being written here.

## Frozen sources and inherited lemmas

All source rows are at public main
`5de2e71f4000e02599919bf3dcb18c7671b67445`:

| Source | Bytes | SHA-256 |
| --- | ---: | --- |
| `canon/CANON.md` | 473631 | `82b29c75eb007c71d73dc33e63270d0e0895fdf41886499fcf79b928826855b5` |
| `probes/P-QDD-U-NATIVE-READBACK-1/PROOF.md` | 12710 | `2df18b2118d0a2e483c8089976eda8485127951efa9e2f3364d38ac2aab0d76c` |
| `probes/P-QDD-U-NATIVE-READBACK-1/verify.py` | 21468 | `3e8281942278b420c4406a7f848e8e8c568bf6beec9b9c694948ec760623b5f7` |

The public native-readback proof supplies the origin-zero synchronization
at n=3, the exact five-label chart and its universal native-step identity,
the QDD LOW formula, and the first-tick merger of distinct original records.
These are inherited mathematical lemmas, not new synchronization or head
recovery claims. Their source statuses are not promoted here. PREREG.md
restates all five generators and the chart used by the accepted code.
The verifier is self-contained and imports no source file at execution time.

## Complete proposition and proof spine

1. [MEMORY-PROOF.md](MEMORY-PROOF.md), sections 1-2, proves the bijective
   present-state coordinates `(z,V,r)` and the exact two native maps
   `(z,V,r)->(5-z,-V,-r)` and `(z,V,r)->(z,-V,1-r)`. Their composition
   visits all ten `(z,r)` pairs with fixed V. Consequently `R={V,-V}`
   is the maximal invariant checkpoint quotient: exactly 313 fibres,
   with 312 of size 20 and one of size 10. Every invariant fixed reader
   is exactly a function of R, including nonlinear readers.

2. [CLOCK-PROOF.md](CLOCK-PROOF.md), sections 1-4, derives the 100-state
   clock recursion from literal Thue-Morse and S. Four explicit digit
   words connect the full graph and establish M^77>0. An exact positive
   stationary vector has entries 1/150 or 1/75 according to bit equality.
   Integer minorization gives uniform contraction for every dyadic block;
   a remainder bound proves convergence for every prefix length. Thus
   these are actual fixed-origin densities, not only stationary weights.

3. CLOCK-PROOF section 5 transports those densities through all five-label
   charts, accounts for odd/even atom collisions, and proves the complete
   checkpoint and decorated weight multisets for zero and nonzero first
   four labels. Every checkpoint in the corresponding R fibre has positive
   density along each actual chart trajectory. The fifth label changes
   neither the atom weights nor the support. The two oriented nonzero
   vectors share a checkpoint law but can have different decorated laws.

4. Positive recurrence implies that a fixed checkpoint reader eventually
   equal to one value is equal to that value on the entire fibre. Hence
   it was constant at every synchronized checkpoint already. This proves
   the permanent blank-to-written obstruction after synchronization,
   independently of how late a proposed write is scheduled. MEMORY-PROOF
   section 4 and CLOCK-PROOF section 6 state the exact scope.

5. [FREQUENCY-PROOF.md](FREQUENCY-PROOF.md) identifies all fixed readers
   with whole-atom partitions. An exact finite recurrence exhausts all
   one-bin and two-bin masses; an independent integer allocation argument
   proves the complete triangular spectra. Accepted LOW fractions are
   exactly F_60 or F_30 on nonzero or zero protected vectors, and F_30
   or F_15 after restriction to either driver bit. F_D means all reduced
   rational numbers in [0,1] with denominator at most D. Empty acceptance
   is UNDEFINED. These are per-orbit spectra, not a claim of simultaneous
   arbitrary target assignment on overlapping orbits by one global reader.

6. MEMORY-PROOF sections 5-6 give an explicit target-independent static
   codebook for all 313 messages and exact QDD nonfactorization witnesses.
   Initialization of that codebook is not a native write or an independent
   system/apparatus preparation. The same-R current weights 1/16 and 1/136
   differ, and the original-head weights 0 and 9/14 become indistinguishable
   after a native merger. Storage does not repair erased original records.

7. Only after the complete native frequency classification is fixed is
   comparison made with supported QDD targets. The reduced denominator
   of 1/256 exceeds 60, so it is outside every admitted accepted-event
   spectrum. The other seven exclusions are retained by exact enumeration
   of all supported targets. Thus the entire supported QDD weight law is
   impossible in this complete restricted reader class. This is not a
   statement that the complete physical apparatus or sampling class is empty.

The mathematical status proposed by this proof is T at these exact L1 scopes,
subject to formal review; no public Canon promotion occurs in this probe.
The positive storage statement and negative writing/frequency boundaries
form one proposition and must not be separated into a misleading claim of
measurement closure.

## Audit, review and limits

The accepted verifier exhausts finite native states, controls, graph components,
clock states, all chart labels, supported piston tuples, and all whole-atom
allocations. It checks the preregistered ordinary and enormous clock samples
as audits of the universal algebra, not as their logical proof. Every group
must complete, and any discrepancy is retained for mathematical versus
implementation diagnosis according to PREREG.md. The formal code is frozen
before the formal run, despite the fully disclosed prior incubation results.

The proofs and class boundaries have a separate same-session static review.
The verifier has no external data, private imports, filesystem or network
effects, floating point or random sampler. No physical effect, instrument,
ready state, context, coupling, event, occurrence law, reset or L1-to-L5/L6
bridge is supplied. The physical decoder and QDD apparatus obligations remain
open regardless of a passing audit of this exact mathematical boundary.
