# P-A4-RAMIFIED-HODGE-TRACEKERNEL-1 result

Status: PASS at the frozen L1 mathematical scope.
No scientific falsifier fired. TRACEKERNEL-CURVATURE-FORCING [O] remains open.

The immutable public pin is
`f259d00e471ef22fa3417dd7967601cd1b24aa52`. Public readback before the first
formal execution matched PREREG.md and verify.py byte for byte. The pinned
verifier exited zero with empty stderr and produced the single EXPECTED.txt.

The exact integral and ramified seam is:

```text
K = sqrt(5) star is integral on Lambda^2 A4,
K^2 = 5 I_6,
Kbar^2 = 0,
rank Kbar = 3,
im Kbar = ker Kbar = ell wedge Vbar.
```

Thus the two real Hodge triples are the +/-sqrt(5) eigenspaces of one integral
operator, while at the ramified prime five their reductions form one
three-dimensional image/kernel self-extension rather than two separate
semisimple triples.

The root Gram reduction has kernel <ell> and image exactly the public
trace-kernel carrier W_5. Its induced quotient map

```text
Vbar/<ell> -> (W_5,g_5)
```

is an exact residual isometry, not a dimension-only identification. The
alternating quotient home map descended from Kbar transports, in the frozen
marked orientation, to exactly minus the public metric-volume bracket of
TRACEKERNEL-F5-HODGE-BRACKET. The comparison scalar is the preregistered -1;
no orientation was changed after execution. The two marked generators also
pass exact A5 equivariance and residual-metric preservation.

Prospective Canon scope: one L1 theorem identifying the public finite W_5
Hodge/bracket carrier as the ramified shadow of the integral A4 Hodge system.
This probe itself creates no Canon status. The required clean GitHub x86_64
and aarch64 jobs must replay the same unchanged verifier against the one
EXPECTED.txt, and the aggregate check must pass the reviewed PR head before
computation-grade T acceptance.

This result does not close TRACEKERNEL-CURVATURE-FORCING [O]. That obligation
still quantifies over the complete admissible L2 spatial-commutator/operator
class and requires its own L2-to-L1 gate. No physical space, time, curvature
selection, decoder, probability, measure, SI quantity or L2-L6 lift is made.
