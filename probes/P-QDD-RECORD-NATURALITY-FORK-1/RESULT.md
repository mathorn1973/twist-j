# P-QDD-RECORD-NATURALITY-FORK-1 result

Status: `PROVED AND AUDITED IN THE FROZEN CLASS / PUBLIC REPLAY PENDING / CANON UNCHANGED`

## Disposition

```text
strict law naturality:
  The moving branch is a natural endomorphism of the one-object record
  presentation groupoid. Its S_4 centralizer has dimension one, so the effect
  equation leaves T=+Q_k or T=-Q_k, one physical class under the registered
  sign equality.

weak quotient covariance:
  The complete rational G-orthogonal normalizer is
  {+rho(h)Q_k,-rho(h)Q_k : h in S_4}. Aut(S_4)=Inn(S_4), so the family is
  complete. It has 48 algebraic members and 24 registered sign classes.

exact breaker:
  For a HIGH-label transposition tau, T_tau=rho(tau)Q_k has the same effect,
  support, pointer result, and terminal HIGH symbol as Q_k. It normalizes the
  full record stabilizer, is involutive and ordinarily repeatable, but is not
  +-Q_k and is not projectively idempotent. It commutes with 4 of the 24 S_4
  elements and fails 20 strict naturality squares.

observable quotient:
  All 48 normalizer members act trivially only after adopting the new orbit
  equality under +-S_4. Under the registered sign equality they remain 24
  classes. Complete observation can identify gauge representatives; it does
  not force the microscopic law to be strictly natural.

target:
  The comparison is last. At k=2, P_2=E_low and Q_2=E_high. The strictly
  natural class is Lueder, while the transposition witness realizes the same
  HIGH effect without Lueder selection.

decision:
  NATURALITY-FORK.

integrity:
  one formal execution; exit zero; empty process stderr; 10/10 exact gates
  PASS; stdout byte-identical to EXPECTED.txt.
```

## Scientific conclusion

The positive route is exact but narrower than the phrase "the record cannot
distinguish the labels" suggests.

If the apparatus law itself is required to be a natural endomorphism under
every invisible relabeling, then the law commutes with the full record
stabilizer and Lueder follows inside the frozen class.

If the requirement is only that the law descend uniformly to invisible-label
orbits, then the correct object is the normalizer, not the centralizer. That
condition leaves 24 post-state classes under the registered equality. A
transposition is the smallest exact witness.

If all invisible relabelings are instead declared gauge, those 24 classes
collapse observationally, but this changes the physical equality. It does not
derive the Lueder representative under the current sign-only equality.

Therefore neither a typed binary record nor complete observation of its orbit
quotient proves strict law naturality. A positive closure of O2 must
independently justify one of two stronger moves:

```text
1. strict law naturality under the fixed S_4 action; or
2. enlargement of physical post-state equality to the +-S_4 gauge quotient.
```

The probe adopts neither.

## Exact boundary

This result sharpens the previous record-completeness theorem.

```text
strict naturality       centralizer  -> 1 registered sign class;
weak covariance         normalizer   -> 24 registered sign classes;
extended gauge equality quotient     -> 1 newly identified class.
```

The distinction is not wording. It changes the class count from one to 24.

The current public decoder is a typed partial read-only interface. This probe
does not complete it, identify apparatus records with `D_clock`, or derive a
dynamical law from decoder outputs. Global blocker O2 of
`QDD-INSTRUMENT-APPARATUS [O]` remains open.

## Proposed registry consequences

After the required byte-identical public replay, a later sealed Canon fold may
register at most:

```text
QDD-RECORD-NORMALIZER-CLASS [T]
QDD-OBSERVABLE-QUOTIENT-NONSELECTION [T]
QDD-LAW-NATURALITY-VS-GAUGE-BOUNDARY [T]
```

Their common scope is the exact L4 J-simplex record class frozen in
`PREREG.md`. None claims that strict naturality, the enlarged gauge equality,
or this apparatus class is already forced by `J`, `Omega`, `U`, the decoder,
or Nature.

## Evidence boundary

This is L4 apparatus/support only. It supplies no realized event stream,
sampling law, L6 measure, decoder completion, SI statement, or Bell causal
account.

O1 is untouched. The only sampling statement is:

```text
SAMPLING NOT PROVIDED
```
