# P-TM-CHECKPOINT-HULL-STABLE-IMAGE-1 result

Status: **candidate-T / L1 / CONFIRMED LOCALLY / PUBLIC CANON UNCHANGED**.

The first and only local formal execution of the immutable public verifier
exited zero, wrote empty stderr, and produced the exact 946-byte committed
`EXPECTED.txt`. All nine audit gates passed and no frozen scientific
falsifier fired. The universal and topological clauses remain carried by the
written proofs in `PREREG.md`, not by finite sampling.

## Frozen decision

```text
CONFIRMED          SELECTED LOCALLY
SCIENTIFIC-FIRED   NOT SELECTED
STOP               NOT SELECTED
ABANDONED-PIN      NOT SELECTED
ARCHITECTURE GATE  PENDING
```

## Candidate mathematical scope

For the newly defined two-sided checkpoint hull skew-product
`V_hull: K_TM x F_5^6 -> K_TM x F_5^6`, the synchronized graph
`X_stab` is the exact nine-step stable image, nine is least,
`V_hull|X_stab` is a homeomorphism, `R_cp` is its involutive reversor,
the full product map is non-injective, and evaluation at zero conjugates the
natural extension with the stable restriction.

This does not identify the new hull extension with the current public
autonomous state `U`. It selects no physical carrier and states no theorem
about invariant measures, Haar measure, `A_A`, decoders, event streams,
physical time reversal, arrows of time, trials, or observations. Natural
extension coordinates are auxiliary L1 states, not L5 records.

The required GitHub x86_64/aarch64 byte-identity gate and manual security
review remain open. Active `[T]` status, dependencies, and any Canon text
belong only to a separately reviewed v75 fold.
