# P-QDD-PASSIVE-READING-FAMILY-1 result

Status: PASS for the completed exact local audit at the preregistered scope.

The public pin is `dfcf68a664203a1d6a1692624e795ccb656ddefa`.
Its three source files were read back from that public commit and matched
the local bytes before the first formal execution. The accepted verifier
and preregistration were not changed after the pin.

The first formal run used Ubuntu 22.04.5 LTS, x86_64, CPython 3.10.12.
It exited 0 with empty stderr. Its exact LF stdout is EXPECTED.txt:
1688 bytes, 87 lines, SHA-256
`4dc1c83c6eb069c3b0acb7a4ac6286b32f58ce8929b9469fe03af6ffca6bf866`.
RUN.md records the remaining neutral environment and custody fields.

All frozen checks passed: the three rank-1,1,2 projectors, eight distinct
effects, 64 products, eight complements, three coefficient identities,
five independently enumerated partitions, 3125 balanced-source records,
6240 supported branch comparisons, 78125 native records and 78125 explicit
q,r-invariance checks. All 7500 coarsening and 13750 composition equalities
passed. The coherent-domain checks, six malformed-record rejections,
literal source-ID mismatch and non-refinement rejection also passed.
No preregistered mathematical falsifier fired.

PROOF.md supplies the universal exact proof; the finite verifier audits its
implementation. The proof covers Q^4 as a mathematical extension and the
unchanged adopted source K_QDD through beta_QDD. It also proves composition
on all coherent Record_sigma snapshots. It does not classify all possible
source readers or select a reading family.

This local result alone is one architecture. Public promotion requires
both required GitHub architecture jobs and their aggregate check to match
the same committed EXPECTED.txt byte-for-byte on the reviewed PR head.
Those job records remain the authority for the public two-architecture gate.

An independent proof may support the proposed L1 theorem. Owner selection
of the five reading IDs is a separate D decision in a separate Canon fold.
This result makes no apparatus, event, instrument, probability-resource,
pointer, reset or entropy-closure claim and changes no existing Canon status.
