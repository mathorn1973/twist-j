# P-U-POINT-SOURCE-READOUT-DICHOTOMY-1 result

Status: PASS at the frozen L1 scope. No falsifier fired.
The first formal run used the unchanged public pin and completed with exit
zero and empty stderr. No source, domain or threshold was repaired.

The self-contained proof establishes the arbitrary-five-point dichotomy:
a collision kills a nonzero norm-two contrast in H; in the injective case
every classical endpoint effect is Pi diag(w) Pi on H, 0<=w_s<=1, and
the only sharp projectors are 0 and I_H. The proof covers every finite
deterministic protocol and the entire continuous effect class.

The exact audit checks all 52 endpoint partitions, 51 killed contrasts,
all 25 ordered quadratic coefficients of the compression identity and all
32 binary simplex masks. The disconnected positive control has four sharp
effects. The general commutation criterion is a proof lemma, not an
additional claim for registration.

Prospective single promotion: U-POINT-SOURCE-SHARP-READOUT-NOGO. Its public
registration remains a separate reviewed Canon fold after the required
two-architecture gate. RUN.md records the local architecture only.

The theorem concerns the stated five-point free encoding and classical
endpoint-reader class. It excludes neither other coherent encodings nor
non-diagonal coherent readout, and does not declare the complete physical
apparatus class empty. Physical preparation/adoption, event, occurrence,
record/reset, family completeness and physical layer gates remain open.

Completed incubation #1005 is mathematical provenance only; no old audit
was imported or executed. Pin 2e0600eba2a9bc0b27262ebbd16103ef8f49388f
and all prospective sources remain unchanged. No Canon file is changed.
