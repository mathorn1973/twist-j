# P-JIPC-WP3E-EFFECTIVE-MELLIN-SEEDS-1 result

Status: **candidate-T / L1 / JIPC-WP3E-EFFECTIVE-MELLIN-SEEDS-CONFIRMED / PUBLIC CANON STATUS UNCHANGED.**

The first formal execution of the immutable public verifier exited zero,
wrote empty stderr and produced the exact committed `EXPECTED.txt` bytes. All
bounded gates and all 12 named negative proof controls passed. No scientific
falsifier fired and no threshold moved.

## Theorem result

Let `p_M=p=pi_atan` denote only the exact Machin-series Cauchy name frozen in
`PREREG.md`, with

```text
3 < p_M < 16/5,
D = {s in C : Re(s)>0}.
```

For each frozen tuple

```text
E=(2,1,0,1), O=(2,1,1,1), C=(4,2,0,2),
```

the independently instantiated logarithmic-coordinate improper integral is
absolutely convergent on `D`, has a total effective uniform holomorphic name
on every rational compact rectangle in `D`, and defines a holomorphic
function there. The written proof supplies explicit rational left-tail,
right-tail, midpoint, coefficient-name and complex-ball evaluator budgets and
proves termination, gluing and locally uniform convergence.

The cofinal substitution `x=exp(u)` proves the three exact positive-real
representations displayed in `PREREG.md`. This is within-branch compatibility
only. It neither uses nor establishes a cross-branch identity.

## Why this is candidate-T

The universal quantifiers are carried by the self-contained written proof
relative to the public positive `COMPLEX_BALL_MELLIN_TCB/v1`. The finite
verifier is an audit, not a sampled substitute for that proof. Independent
static review accepted the proof/code contract, and the first pinned audit
completed exactly. Therefore the bounded-`C` fallback is not selected and the
pre-PR scientific ceiling is `candidate-T`.

This is not yet an active public `[T]` row. The required byte-identical Python
3.12 x86_64 and aarch64 pull-request jobs have not run, no pull request has
been opened, and any Canon or registry treatment remains a separately claimed
and sealed fold.

## Frozen decision

```text
CANDIDATE-T  SELECTED
BOUNDED-C    NOT SELECTED
FIRED       NOT SELECTED
STOP        NOT SELECTED
```

None of the frozen F1-F10 scientific falsifiers fired. The public claim lock
is issue 566 and the immutable preregistration pin is
`46004772f3a6510791adf2ae4afd14a8a9f7f5af`.

## Scope firewall

The name `pi_atan` is not identified here with a circle, Gaussian, Gamma, SI,
physical or library constant. This result does not establish `E(s)O(s)=C(s)`,
the WP3F identity-theorem step, a generic externally supplied positive-real
oracle, meromorphic continuation, a functional equation, Fourier or Poisson
theory, a local zeta object, an archimedean place, or any L2-L6 lift. WP3F
remains `UNRUN`.

```text
FORMAL_VERIFICATION = NOT_CLAIMED
PROTOCOL_VERDICT    = NO_VERDICT
SAMPLING            = NOT PROVIDED
```

The maximum later public use, after the required architecture gate and a
separate Canon fold, is:

```text
JIPC-WP3E-EFFECTIVE-MELLIN-SEEDS [T], L1.
```
