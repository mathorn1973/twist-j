# Pointed decoder prefix consistency

Status: **CONFIRMED / PUBLIC CLAIMS UNREGISTERED / CANON UNCHANGED**.

Claim: `POINTED-DECODER-PREFIX-CONSISTENCY`.

The chosen decoder `DECODER-POINTED-BATCH-1` is now an executable exact
mathematical candidate with all three stages and all three reading legs.
All ten frozen gates passed in the sole initial Linux execution, with exit
zero, 521 bytes of exact stdout in 17 lines and empty stderr. No falsifier
fired. The source, choices, thresholds and verifier were unchanged after the
public preregistration pin and its complete byte readback. RUN.md supplies
the immutable commit, eleven-file inventory and raw-capture checks.

## Earned mathematical scope

For the explicitly chosen dictionary on every pointed source
`h in N_0 x F_5^6`, every finite prefix is uniquely defined in exact
arithmetic and agrees with the corresponding truncation of any longer prefix.
The full stream is defined through those compatible prefixes. The uniform
proof is PREREG.md section 3; bounded execution checks audit that proof and
the implementation rather than enumerate the unbounded set of trajectories.

* Actual checkpoints follow autonomous U; absolute counter, elapsed age and
  the exact cycle coordinate remain distinct. Readouts do not feed U.
* On K_QDD the five-field direct cyclotomic write agrees with the registered
  QDD dictionary. Applying the same head rule to nonzero-counter pointed
  sources is a disclosed adapter choice. LOW/HIGH is never equated to the
  five-cell coincidence profile.
* The two distinct selected integer source injections obey tesseract dF=0
  and torus boundary(j)=0 with zero windings. Each is nontrivial and has
  zero source as its exact zero fibre. The finite-support D3 state obeys the
  registered homogeneous recurrence with the selected two-slice initial data.
* Each ideal atomic batch contains exactly the complete finite within-cell
  Cartesian incidence population of fresh signed S/R fibres. Its compact
  ranges, rank/unrank bijection, counts, zero record, persistence, reset,
  passive readback and ordered append obey the specified mathematical laws.
* The manifest and code agree on eighteen record schemas and sixty-nine
  fields, their stage owners and reading legs, exact representations and
  explicit unresolved physical requirements.

G01--G10 all report PASS. The exact transcript is EXPECTED.txt. This is a
conditional theorem about the fixed chosen model, supported by the supplied
uniform argument and its exact conformance audit. It does not prove uniqueness
or physical necessity of the selected choices. At this initial recording,
independent GitHub x86_64/aarch64 replay is pending; its later evidence belongs
in RUN.md and the reviewed public PR.

## Choices and unresolved physical completion

The 14 named choices remain choices: the pointed source and reset anchor,
field assignments, centering/A clock, complete atomic batch and zero rule,
the two Maxwell injections, photon seed, time origin, provenance-preserving
history and interpretation boundaries. A conformance PASS does not convert
any choice into a physical deduction.

The apparatus here is an ideal encoded writer. Its mathematical context,
state transition and history do **not** certify a physical effects/instruments
family, coupling, pointer/reduction, realized outcome law or detector.
The separately tagged Maxwell and wave sources do not yet establish one
physical source-current-propagator-detector chain. Shared head coefficients
and an aligned counter are not that missing physical coupling.

Consequently physical decoder completion remains UNRESOLVED.
`COINCIDENCE-RECORD-FREQUENCY` remains candidate-H / UNTESTED / STOP and
unregistered. `QDD-INSTRUMENT-APPARATUS`, instrument-class completeness,
terminal event semantics, Bell causal accounting, photon realization/F3,
physical geometry selection, SI calibration and L6 measure/self-location
retain their current Canon statuses and gates. No probability sampler,
temporal ensemble or selected cell outcome is introduced.

The pointed auxiliary header makes equality decidable for the generated
presented histories. It does not solve physical history equivalence after
discarding that provenance. Mathematical totality assumes finite but
unbounded ideal resources and does not promise a uniform runtime bound.

## Disposition

Publish this result with the complete immutable source and execution record,
including any later independent replay or review findings. No earlier probe
is resumed, renamed, repaired or reclassified. This result creates no public
registry row and changes no Canon file. Any later theorem-grade fold must
retain exactly this conditional mathematical scope and its physical boundary.
