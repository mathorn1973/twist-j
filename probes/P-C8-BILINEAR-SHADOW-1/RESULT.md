# P-C8-BILINEAR-SHADOW-1 result

status: theorem candidate at T, L1 scope, formal aarch64 leg 6/6 ALL PASS;
the two-architecture gate completes with the required GitHub x86_64 check at
pull-request time. No falsifier fired. No frozen file was amended after the
pin.

Earned content at the frozen scope, conditional on the GitHub leg:

```text
AXES        <tau> = F_5^* union tau F_5^*; the order-8 elements are exactly
            {+-tau, +-eta}, each squaring into {J, phi}.
GALOIS      Frobenius is conjugation; it fixes the even layer pointwise,
            negates the odd layer; N(x) = N(-x) on all of F_25.
SWAP        Frob(Y_n^+) = Y_n^- for all n: the registered branch involution
            of SQRT-PHI-DIGIT-LIFT is the Galois action; the sign branch is
            a Galois gauge choice.
RECORD      (Theta_n always; Y_n at even s2; Y_n Y_m at odd pairs) is
            F_5-valued and branch-invariant; branches differ exactly at odd
            times; mixed-parity products are off-base and branch-dependent.
REFINE      the norm channel reads exactly s2 mod 4; the record reads s2
            mod 8 at even times and s2(n)+s2(m) mod 8 at odd pairs, strictly
            more; the residual branch datum is exactly one global odd-layer
            sign.
```

Proposed disposition (separate reviewed fold, not part of this probe): one
registry row C8-BILINEAR-SHADOW [T] in section 3, evidence this probe; the
SQRT-PHI-TIME-GRAVITY [O] frontier row may later cite the record-class sign
invariance for its branch-equivalence clause. This probe selects no branch
and opens no bridge.
