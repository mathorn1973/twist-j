# Reservoir quadratic partition and QDD processing boundary: result

Status: **CONFIRMED**, two conditional mathematical claims.

**PUBLIC CLAIMS UNREGISTERED / CANON UNCHANGED / PHYSICAL COMPLETION UNRESOLVED.**

## A. DECODER-RESERVOIR-QUADRATIC-PARTITION

For the chosen finite-support rational scalar D3 wave, centered source
`Bz=(0,Sz)` with `z in Q^4`, finite positive rational conductance context
(including empty ports), fresh zero incoming slots and every finite horizon n,
the port rows are linear in z. Their matrices `M_(t,x)=2 gamma_x ell^T ell`
and the residual defined directly by `z^T R_n z=2E(P_n(z))` are rational
symmetric positive semidefinite and obey

```text
sum_(t<n,x in R) M_(t,x)+R_n=G,       G=I_4-e e^T/5.
```

Earlier port slots persist under prefix extension. Preselected disjoint
complete grouping retains a positive partition, with residual separate.
On nonzero sources the energy shares sum to one, are invariant under
nonzero rational source scaling and have the exact G-metric trace spelling
`F_j=G^(-1)M_j`, `rho_z=z z^T G/(z^T G z)`, `tr(rho_z F_j)=w_j`.
At zero source the normalized share is explicitly undefined. Threshold
counts are a different reading; they can change under source scaling even
when the energy shares do not.

The uniform proof uses the inherited positive sum-of-squares density and
actual final-wave energy, not positivity inferred from `G-sum M`. It makes
no complete-absorption or limiting-horizon assertion. Warm external inputs,
adaptive source-dependent contexts and postselection are outside this scope.

## B. DECODER-RESERVOIR-QDD-POSTPROCESSING-OBSTRUCTION

For this source and coupling, whenever the origin is an active port and
n>=1, no state-independent nonnegative complete two-output processing of
the finite energy partition gives both sharp algebraic QDD LOW/HIGH target
weights. The complete declared processing class has one real coefficient
`a_j in [0,1]` per fine port slot and the residual, with outputs
`sum a_j M_j` and `sum (1-a_j) M_j`.

The first origin slot is a positive multiple of `h^T h`, where
`h=(1421,-349,-349,-349)/1620`. It has positive values on both balanced
sources `(1,-1,0,0)` and `(1,1,1,1)`, on which opposite QDD target weights
vanish. Positivity would force its LOW coefficient to be both zero and one.
The proof applies to every admitted positive origin conductance and every
positive finite horizon. Additional positive groups and residual energy
cannot cancel the obstruction. Deterministic coarse-graining and prior
pooling are included; a finite coefficient sample is not substituted for
the complete-class argument.

This confirms a restricted negative theorem, not a physical F claim or an
impossibility result for all apparatuses, nonlinear/source-dependent rules,
coherent amplitude transformations, changed interactions or Born laws.
No positive or negative closure of a physical owner row follows.

## Gates, falsifiers and evidence

All eight frozen gates passed in the sole initial clean Linux run, with
exit zero and empty stderr. EXPECTED.txt is the exact 613-byte, 16-line LF
transcript. The complete public source pin is
`87b085422dbe7ed7975333f14c884431ea9c2f36`; all six new source files and both
inherited executable dependencies were unchanged before and after execution.
RUN.md gives the full pin, hashes, byte counts, command and provenance.

The finite audit compares independent pointwise source/stencil propagation
and residual-energy polarization across four contexts and horizons 0..3,
checks all principal minors of the four-dimensional forms, and audits
prefix/grouping, G-metric traces, both exact witnesses, zero handling and
threshold/scaling boundaries. No required scientific falsifier fired.
Its finite coverage audits the uniform proof, not a physical experiment.

Independent GitHub **x86_64 and aarch64 replay PASS** in
[workflow 33955143771](https://github.com/mathorn1973/twist-j/actions/runs/33955143771),
with the same exact verifier and stdout hashes on both architectures.
RUN.md records the jobs and source/result pins. The aggregate required check,
all 155 tool tests and repository checks passed. Independent post-result
scope, integrity and security/license review passed. The uniform proof and
its independent computation audit concern only the two stated conditional
mathematical claims; formal public registration still requires a separate fold.

## Choices and physical boundary

The scalar carrier, norm-exposed source, port law, conductance, cold slots,
threshold and comparison class are explicit mathematical choices. Normalized
energy shares do not establish physical effects, instruments, event or
occurrence laws. Raw threshold ordinals are not exclusive QDD outcomes or
photon counts. Residual energy is retained and no target-dependent coupling
or readiness is inserted.

The #539 physical-profile proposal, QDD apparatus/terminality/class-completeness,
ordered sampling, post-state instruments, Bell accounting and all new
L1-to-L5/L6 lifts remain unresolved. COINCIDENCE-RECORD-FREQUENCY stays
candidate-H / UNTESTED / STOP. The #744 photon contract and #756 F3
NOT_SATISFIED remain; production #742 stays FORBIDDEN. No predecessor, Canon,
registry, frontier or workflow was changed. Any registration requires a
separate earned Canon fold.
