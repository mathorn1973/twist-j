# P-ENTROPY-MEASURABLE-OBSTRUCTION-1

Status: prospective preregistration; candidate-T, NON-CANONICAL.
Owner: A. M. Thorn / direct closure session 2026-09-20.
Public lock: #1081. Branch: probe/P-ENTROPY-MEASURABLE-OBSTRUCTION-1.
Parent: ENTROPY-LAYER-BRIDGE [O].
Source: Public Canon v90 ACTIVE at public main/tag
c7223ba461e264bde96eaedee4430704580235d3; content
b67d27bb291ec820cbc862d44dc225d0be93df3b. CANON.md SHA-256
8ff757009ab1e46a66e0d341d7e9408f5c93cb966633b3e1755083b081c6665f,
676201 bytes. The source is fixed independently of the prospective result.

## Exposure and collision boundary

The proof was derived and independently reviewed before this pin; this is
proof-first, result-exposed work, not blind discovery. No accepted verifier
or breaker has been executed or imported before pinning. Syntax checks are
permitted. The author's instruction authorizes the direct attack and public
repository work with the connected identity.

Public issues, remote heads, registry, probes and the older entropy branches
were examined. #241 was closed not planned without a formal pin. Its former
target was the depth-five fibrewise-bijective subclass. The historical
notes/entropy-selection-recon and breaker branches are preserved. No old pin
is resumed, edited, disposed of or assigned new authority. This new probe
quantifies over the ENTIRE registered measurable Route A class and uses no
629-component mismatch, finite-depth restriction or finite-window inference.

## 1. Equation

Freeze exactly the registered product source:

    Y=O_(K,lambda), K=Q(zeta_5), lambda=1-zeta_5,
    mu=m_TM x h_lambda,
    tau(kappa,y)=(S kappa,Jy), J=1+zeta_5^2.

Here K_TM is the two-sided Thue-Morse substitution hull, m_TM its invariant
probability and h_lambda normalized additive Haar. The checkpoint target is
X=F_5^6 with the unchanged five affine generators and selector
F_t(x)=g_(z(x)+2t)(x). Equality is mu-almost everywhere. P is measurable and
total; no continuity, bounded context, finite residue depth, injectivity,
fibrewise bijectivity, regularity or source-independent formula is imposed.

The primary candidate theorem is:

    There is no measurable P:K_TM x Y -> X with
    P(S kappa,Jy)=F_(kappa_0)(P(kappa,y)) mu-almost everywhere.

Consequently the registered subset A_A, which additionally requires Law_W,
is empty. This is a complete negative decision of that fixed parent class,
conditional only on the validity of the complete written proof. It is not
an assertion about every possible source or physical bridge.

Load-bearing proof gates:
G1. Exact equivariance forces z(P)=4+2 kappa_-1 and therefore
    r(tau x)=[kappa_-1=kappa_0]-r(x).
G2. Inducing on the aligned Thue-Morse two-tower with its exact normalized
    product measure gives q(Sb,J^2y)=q(b,y)+(b_-1 XOR b_0).
G3. After a base shift and an independent auxiliary five-cycle used ONLY
    in the contradiction proof, Chinese remaindering gives a Z/10 label
    beta with beta mod2=a_0 and beta'=beta+3(a_0 XOR a_1)-2.
G4. sigma(j)=(j,j+1,j-1,j) on Z/10 is primitive and aperiodic and is exactly
    this five-point extension of the Thue-Morse base. Its invariant measure
    is uniform on each five-point fibre.
G5. Repeated substitution blocks force omega^(3*4^k)->1 for each measurable
    eigenvalue omega. For omega of order dividing 2*5^a this forces omega
    in {1,-1}; their eigenspaces are already lifted from the base. There
    is no such eigenfunction in a nontrivial five-fibre Fourier sector.
G6. J^2 on finite lambda quotients and the auxiliary five-cycle have orders
    dividing 2*5^a. Finite-quotient functions are dense, giving a complete
    pure point eigenbasis without any fixed-depth restriction on P.
G7. Expanding the supposed unit-modulus fifth-root label in that eigenbasis
    gives a forbidden nontrivial-fibre eigenfunction for any nonzero
    coefficient. All coefficients must vanish, a contradiction.

PROOF.md supplies every universal and almost-everywhere step. The repeated
block lemma is the main spectral proof. Dekking's primary constant-length
substitution theorem is a separately identified consistency check, not an
unproved premise of the main argument. This simplification was made before
any formal pin or gate execution.

## 2. Code

Pin PREREG.md, PROOF.md, verify.py, break.py and BREAK.md together before
execution. The verifier and independent breaker use only exact Python
standard-library arithmetic. The breaker author does not open verify.py.
Both authors know the conjectured conclusion and proof; implementation
independence is not claimed as blind mathematical discovery.

Commands from repository root after exact public readback:

    python3 probes/P-ENTROPY-MEASURABLE-OBSTRUCTION-1/verify.py
    python3 probes/P-ENTROPY-MEASURABLE-OBSTRUCTION-1/break.py

The finite audits reconstruct native involutions and trace laws, stable
r-coordinate updates, exact length-nine language/reset, tower signs,
substitution primitivity/parity/translation, legal adjacent pairs, repeated
blocks, digit identities and CRT. They audit premises; neither program
claims to enumerate all measurable maps or to prove the spectral limit.

## 3. Carrier and data

Complete finite checkpoint carrier F_5^6; both control bits; all binary
pairs and F_5 r-values; ten substitution letters and their finite adjacent
language; all 100 CRT transitions; integer digit and return witnesses.
verify.py audits the digit identities for 0<=n<65536 and the two exact
height witnesses 3 and (4^10-1)/3. Finite ranges are not promoted to
all-time proofs. BREAK.md fixes its independent reconstruction methods.

No experimental dataset, fitted parameter, floating point, random sample,
numerical rank or tolerance is admitted. The main proof uses the exact
registered source measure, not a physical occurrence or Born law.
The external primary reference is bibliographic and reviewed in PROOF.md;
no external runtime input, downloaded code or redistributed article is used.

## 4. Systematics

Audit the direction of every shift, the previous/current bit convention,
the normalized measure of the aligned tower, and the distinction between
integer counter orbits and the two-sided stationary source. Carry out all
almost-everywhere statements on countable invariant conull intersections.
Check the auxiliary clock is an enlargement for contradiction, never a
replacement source. Establish five lifts and conditional uniformity before
using fibre orthogonality. Establish a positive-measure repeated-block bound
before the cylinder approximation argument. Establish full pure-point
completeness before taking Fourier coefficients.

Run in LC_ALL=C, LANG=C, PYTHONDONTWRITEBYTECODE=1, PYTHONHASHSEED=0,
TZ=UTC. Record the first stdout verbatim, exit, empty stderr, hashes, bytes,
neutral platform, architecture and Python version. Required public replay
uses the unchanged x86_64 and aarch64 workflow and aggregate check. The
independent breaker is not counted as another architecture.

## 5. Failure threshold

Any invalid G1-G7 implication or admitted measurable equivariant P rejects
the candidate full obstruction. An incorrect finite identity rejects the
corresponding premise. In particular, failure of conditional uniformity,
the repeated-block lower bound, eigenfunction approximation, spectral order
bound, or coefficient completeness may not be hidden by narrowing the class.
The parent remains O unless the entire proof survives at its original scope.

A code/custody/incomplete-run defect is STOP and follows POLICY.md's consumed
pin rule. Preserve completed scientific disagreements. Never edit the frozen
proof, implementation, threshold or scope after first execution to recover
the same identifier. Missing physical sampling is not a theorem of sampling
impossibility.

## 6. Action layer and disposition

MULTI: internal to the already registered GATE-L2-L5-ENTROPY-BRIDGE, from
its L2 source to its L5 checkpoint readout. No new cross-layer identification
is introduced. The source probability is part of the frozen mathematical
contract; no L6 physical measure is adopted.

Deliver the complete proof, exact audits, independent review, immutable pin,
first-run outputs, public two-architecture acceptance and bounded RESULT.md.
If all gates survive, recommend the full negative disposition A_A=empty for
a separate reviewed Canon fold. This probe itself does not edit the Canon,
Registry, Frontier, dependencies, gates, release or authority files.

No general failure of native U, no impossibility of its actual counter
orbits, no rejection of the J axiom, and no physical apparatus, occurrence,
SI or universal decoder impossibility follows. Changed or correlated source
laws, non-pure-point extra dynamics and different bridges lie outside the
frozen conclusion.
