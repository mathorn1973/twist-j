# Proposed parent disposition for a separate Canon fold

NON-CANONICAL. This is a review proposal, not a registry edit.

## Existing decision owner

ENTROPY-LAYER-BRIDGE [O] currently asks whether the complete registered
class A_A contains a measurable equivariant map from the specified product
source with Law_W. Its negative criterion is exactly A_A=empty. The present
proof satisfies that criterion without restricting the class; indeed it
rules out equivariance before Law_W.

## Proposed registry changes

1. Add ENTROPY-MEASURABLE-OBSTRUCTION [T], scoped to the exact source,
   probability, update, target and almost-everywhere equality in PROOF.md.
   Its falsifier is one admitted measurable equivariant P or failure of a
   load-bearing proof step. Evidence: this complete accepted probe.
2. Give ENTROPY-LAYER-BRIDGE its negative F disposition at the unchanged
   Route A scope. State explicitly that A_A is empty, not merely unknown
   or empty within a finite-cylinder subclass. Preserve its stable claim ID.
3. Remove that decided row from the live frontier and update the existing
   L2-to-L5 bridge gate to its typed negative disposition, using the active
   gate schema. Do not remove the history, finite results or older probes.
4. Record in CORE that this specific stationary independent-source route
   is excluded. Preserve all remaining physical apparatus, occurrence,
   clock, scale and other source-selection obligations.

The final fold must bind the exact accepted probe head and CI evidence,
check existing dependents and gate ownership against the then-current
registry, regenerate the normative views and pass the full two-architecture
Canon replay. This proposal does not authorize a guessed gate-kind value,
a mass status rewrite or an automatic replacement source.

## Content ceiling

A source change is not a repair within the falsified class. If the program
pursues another route, it must name its source, law, time action and exact
comparison before testing. In particular, the obstruction does not force a
physical interpretation of the generalized Morse extension used in its
proof.
