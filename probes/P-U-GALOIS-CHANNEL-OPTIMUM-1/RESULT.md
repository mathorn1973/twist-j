# P-U-GALOIS-CHANNEL-OPTIMUM-1 result

Status: PASS at the frozen L1 mathematical comparison scope.
No scientific falsifier fired. No physical QDD owner changes status.

The immutable public pin
`cb8258a481a1cbf1bfe3e815c9d5cebbeaf41065` and all six source files
were read back before execution. The independent standalone breaker ran
first, then the combined author verifier and breaker. Both exited zero
with empty stderr and unchanged source bytes under the 600-second bound.
RUN.md records the combined formal local run; REVIEW.md records the
independent construction, proof review and standalone run.

The self-contained proof establishes the following conditional mathematics:

- Over all complex CPTP maps M16(C)->M4(C) transferring the complete
  unchanged Galois code C rho C* to F rho F*, maximum mean and maximum
  worst-coordinate agreement with the native raw endpoints are both 5/8.
  The sharp minimum worst coordinate-distribution total-variation error
  is 3/8. The explicit Phi17 and Phi5 both attain every raw bound.
- Exact full-code transfer forces the common-environment decomposition
  Phi(X)=DXD*+Psi(QXQ). Equality in every positive slack and all complex
  completeness blocks fixes the four complement coordinate effects,
  each of rank four. Every optimizer consequently needs purified auxiliary
  dimension at least five, and Phi5 attains five.

The exact finite certificates verify every source matrix unit, raw input
and declared control, full U20 orthogonality and factorization, effects,
slacks and Gram identities. The independently authored breaker also finds
Choi ranks five and seventeen. These checks audit constructions; the
universal complex-class optimum and dimension lower bound come from the
written proofs rather than a finite channel search.

Unique effects do not make the global channel unique: raw e_(0,0) has
density entry(0,1) -1/8 under Phi5 and +1/16 under Phi17. A four-state
auxiliary transfers the full code when optimal raw agreement is omitted;
its raw output coordinate law is uniform. The point-exact CPTP control
separately loses the required coherent target. All residual outputs of
U20 remain counted, and the matched loader/inverse-loader cancellation is
explicit.

Prospective Canon claims are U-GALOIS-CPTP-POINT-COMPATIBILITY and
U-GALOIS-OPTIMAL-AUXILIARY-DIMENSION, at these literal conditional
mathematical scopes. This probe itself creates no Canon status. The
required clean GitHub x86_64 and aarch64 jobs must replay the same
unchanged sources against the one EXPECTED.txt, and aggregate check must
pass the reviewed PR head before public acceptance. The local x86_64
record alone is not the required two-architecture gate.

The independent implementation was frozen without reading author code,
but the target statements and incubation results were already exposed.
Its successful output is not a blind prediction. Neither repeated runs
nor different architectures alone constitute an independent proof.

The standard CPTP framework is an external premise. Formal raw basis
states have not been established as independently preparable orthogonal
physical states. U, N, Phi17, Phi5 and U20 remain distinct. No physical
source loading, native coupling, exclusive event, occurrence law,
terminal post-state, persistent record, reset, apparatus-family completeness
or physical cross-layer bridge is supplied. Every broader QDD obligation
retains its public scope and status.
