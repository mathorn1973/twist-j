# Independent mathematical and implementation review

Reviewer: A. M. Thorn / Codex-scope-review-20260919.
Disposition: **ACCEPT both claims at the preregistered L1 mathematical
scope**, subject to required public PR architecture/integrity checks and
the separate Canon fold. This review creates no public status or physical
dictionary and closes no physical QDD owner.

## Independence and sequence

The proposed constants and constructions were exposed in the intake
proposal and statement drafts. The reviewer read the inherited public
Galois-code proof and final formal preregistration. Neither source audit
code, new author verify.py nor new author PROOF.md was read before
BREAKER.py was independently written and locally frozen at SHA-256
`62b73abac792adb21a32d0e7becf33a83bd74542156f93802a65611022b5b11e`.
Only AST parsing occurred before the public pin. The new author PROOF.md
was read after that code freeze; the author verifier and source audit code
remained unread by this reviewer through first-run assessment. Output
comparison occurred after execution. This is result-exposed independent
implementation and reasoning, not blind prediction.

The shared public pin is
`cb8258a481a1cbf1bfe3e815c9d5cebbeaf41065`, with sole parent
`e57d4506d5b28bf8cb4979c4e29db6b10b2441f2`. Before execution the lane
verified public readback and exact identity of all six pinned files.
The reviewer checked the commit ancestry and the code/proof/output hashes
against the actual capture record. Reviewed PROOF.md SHA-256 is
`84f339e9dd1970256bbfc159936425186f3765b9d73e9705423ec46c79e9b481`.
The lane operator executed the code; the reviewer inspected the captured
output and neutral run metadata.

## Complete-class mathematical assessment

The source-basis coordinate map is explicitly invertible with Gram
`I4-ones4/5`; consequently the normalized code comparison covers every
complex source operator without silently replacing the native source.
Finite Kraus/isometry representation is derived from complete positivity
and the spectral theorem. Exact coherent transfer then fixes one common
environment vector. Surjectivity onto the full four-dimensional output
forces the complement into its orthogonal environment sector and removes
all code/complement cross terms. A proof using only pure basis tests would
not justify this step; the present proof includes all source matrix units.

The universal 5/8 mean bound follows from positive slack operators on the
four complement source blocks and completeness of the effects. Minimum
agreement is bounded by the mean, and both displayed channels attain the
bound at every raw point. The independent check confirms their entire
raw coordinate laws and explicit off-code density difference, so equality
of effects is not confused with equality of global channels.

The equality proof disposes of every positive slack. Each optimal effect
is supported on its four-dimensional q-vector span. The simplex
outer-product diagonal minor `(1/2)I+(1/16)J` is invertible, including for
complex coefficients. Applying it to every diagonal and off-diagonal
source block of completeness forces exactly the asserted effects. Thus
the proof covers arbitrary complex CPTP maps, not only the rational
examples tested by code. Worst-coordinate equality also forces mean
equality and receives the same classification.

The purified-environment lower bound is valid: the common code direction
uses one line, and an effect of rank four requires at least four further
directions in its orthogonal complement. The five-Kraus construction
attains five. Independent exact elimination finds Choi ranks five and
seventeen for the two distinct optimizing examples. The exact four-state
control transfers the code with raw agreement 1/4, confirming that the
five-state minimum requires optimization. Mixed auxiliaries are counted
after purification.

The point-exact control is separately typed and incompatible with the
fixed coherent target. All U20 outputs, its complete factorization and
loader cancellation are included. No residual ports are discarded. The
output auxiliary is constant on the code and therefore supplies no
LOW/HIGH record. No mathematical gap, failed exact certificate, moved
threshold or physical inference outside the declared scope was found.

## First post-pin independent execution

The standalone breaker ran before the combined author-plus-breaker audit:

```text
command: python3 probes/P-U-GALOIS-CHANNEL-OPTIMUM-1/BREAKER.py
platform: Ubuntu 22.04.5 LTS
architecture: x86_64
python: 3.10.12
started_utc: 2026-09-19T16:02:06.539348+00:00
exit_code: 0
stdout_bytes: 88
stdout_lines: 1
stdout_sha256: 1908ef044b2789ee3d27549f5e394ffbfcd93f1eb1ff4cc37cd0b2d0d70b2561
stderr_bytes: 0
stderr_sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
```

The bytes are retained in BREAKER-EXPECTED.txt and match the predeclared
successful line. The combined verifier began at
`2026-09-19T16:02:27.692141+00:00`, exited zero with empty stderr and
produced 11565 bytes/two lines, SHA-256
`9732399d5eee7a839ec55a65f7cdac982741f581dd9c19cb53769c747df52ed1`.
Its last line is byte-identical to the standalone breaker output. The
reviewer recomputed both retained output hashes. The capture records bind
the same unchanged six source hashes, including the code hash above.
RUN.md records the complete author run and EXPECTED.txt is the single
combined required replay target.

Required x86_64/aarch64 Python 3.12 jobs execute the same companion through
verify.py. Byte identity on those jobs is reproduction. The independent
code construction and universal proof reasoning are separate contributions.

## Accepted boundary

The accepted claims concern all complex CPTP maps on the fixed M16-to-M4
comparison, exact transfer of the fixed normalized coherent code, the
specified raw endpoint coordinate metric and purified auxiliary dimension.
CPTP is an external mathematical premise. The raw basis points have not
been established as separately preparable orthogonal physical states.
Coordinate error 3/8 is neither quantum trace distance nor QDD weight or
experimental error. U, its free point pushforward, Phi17, Phi5 and U20
remain distinct objects. There is no physical loading, coupling, exclusive
outcome, occurrence law, terminal event, persistent record, reset, complete
apparatus family or physical layer bridge. The three current QDD owners
remain O at their unchanged registered scopes.
