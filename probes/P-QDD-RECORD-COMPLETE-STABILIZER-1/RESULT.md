# P-QDD-RECORD-COMPLETE-STABILIZER-1 result

Status: `PROVED AND AUDITED IN THE FROZEN CLASS / PUBLIC REPLAY PENDING / CANON UNCHANGED`

## Disposition

```text
simplex:
  The five public J-orbit vertices form a rational regular simplex whose
  complete vertex-set automorphism group is S_5.

record partition:
  At token k, the binary partition {k}|other has complete stabilizer
  S_k ~= S_4. Its group average gives P_k of rank one and Q_k of rank three.

complete-record class:
  The rational moving-support centralizer of S_k has dimension one and basis
  Q_k. The effect equation leaves exactly T=+Q_k and T=-Q_k, which form one
  physical post-state class under T ~ -T. Strict representative idempotence
  selects +Q_k.

affine boundary:
  Retaining the smaller affine stabilizer H_k ~= C_4 leaves centralizer
  dimension three with basis R_k,C_k,J_k. The exact nonterminal witness
  T_*=R_k-C_k commutes with every affine stabilizer element but fails to
  commute with 16 of the 24 complete S_4 stabilizer elements at every token.

target:
  The comparison is last. At k=2, P_2=E_low and Q_2=E_high, so the unique
  record-complete physical class is the Lueder class.

decision:
  RECORD-COMPLETE-SELECTION.

integrity:
  one formal execution; exit zero; empty process stderr; 12/12 exact gates
  PASS; stdout byte-identical to EXPECTED.txt.
```

## Scientific conclusion

Inside the frozen rational J-simplex apparatus class, the positive selector is
not record persistence, no-feedback, reversibility, or ordinary repeatability.
It is the stronger symmetry statement:

```text
record-partition completeness:
  labels not distinguished by the terminal record are not available to the
  apparatus law.
```

For the binary record `{k}|other`, this requires the full `S_4` stabilizer of
the four unrecorded vertices. That symmetry destroys the residual
`Q direct-sum Q(i)` affine centralizer and leaves only scalar motion on the
HIGH support. The effect equation then yields `+-Q`, one physical class.

The result also identifies the exact obstruction. The public J-affine labels
carry more structure than the binary record. If that residual phase ordering
remains physically available, the smaller `C_4` stabilizer permits the
nonterminal `R-C` witness and the wider nonselection family.

## Exact boundary

This probe proves what record-partition completeness would buy. It does not
derive that premise from `J`, `Omega`, `U`, the decoder, the record-writing
protocol, or Nature.

Therefore global blocker O2 of `QDD-INSTRUMENT-APPARATUS [O]` remains open.
The remaining positive question is whether the physical apparatus is required
to forget the four affine phase labels once the record stores only HIGH, or
whether those labels remain legitimate hidden post-state data.

## Proposed registry consequences

After the required byte-identical public replay, a later sealed Canon fold may
register at most:

```text
QDD-RECORD-PARTITION-STABILIZER [T]
QDD-RECORD-COMPLETE-LUEDER-SELECTION [T]
QDD-AFFINE-RESIDUAL-LABEL-BOUNDARY [T]
```

Their common scope is the exact L4 class frozen in `PREREG.md`: the rational
J simplex, its complete vertex-set symmetry, the binary record partition, the
full partition stabilizer, and the affine-stabilizer boundary.

None says that record-partition completeness is already a law of TWIST-J, that
every J-native apparatus belongs to this class, or that the public decoder is
complete.

## Evidence boundary

This is L4 apparatus/support only. It supplies no realized event stream,
sampling law, L6 measure, decoder completion, SI statement, or Bell causal
account.

O1 is untouched. The only sampling statement is:

```text
SAMPLING NOT PROVIDED
```
