# TRC1 composition and identifiability: result

Status: **PROOF-SURVIVES (NON-CANONICAL)**.

The first pinned Linux audit returned `PROOF_AUDIT_PASS`: **52,401 exact
checks, zero failures**. The generated MODEL-side chain passed its frozen
finite audit: once-prepared source, cold evolution, complete signed records,
ordinal crossings, persistence, END and the supplied ensemble pushforward.
Declared record and type forgeries were rejected. This is conditional
mathematical conformance, not physical decoder closure.

The [proof](PROOF.md) establishes that, on a fixed finite source family,
calibration Ap determines prediction Bp for every mixture precisely when
`ker[1;A]` is contained in `ker B`. The run retained the actual-chain
nonidentifiability witness: e0 and e1 have the same scalar QDD calibration
(total 4/5; LOW/HIGH 1/20 and 3/4), but their full densities differ and are
excluded from that calibration. At origin conductance 1, threshold 1/16,
horizon 1, deposits `1421^2/4860^2` and `349^2/4860^2` give counts 1 and 0.
Mixtures (1/3,2/3) and (2/3,1/3) therefore predict crossing probabilities
1/3 and 2/3 while retaining equal calibration.

The e0/2e0 control gives counts 0 and 2 at threshold 1/8; its calibration
omits absolute energy. A stipulated source-indicator row passes the positive
sufficiency control without establishing physical availability. The fair
zero/2e0 two-cut law has two atoms, whereas its marginal product has four;
this compares hypothetical trajectory labels, not physical re-injection.
The 625 signed sources form 313 antipodal classes, giving an upper bound
of 313 visible histories, not 313 distinct measured histories.

The inherited positive-energy processing obstruction also survives its two
null-source controls: one positive slot would need LOW coefficient both
zero and one. It excludes the declared complete nonnegative processing
class, not all apparatuses. No new empirical falsification is claimed.

Issue [#901](https://github.com/mathorn1973/twist-j/issues/901); source pin:
`72c6d70a97c80bb5d4a03ae4c1b9f03d7d6aaa28`.
Linux x86_64, Python 3.10.12; exit 0, empty stderr, approximately 6.504 s.
Stdout: one line, 4,538 bytes; SHA-256:
`d4d23bda513d680a01f63f8ef8e354ce41916ca4776e9b1ecaa14bb44d3ed3b1`.
[EXPECTED.txt](EXPECTED.txt) preserves stdout; [RUN.md](RUN.md) supplies
provenance. [Independent PR CI](https://github.com/mathorn1973/twist-j/actions/runs/34169291542)
passed on x86_64 and aarch64 with Python 3.12 at reviewed head
`bd0184ea4df627ee3d376fcef933f52beb68a83a`; both returned the same stdout
hash above, and the aggregate check passed. This evidence update changes no
accepted source or EXPECTED.txt byte.

Physical preparation, realization, occurrence selection, clock/observation
identification and frequency convergence remain STOP. Fresh cold slots and
ensemble weights are premises; reset does not satisfy #539. No full physical
schema conformance, Born derivation or new dimension follows. No external
measurement payload was admitted. Public Canon v80 remains unchanged.
