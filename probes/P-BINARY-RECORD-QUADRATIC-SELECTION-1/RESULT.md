# Binary-record quadratic-selection result

Status: CONFIRMED NONSELECTION.
Proposed public status: T for the exact mathematical boundary in PROOF.md;
no Registry promotion or physical conclusion is made by this result file.
Scope: L1 exact equality-only relations, symbolic writers and the enumerated
scalar laws on adopted v80 integer channels.
Public lock: [#885](https://github.com/mathorn1973/twist-j/issues/885).

## Decided implication

Binary relation structure, covariance under correspondence-preserving
relabeling, reversible faithful writing, source retention and preservation of
older records do not by themselves force complete Cartesian incidence or a
quadratic count. This unrestricted structural inference is **REFUTED**.

The result is proof-first and result-exposed. The accepted finite audit ran
only after its public pin and passed 109538 exact checks with exit 0 and empty
stderr. No counterexample to the six stated mathematical theorems was found.
The displayed counterexamples are to the stronger implications being tested,
not execution failures and not falsifiers of v80's simplex theorem.

## Complete mathematical classification

For ordered equality-only k-ary relations on a d-element carrier, the orbits
under simultaneous relabeling are the equality partitions pi. Their sizes are
(d)_(b(pi)). Injection-stable nonnegative integer atomic valuations are exactly
the fixed assignments c_pi, with

```text
W_k(d)=sum_pi c_pi (d)_(b(pi)).
```

The degree of a nonzero valuation is the largest occupied block count. Full
Cartesian k-tuples give d^k; restricted triples can instead have d or d^2
tuples. This exhausts equality-only structures, not all native TWIST-J
relations or all possible valuations without the stability premise.

For k=2 the complete class is

```text
W_2(d)=a*d+b*d*(d-1), a,b in N_0.
W_2(d)=c*d^2 for all d iff a=b=c.
```

Thus the square requires equal weights on matching and nonmatching pairs.
Strict positivity alone still permits a=1,b=2. Forgetting a marked matching
and imposing independent permutations on unmarked carriers is a stronger
symmetry premise that does force a single pair orbit. Relabeling a marked
matching while transporting it does not erase the two-orbit distinction.

## Exact counterexamples and native comparison

| Proposed sufficient premises | Exact witness | What it establishes |
| --- | --- | --- |
| Binary arity plus matched-label covariance and stable positive edge weights | a=1,b=2 gives d(2d-1) | Those premises do not imply a pure square |
| Reversible faithful source-preserving writing with persistent older cells | T(s,r) swaps blank and s in the addressed cell; its active graph is {(s,s)} | At d=2 there are 2 matched pairs, not 4 Cartesian pairs |
| Native channel sign/permutation symmetry, normalized scale invariance and the listed zero classes | A_p=abs(sum z)^p, B_p=5 sum abs(z_i-z_j)^p, integer p>=1 | Infinitely many distinct normalized readings satisfy those scalar constraints |
| Nonzero unsigned counting that remains separately additive on signed groups | 0=B(g,h)+B(-g,h) with both terms nonnegative | Such a group pairing must vanish |

For z=(1,1,0,0), A_p=2^p and B_p=20. LOW is 1/11 at p=1 and 1/6 at p=2;
it is strictly increasing across the entire positive-integer power family.
p=2 is exactly the adopted v80 scalar count. The p=1 alternative is not
asserted to obey complete rational-frame additivity or the full QDD record,
density and physical apparatus contracts. The comparison conditions on the
already chosen v80 channel forms and factor 5; it does not derive them.

The symbolic writer has a total involutive extension on every source/cell
state and is covariant under all simultaneous symbol permutations. Fresh
cells and addresses are explicit resources. It records classical finite
symbols; it neither clones unknown quantum states nor derives a physical
memory/reset mechanism. Counting possible matched records is separate from
counting events in time and from assigning probabilities.

## Remaining physical question

The physical measurement-cut hypothesis remains **STOP-DEFINITION**, not a
falsified physical claim. A separately frozen apparatus class could justify
complete incidence, equal orbit weights or another positive pairing by an
independent law. This result identifies what that additional law must do at
the classified scope. Binary language, reversibility and the listed scalar
symmetries alone do not supply it.

No physical source map, apparatus, effect, event, occurrence law, ensemble,
sampling or L6 measure follows. All three physical QDD O / STOP owners and
the adopted v80 quadratic branch retain their existing scopes. A later Canon
fold may assess the proven boundary; this probe changes no normative file.
