# P-TRACEKERNEL-EXTERIOR-CLOSURE-1 result

## Disposition

```text
CONDITIONAL-PASS
L1 ONLY
NO CANON PROMOTION
```

All nine executable exact gate groups passed in the one accepted formal Linux
process. The separate manual/static M1 audit passed against the pinned public
base. The exact transcript is `EXPECTED.txt`; exit code was zero, stderr was
empty, and its 24 lines and 1522 bytes have SHA-256
`f9a95f22ddb8bf5f1ca79be00144cc663adcc1ef484a013eaf482695addbaf93`.

## Scientific conclusion

The proof-first and exact finite audits support these maximum later candidate
conclusions:

```text
[candidate-T, unconditional at L1]
For every prime p, G_p=pI-J has mod-p radical
W_p=ker(sum:F_p^(p-1)->F_p), dim W_p=p-2, and the first-derived residual
form is the nondegenerate restricted dot product.

[candidate-T, conditional on EXACT-HODGE-HOME-CLOSURE]
A bijection Lambda^2 W_p -> W_p has dimension zero or three. The zero case is
the vacuous p=2 carrier; p=5 is the only nonzero home-carrier branch.

[candidate-T, conditional on EXACT-HODGE-HOME-CLOSURE]
At p=5 the metric-volume bracket is sl_2(F_5), independent up to isomorphism
of nonzero volume scaling, and Phi gives the bracket grading 1+2.

[candidate-T, pure finite-field boundary]
Aut(sl_2(F_5))=SO_3(F_5)=PGL_2(F_5) has order 120. Exactly 8 of the 480
faithful public block maps preserve the bracket.

[O]
The public architecture does not force EXACT-HODGE-HOME-CLOSURE.
```

The universal statements are carried by the symbolic proofs in `PREREG.md`.
The finite verifier audits their exact ingredients and complete `F_5` carrier;
two-architecture replay is a computation gate, not the source of a universal
quantifier.

## Exact controls and falsifier state

- `W_2=0` is an empty carrier, not the unrelated nonzero `F_2^4` object.
- The plane has `dim Lambda^2 W_-=1`, so it cannot close home to dimension two.
- The supplied invertible alternating product has determinant one but Jacobi
  sum `-u3`; an unnamed bijection is therefore weaker than the metric-volume
  premise.
- All four nonzero volume scalings satisfy the frozen directed isomorphism.
- The scalar control `A=2I_2` fails bracket equivariance, and the complete
  classification leaves 8 compatible and 472 incompatible block maps.
- No exact mismatch fired. F2 remains armed but did not fire, because the
  public dependency table contains no requirement of full faithful
  `GL_2(F_5)` equivariance for this spatial commutator.
- Sampling was not provided and was not used as evidence or a threshold.

## Evidence and independence boundary

This formal probe is result-exposed. The `p=5` conclusion, `sl_2` triple,
kinematical incompatibility, two exact witnesses, and the `8/480` count were
known before its accepted verifier was written. The verifier implementation
was fresh and did not read or copy the predecessor `break.py`, but freshness
does not convert exposed facts into blind discoveries. The independent breaker
result is a reproduction and is labelled as such in the pinned chain of
custody.

The accepted `PREREG.md` and verifier remained byte-identical to public pin
`6b2c3e85195c92be299668fb5522cf95bc8dce23`. The result files are descendants
of that immutable pin and do not move any equation, threshold, carrier, status
ceiling, or scope firewall.

## Scope firewall

No binary-icosahedral `2I` derivation, spinor carrier, integral lift, decoder,
physical reading, measure, metrology, extension of `ALPHA-SEED`, canonical
curvature operator, or L1-to-L2/L6 lift is earned. `COLOR-SPLIT-12 [D]` and
`COLOR-KINEMATICAL-GL2 [D]` are not promoted. The probe changes no Canon,
registry, frontier, evidence, dependency, gate, tag, or release object.

Any public registry treatment is a candidate for a later separately declared
and sealed Canon fold. This probe itself assigns no public scientific status.
