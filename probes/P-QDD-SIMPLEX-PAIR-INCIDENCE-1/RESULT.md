# Result: P-QDD-SIMPLEX-PAIR-INCIDENCE-1

Status: ABANDONED

Public lock: #877.
Immutable pin: `cf8b5cbe1ceb571fd8769bc71f82bb5da994011e`.

## Administrative disposition

The accepted verifier was publicly pinned and all three pinned files were read
back from GitHub before the first scientific invocation. The invocation then
started under the frozen 600-second scientific ceiling, but the local execution
orchestration terminated before it retained the child exit code and elapsed
record.

The captured stdout file happens to contain the verifier's final diagnostic
`PASS` line and captured stderr is empty. Those bytes are diagnostic only. A
completed public gate requires the exact child exit code together with the
accepted stdout and stderr record. That complete process record was not
retained, so no formal gate is accepted.

This is an integrity/execution STOP, not a mathematical falsifier. The frozen
`PREREG.md`, `PROOF.md` and `verify.py` remain unchanged after the pin.

There is intentionally no `EXPECTED.txt` and no `RUN.md`. No theorem, candidate
status, physical reading, apparatus statement or Canon change is earned by this
identifier.

The identifier is permanently consumed and must not be reused, resumed,
repaired, renamed or rerun. Any continuation requires a fresh issue, probe
identifier, preregistration and accepted verifier, and must name this abandoned
predecessor before its own pin.

## Scientific boundary

Nothing in this administrative record changes the mathematical argument written
in `PROOF.md`; it simply gives that argument no public result under this spent
pin. Existing public QDD incidence, stabilizer, reading-family and physical
apparatus statuses remain unchanged.
