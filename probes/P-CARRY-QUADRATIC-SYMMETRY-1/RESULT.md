# P-CARRY-QUADRATIC-SYMMETRY-1 result

Date: 2026-08-21

```text
DECISION:   CARRY-QUADRATIC-SYMMETRY-CONFIRMED
CHECKS:     8 of 8 PASS on accepted local leg, exit 0, empty stderr
PIN:        6229acdeb8bce1afca61c8f4202821c1ebb2e5d0
CLAIM LOCK: issue 316
LAYER:      L1 finite/binary arithmetic only
```

No threshold or theorem scope moved after the pin. No mathematical falsifier fired. The public two-architecture workflow is still required before merge.

## 1. Prime-free carry theorem

The written proof in `PREREG.md` establishes the all-n theorem; the verifier audits finite boundaries and the complete n=4 automorphism action.

Among pure squarefree homogeneous quadratic Boolean forms, the S_n-invariant space is one-dimensional, generated by

```text
q_n=e_2=sum_(i<j) x_i x_j=binom(popcount(x),2) mod 2.
```

Lucas gives the carry hierarchy `e_1,e_2,e_4,e_8,...`; therefore `e_2` is the unique nonzero permutation-invariant pure quadratic carry layer.

Its weight law is exactly

```text
q_n(x)=0 iff popcount(x) mod 4 is in {0,1}.
```

Hence the least positive weight period is four. The first arity at which a non-atomic singular point can occur is n=4, where

```text
P_4={e_1,e_2,e_3,e_4,1_4},
|P_4|=5,
sum_(x in P_4) x=0.
```

Thus the cardinality five is an output of the quadratic carry geometry, not an input rational prime or order-five assumption.

## 2. Exceptional symmetry birth

At n=4 the complete automorphism group has order 120 and induces every permutation of the five singular points. This is the already registered fixed-frame `CARRY-PENTAD [T]` S5 fact, used here only at the n=4 clause.

For n=5,6,7 the exact singular counts are 11,27,63 and the full symmetric group is too large to be an image of GL(n,2). For all n>=8 the written bound

```text
binom(n,4)>=n^2+2
```

implies the same order obstruction. Therefore n=4 is the unique minimal arity where the pure quadratic carry layer has a non-atomic singular point and its automorphism group acts as the full symmetric group on the complete singular locus.

## 3. What this closes and what it does not

This removes the old conditional wording that one must first fix order five in order to explain why the carry pentad lives in four bits. The carry-side direction is now prime-free:

```text
unique pure quadratic carry layer e_2
  -> least period 4
  -> first non-atomic singular arity 4
  -> five-point singular locus
  -> exceptional full S5 action.
```

It does not select a rational prime, a C5 subgroup, cyclotomic field, cycle, orientation, exponent, J, or the step form I+C^a. The numerical identity 5=2^2+1 is an output/control, not a new theorem claim.

The integral/cyclotomic content remains owned by `CARRY-PENTAD [T]`. The new `P-J-BINARY-NORM-DESCENT-1` is a separate J-side descent seam and is not evidence for this pinned probe.

## 4. Infrastructure disclosure

A first post-pin invocation through the ChatGPT Python wrapper produced the correct 8/8 verifier stdout but also 754 stderr bytes from an unrelated spreadsheet-runtime startup hook before verifier execution. It is rejected as evidence. A subsequent invocation using system CPython produced the accepted 8/8 output with empty stderr. No scientific condition, threshold, source code, preregistration byte or verifier byte changed between them.

## 5. Scope firewall

L1 finite/binary arithmetic only. No zeta, p=5 selection, J derivation, Thue-Morse derivation, decoder, Born rule, apparatus, measure, spacetime, force, SI value, or L2-L6 lift.
