# P-J-HODGE-HERM2-LOXODROME-1 result

Status: PASS at the frozen L1 mathematical scope.

The exact four-dimensional predictive carrier E_J is L-invariant. The natural
wedge pairing restricted to it has the preregistered signature certificate
(3,1), and L preserves that form exactly.

The restriction has the exact factorization

```text
char_(E_J)(X) = (X^2-3X+1)(X^2-phi X+1).
```

Thus its hyperbolic pair is phi^2,phi^-2 and its compact pair is the primitive
tenth-root pair zeta_10^+/-1. Independently in Q(zeta_5),

```text
a_H = -J^-2 = phi^2 zeta_10.
```

The normalized diagonal Herm2 action associated with diag(a_H,1) therefore
has exactly the same modulus and phase factors. In standard real Lorentz
language these algebraic data read as rapidity 2 log(phi) and rotation angle
pi/5. The probe establishes the algebraic loxodromic invariants, not a
canonical physical-carrier identification.

No scientific falsifier fired. No physical spatial dimension, physical time,
native-U bridge, decoder selection, probability, measure, SI quantity,
curvature selection or L2-L6 lift is supplied.

The required clean GitHub x86_64 and aarch64 replay and aggregate check remain
the public acceptance gate.