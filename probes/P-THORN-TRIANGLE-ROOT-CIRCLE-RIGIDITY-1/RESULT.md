# P-THORN-TRIANGLE-ROOT-CIRCLE-RIGIDITY-1 result

Status: **ABANDONED**.

The formal gate never ran. After the immutable preregistration pin was pushed
and read back, the accepted verifier's source firewall was found to require the
entire 353145-byte Public Canon v68 file as a local raw checkout object. This
session has authoritative GitHub access only through the repository connector
and no raw Git checkout or network path into the execution runtime. The Canon
bytes and blob identity are readable through GitHub, but they cannot be
materialized into the local runner without changing the accepted verifier or
copying an unattached snapshot. Either action would violate the frozen pin and
the public source discipline.

No invocation of `verify.py` occurred after the pin. No exact stdout was
produced, so this directory intentionally contains no `EXPECTED.txt` and no
`RUN.md`. No scientific conclusion, status, evidence, computation, negative
result, or promotion is earned. The equation and falsifiers were not tested.

The identifier `P-THORN-TRIANGLE-ROOT-CIRCLE-RIGIDITY-1` is consumed and must
not be reused, renamed, resumed, amended, or treated as evidence. A successor
must use a fresh issue, probe identifier, preregistration pin, and verifier,
and must name this abandoned predecessor and the transport defect before its
own pin.
