# P-J-SIMPLEX-TIGHT-FRAME-DILATION-BOUNDARY-1 result

Status: **candidate-T / L1 / TWO CLAIMS CONFIRMED / TWO-ARCHITECTURE
COMPUTATION GATE PASS / PUBLIC CLAIMS UNREGISTERED / CANON UNCHANGED**

## Recorded decision

```text
J-SIMPLEX-TIGHT-FRAME-DILATION:          CONFIRMED
J-SIMPLEX-QUADRATIC-SUPPORT-RIGIDITY:    CONFIRMED
gates:                                    15/15 PASS
exit/stderr:                              0 / empty
stdout:                                   byte-identical to EXPECTED.txt
SCIENTIFIC-FIRED-A/B:                     NOT SELECTED
STOP:                                     NOT SELECTED
ABANDONED-PIN:                            NOT SELECTED
ARCHITECTURE GATE:                        PASS
MANUAL SECURITY REVIEW:                   PASS
```

The immutable verifier was executed exactly once after public pin
`3194c6f6dcd24b9e1c552be34ed6b103b2b4ade6` and byte-for-byte remote
readback. Its 21-line stdout has SHA-256
`5366e476df3c4c3e4f6b9219b334919b398932e725759008f956738e9da9aae7`.
Every frozen gate passed and no scientific falsifier fired.

## Claim A: full-cell copy and compression obstruction

On the orthogonal five-cell carrier, the integral permutation

```text
C_add(e_i tensor e_j)=e_i tensor e_(j+i mod 5)
```

has determinant one and exact order five. At the ready label `e_0`, it gives
the isometric embedding

```text
K e_k=e_k tensor e_k,
C_add(d tensor e_0)=sum_k d_k e_k tensor e_k.
```

Both exact algebraic contractions of this joint vector are

```text
diag(d_0^2,d_1^2,d_2^2,d_3^2,d_4^2).
```

For the two exposed inputs, the diagonal lists and totals are

```text
(16,1,1,1,1),       total 20,
(25,0,25,25,25),    total 100.
```

After compression by `P_V tensor P_V`, the marked images are
`u_k tensor u_k`. Their Gram matrix has diagonal `16/25`, off-diagonal
`1/25`, and rank five. The source simplex Gram has rank four and its unique
relation is `sum_k u_k=0`, while

```text
||sum_k u_k tensor u_k||^2=4.
```

Therefore no linear map from `V` to `V tensor V` sends every `u_k` to
`u_k tensor u_k`; equivalently, compressed copying cannot factor through the
augmentation quotient. Orthogonal full labels can be copied before
compression, but the nonorthogonal quotient vertices cannot be copied after
the relation-bearing compression.

## Claim B: conditional PSD quadratic support rigidity

Let `W_k` be symmetric positive-semidefinite operators on `V` and
`w_k(d)=<d,W_kd>`. Under the frozen hypotheses

```text
d_k=0 implies w_k(d)=0,
g W_k g^-1=W_(k+1),
sum_k W_k=I_V,
```

the dark hyperplane is `H_k=u_k^perp`. PSD Cauchy-Schwarz promotes quadratic
vanishing on `H_k` to `H_k subset ker W_k`. The exact symmetric constraint
space then has dimension one and is generated by `E_k=u_k u_k^T`. Thus

```text
W_k=c_k E_k,
c_k>=0.
```

Cyclic covariance makes the coefficients equal and the tight-frame identity
`sum_k E_k=I_V` fixes their common value to one. Therefore

```text
W_k=E_k,
w_k(d)=d_k^2.
```

Both qualifications are necessary. The family

```text
F_k^(t)=tE_k+((1-t)/5)P_V
```

is PSD, covariant, and normalized for every `0<=t<=1`, while exact darkness
selects `t=1`. Without PSD, the indefinite cross operator
`u_k h^T+h u_k^T` for nonzero `h in H_k` is dark on the whole hyperplane but
is not proportional to `E_k`.

This is a conditional uniqueness theorem within an assumed PSD quadratic
class. It does not derive quadraticity for arbitrary responses and does not
adopt exact darkness as a physical law.

## Required two-architecture workflow

```text
pull_request:          807
tested_head:           ea6ed6a87e3c04277dd993225a899320880f2316
workflow_run:          33889091910
x86_64_job:            101076071935 success
aarch64_job:           101076072392 success
aggregate_check_job:   101076167052 success
verifier_sha256:       f964e45237315095221dd26f3e331c1e2f01b41920daf2b35e0399e4dbc4dc64
stdout_sha256:         5366e476df3c4c3e4f6b9219b334919b398932e725759008f956738e9da9aae7
stdout_bytes/lines:    1550 / 21
byte_identity:         PASS on x86_64 and aarch64
manual_security:       PASS, review 5114864736 on all five named probe files
```

Both architecture jobs passed repository policy, all unit tests, Canon,
ledger, gate-contract, changed-probe reproduction, and exact transcript
comparison. The aggregate job reported `TWO-ARCHITECTURE CHECK PASS`. Their
common verifier receipt is

```text
VERIFY PASS P-J-SIMPLEX-TIGHT-FRAME-DILATION-BOUNDARY-1 f964e45237315095221dd26f3e331c1e2f01b41920daf2b35e0399e4dbc4dc64 5366e476df3c4c3e4f6b9219b334919b398932e725759008f956738e9da9aae7
```

Manual security review of all five files passed without importing or
executing the verifier. It found fixed exact inputs, bounded loops,
deterministic output, and only the standard-library `Fraction` import. It
found no file input, network, subprocess, shell, environment read, dynamic
execution, secret, personal data, or private-machine identifier.

`RUN.md` remains the immutable historical record of the sole local formal leg
and therefore retains `architecture_gate: PENDING`; this result section closes
that gate with the public workflow receipt.

## Earned scope and firewalls

The novel result is the combined marked full-cell-copy/compression boundary
and the conditional support theorem. The simplex and normalized projector
facts already exposed through PR #802 are not reclaimed.

The words `system`, `record`, `ready`, `Gram`, and `dilation` remain algebraic
labels. No physical partial trace, preparation, apparatus, instrument,
pointer, event, outcome, occurrence law, record population, coincidence,
probability, frequency, sampling, self-location, decoder completion, or
single-run randomness is supplied. In particular, the diagonal value `d_k^2`
is not yet a count or frequency.

Public Canon, Registry, Frontier, gates, dependencies, dictionaries,
`QDD-INSTRUMENT-APPARATUS [O]`, and `STATUS.md` remain unchanged. Any later
registration or L2 through L6 lift requires a separate fold.
