# PREREG: P-J-HODGE-PREDICTIVE-CLOSURE-1

Status: FORMAL PREREGISTRATION PIN.
Owner: connected GitHub account, explicitly authorized by the author in chat on 2026-09-19
Action layer: L1
Authority basis: Public Canon v89
Issue lock: #1047

## Equation / decision target

Let V=A4 tensor Q in the marked augmentation-root basis a_i=e_i-e_0, with Gram H=I+11^T, coordinate five-cycle C, M=I+C^2, W=Lambda^2 V, and L=Lambda^2 M. Over Q(sqrt(5)), let K=sqrt(5)* be the scaled Hodge operator and P_+, P_- the two Hodge projectors.

For either fixed sign, freeze the future-invisible space U_+ = intersection_(n>=0) ker(P_+ L^n), and symmetrically U_-.

Primary targets:
- dim U_+ = dim U_- = 2;
- the fixed-J observation rank is four;
- rank(P_+ L P_-) = rank(P_- L P_+) = 1;
- the L-invariant hull of either Hodge triple has dimension four.

For the marked A5 generated by the five-cycle (0 1 2 3 4) and three-cycle (0 1 2), the complete future-word class generated by Lambda^2(A5) and L is frozen. The proposed joint future-invisible space is zero, so arbitrary A5/J future words require all six dimensions. A finite word certificate reaching stacked observation rank six is sufficient.

An explicit decomposable-bivector control must exhibit two decomposable inputs with equal current fixed-sign Hodge output and unequal next output.

## Code

verify.py is standard-library only and exact. Fraction arithmetic and an exact two-component Q(sqrt(5)) representation are admitted. No floating point, randomness, external package, network data or fitted tolerance is allowed.

The committed verifier is an audit, not by itself the proof of every universal clause. A theorem-grade result requires the exact derivation plus the frozen finite certificates.

## Carrier / data

All matrices are generated from the marked A4 basis. The inherited public CARRY-PENTAD identification of I+C^2 with M_J is a dependency, not a new claim.

## Systematics

The A4 orientation and Hodge sign are frozen before execution. Plus and minus reads are distinct. The marked A5 generators and all equality conventions are fixed. No post-run orientation change, threshold change or target weakening is allowed.

## Failure threshold

No tolerance. The probe fires on any exact counterexample to the frozen rank, invisibility, hull, word-class or decomposable-witness clauses. Integrity mismatch or changed authority is STOP.

## Explicit nonclaims

No physical spatial dimension or time is derived. No native-U bridge, decoder completion, probability, measure, SI scale, L2-L6 lift or Canon promotion is asserted by this probe alone.
