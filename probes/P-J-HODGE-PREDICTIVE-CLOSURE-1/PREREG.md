# PREREG: P-J-HODGE-PREDICTIVE-CLOSURE-1

**FORMAL PUBLIC PROBE PREREGISTRATION / NO CANON STATUS.**
Owner: A. M. Thorn / J-Hodge predictive closure session 2026-09-19
Action layer: L1
Authority basis: Public Canon v89
Issue lock: #1047

## Equation / decision target

Let `V=A4 tensor Q` in the marked augmentation-root basis `a_i=e_i-e_0`,
with Gram `H=I+11^T`, coordinate five-cycle `C`, `M=I+C^2`,
`W=Lambda^2 V`, and `L=Lambda^2 M`.  Over `F=Q(sqrt(5))`, let
`K=sqrt(5)*` be the scaled Hodge operator and

```
P_+ = (I + K/sqrt(5))/2,
P_- = (I - K/sqrt(5))/2.
```

For a fixed sign define the future-invisible space

```
U_+ = intersection_(n>=0) ker(P_+ L^n),
U_- = intersection_(n>=0) ker(P_- L^n).
```

Primary decision target:

```
dim U_+ = dim U_- = 2,
rank Obs_+ = rank Obs_- = 4,
rank(P_+ L P_-) = rank(P_- L P_+) = 1.
```

Here `Obs_+` is the stacked linear observation map `x -> (P_+ L^n x)_n`.
By Cayley-Hamilton it is enough to stack `n=0,...,5`.

For the marked `A5` generated by the five-cycle `(0 1 2 3 4)` and
three-cycle `(0 1 2)`, define the complete future-word class generated by
`Lambda^2(A5)` and `L`.  The joint future-invisible space is required to be
zero.  A finite certificate is accepted if a frozen finite word set has
stacked observation rank six, because that already forces the complete
intersection to be zero.

## Code

`verify.py` is standard-library only and uses exact `Fraction` arithmetic and
an exact two-component implementation of `Q(sqrt(5))`.  No floating point,
randomness, external package, network access or fitted threshold is admitted.

## Carrier / data

All matrices are constructed internally from the marked `A4` basis.  The
public multiplication-by-J matrix is included only for an exact audit of the
inherited CARRY-PENTAD bridge via an explicit unimodular conjugacy.  No
external data are used.

## Systematics and controls

1. The Hodge sign is frozen by the marked orientation of the `A4` root basis.
2. Both plus and minus reads are checked independently.
3. `C` and the marked three-cycle are checked to generate exactly 60
   permutations before they are used as `A5` generators.
4. The verifier checks exact commutation of the two `A5` wedge generators with
   the scaled Hodge operator.
5. A decomposable-bivector control uses `x=e_12` and `y=*x`; both satisfy the
   Pluecker relation exactly, have identical current plus output, and are
   required to have unequal next plus output.
6. No physical time, spatial dimension, decoder, native-U intertwiner or
   higher-layer reading is part of the carrier.

## Failure threshold

There is no numerical tolerance.  The probe fires on any exact failure of:

- the marked `A4/J` conjugacy audit;
- Hodge ranks `3+3`;
- either directed one-step cross rank being exactly one;
- either fixed-J observation rank being exactly four;
- either invariant hull having dimension exactly four;
- the frozen word-depth certificate reaching rank six;
- the decomposable current-output collision and next-output separation.

Any integrity mismatch, changed verifier bytes, changed preregistration bytes,
or changed authority basis is STOP, not a scientific result.

## Explicit nonclaims

No physical spatial dimension is derived.  The fourth predictive coordinate
is not identified with time.  No native-U bridge, decoder completion,
probability, measure, SI scale, L2-L6 lift or Canon promotion is asserted by
this probe alone.
