# P-J-HODGE-PREDICTIVE-CLOSURE-1 result

Status: PASS at the frozen L1 mathematical scope.
No scientific falsifier fired. No physical dimension, time or decoder claim is made.

The immutable public pin is
`8766a31ccbcb59db8b5161fcd186852c1e773fd9`. Public readback before the first
formal execution matched PREREG.md and verify.py byte for byte. The pinned
verifier exited zero with empty stderr and produced the single EXPECTED.txt.

The exact result is:

```text
Hodge output dimension                 3
fixed-J minimal predictive dimension   4
full A5/J future-word dimension        6
```

More precisely, both Hodge eigenspaces have dimension three. Each directed
one-step J cross block has rank one. For either fixed Hodge output the stacked
future observation map has rank four, equivalently its future-invisible space
has dimension two and its L-invariant hull has dimension four. For the frozen
interleaved word class generated by L and the two marked A5 generators, the
stacked observable ranks at word depths 0,1,2,3 are exactly 3,4,5,6; hence the
complete joint future-invisible space is zero.

The decomposable control also passes: two decomposable bivectors have the same
current plus-Hodge output and different next plus-Hodge output. Therefore the
current Hodge triple alone cannot carry an autonomous update on all declared
decomposable inputs.

The marked A4 operator I+C^2 is independently checked against the inherited
public J-step by the frozen integral conjugacy. The probe does not re-register
CARRY-PENTAD or claim a new selector.

Prospective Canon scope: one L1 theorem on exact predictive closure and its
six-dimensional arbitrary-word boundary. This probe itself creates no Canon
status. The required clean GitHub x86_64 and aarch64 jobs must replay the same
unchanged verifier against the one EXPECTED.txt, and the aggregate check must
pass the reviewed PR head before computation-grade T acceptance.

The result does not identify the fourth predictive coordinate with physical
time, derive physical spatial dimension, intertwine the native U update with L,
complete the decoder, or supply probability, measure, SI scale or any L2-L6
lift.
