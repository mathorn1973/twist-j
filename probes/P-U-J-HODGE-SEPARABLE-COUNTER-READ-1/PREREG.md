# PREREG: P-U-J-HODGE-SEPARABLE-COUNTER-READ-1

FORMAL PUBLIC PROBE. Action layer L1. Issue #1069.
Base: Public Canon v89 main 08d7984d97c85d89d0b22a400762a5140fcb3bb5.

Let X=F_5^6 with public trace z(x)=sum_i x_i mod 5 and declared checkpoint
involutions (g_0,...,g_4)=(a,b,c,d,e). Let E be any set with a bijection
L:E->E. Consider total reads on all Omega=N_0 x X of the frozen form

    R(n,x)=L^n F(x),

where F:X->E is fixed and has no other n-dependence.

Because theta_0=0 and theta_1=1, global exact intertwining

    R(U(n,x))=L R(n,x)     for every (n,x) in Omega

is equivalent to the two equalities, for every x,

    F(g_(z(x)) x)=F(x),
    F(g_(z(x)+2) x)=F(x).

Let ~ be the equivalence relation generated by these equality edges.

Frozen targets:

G1 PROOF.md proves the iff statement by cancellation of L^(n+1). It must use
the full product Omega and may not replace it by one prepared orbit.

G2 verify.py enumerates all 15625 checkpoints and both frozen edges for every
state, constructs X/~ exactly, prints the number of components and exact
component-size histogram, and independently reconstructs the same components
by graph search.

G3 PROOF.md classifies all set-valued F: admissible F are exactly arbitrary
functions on X/~ pulled back along the quotient.

G4 On the free linear carrier Q^X, quotient by all edge differences e_x-e_y.
PROOF.md shows its dimension is the component count. verify.py audits the
rank certificate through a spanning forest: forest edge count=|X|-components.

G5 For every component, compute the set of trace values z(x) it contains and
print the exact histogram of trace-set cardinalities.

G6 Verdict CHECKPOINT-ERASED if X/~ has one component; otherwise
CHECKPOINT-QUOTIENT with the exact component count. Both are accepted.
Cardinality alone does not establish a physical/source sufficiency claim.

G7 The explicit L^n factor is allowed by construction but is not claimed to
be independently derived or physically selected. The probe classifies only
the remaining checkpoint factor F.

verify.py is exact standard-library finite arithmetic, exhaustive over X and
with no randomness, sampling or floating point. Runtime/integrity failure is
STOP.

No physical time, observer, decoder, occurrence law, preparation restriction,
native-Hodge identity, measure, SI, Canon promotion or L2-L6 lift. Restricted
domains may have different quotients and remain outside this total-Omega
theorem. The author explicitly authorized the connected GitHub identity.
