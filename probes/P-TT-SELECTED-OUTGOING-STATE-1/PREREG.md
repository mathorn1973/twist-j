# Preregistration: complete selected outgoing TT state

PUBLIC; NON-CANONICAL. P-TT-SELECTED-OUTGOING-STATE-1.
Owner: A. M. Thorn / selected-TT-state session 2026-09-20.
Lock: https://github.com/mathorn1973/twist-j/issues/1097.
Base: `1d3e70433f850d03aa210374d8cda16dd97a0c10`.
Authority: Public Canon v90 ACTIVE, unchanged by this probe.
The author approved continuation to the complete outgoing TT state,
explicit reasoned model choices and the connected GitHub contributor
identity. Selection and analytic results precede the pin; this is not
blind discovery or comparison with experimental data.

## 1. Equation and conditional claims

Freeze the full ETH-TT-1 contract in MODEL.md. Keep the registered K1
word law, source Phi=H1-H0 and isolated outgoing rational recurrence.
With two independent uniform signs fixed for the whole packet, select

    v_m(r)=epsilon_+ sqrt(max(h_m(r),0))
           +i epsilon_- sqrt(max(-h_m(r),0)).

The common source word and the two signs determine every counter and
all joint moments. The claims are a normalized consistent complete law,
v^2=h everywhere, exact sign-class symmetry, 25 distinct outgoing
histories, full vector and quadratic-readout moment formulas and the
inherited action normalization. The primary equation is the h recurrence,
not the singular Euler equation obtained by substituting h=v^2 alone.

The independently selected composite comparison s=|v|^2=|h| uses coefficient
one, the unitary five-site Fourier transform and the same canonical scale
as h. Connected powers define R_TI, not a cosmological r_T. The complete
first-emission table is frozen in MODEL.md. At all m>=2 every intensity
denominator is positive; zero initial slices have undefined ratios.

The all-counter results rest on written induction, covariance and
algebraic-integer arguments. Finite replay does not replace those proofs.

## 2. Code, carrier, data and custody

Runtime uses only the Python standard library, exact rational/cyclotomic
arithmetic and algebraic square-root certificates. No floating arithmetic,
random sampling, numerical optimization or target dataset is used. Formal
radical identities are evaluated in an exact polynomial quotient with
sqrt(q)^2=q and then interpreted at positive real roots. Nonzero or order
claims must not be inferred merely from an unreduced formal expression.

The verifier is self-contained. It reads no Canon files, predecessor
verifier, network, external data or runtime fixture. The four-word masses,
five labelled sites, source signs, onset, L and Fourier conventions are
frozen here and in MODEL.md. Exact audit horizon is m=0,...,10, including
all ten words and all four persistent sign pairs. It is an audit horizon,
not a new physical time cutoff.

Before first scientific execution commit, push and read back exactly:

    probes/P-TT-SELECTED-OUTGOING-STATE-1/PREREG.md
    probes/P-TT-SELECTED-OUTGOING-STATE-1/MODEL.md
    probes/P-TT-SELECTED-OUTGOING-STATE-1/PROOF.md
    probes/P-TT-SELECTED-OUTGOING-STATE-1/REVIEW.md
    probes/P-TT-SELECTED-OUTGOING-STATE-1/verify.py
    notes/canon/SELECTED-TT-STATE-FOLD-PROPOSAL.md

Written analytic derivations and static syntax checks are permitted before
the pin. No scientific gate execution, development run, or numerical
search is permitted before it. A separate reviewer checks the mathematics
without reading the new verifier. The first exact run follows public
readback from a clean pinned tree. Frozen bytes never change afterwards.

## 3. Frozen audit obligations

G1. Audit all ten source words and masses, their unchanged u/H/Phi map,
static/active split, plus/minus pairing, rational L and recurrence for
m=0,...,10. Confirm symmetry, zero mean and transfer representation.

G2. Audit the full 40-label law, 25 distinct vector histories, exact
atom masses and all finite-prefix restrictions. Verify seven tensor
histories and the per-slice support counts for m>=2 within the horizon.
Retain the zero atom of mass 1/3 throughout.

G3. Audit the exact square-root certificates, v^2=h, |v|^2=|h|,
doublet square/determinant identities and zero handling. Roots are not
silently confined to Q(zeta_5), and source b_src is not identified with v.

G4. Audit reflection, halfturn and polarization-line redescription on the
four persistent sign pairs. Active packets require four distinct lifts;
the invariant distribution in this frozen class is uniform. Reject any
claim of isotropy or uniqueness outside the sign-coherent class.

G5. Audit zero vector mean, global zero pseudo-covariance and the complete
covariance formula at all space-time pairs within the horizon. Check the
general sign-parity moment rule and the fourth-order contractions against
direct tensor kernels. This is an exact finite atomic law, not Wick closure.

G6. Audit C_Phi, its rank two and nonzero eigenvalues 5/12,1/12;
transported single-slice and prefix covariance rank two. Audit the algebraic
eigenvalue/trace ingredients of the all-counter invertibility proof and
the finite transfer ranks, while attributing the universal claim to PROOF.

G7. Audit the inherited action coefficient and canonical h and vector
scalings symbolically. Include the silent-zero pulled-back variational
branch as a negative control: h's forced equation and the pulled-back
equation must not be identified at v=0.

G8. Audit connected tensor/intensity powers for every slice in the
horizon and the full Fourier covariance transport at onset, retaining
off-diagonal entries. The m=2 table must equal MODEL.md
exactly, including Parseval totals 1/2 and 1/4, all five modes and the
displayed ratios. For m>=2, every I_k>0, T_0=0 and T_k>0 for k!=0.
Counters 0,1 give undefined ratios, never a fabricated 0/0 value.

G9. Audit exact boundary witnesses: refreshing signs changes temporal
vector correlations; a biased sign choice violates the selected symmetry;
Fourier transformation and pointwise squaring do not commute; scalar raw
and connected powers differ; and the square-pullback equation admits a
silent branch excluded by the primary forced recurrence.

G10. Audit an explicit non-Gaussian fourth-moment witness against the
Gaussian value with the same second moments. Verify the zero-history
mass 1/3 is retained and both pre-onset comparisons are undefined.
Together the gates cover all declared finite word/sign, site/counter,
mode and point-pair domains. This certifies the finite audit scope,
not physical adequacy or an all-apparatus theorem.

## 4. Systematics and declared selections

The inherited source already fixes unit amplitude, one isolated impulse,
complete channel transfer, forward sign, plus frame and planar propagator.
Count these assumptions; do not advertise this as deriving the architecture.
New inputs are persistent sign-sector coherence, conditional reflection/
halfturn symmetry with source-independent sign law, and the composite
intensity comparison with unit relative coefficient. The four-state sign
law follows from the first two choices. Its two bits are supplied
preparation labels, not an identified native counter mechanism.

The full state is not spatially homogeneous or stationary in the counter;
full Fourier covariance matrices must not be replaced by just their
diagonals. Initial static words are not rejected. No delayed conditioning,
normalization on active packets, fitted scalar coefficient, altered
source law or omission of the zero Fourier slot is allowed.

The intensity is a derived invariant composite, not an independently
propagating scalar or primordial curvature perturbation. The finite
R_TI comparison must not be promoted to r_T, an observed tensor ratio,
a cosmological prediction or a physical scalar normalization. No
experimental threshold is inspected or selected.

## 5. Failure threshold and disposition

Any exact counterexample to a stated finite gate or universal theorem
within its frozen hypotheses rejects that assertion; no tolerance is
permitted. A completed verifier with a scientific counterexample retains
its exact output and receives the corresponding negative result. Neither
its threshold nor its source/field law is repaired in place.

Authority, missing evidence, pin/readback, custody, runtime or architecture
failures are integrity STOP. If the frozen gate never completes, including
an exception before a completed scientific transcript, preserve the pin
and close this identifier ABANDONED under POLICY; a repaired successor
requires a new identifier. No failed or abandoned pin may be hidden.

Public acceptance requires unchanged frozen files, a completed exact local
run with exit 0 and empty stderr, byte-identical x86_64/aarch64 replay,
aggregate check and independent written review. EXPECTED.txt, RUN.md and
RESULT.md are added only after the first completed execution. A neutral
ACCEPTANCE.md may subsequently bind immutable result-stage CI; final-head
and merged-main checks are recorded separately in the pull request.

## 6. Action layer and owner boundary

All mathematical claims here are L1. Proposed physical gates
GATE-L1-L4-SELECTED-TT-STATE,
GATE-L4-L5-SELECTED-TT-READOUT and
GATE-L5-L6-SELECTED-TT-COMPARISON remain UNPASSED. Merely naming them
does not register or pass a gate. No Canon or existing ledger row changes.

TT-VECTOR-STATE-NORMALIZATION retains its original full positive and
negative clauses. This probe supplies one complete outgoing state and a
finite composite comparison, not the remaining physical scalar field,
its relative action/units or the coordinate/epoch/wave-number mapping.
The existing moment-underdetermination theorem and singular-pullback
obstruction retain their scopes. There is no physical apparatus, native
root-sign source, nonlinear gravitational completion or repeated-source
occurrence claim.
