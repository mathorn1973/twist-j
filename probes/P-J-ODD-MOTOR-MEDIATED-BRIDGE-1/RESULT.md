# Result

Status: `ABANDONED`.

```text
probe            P-J-ODD-MOTOR-MEDIATED-BRIDGE-1
public lock      issue #526
formal gate      stopped before execution
formal result    none
recorded         2026-08-24, Public Canon v62
```

## Why the gate never ran

Mandatory readback found that the pushed verifier blob did not match the
accepted local bytes. The lane stopped before the verifier was executed, so
there is no exact stdout to pin, no `EXPECTED.txt`, no `RUN.md`, no fired
falsifier and no scientific result.

This reason is disclosed by the successor
`P-J-ODD-MOTOR-MEDIATED-BRIDGE-2` in its public preregistration on `main`.
The successor repairs nothing here and imports no result from this lane.

## The identifier is consumed

`P-J-ODD-MOTOR-MEDIATED-BRIDGE-1` is spent. It must not be reused, renamed,
repaired, rerun or resumed, and no part of this lane may be cited as
evidence. The pin and accepted verifier stay exactly as frozen; this record
is the only file added.

## Canon effect

None. No claim is registered, promoted, lowered or retired; no registry,
frontier, gate, dependency or Canon text changes; the live `H` and `O` count
is unchanged.
