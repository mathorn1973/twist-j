# P-J-PLENUM-POLAR-GAUSS-1 result

Status: **L1 / CLAIM A SCIENTIFIC-FIRED / CLAIM B candidate-T CONFIRMED /
TWO-ARCHITECTURE COMPUTATION GATE PENDING / PUBLIC CLAIMS UNREGISTERED /
CANON UNCHANGED**

## Recorded decision

```text
J-PLENUM-POLAR-GAUSS:             FIRED
J-PLENUM-POLAR-ORBIT-SEPARATION: CONFIRMED
gates:                            15/16 PASS
failed gate:                      G02 CENTERING
exit/stderr:                      0 / empty
stdout:                           byte-identical to EXPECTED.txt
SCIENTIFIC-FIRED-A:               SELECTED
SCIENTIFIC-FIRED-B:               NOT SELECTED
STOP:                             NOT SELECTED
ABANDONED-PIN:                    NOT SELECTED
ARCHITECTURE GATE:                PENDING
MANUAL SECURITY REVIEW:           PENDING
```

The immutable verifier was executed exactly once after public pin
`501f0ef860af56e8c328e950f52f42623c507c09` and byte-for-byte remote
readback. Its 22-line stdout has SHA-256
`34ec9a43130413fbb98c272af1b6d6f0fcd9ae487876c72b0d7b8c2914e71a61`.
Gate G02 fired claim A. The remaining fifteen gates passed and claim B was
confirmed under its independently frozen gate set.

## Exact G02 diagnosis

The mathematical centered-lattice statement and the frozen executable test
were not the same determinant.

The preregistration proves that the image lattice is generated by

```text
D(e_0), D(e_1), D(e_2), D(e_3).
```

In the basis `f_j=e_j-e_4`, their coordinate matrix is

```text
5I_4-1_4 1_4^T,
```

whose eigenvalues are `1,5,5,5` and whose determinant is `125`. This is the
correct index of `im D` in `V_Z`.

The accepted verifier instead constructed the restriction of the operator
`D=5I-N` to the domain basis `f_0,...,f_3`. Since `N` vanishes on `V`,

```text
D|V=5I_V,
det(D|V)=5^4=625.
```

It then compared that determinant with `125`, so the frozen Boolean G02 is
false. This is a specification error in the accepted audit, not evidence that
the displayed index-125 theorem is algebraically false. Nevertheless the
probe's public decision rule says that any exact G02 mismatch fires claim A.
The result therefore remains `SCIENTIFIC-FIRED-A`; the theorem is not earned
by this probe and the verifier may not be repaired or rerun under this
identifier.

Any clean re-test requires a fresh successor identifier, claim lock,
preregistration, immutable pin, and execution. A successor must distinguish
the image-generator matrix from the operator restriction in both prose and
code.

## Claim B: exact orbit separation

All owning gates of claim B passed. The accepted exact audit confirms the
finite premises for

```text
A^2=5g^2-N,
A* A=5-N,
||A^n d||^2=5^n||d||^2                  for d in V,
```

and for the supported vertex

```text
d_0=(4,-1,-1,-1,-1),
A d_0=(5,0,5,-5,-5).
```

The two coordinate-square lists are

```text
(16,1,1,1,1),
(25,0,25,25,25).
```

For the raw step, the exact audit confirms

```text
J^n d_0=5(1+g^2)^n e_0-2^nN,
q_n=20,30,70,180,470,1230,3220,8430,22070,
q_(n+2)=3q_(n+1)-q_n,
J^8d_0=(29,29,-76,94,-76).
```

The boost and raw one-step coordinate-square lists are different:

```text
B d_0: (20,5,0,0,5),
J d_0: (9,4,9,4,4).
```

The raw `J` norm ratios `3/2` and `2` on the two frozen witnesses also prove
that `J` has no state-independent quadratic-norm multiplier. By contrast,
`A` has multiplier five and `U_5=A/sqrt(5)` has multiplier one on `V`.

This is a publicly unregistered candidate-T/L1 conclusion supported by the
written proof and passed exact gates. It is not a physical reading.

## Passed but unearned claim-A components

Gates G03 through G10 passed, including the full-register correction

```text
U_5B=BU_5=J-(2/5)N,
```

the sector polar identities, positivity inputs for `B`, Galois transport,
and the marked 80/40 group census. They remain transparent subresults of this
fired combined claim. The probe does not promote the combined
`J-PLENUM-POLAR-GAUSS` claim because its frozen G02 dependency failed.

## Earned scope and firewalls

This result stops at exact L1 algebra. `4^n` remains only an unreduced
signed-word count and `5^n` only a quadratic-norm multiplier. Coordinate
squares are algebraic data, not probabilities or frequencies.

No unit ontology, path population, Born rule, preparation, apparatus, event,
realized outcome, occurrence law, record, coincidence, sampling law,
self-location, decoder completion, physical time, gravity, SI scale, or L2
through L6 bridge is established. `QDD-INSTRUMENT-APPARATUS [O]` and the
registered apparatus nonselection theorems remain unchanged.

Public Canon, Registry, Frontier, gates, dependencies, dictionaries, and
`STATUS.md` are unchanged. Any later registration requires a separate fold.
