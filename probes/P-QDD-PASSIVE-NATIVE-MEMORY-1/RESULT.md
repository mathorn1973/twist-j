# Original passive-record recovery: exact memory minimum

Status: COMPLETED / PASS / L1 ONLY; independent proof and exact finite audit.
This result proposes theorem-grade content for a later reviewed Canon fold.
It does not itself change the public registry or the native state space.

Owner: A. M. Thorn. Reservation: [#1025](https://github.com/mathorn1973/twist-j/issues/1025).

## Public pin and completed audit

The three frozen files were committed at
`2f20f729c0173c856a1bc1ace20190154348edbd`, pushed, read back byte for byte,
and fetched before the first formal Linux execution. [RUN.md](RUN.md)
records the immutable file hashes and neutral execution metadata.

All eight preregistered audit groups completed with zero failures, covering
all 15625 native heads, 625 balanced vectors, 3125 exact F3 fibers and every
selected partition. The result is `PASS`, with exit zero and empty stderr.
The 1315 bytes of [EXPECTED.txt](EXPECTED.txt) have SHA-256
`dfc9c0c47c814fbc6b58437f8cc40f9d60452c898094811a28cb2ffc15c0833d`.
No scientific falsifier fired and no frozen target changed. The required
public x86_64 and aarch64 jobs must reproduce these same bytes; the first
local run alone does not claim that those external jobs have completed.

## Scientific result

For recovery of the original complete passive record from the indexed
current native checkpoint, every fixed time n>=3 requires and admits
exactly three distinguishable added read-time states. The same minimum
holds for an all-time reader, every one of the five selected partitions,
the whole family jointly, and the restriction to supported sources.

The lower bound is the frozen three-source collision with original total
weights 6/5, 64/5 and 4/5. Even PI-ALL retains those distinct total fields.
The upper bound is an explicitly initialized static phase-class tag with
classes {0}, {1,2}, {3,4}. Inherited synchronized-sheet bijections extend
the finite inverse argument to every later clock; deterministic continuation
establishes sufficiency at clocks 0, 1 and 2 as well. Three states are not
three bits, and necessity is not asserted for time zero alone.

Without an added tag, the exact maximum preparation cardinalities are:

| Partition | Full domain | Supported sources only |
| --- | ---: | ---: |
| PI-ALL | 7310 | 7310 |
| PI-TRACE | 6845 | 6845 |
| PI-LEG | 6601 | 6601 |
| PI-PAIR | 6598 | 6598 |
| PI-ATOMS | 6556 | 6556 |

The proof gives the factor criterion, inverse piston classes, explicit
largest-class construction and supported-domain equality. The audit
evaluates the complete finite inventories and independent coordinate,
affine and scalar/matrix formulations. Prior exploratory exposure remains
explicitly disclosed in [PREREG.md](PREREG.md).

## Scope and disposition

The existing K_QDD retains its original head and its adopted passive maps
remain valid. This theorem instead studies recovery from a different,
information-losing current-checkpoint input. Its positive repair adds
initialized information; it does not discover that register inside U.
No physical memory cost, apparatus, preparation law, event, frequency,
sampling or cross-layer lift is supplied. The physical QDD obligations
remain open. This probe is complete; its identifier must not be resumed
or reused. Public Canon v87 remains active until a separate reviewed fold.
