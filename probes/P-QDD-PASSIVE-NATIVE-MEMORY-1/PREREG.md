# P-QDD-PASSIVE-NATIVE-MEMORY-1

Status: FORMAL PREREGISTRATION / RESULT-EXPOSED / PROOF-FIRST / L1 ONLY.

Owner: A. M. Thorn. Date: 2026-09-16.
Public reservation: [issue #1025](https://github.com/mathorn1973/twist-j/issues/1025).
Branch: `probe/P-QDD-PASSIVE-NATIVE-MEMORY-1`.
Directory: `probes/P-QDD-PASSIVE-NATIVE-MEMORY-1/`.

The witnesses, numerical inventories and proposed memory conclusion were
obtained and inspected in prior noncanonical exploratory work. They are
explicitly result-exposed. This probe is a fresh formal audit of those
claims and their written proof, not a blind discovery or prospective
prediction of unknown counts. The newly authored accepted `verify.py` has
not been executed or imported before its public pin. Only static source,
syntax and mathematical review precede the pin. The exploratory code,
private paths and outputs are neither runtime dependencies nor public
evidence of this probe. No sealed probe is resumed or changed.

The complete `PREREG.md`, `PROOF.md` and `verify.py` are committed and pushed
together, then read back byte for byte before the first formal execution.
This document does not invent a preregistration commit or report a completed
run. File hashes and the immutable pin belong to the subsequent run record.

## Authority and exact public dependencies

```text
STATE: ACTIVE
CANON: Public Canon v87
AUTHORITY: mathorn1973/twist-j main
BASE_COMMIT: fd512f50d90382124e7c00afa926c8083fd56e06
TAG: canon-v87
TAG_TARGET: fd512f50d90382124e7c00afa926c8083fd56e06
CONTENT_COMMIT: 41c8d4229b71437f09957d959975dc1772e95806
CANON_SHA256: a2517c6d8efb1969a7258f94d9874cca3b5b9beb1aecdfd2e39b56f33961a917
CANON_BYTES: 634207
```

The coordinator freshly verified the public main/tag identity, normative
hashes and required publication checks before reserving this fresh scope.
The issue fixes the public work reservation. Its collision and public
readback records remain the coordinator's provenance, not scientific inputs.

Public mathematical dependencies at that baseline are:

- `QDD-OBSERVABLE-READING-FAMILY` [D]: the five selected total passive maps
  on existing K_QDD, with exactly the adopted coherent five-field schemas.
- `QDD-THREE-ATOM-PARTITION-COMPLETENESS` [T]: the exact atom weights,
  positive total, zero branch and coarsening laws used here.
- `U-NATIVE-CHART-AND-QDD-READBACK` [T]: exact F_3 fibers, subsequent
  bijective synchronized sheets and the native current-state factor
  criterion. Its old density-bearing QDD record is not silently substituted
  for the passive schema studied here.

| Frozen public source path | Bytes | SHA-256 |
| --- | ---: | --- |
| `canon/CANON.md` | 634207 | `a2517c6d8efb1969a7258f94d9874cca3b5b9beb1aecdfd2e39b56f33961a917` |
| `probes/P-QDD-PASSIVE-READING-FAMILY-1/PROOF.md` | 8631 | `fb2550d335e421272d80a1402d7feda15e72fb9b8c700028329b298d4359e0a7` |
| `probes/P-QDD-U-NATIVE-READBACK-1/PROOF.md` | 12710 | `2df18b2118d0a2e483c8089976eda8485127951efa9e2f3364d38ac2aab0d76c` |

These identify public premises. The accepted verifier is self-contained
and reads or executes none of those files at runtime.

## Field 1: equation

The simultaneously frozen [PROOF.md](PROOF.md) fixes all equations and the
universal arguments. For each of the five literal partition IDs pi, write
rho_pi(x)=R_pi(kappa_x), where kappa_x is the unchanged complete pointed
origin-zero native history and beta_QDD reads its distinguished head.

At fixed n>=3, current-checkpoint readback on a preparation set A exists
exactly when rho_pi is constant on each admitted F_3 fiber. A supported
three-origin witness over checkpoint (0,0,0,1,0,0) has original total
weights 6/5, 64/5 and 4/5. Each selected record retains that field, so all
five partitions require at least three added distinguishable read-time
states on the full domain, also on supported sources only.

The conditional extension U'(n,x,c)=(U(n,x),c), with c initialized from
original-phase classes {0},{1,2},{3,4}, recovers each passive record and the
whole family at every time. The all-time minimum is three because it must
also work at n=3. We do not assert that a reader at n=0 alone needs three
extra states. This tag is added state, not a native coordinate or three bits.

The four exact F_3 same-record multiplicity patterns are (2,2,1),(3,2),
(4,1),(5), in this fixed table order:

| Partition | Initial distinct records | Four pattern counts | Maximum full preparations | Maximum supported preparations |
| --- | ---: | --- | ---: | ---: |
| PI-ALL | 19 | 2510,200,385,30 | 7310 | 7310 |
| PI-TRACE | 35 | 2774,116,226,9 | 6845 | 6845 |
| PI-LEG | 62 | 2922,58,142,3 | 6601 | 6601 |
| PI-PAIR | 63 | 2924,56,143,2 | 6598 | 6598 |
| PI-ATOMS | 65 | 2946,54,123,2 | 6556 | 6556 |

Each preparation maximum equals the sum of the largest same-record class
size over all 3125 fibers. The proof gives the universal optimum formula,
the explicit finite sum defining each pattern count, the supported-domain
argument, and a largest-class construction. These are cardinality optima,
not chosen physical preparation laws.

## Field 2: code

The accepted `verify.py` uses only the Python standard library. Arithmetic
is integer or `fractions.Fraction`; there are no tolerances, fitted values,
random samples, external data, filesystem reads/writes, network access or
imports from previous probes. Non-optimized Python is required and `-O` is
explicitly rejected. All scientific stdout is deterministic ASCII JSON
with LF termination and no timing or host-dependent fields.

Eight frozen component labels identify the complete audit:

| Gate label | Exact audit domain and content |
| --- | --- |
| `NATIVE_KERNELS` | All 15625 heads under each of five coordinate and independently transcribed affine generators, their involutions, and all three direct steps for every origin. |
| `FIVE_FIELD_RECORDS` | All 625 piston tuples and five partition IDs; complete scalar record versus G-matrix/projector contraction, exact field names, support, positivity, raw/normalized coherence and zero tags. |
| `ALL_SOURCE_FACTOR` | All 15625 heads times five IDs; equality with their declared balanced original-piston record, including every q,r choice. |
| `EXACT_F3_FIBERS` | All 3125 fibers; size five, synchronized sheet, analytic inverse heads in phase order and all three inverse piston classes. |
| `INITIALIZED_READBACK` | Every origin at n=0,1,2,3 grouped by native checkpoint and correctly initialized static tag, for all five records; explicit inverse-class record decoder in all F_3 fibers. |
| `THREE_STATE_WITNESS` | The three frozen supported origins, their common F_3 image, three exact total weights and pairwise distinct complete records for every ID. |
| `PARTITION_INVENTORIES` | Complete initial record counts, all four same-record fiber patterns, maximum number of distinct records per fiber and five preparation optima. |
| `SUPPORTED_MAXIMA` | Fiber-by-fiber supported largest classes, equality to full largest classes, explicit distinct preparation sets and all five supported totals. |

These are L1 finite audit labels, not cross-layer bridge gates. Infinite
clock scope rests on the inherited synchronized-chart theorem and the
written argument; finite snapshots do not pretend to exhaust all clocks.

A completed audit prints all eight nonempty groups with check/failure
counts, the complete finite inventory and aggregate `PASS` or `FALSIFIED`.
It exits zero even for a completed `FALSIFIED`: execution completion and
scientific success are different predicates. Each failed group retains its
first exact witness in stdout. A mathematical failure has zero tolerance.
Unexpected exceptions or incomplete groups print `STOP` and exit nonzero;
they do not constitute a valid completed gate or a theorem counterexample.
An implementation/reference disagreement requires independent diagnosis
before the scientific RESULT classifies it as mathematics or integrity.

## Field 3: carrier or data

Native carrier: Omega=N_0 x F_5^6, coordinate order
(p1,p4,p1p,p4p,q,r), phase=sum(x) mod5, Thue-Morse selector
theta_n=popcount(n) mod2 and generator index (phase+2theta_n) mod5.
The five formulas, exact inverse fiber and class formulas are in PROOF.

Decoder carrier: the existing K_QDD of complete pointed origin-zero
histories with distinguished head retained. Its 15625 heads parameterize
the whole source; exactly 25 have zero pistons and 15600 are supported.
The balanced image has 625 vectors. There is no onset choice or source
domain extension. Later-checkpoint balanced reading is not adopted.

For a fixed pi the five fields are partition_id, support_state,
total_weight, block_weights, normalized_weight_state. The zero branch is
(pi,ZERO_SUPPORT,0,ordered-zero-tuple,ZERO_DENOMINATOR); the supported
branch has m>0, nonnegative ordered weights summing to m, and literal
(NORMALIZED,raw/m). Literal ID, order, tuple lengths and tags are part of
equality. No field is independently discarded when forming class counts.

The readback carrier is the reachable indexed current checkpoint (n,F_n(x)),
augmented only by the explicit initialized tag when declared. The original
head is not an unreported reader input. No physical probabilities, external
datasets, detector observations or empirical sampling assumptions enter.

## Field 4: systematics and frozen procedure

The principal failure risks are a wrong native generator transcription,
clock shift, a mistaken original/current-head substitution, dropping the
total or normalized field, merging partition IDs on coincident values,
incorrect zero treatment, and calling initialized added memory native.
Independent affine and scalar/matrix formulas, exhaustive finite domains,
explicit schema checks, inverse-fiber comparisons and the written carrier
restrictions address these risks. Source normalization is never inferred
from the numerical equality of another schema.

Prior result exposure is permanent; a successful run cannot be called blind.
Only static compile/syntax parsing and source/proof review are authorized
for this newly authored verifier before its public pin. After static review,
the coordinator commits and pushes the three complete files and records
their parent, immutable commit, hashes and public byte readback. The first
formal execution uses a clean Linux checkout fetched from that exact public
pin, with `LC_ALL=C LANG=C TZ=UTC PYTHONHASHSEED=0
PYTHONDONTWRITEBYTECODE=1`, running from the repository root:

    python3 probes/P-QDD-PASSIVE-NATIVE-MEMORY-1/verify.py

Capture stdout/stderr separately and record neutral Linux platform,
architecture, Python version, exit status, byte counts and hashes in RUN.md.
The available first local Linux runner is Ubuntu 22.04, x86_64, Python
3.10.12; required public architecture jobs use Python 3.12. Confirm pinned
bytes and clean source before and after execution. A successful completed
run produces one exact EXPECTED.txt and the scientific RESULT.md. Required
x86_64 and aarch64 PR jobs compare against that same stdout byte for byte.

No pinned file is amended, rebased, squashed or force-pushed. Later commits
add run and result records without altering frozen targets. If no valid
formal gate completed, the original pin remains and the consumed identifier
is disposed as ABANDONED under POLICY; a corrected successor needs a fresh
identifier and pin. A completed falsifier is preserved and reported, never
relabelled as abandonment to avoid a scientific result.

## Field 5: failure threshold and dispositions

One admitted exact counterexample suffices to fail the corresponding claim:

- A declared complete passive record differs from its fixed scalar/matrix
  specification, has incorrect support/zero behavior, or uses another ID,
  order, field inventory or input head.
- The displayed supported origins do not merge with the three distinct
  required total fields; or a correctly initialized tag fails to recover
  an original complete record on its stated reachable domain.
- A stated F_3 inverse, same-record pattern, record count, preparation
  optimum, supported optimum or witness construction is false.
- The factor criterion or inherited-bijection application admits a genuine
  conflicting original record at the same declared readback input.

The threshold is exactly zero, with no movable numerical tolerance or
post-run adjustment. Authority, pin, byte-integrity, implementation,
runtime, incomplete execution and output-custody failures are integrity
STOP conditions; they do not earn success or alone falsify the mathematics.
The RESULT distinguishes an exact scientific counterexample from an
implementation mismatch and records every completed failure permanently.

## Field 6: action layer and proposed consequence

The action layer is L1 throughout. The proposed result is a theorem-grade
memory bound and factor/optimization argument for the selected passive
family, together with exact finite inventories accepted only after the
required pinned computational audit. Neither preregistration nor test exit
code independently confers a Canon status; any fold is separately reviewed.

No new passive-family choice, native state variable, physical register,
apparatus, preparation mechanism, post-state instrument, realized event,
occurrence law, frequency, sampling, L6 measure or L1-to-L4/L5/L6 lift is
claimed. The three physical QDD O owners remain unchanged. Existing
head-retaining R_pi maps remain valid; this probe addresses their recovery
from a different, explicitly information-losing current-checkpoint input.
