# RESULT. P-DE-TRACE-DENSITY-1

Status: TWO-ARCHITECTURE COMPUTATION GATE PASS. No registry, Canon,
dictionary, release, or authority change is made by this probe.

The formal Ubuntu 24.04 aarch64 run and GitHub Linux x86_64 run
`29774669798` both reproduce the pinned verifier and the exact 3313-byte
stdout with SHA-256
`62127bf909d1d50624f77d744905f3c99287db816365a27b5ef458f3fddec0c5`.
All 21 verifier gates pass; the adversarial break path failed to kill.

Outcome in the frozen issue #88 relation-set test:

```text
OUTCOME: NONUNIQUE
  STOP excluded         for the frozen algebraic R(v12) test only; the trace
                        line and chi = log a are typed, while no physical
                        L2-to-L5 transport is claimed
  NEGATIVE excluded     q = 1/p survives every frozen relation
  PASS-EARNED excluded  at least two inequivalent characters survive R(v12),
                        including q = 0 and q = 1/5
```

Statements supported at the frozen computation scope:

```text
UNDERDETERMINATION   the explicitly listed relation set R(v12) does not
                     select the density character of the trace sector; the
                     displayed continuity identity admits the two exact
                     witnesses q = 0 and q = 1/5, while R1..R4 contain no
                     non-circular selector between them.
CONDITIONAL UNIQUE   given Delta_DE := gamma_tr, the selected member is
                     unique: q = 1/5, w_DE = 1/(pd) - 1 = -14/15 exactly at
                     p = 5, d = 3. This is conditional algebra, not an owner
                     decision or a derivation of the dictionary.
NOT UNIVERSAL        applying the same formal transport to the spatial-base
                     Gram weight gives w = -2/3, which differs from the dust
                     and radiation controls. No physical universal transport
                     theorem is claimed.
```

Consequences for `DE-CONFORMAL-WEIGHT [O]`: its positive closure as currently
worded is not derivable from the frozen `R(v12)` relation set. The probe does
not exclude future registered structure outside that set. The remaining
legal dispositions are an explicit owner decision accepting the narrow
homogeneous L5 dictionary at D-grade scope, or leaving the row open. That
decision belongs to a separately authorized sealed fold.

The permanent mathematical falsifier is a registered non-circular exact
relation that uniquely pins `q` within the declared scope. Negative controls
all fired correctly: lambda = +1 gives the conformal ghost -6;
`Delta := 3/4` gives `w = -3/4`; `p = 2` gives `-5/6`; and `p = 7` gives
`-20/21`.
