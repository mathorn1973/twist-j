# Passive quadratic prediction: exact seven-calibration completion

Status: COMPLETED / CONFIRMED / CONDITIONAL L1; independent proof with an
exact computational audit. This result proposes one theorem-grade row for
a later reviewed Canon fold, not an immediate Canon status change.

Owner: A. M. Thorn. Reservation: [#1023](https://github.com/mathorn1973/twist-j/issues/1023).

## Pin and completed audit

The immutable public pin is
`6e96783644224338aff00b63e2f555db27d71a2d`, a direct child of the declared
v87 baseline. PREREG.md, PROOF.md and verify.py were published, read back
byte for byte and fetched before the first formal Linux execution.
[RUN.md](RUN.md) records the file hashes and neutral execution metadata.

All seven preregistered groups passed. The run exited zero, wrote no stderr
and produced exactly the 461 bytes in [EXPECTED.txt](EXPECTED.txt), SHA-256
`62f0748afdfe00a2f898d0f0516d3576d67c3a6eba5d0dbf57342fa0e2600f5c`.
No scientific falsifier fired. No pinned source, coefficient, scope or
threshold changed. Public x86_64 and aarch64 checks must reproduce these
same bytes; their completion is an external PR-head check, not a conclusion
drawn from the local run metadata.

## Scientific conclusion

A homogeneous rational quadratic factors through the finest selected
passive record exactly when its coefficient form lies in the three-atom
span. The equivalence already holds on the actual balanced source grid.
Even knowledge of the full passive-record probability law leaves seven
independent quadratic directions undetermined. For arbitrary preparation
ensembles, seven additional fixed rational scalar expectations are therefore
necessary and sufficient to identify all homogeneous quadratic means.
The necessity includes arbitrary fixed scalar functions as calibrations.

The seven frozen off-origin first-step deposited-energy functions attain
that bound. Their scaled determinant is
`-176542678173169038600000000000000000`; their unscaled forms have rank ten
together with the three atom forms. Independent stencil and field-level
calculations confirm all response rows.

The explicit omitted-origin identity holds coefficient by coefficient,
and for all 625 balanced vectors. Its additive prediction-error bound is

    |delta D0| <= A epsilon_atom + B epsilon_deposit,
    A=84944136907/246938981400,
    B=751911453/185050550.

This is sharp on the unrestricted componentwise additive error box. A
comparison to an observed origin also includes the origin's own error.
The last coefficient vanishes: this one target uses six deposit readings,
whereas seven is the minimum for the universal quadratic target class.
Identical finest records at e0 and e1 still allow unequal origin deposits
and distinct first-step counts at the frozen threshold.

The proof supplies the universal factorization and identifiability claims,
including normalization and positive ensemble witnesses. The exact verifier
audits their finite constraints, response construction, determinant,
polynomial reconstruction, error certificate and source collision.
Previously exposed exploratory results remain disclosed in the preregistration.

## Boundary and disposition

The additional functions are separate calibrations, not extra passive-record
fields. All results use a stipulated source injection, reservoir context
and mathematical ensemble law. They do not identify threshold laws in
general or derive an apparatus, occurrence law, empirical frequency or
cross-layer lift. The physical QDD obligations remain open. The algebraic
held-out origin is not an unseen experimental dataset.

The probe is complete and its identifier must not be reused or resumed.
Public Canon v87 remains the active authority until a separate reviewed fold.
