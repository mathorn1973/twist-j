# P-PHOTON-Z5-DUAL-WARD-CROSSCHECK-2 immutable execution pin

Status: PRE-EXECUTION / ZERO EVIDENTIAL WEIGHT

The first commit after public base
`d0bc920b27117ea4a409282e3481340f50433763` that adds this final file,
the complete twelve-file source package, `SOURCE_SHA256SUMS` and
`INPUT_SHA256SUMS` to branch
`probe/P-PHOTON-Z5-DUAL-WARD-CROSSCHECK-2` is the immutable source pin.  It
must have exactly that public base as its sole parent.  Its full commit SHA and
both manifest digests are authorized only by the exact public issue-#756 pin
receipt.

The fresh identifier was reserved at `issuecomment-5498022449`.  The consumed
CROSSCHECK-1 record, its `STOP_DUAL_MIXING`, and the successful L3/L4 mobility
qualification remain immutable.  No old dual transcript is a runtime input.

No formal L6/L8 primal or dual state was opened before this file.  Only static
checks, compilation and the frozen L3/L4 fixtures were permitted.  After
public source and receipt readback, the runner must atomically claim both

```text
refs/probe-attempts/P-PHOTON-Z5-DUAL-WARD-CROSSCHECK-2
refs/heads/probe-attempts/P-PHOTON-Z5-DUAL-WARD-CROSSCHECK-2
```

at the pin and read the public ref back before compilation or any formal
state.  An existing or failed claim consumes the authorization without data.

The sole formal command is the repository-root invocation printed in
`PREREG.md`.  Immediately after both attempt refs are claimed and publicly
read back, one 48-hour deadline begins and covers toolchain checks through
formal capture.  The command runs eight exact engine-to-reader pipelines with
four workers.  The full 2,048-frame engine streams exist only in
OS pipes.  The only persistent dual outputs are the eight allowlisted filtered
JSONL files, each at most 5,000,000 bytes and retaining exactly 128 canonical
state frames.

Allowed result additions are exactly four registered primal logs, eight
registered dual logs, `PRIMAL_RUNS.tsv`, `DUAL_RUNS.tsv`,
`OUTPUT_SHA256SUMS`, `ANALYSIS.txt`, `EXPECTED.txt`, `RUN.md` and `RESULT.md`.
Their first commit is a direct child of the pin and changes no other path.

There is no rerun, extension, replacement, seed change, output repair or
threshold movement.  No source, input, state encoding, schedule, statistic,
separation, block rule, lazy gate or terminal may change after this pin.
Only a merged, publicly read-back `DUAL_CROSSCHECK_PASS` can satisfy #757/F3;
production #742 and Public Canon v74 otherwise do not move.
