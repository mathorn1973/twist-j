# P-PENTAGON-ONLY-DILATIONS-1 result

Status: `PROVED AND AUDITED / ROUTE DEAD / CANON UNCHANGED`

## Disposition

```text
theorem:    the pentagon-tower dilation deficiency holds exactly:
            dist(g_q, span{g_(5^m): m <= M})^2 = (1/12)(1 - 1/q^2) for
            every q coprime to 5, constant in M, best approximant
            (1/q) g_1. Carried by the written proof in PREREG.md; the
            finite audit passed 6/6 with the Gram re-derived by an
            independent exact integration path.
route:      PENTAGON-ONLY-DILATIONS is dead. Restricting Nyman-Beurling /
            Baez-Duarte dilations to the pentagon tower can never reach a
            cross-prime target; the deficiency is a fixed positive
            rational per direction.
integrity:  no STOP. One formal execution, exit zero, empty stderr,
            stdout equal to EXPECTED.txt.
```

## Proposed registry consequence (a later sealed fold, not this probe)

J-LI-PENTAGON-DILATION-DEFICIENCY [T] and PENTAGON-ONLY-DILATIONS [F],
exact row texts frozen in PREREG.md, canon section 16. No live row moves;
RH stays O. The F row exists to prevent a re-attempt of a route the
arithmetic already forbids, in the same genre as
SUZUKI-LOCAL-CAPACITY-NOGO.

## Evidence boundary

Local formal leg x86_64 (Ubuntu 24.04.4 LTS, CPython 3.11.15); the
pull-request workflow supplies the x86_64 and aarch64 replays against
EXPECTED.txt, completing the repository two-architecture computation gate.
The universal statements are carried by the written proof with its two
imports named as motivation only (Nyman-Beurling, Baez-Duarte, cited not
reproved); the verifier audits finite instances and the proof's two exact
identities. Nothing here advances RH or any physical row.
