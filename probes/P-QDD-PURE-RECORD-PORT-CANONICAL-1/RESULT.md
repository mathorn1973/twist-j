# P-QDD-PURE-RECORD-PORT-CANONICAL-1 result

Status: `PROVED AND AUDITED IN THE FROZEN CLASS / PUBLIC REPLAY PENDING /
CANON UNCHANGED`

## Decision

```text
PORT-CANONICAL
```

One formal execution returned zero, wrote empty process stderr, and produced
the exact committed 25-line output with 17/17 gates passing. The accepted
verifier was not executed before its public pin and was not rerun afterward.

## 1. The L4 source is forced by the ordered-composition class

Let `W=QV` be the target-independent rank-three support and

```text
A = Q D_J Q |_W.
```

The exact compressed matrix has determinant `-1/4`, so `A` is invertible. For
one supported branch `T=OQ`, with `O` a rational `G`-orthogonal automorphism of
`W`, define

```text
S_ord = { [O A v], [A O v] : O in O(W,G)(Q), 0 != v in W }.
```

Every ordered output is a nonzero vector of `W`. Conversely, `O=I` is
admissible and for every `0 != w in W`, choosing `v=A^-1 w` gives `A v=w`.
Therefore

```text
S_ord = (W minus {0})/{+/-1}.
```

The full rational HIGH-support sign quotient is not an added coding convention.
It is exactly the common source generated by the frozen L4 comparison itself.
This is the new source theorem beyond PR #504.

## 2. The direct pure-record port has no remaining mathematical choice

Freeze the direct read-only port class by

```text
m([v]) = v^T G v,
```

with the normalized public affine quadratic form, and require `rho([v])` to be
`G`-self-adjoint, idempotent, rank one, trace one, with image `Q v`.

A self-adjoint idempotent has kernel the orthogonal complement of its image.
Thus the image and `G` determine the density uniquely:

```text
rho(v) = v v^T G/(v^T G v).
```

Hence the admissible port class is the singleton

```text
b_W([v]) = (v^T G v, v v^T G/(v^T G v)).
```

The reconstruction identity

```text
v v^T = m(v) rho(v) G^-1
```

shows that its fibres are exactly the frozen sign classes. No new dimensionless
parameter, output gauge, source-to-`K` code, or U-tail lag survives in this
class.

The scalar normalization is not chosen in this probe. Merged PR #496 proves
that the full affine system has one positive invariant quadratic ray, and the
already public trace pairing fixes the displayed normalization. This probe uses
that theorem input and proves the direct-port uniqueness after the source is
fixed.

## 3. The port is conservative over the existing finite QDD record

The public balanced carrier has a nonzero overlap with `W`. The exact audit
finds

```text
overlap vectors:      84
overlap sign classes: 42
```

On every one of those vectors, `b_W` equals the already public
`total_weight` and `density` formulas of `D_QDD_direct`. Thus the port changes
no existing finite record and introduces no numeric constant.

This is not a repair of the U-congruence no-go from PR #504. The classes are
different:

```text
#504 dynamic/static encodings: source -> K, write/encoding convention required
this probe:              L4 source -> read-only record, feeds_U=FALSE
```

The empty motor-to-tail class therefore remains empty.

## 4. The port reads the internal commutator

The verifier audits one target-independent rational reflection for which

```text
Xi = O A-A O
```

has rank two. The two ordered outputs have different pure records. The written
reconstruction theorem gives the general statement already established in the
lineage: a nonzero internal commutator cannot remain hidden from this port on
the frozen ordered-composition source.

No duplicate global commutator row is proposed here. The new results are the
source theorem and direct-port uniqueness.

## 5. Typed schema consequence

Public Canon v59 keeps the current decoder signature

```text
D_matter : dom(D_matter) subset K -> MatterData.
```

The completion contract separately provides a generic `bridge_manifest[]` with
source, target, domain, codomain, map, dependencies, layer endpoints and gate
IDs. Therefore the direct L4-to-L1 read-only map can be represented as a
separate bridge object without changing the `D_matter` source signature and
without an artificial encoding into `K`.

This does not mean the bridge is publicly adopted. The current gate table still
contains no `GATE-L4-L1-QDD-PURE-RECORD`; source and port identifiers, physical
field/equality ownership, and gate adoption remain absent. This probe changes
none of those normative files.

## 6. Target comparison and O2 boundary

Only after the source and port class are settled,

```text
P=E_low,
Q=E_high.
```

No target effect was used to define the source or choose the port.

The result removes a mathematical ambiguity left after PR #504:

```text
ordered L4 source:        canonical in the frozen class
read-only pure-record map: unique in the frozen class
finite overlap:            exactly compatible
U write/feed:               none
public gate adoption:       absent
```

Global O2 remains open. A later physical step must still justify adoption of
this direct port and the claim that a saturated physical event is complete with
respect to the full post-state record, or provide another independently
justified selector. This probe does not prove that every physical apparatus is
in the frozen rational class.

## Candidate rows

After byte-identical public x86_64 and aarch64 replay, a later separate fold may
register at most:

```text
QDD-PURE-RECORD-ORDERED-SOURCE [T]
QDD-PURE-RECORD-PORT-UNIQUENESS [T]
QDD-PURE-RECORD-PORT-CONSERVATIVE-OVERLAP [T]
```

All are restricted to the exact L4 source, sign equality and direct read-only
port class frozen by this probe.

## Evidence boundary

No Canon, registry, frontier, gate-table, workflow or release change. No
`D_matter` completion or `QUADRATIC-DECODER-DATA` move. No L5 stream or L6
measure. O1 is untouched.

```text
SAMPLING NOT PROVIDED
```
