# P-SNAP-OCCURRENCE-IDENTITY-1 result

**PROOF-SURVIVES / NON-CANONICAL L1 MATHEMATICS.**
First local exact audit: PROOF_AUDIT_PASS, 2025435 checks, no mismatches,
exit 0 and empty stderr. Required two-architecture reproduction is separate
reviewed-head evidence. Public Canon v80 and all physical owners are unchanged.

## What the proof settles

An autonomous address on the count carrier has exactly two kinds of equality
kernel: every occurrence index stays distinct, or there is a finite transient
followed by a periodic identification. Successor cancellation removes the
transient but still permits every period, including complete identification.
Thus these weak laws do not select fresh occurrence identity. The result
classifies autonomous address quotients, not arbitrary observations of a
richer source and not all possible physical event equivalences.

Append compatibility is equally insufficient to choose an exact log. Even
cancellation of a common appended symbol permits Parikh counts and phase
summaries. A stronger precise boundary works: separate the empty log, and
recover both the final payload and its predecessor class from every append.
These laws force ordinary word equality. Repeated equal payloads then occupy
distinct positions because the log contract preserves them. They need not
contain independent information, and no physical write follows from the
mathematical word definition.

For native U, the inherited exact language and recurrence theorems imply that
a fixed length-L decorated-window ID reader produces at most 20,000 L
distinct labels per trajectory, with every produced label recurring with
bounded gaps. It cannot assign a new literal ID to every accepted occurrence,
even with an infinite declared output codomain. A fixed finite apparatus
state A gives the cardinality bound |A|20,000 L, without a general recurrence
claim for added dynamics. Distinct events whose identity comes from an
external log position or relation are not excluded. The complete native
state contains the unbounded counter; explicit counter reads enlarge this
reader class and provide mathematical addresses without choosing Snaps.

Finally, the tagged hexagonal carrier has a sharp emission cost. If every
tag must be a distinct newly appended atomic entry and each tick allows
at most c entries, completing radius R requires at least

```
ceil((R+1)^3/c) ticks,  c>=1.
```

The displayed rank/unrank attains the bound: at budget one, layer r occupies
slots r^3 through (r+1)^3-1. This simultaneously realizes every radius prefix
on one mathematical counter sequence. Counts are cubic in radius and linear
in elapsed ticks. Batching attains the general budget bound. A single entry
describing a large set, or a relation reconstructed among existing entries,
is a different counting contract and need not have this many writing events.

## Surviving construction and excluded inferences

The construction supplies fresh counter addresses, recoverable formal logs
and an exact serialization of tagged geometric incidences. It excludes these
inferences within the declared classes:

- homogeneous or cancellative address update => fresh event identity;
- weak append compatibility => preservation of exact history;
- fixed finite native-window reading => endlessly fresh printed IDs;
- cubic incidence cardinality => cubic atomic writes in the same number
  of one-entry-per-tick native updates.

There is no fired mismatch against the frozen mathematical targets. The
negative conclusions are explicit nonimplication and capacity proofs, not
empirical falsifications and not new canonical F rows.

## Physical disposition and next admissible attack

The physical question remains **STOP-DEFINITION**. A new event requires a
specified event equality and emission law, and a material record additionally
requires admitted persistence resources. Counter labels or list positions
alone do not derive those physical facts. The existing conditional reservoir
account shows how one chosen interaction protocol distinguishes new lifetime
ordinals from passive rereading; its threshold, channels and fresh storage
remain premises.

The companion [incubation](../../notes/C-SNAP-OCCURRENCE-IDENTITY-N.md)
maps the next attack to the existing #539 apparatus owner: freeze one
independently motivated transition/emitter and its complete resources, then
compare repeated equal-payload interactions with repeated passive reads.
This result neither creates a competing physical schema nor derives a
universal Snap, Born law, L6 probability, or spatial dimension.

No Canon, Registry, Frontier, gate, authority, release or earlier probe is
changed. A later canonical promotion would require its own declared fold.
