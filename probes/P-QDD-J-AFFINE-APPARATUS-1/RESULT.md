# P-QDD-J-AFFINE-APPARATUS-1 result

Status: `PROVED AND AUDITED IN THE FROZEN CLASS / CANON UNCHANGED`

## Disposition

```text
class:       the complete frozen target-independent AGL_1(F_5)-covariant
             family A_JAFF contains four exact rational reversible
             system-pointer-memory couplings U_a, a in F_5^x;
target:      after the class is frozen, memory token k=2 gives
             P_2=E_low and Q_2=E_high, so all four members realize the
             same ordered QDD effects and occurrence weights;
post-state:  the four moving-branch maps are pairwise inequivalent under
             the registered K ~ +/-K post-state equivalence;
Lueder:      a=1 is the Lueder pair, but is only one of four;
involution:  even after requiring the complete coupling to be
             self-adjoint and involutive, exactly a in {1,4} survive and
             they remain two inequivalent target-realizing instruments;
extra rule:  zero moving-branch displacement, equivalently identity action
             on im(E_high), uniquely selects a=1, but this is an added
             identity/minimal-disturbance premise and is not derived from
             the frozen class axioms;
decision:    NONUNIQUE;
integrity:   no scientific falsifier fired; one formal execution, process
             exit zero, empty stderr, 11/11 gates PASS, stdout exact.
```

## Proposed registry consequence

A later sealed Canon fold, not this probe, may register at most:

```text
QDD-J-AFFINE-APPARATUS-CLASS [T]
QDD-J-AFFINE-APPARATUS-NONSELECTION [T]
```

Their scope is only the explicitly frozen class in `PREREG.md`: the rational
A_4 simplex generated by the J phase motor, its complete AGL_1(F_5) action, a
binary pointer, a five-token memory, stabilizer-average projectors, and the four
field multipliers. The result is theorem-grade inside that class.

It is not a theorem that every admissible J-native apparatus lies in this
class. Therefore blocker O2 of `QDD-INSTRUMENT-APPARATUS [O]` is not globally
closed. The probe kills this natural restricted selector route: reversibility,
full J-affine covariance, memory preservation, and even self-adjoint
involutivity do not select Lueder without an additional disturbance rule.

## Evidence boundary

This is L4 apparatus/support only. No realized event stream, sampling law, L6
measure, decoder completion, SI statement, or physical uniqueness theorem is
introduced. O1 is untouched and the only sampling statement is
`SAMPLING NOT PROVIDED`.

The local formal leg is Debian GNU/Linux 13, x86_64, CPython 3.13.5. The pull
request workflow must reproduce the committed `EXPECTED.txt` byte for byte on
GitHub x86_64 and aarch64 before the public two-architecture computation gate
is complete. No Canon, registry, frontier, workflow, threshold, or existing
probe file is changed by this probe branch.
