# P-QDD-J-CENTRALIZER-TERMINALITY-1 result

Status: `PROVED AND AUDITED IN THE FROZEN CLASS / CANON UNCHANGED`

## Disposition

```text
class:
  For every memory token, the complete rational stabilizer centralizer on the
  moving three-space is Q R direct-sum Q C direct-sum Q J, with J^2=-C.
  The exact reversible effect-preserving class is
  T(e,r,s)=eR+rC+sJ, e in {+1,-1}, r,s in Q, r^2+s^2=1.

negative route:
  The rational-circle family contains infinitely many physically inequivalent
  post-state instruments at the same ordered effects and occurrence weights.
  Reversibility, full AGL_1(F_5) covariance, memory preservation, pointer
  no-leakage, and ordinary same-outcome repeatability select nothing.
  The self-adjoint involutive subclass has four algebraic members and exactly
  two physical classes modulo T ~ +/-T.

positive route:
  Fresh-pointer ray terminality forces T=+Q or T=-Q, which is one physical
  post-state class. Strict branch idempotence T^2=T fixes T=Q. After the
  deliberately later target comparison, this is the Lueder pair.

decision:
  BIFURCATION-PASS.

integrity:
  one formal execution; exit zero; empty process stderr; 14/14 exact gates
  PASS; stdout byte-identical to EXPECTED.txt.
```

## Exact scientific boundary

The positive and negative statements are compatible rather than contradictory.

Without a terminality law, the complete frozen class is infinitely
nonselective. With fresh-pointer post-state terminality, exactly one physical
class survives. With strict branch idempotence, one algebraic representative
survives.

The probe does not derive terminality from the current public architecture and
does not adopt it as a physical law. It identifies terminality as an exact,
decision-bearing missing premise. Therefore global blocker O2 of
`QDD-INSTRUMENT-APPARATUS [O]` remains open.

Ordinary measurement repeatability is not enough. It holds throughout the
infinite family because every high branch maps into the high support. The
stronger terminality condition says that a fresh identical apparatus causes no
further post-state motion on repetition.

## Proposed registry consequences

A later sealed Canon fold, not this probe, may register at most:

```text
QDD-J-CENTRALIZER-APPARATUS-CLASS [T]
QDD-J-CENTRALIZER-NONSELECTION [T]
QDD-J-TERMINALITY-SELECTION [T]
```

Their common scope is only the exact target-independent L4 class frozen in
`PREREG.md`: the rational J simplex, the complete AGL_1(F_5) action, the full
rational orthogonal stabilizer centralizer, a binary pointer, and five-token
memory.

The first row is the class theorem. The second is the infinite nonselection
theorem and the two-class self-adjoint involutive boundary. The third is a
conditional selection theorem: ray terminality selects the Lueder physical
class and strict idempotence fixes its positive representative.

None of the three says that every J-native apparatus belongs to the class, or
that terminality follows from `J`, `Omega`, `U`, the decoder, or Nature.

## Evidence boundary

This is L4 apparatus/support only. It supplies no realized event stream,
sampling law, L6 measure, decoder completion, SI statement, Bell causal
accounting, or physical uniqueness theorem outside the frozen class.

O1 is untouched. The only sampling statement is:

```text
SAMPLING NOT PROVIDED
```

The local formal leg is Debian GNU/Linux 13, x86_64, CPython 3.13.5. The
pull-request workflow must reproduce the committed `EXPECTED.txt` byte for byte
on public x86_64 and aarch64 before the computation gate is complete. No Canon,
registry, frontier, gate, workflow, threshold, or existing probe file is
changed by this probe branch.
