# PREREG. P-TM-SYM2-REVERSAL-CLOSURE-1

Status: PIN CANDIDATE PREPARED FOR THE OWNER; FORMAL GATE NOT RUN.

This preregistration belongs to a public formal-lock issue to be opened
by the owner before the branch, and consumes the public predefinition
snapshot pinned inside this directory. The completed public antecedents
`P-TM-SYM2-MEASURE-1` and `P-TM-SYM2-SEMILINEAR-GAUGE-1` are consumed
only through their pinned public transcripts; neither old verifier is
copied, imported, invoked, or replayed.

Honesty declaration. The route of this probe is derivable by hand from
public data and was derived in the project incubation lane
(`C-TM-SYM2-REVERSAL-CLOSURE-1`; independently corroborated structure in
`C-TM-SYM2-TWOFOLD-NOGO-1`). This pin therefore claims validation under
frozen thresholds on two architectures, not blind discovery. No expected
route, translation value, transport verdict, orbit partition, or output
hash is embedded in any pinned file; both scientific routes are live and
first-class. An incubation twin of the verifier bytes was executed once
in the incubation lane as an engineering integrity check; the formal run
below remains the single authorized execution under this probe id.

## 1. Public authority and probe identity

```text
probe:                  P-TM-SYM2-REVERSAL-CLOSURE-1
branch:                 probe/P-TM-SYM2-REVERSAL-CLOSURE-1
path:                   probes/P-TM-SYM2-REVERSAL-CLOSURE-1/
owner:                  A. M. Thorn
public formal lock:     issue #138, opened before branch
definition issue / PR:  issue #136 / PR #137, merged and publicly
                        read back before the formal lock
branch parent:          d8f91078760ed904ae748883dfe2b8fd98c97af9
Public Canon:           re-read STATUS.md on public main at pin time;
                        prepared against v16 ACTIVE, content commit
                        a96f6c7a8ed63c2234977cb1c7a3432fd315bd7a,
                        Canon SHA-256 836b8d642f5209d46a5b833a3a1e7a
                        1acc14a249e83066af1adb21845242d4a9, main
                        d78f6a9; a canon advance invalidates nothing
                        here unless it touches the TM-SYM2 rows, but
                        the authority block must be refreshed
owner row:              TM-SYM2-MEASURE [H]
scientific layer:       L5 structural candidate equivalence only
excluded layer:         L6 physical measure
```

## 2. Frozen sources and verifier identity

The immutable initial pin contains exactly six files: this PREREG,
`verify.py`, `check_pin.py`, and three byte-for-byte source snapshots.

```text
SOURCE-PREDEFINITION.md
  public source:  notes/canon/P-TM-SYM2-REVERSAL-CLOSURE-1-PREDEFINITION.md
                  (published under the definition issue above)
  bytes           8125
  SHA-256         557b27c65870f25903b08c8830bfd55c0e91f5caa1221d6f2fedb2ff8e90add1

SOURCE-MEASURE1-EXPECTED.txt
  public source:  probes/P-TM-SYM2-MEASURE-1/EXPECTED.txt
  bytes           9879
  SHA-256         395209f15d0943f38b1d8af4f6d20769c5e113c65bac9455944613ab3b40726f

SOURCE-SEMILINEAR-EXPECTED.txt
  public source:  probes/P-TM-SYM2-SEMILINEAR-GAUGE-1/EXPECTED.txt
  bytes           29205
  SHA-256         47e04141e0ee0b290a4ca91b1b0b977ad2674d555ab296aaa371177f662bfd19

verify.py
  bytes           21548
  verify.py SHA-256:     03f5cf7e49b5a03ccb9ee2a0d7383737ab0f84d0d1da7c04a6c4dce481ba77ad
  command         python3 probes/P-TM-SYM2-REVERSAL-CLOSURE-1/verify.py
  arguments       none
  implementation  Python standard library only; exact arithmetic only
                  (int, Fraction, Q(sqrt5) as Fraction pairs); no float
```

The first snapshot is the frozen definition surface of this probe. The
second is inherited evidence for the sealed facts `|Sel_class| = 48`,
gauge order 12, free action, four blocks of 12 with complete published
membership. The third is inherited evidence for the sealed facts
`|Gamma_sl| = 24`, exponent-one count 12, coset character `(1,1)`,
residual invariant `chi_Q chi_F`, two selector orbits of 24. Inherited
facts are integrity constraints, not outputs of this probe.

## 3. Frozen equation and carriers

Exactly as in the pinned predefinition snapshot: the six-word carrier
`W3` with negation `N` and reversal `R(abc) = cba`; the registered six
lines over `K = Q(sqrt5)` with `sigma_line = (v1 v2)(v3 v4)(v5 v6)` and
the oriented pairs of the semilinear antecedent; the 48-member
equivariant selector class; the characters `chi_Q` (pair-permutation
sign) and `chi_F` (flip parity) on `W = Cent(sigma_line)`; selector
fibers through the free torsor structure; precomposition by the three
structural involutions `N`, `R`, `N o R`; transports
`s o b o s^(-1) in W`; realizability at Galois exponents `e in {0,1}`
over `K` by the incidence system `B tau^e(v_i) = lambda_i v_(p(i))`;
the extended equivalence generated by `Gamma_sl` postcomposition and
`R` precomposition. Field enlargement beyond `Q(sqrt5)` is out of
scope. No gauge is adopted by any outcome.

## 4. Frozen systematics and complete scan

The verifier performs one self-contained deterministic calculation in
frozen stage order, each stage gated before the next:

```text
A  drive and carrier: popcount drive equals the substitution fixed
   point (4100 letters); centered windows and length-3 factors both
   equal W3; N, R involution and commutation facts; palindrome set;
   child maps grounded at every counter m in [1, 2048]; the reversal
   intertwiner R o E_even = N o E_odd o R; the frozen transfer, its
   rank-5 certificate, the uniform stationary law, and its N- and
   R-invariance.
B  sources and class: snapshot byte identity; the four published blocks
   parsed with complete membership; the complete 46656-map function
   space scan yielding the class; class equality with the block union;
   class stability under R- and N-precomposition; two independent fiber
   routes agreeing on all 48; each block a single fiber, four distinct
   fibers.
C  field scan: exact Q(sqrt5) reconstruction of the six lines; squared
   cosine 1/5 on all fifteen pairs; the 48-element centralizer; all 96
   incidence cases decided by TWO independent internal methods, a
   projective four-line frame construction and a full RREF nullspace
   with determinant-polynomial grid witness on {0,1,2,3}^d, with
   mandatory per-case agreement and no frame degeneracy; exponent-zero
   image equal to ker(chi_Q) cap ker(chi_F) of order 12; exponent-one
   image matching the inherited count with one common character equal
   to the inherited coset character; Gamma_sl order and selector orbit
   partition matching the inherited fields, orbits constant in
   chi_Q chi_F.
D  the information-bearing computation: the exact identity
   s o N = sigma_line o s; uniform fiber translations of N, R, NR with
   the additivity identity t_NR = t_N + t_R; transports centralizing
   sigma_line, carrying the translation character, with a uniform
   realizability verdict per involution; the complete orbit partition
   of the extended equivalence; the route derived by the frozen table
   of section 5.
```

No filtering, no candidate preselection, no randomness, no clock
dependence, no network, no filesystem write, no float.

## 5. Frozen route derivation

Write `t_R` for the computed fiber translation of R-precomposition and
`toggle = t_R[0] + t_R[1] mod 2`. The route is derived, not selected:

```text
toggle = 1  ->  ROUTE: REVERSAL-TOGGLE
                consistency required: one extended orbit of size 48
toggle = 0  ->  ROUTE: REVERSAL-SILENT
                consistency required: two extended orbits of size 24
```

A translation-orbit inconsistency is STOP, not science. The transcript
carries one scientific block with the fixed field grammar

```text
SCIENTIFIC_RESULT_BEGIN
ROUTE: <REVERSAL-TOGGLE | REVERSAL-SILENT>
TRANSLATION_N: (a,b)
TRANSLATION_R: (a,b)
TRANSLATION_NR: (a,b)
TRANSPORT_N: <REALIZABLE-E0 | REALIZABLE-E1 | NONREALIZABLE>
TRANSPORT_R: <REALIZABLE-E0 | REALIZABLE-E1 | NONREALIZABLE>
TRANSPORT_NR: <REALIZABLE-E0 | REALIZABLE-E1 | NONREALIZABLE>
EXTENDED_ORBIT_COUNT: <1 | 2>
EXTENDED_ORBIT_SIZES: <48 | 24,24>
RESULT: PASS
SCIENTIFIC_RESULT_END
```

All angle-bracket values are computed fields. Both routes are valid
exit-zero scientific results; neither is expected by any pinned byte.

## 6. Frozen failure thresholds

Any one of the following is STOP, not a scientific route and not
permission to edit or rerun this probe id:

- missing, additional, renamed, or changed initial-pin file; snapshot
  byte or SHA-256 mismatch; non-ASCII or CR bytes in a pinned file;
- unresolved or mismatched verifier hash; static pin gate failure;
- forbidden import, external process, network access, filesystem write,
  argument reading, randomness, clock dependence, or floating point;
- any stage A, B, or C structural certificate failure, including any
  disagreement between the two realizability methods on any of the 96
  cases, or any mismatch against an inherited published field;
- nonuniform fiber translation or transport verdict across the class;
  transport character different from its involution's translation;
  failure of t_NR = t_N + t_R; a transport outside the centralizer;
- translation-orbit inconsistency under the frozen table of section 5;
- nondeterministic or malformed transcript, missing or multiple
  scientific block, nonzero formal exit, nonempty formal stderr,
  timeout, or cross-architecture byte mismatch.

STOP carries no conclusion about the reversal question. A frozen-file
defect invalidates this probe id; thresholds, grammar, equations, and
carriers never move after the public pin.

## 7. Action layer and scope firewall

This probe is confined to a candidate equivalence enlargement on the
already frozen L5 selector carrier. No outcome: edits any canon table
or release metadata; changes `TM-SYM2-MEASURE [H]` or the published N2
and SEMILINEAR-DOUBLE records; adopts `R`, `N`, `N o R`, or `Gamma_sl`
as gauge; turns domain negation into gauge; selects a selector or
character fiber; claims microscopic canonicality; derives physical
probability, a Born halving, or any L6 measure theorem; or decides the
owner fork between quotienting the reading orientation and carrying it
as a physical datum. Any adoption or Canon treatment is a separate
owner decision and separately reviewed fold after the result.

## 8. Static pin gate and formal execution budget

Before pin publication, `check_pin.py` must exit zero from the
repository root: inventory of exactly six files, snapshot hashes,
ASCII/LF discipline, resolved verifier hash matching the pinned bytes,
import whitelist, no float constant, no argument reading, probe-local
reads only, both route names present with no expected-route literal.
The static gate reads bytes and parses source only; it must not import
or execute the verifier.

The pin commit, all six file SHA-256 values and byte counts, and public
readback are published on the formal-lock issue before execution. Only
after that readback is exactly one formal scientific execution
authorized:

```text
public platform field:  neutral Linux distribution/version only
public architecture:    aarch64
formal command:         python3 probes/P-TM-SYM2-REVERSAL-CLOSURE-1/verify.py
arguments:              none
environment:            LC_ALL=C LANG=C PYTHONDONTWRITEBYTECODE=1
                        PYTHONHASHSEED=0 TZ=UTC
external timeout:       600 seconds
deterministic runs:     exactly 1
```

`RUN.md` records UTC start/finish, neutral platform, architecture,
Python version, detached commit, pre/post clean state, command,
environment, exit, exact stdout/stderr bytes and SHA-256, LF/CR/final
byte metadata, all pinned hashes, the route, and
`deterministic_executions: 1`. No machine nickname or private path.

Only after the single aarch64 run may `EXPECTED.txt`, `RUN.md`, and
`RESULT.md` be added. The pull request changes only this probe
directory. The required clean GitHub Linux/x86_64 replay must use
identical pinned hashes, exit zero, write empty stderr, and reproduce
stdout byte for byte. After the pin there is no amend, rebase, squash,
force-push, threshold repair, or rerun; the branch may merge only by
merge commit. Either valid scientific route is preserved first-class.
