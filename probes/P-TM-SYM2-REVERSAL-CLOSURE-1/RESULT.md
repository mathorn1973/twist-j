# P-TM-SYM2-REVERSAL-CLOSURE-1 result

Status: SCIENTIFIC RESULT; TWO-ARCHITECTURE COMPUTATION GATE PASS;
PUBLIC CLAIM UNCHANGED

## Recorded decision

```text
route:                 REVERSAL-TOGGLE
exit:                  0
stderr:                empty
translation N:         (0,1)
translation R:         (1,0)
translation NR:        (1,1)
transport N:           NONREALIZABLE
transport R:           NONREALIZABLE
transport NR:          REALIZABLE-E1
extended orbit count:  1
extended orbit sizes:  48
result:                PASS
```

This is a valid positive classification of the preregistered candidate
extended equivalence. It is not a normative gauge adoption and it changes
no public claim.

## Exact classification certificate

All 29 certificates passed. The verifier reconstructed the frozen drive and
transfer, the complete 48-member selector class and its four character
fibers, and the inherited 24-element semilinear image with two selector
orbits of size 24. It decided all 96 exact incidence cases independently by
projective-frame and RREF-nullspace methods, with agreement on every case.

Precomposition by N, R, and NR translates every selector fiber uniformly:

```text
t_N  = (0,1)
t_R  = (1,0)
t_NR = (1,1) = t_N + t_R
```

The corresponding N and R line transports are not realizable by either
frozen projective exponent over `Q(sqrt5)`. The NR transport is realizable
at exponent one. These verdicts are uniform on the complete selector class.

Since `t_R = (1,0)`, reversal toggles the residual invariant
`chi_Q chi_F`. The frozen route table therefore yields
`REVERSAL-TOGGLE`, consistently with one orbit of size 48 under the
candidate extended equivalence generated by `Gamma_sl` postcomposition and
R precomposition.

## Scientific meaning

Within the preregistered candidate extended equivalence, the residual bit is
a reading-orientation bit: reversing the drive reading toggles it, and the
extended action dynamically closes the two semilinear selector orbits into
one orbit of 48.

This does not make reversal a postcomposition realization and does not
adopt it as gauge. In particular, the nonrealizable R transport and the
positive reversal-toggle classification answer distinct questions and are
mutually consistent.

## Immutable pin and formal evidence

```text
public lock:           issue 138
definition issue:      issue 136
definition PR:         PR 137
parent commit:         d8f91078760ed904ae748883dfe2b8fd98c97af9
pin commit:            00235474b2b826e3f2ddd08bcfde1f42a2a3e6b1
PREREG.md SHA-256:     7c592e5ccd689e314c50fc9020b400ab78320c8ea660bcf35d8c624afbde2c82
verify.py SHA-256:     03f5cf7e49b5a03ccb9ee2a0d7383737ab0f84d0d1da7c04a6c4dce481ba77ad
check_pin.py SHA-256:  b34f20b33baeb3711d877a91a7d743f8545210f5b7370b56a5f58d62841e4f8b

aarch64 platform:     Ubuntu 24.04
aarch64 Python:       Python 3.12.3
aarch64 checkout:     clean, detached at the exact public pin
aarch64 executions:   1
aarch64 exit/stderr:  0 / 0 bytes
x86_64 workflow run:  30031502906
x86_64 workflow job:  89288896540
x86_64 tested merge:  154c346fc6bfce1f5be363bd8dd44903d0b57235
x86_64 platform:      Ubuntu 24.04.4 LTS
x86_64 runner image:  ubuntu-24.04 20260720.247.2
x86_64 runner:        2.336.0
x86_64 Python:        CPython 3.12.13
x86_64 exit/stderr:   0 / 0 bytes
x86_64 byte identity: PASS
stdout SHA-256:       687174ea019ba5aa66ad119f581e227ac863e3dbc078312e3d6aa254dab2ea1a
stdout bytes/lines:   3016 / 41
stdout CR/final byte: 0 / 0a
result:               PASS / REVERSAL-TOGGLE

architecture gate:    PASS
```

The complete neutral metadata and exact raw stdout are public in issue #138
comment `5061491530`. `EXPECTED.txt` is byte-identical to that raw stdout.
The pinned six files remain unchanged.

## Scope firewall

This result classifies a candidate enlarged L5 equivalence only. It does not
retroactively replace the frozen projective-linear gauge, change the
published N2 or SEMILINEAR-DOUBLE records, or normatively adopt R, N, NR, or
`Gamma_sl` as gauge. It does not select a selector or claim microscopic
canonicality.

The result supplies no L5-to-L6 bridge, physical probability theorem, Born
halving, or physical measure. `TM-SYM2-MEASURE` remains `[H]`. Whether the
reading-orientation bit is quotiented or retained as a physical datum is
deferred to a separate owner decision and separately reviewed fold.

Field enlargement and the incubation no-go robustness question, including
any claim over `Q(zeta_5)`, are outside this probe's evidence and scope.
No Canon, registry, frontier, dependency, gate, status, release, or
authority file is changed by this evidence-only record.

## Architecture gate

The sole formal aarch64 leg passed. The first clean GitHub Linux x86_64
replay of the identical pinned verifier also passed at tested merge commit
`154c346fc6bfce1f5be363bd8dd44903d0b57235`: exit zero, empty stderr, and
byte-for-byte reproduction of `EXPECTED.txt`. The two-architecture
computation gate is therefore PASS. The final-head policy workflow remains
the merge-eligibility check for this evidence update.
