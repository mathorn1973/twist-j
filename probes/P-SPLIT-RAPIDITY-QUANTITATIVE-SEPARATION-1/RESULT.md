# RESULT P-SPLIT-RAPIDITY-QUANTITATIVE-SEPARATION-1

## Local result

```text
local_status: PASS
probe_status: SURVIVED
checks: 18 of 18 PASS
fired_falsifiers: none
stop_conditions: none
reproduction_status: PASS
```

The one post-pin local execution returned 0, wrote no stderr, and reproduced
the frozen 1110-byte stdout exactly. The result audits finite exact carriers
for two separately decided written theorems; their universal quantifiers are
carried by the proofs in `PREREG.md`, not inferred from the finite census.

## Quantitative separation result

The exact audit survived effective-vector merging, both finite sharpness
witnesses, determinant nonvanishing, nearest-channel uniqueness, the exact
metric identity, gauge invariance, orientation reversal, and the independent
relation/tie breaker. The positive-norm controls retain the parity-sensitive
absolute lattice condition

```text
n even: |D_c| in sqrt(5) Z_{>0}
n odd:  |D_c| in Z_{>0}.
```

The determinant itself is not required to be positive; the `491/1429`
ordinary control includes the valid value `-182 sqrt(5)`. Gate G6 audits the
mixed-sign finite norm/lattice census and exact-metric numerators. N2 and N3
supply the explicit positive-norm `491/1429` controls; the universal
positive-norm parity statement is carried by the proof in `PREREG.md`, not by
the finite audit.

For the uniform positive-norm `421/431` sum channel, the retained computed
control is

```text
d_L  = 0.0011737895036417...
d_2L = 0.4800380355559618...
```

No mixed-sign decimal comparison is imported into this result.

## Fejer Gram result

The audit survived the `P(c) <= X^2` two-address budget, normalized Fejer
weights, the two-points-per-half-open-shell count, the singleton identity, and
the exact normalization STOP. The written Schur proof therefore retains

```text
||A_K - I|| <= pi^2 L^2 / (12 (K+1)^2 delta_A^2)
             <= pi^2 L^2 / (12 (K+1)^2 asinh(1/(2X))^2),
```

with the singleton handled separately and the uniform asymptotic conclusion
stated over families with `K(X)/X -> infinity`.

The `491/1429` rungs `22` and `182` are only the ordinary sign-channel
controls. The doubled-phase normalization falsifier is the effective vector
`(2,2)`, the translate `phi^-3 x^2`, trace `29`, and numerator `841`. It shows
that the unchanged `X` budget cannot be reused. The corrected integral phase
budget is `P(nu c)=P(c)^|nu|`.

## Public reproduction and rows earned

The required GitHub aarch64 job reproduced the pinned verifier and the same
stdout SHA-256 byte for byte. With the accepted local x86_64 leg, the public
two-architecture reproduction is complete. The written proofs and their
finite audit support these separately registered theorem rows:

```text
SPLIT-PRIME-RAPIDITY-QUANTITATIVE-SEPARATION  T  10. Relativity as counting
SPLIT-RAPIDITY-FEJER-GRAM-BOUND               T  10. Relativity as counting
```

The finite audit is not proposed as an additional `C` row.

## Firewall

This result introduces no inert-prime, ramified-prime, Hecke, completed-zeta,
RH, zero-simplicity, decoder, measure, physical, SI, or L2--L6 conclusion. It
does not identify the abstract finite characters with any external mode
tower. A provenance or architecture mismatch is integrity STOP, not a
scientific falsifier.

No Canon, registry, frontier, changelog, hash, release, or public status is
changed by this probe result. The required clean GitHub aarch64 job and the
accepted local x86_64 leg reproduced the pinned bytes on two architectures.
