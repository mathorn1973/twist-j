# P-DQRC-INTRINSIC-SELECTION-1 result

Status: `AUDIT-CONSISTENT / REPARAMETERIZATION-ONLY`

## Disposition

```text
intercept finite audit:  AUDIT-CONSISTENT
beta route:              REPARAMETERIZATION-ONLY
integrity:               no STOP
```

The single owner-authorized formal execution used the publicly pinned
verifier, exited zero, wrote no stderr, and produced the exact 596-byte stream
in `EXPECTED.txt`. Its frozen finite box returns

```text
A_absolute in j=0..32 = {0}
A_fixed    in j=0..32 = {1}
agreements(w,u^[0]; first 40 positions) = 16
```

The written proofs in `PREREG.md`, not the finite search, carry the complete
one-sided word identities and the two universal selector classifications:

```text
u^[0] = 0w,
u^[1] = w,
A_absolute = {0},
A_fixed = {1}.
```

These are two exact but incompatible normalization rules. The result does not
choose between them and does not classify the dense family of raw phases.

## Scope boundary

The beta route remains `REPARAMETERIZATION-ONLY`. Choosing a determinant-line
scale merely rewrites `beta=c^2`; this probe does not select `c` or `beta`.
`DQRC-H-COEFFICIENT-NONSELECTION [T]` remains controlling and unchanged.
The QPAIR coefficient remains a read-only sentinel, the silver frequency is
not independent evidence, and `F-DQRC-ANTIFIT` is neither narrowed nor lifted.

No physical origin, pre-step or post-step state, clock, decoder, apparatus,
event stream, causal claim, CHSH derivation, or layer lift is obtained. No
`PROMO-*` package is authorized. This probe changes no Canon, Registry,
Frontier, workflow, gate, or existing probe file.

## Evidence boundary

The local formal leg is x86_64 on CPython 3.12.13. In workflow run
`32295818148`, native aarch64 job `96206737767` and x86_64 job `96206738018`
both reproduced the pinned verifier SHA-256
`226824dbc053acd8f41517f5f5103697509172519ea483055e2fd49711e7062f` and
stdout SHA-256
`67eeba11fccc240d7da681357f72620adc741e854165b4b2657b51059bf5342e`
byte for byte. Aggregate job `96206816211` completed the repository
two-architecture computation gate. The public workflow replay remains an
audit of the finite verifier; it cannot add physical meaning or strengthen
the written theorem's declared L1 scope.

No public status is earned by this probe-only record. Any later Canon fold
requires separate review of the self-contained proofs and a separately sealed
content change.
