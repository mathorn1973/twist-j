# P-THORN-PLENUM-QUADRANT-CHARACTERIZATION-2 result

Status: **candidate-T / L1 / THORN-PLENUM-QUADRANT-CHARACTERIZATION CONFIRMED / PUBLIC CANON STATUS UNCHANGED.**

The first formal execution of the immutable public successor verifier exited
zero, wrote empty stderr, and produced the exact committed `EXPECTED.txt`
bytes. All twelve frozen gates passed. No scientific falsifier fired and no
carrier, equation, embedding convention, sign convention, ownership boundary,
or interpretation moved after the pin.

The predecessor `P-THORN-PLENUM-QUADRANT-CHARACTERIZATION-1` remains
`ABANDONED`: its sole formal invocation stopped at G12 because the frozen
scope needle crossed a physical newline. It has no completed formal gate, no
`EXPECTED.txt`, no `RUN.md`, and no scientific conclusion. This fresh
successor changes only the preregistered whitespace handling of that scope
guard; the mathematical theorem and gates G02--G11 are unchanged.

## Fixed carrier and consumed source closure

Work in the declared standard complex embedding of

```text
Q(r),
r=zeta_20=exp(2 pi i/20),
i=r^5,
zeta_5=r^4.
```

For a unit-circle point `z`, put

```text
J_z=1+z^2,
N_z=J_z conjugate(J_z),
S_z=(1-z)(1-conjugate(z)),
T_z=-2 i z^2.
```

The merged source probe owns the conditional equivalence

```text
S_z=1+N_z
iff
Phi_5(z)=0
iff
z is a primitive fifth root.
```

This probe consumes that candidate-T/L1 closure and does not promote or
re-earn it. Its new result is the exact quadrant characterization after the
standard embedding and the displayed square root of minus one have been
fixed.

## Exact fixed-i quadrant table

Let

```text
phi=(1+sqrt(5))/2,
beta=phi^-1,
s_J=the positive square root of 3-phi.
```

For the four source-closure points, exactly

| `z` | `T_z` | `N_z` | quadrant |
|---|---|---|---|
| `zeta_5` | `s_J+i phi` | `phi^-2` | I |
| `zeta_5^2` | `-phi s_J-i phi^-1` | `phi^2` | III |
| `zeta_5^3` | `+phi s_J-i phi^-1` | `phi^2` | IV |
| `zeta_5^-1` | `-s_J+i phi` | `phi^-2` | II |

On all four points,

```text
|T_z|^2=4,
(Re T_z)^2=S_z,
(Im T_z)^2=N_z^-1,
Im T_z=2-N_z.
```

Consequently, with the standard embedding and `i=r^5` held fixed,

```text
S_z=1+N_z,
Re T_z>0,
Im T_z>0
iff
z=zeta_5.
```

Equivalently, inside the already-owned source closure locus, first-quadrant
positivity characterizes the public principal point and hence

```text
J_z=J,
T_z=T_pl=s_J+i phi.
```

The two signs have separate exact roles. `Im T_z>0` selects the contracting
pair `N_z=phi^-2`. Within that pair,

```text
T_(z^-1)=-conjugate(T_z),
```

so the real sign distinguishes `zeta_5` from `zeta_5^-1`.

## Galois obstruction to an intrinsic selector

Every automorphism of `Q(zeta_20)` is

```text
sigma_a(r)=r^a,
a in {1,3,7,9,11,13,17,19}.
```

In the full Galois orbit of the principal mounted point, quadrant I occurs
twice, at `a=1` and `a=7`. The second occurrence satisfies

```text
sigma_7(zeta_5)=zeta_5^2,
sigma_7(i)=-i,
sigma_7(T_pl)=2r.
```

Thus first-quadrant positivity is not invariant under the full cyclotomic
Galois group. It becomes unique only after restricting to the stabilizer of
the declared `i`,

```text
{1,9,13,17},
```

where only `a=1` lies in quadrant I. Replacing `i` by `-i` moves the
first-quadrant root from `zeta_5` to `zeta_5^2`.

The result is therefore a fixed-embedding characterization, not an intrinsic
selection of an embedding, orientation, or principal root.

## Mounted third-root guard

The exact primitive-third-root control survives both the plenum mount and
the first-quadrant signs. For

```text
z=zeta_3^2,
z+z^-1=-1,
N_z=1,
S_z=3,
T_z=sqrt(3)+i,
```

one has

```text
|T_z|^2=4,
(Re T_z)^2=S_z,
(Im T_z)^2=N_z^-1,
Im T_z=2-N_z,
Re T_z>0,
Im T_z>0.
```

But

```text
S_z != 1+N_z
```

and `Phi_5(z)` is nonzero. Hence the mount and quadrant data alone do not
select the pentagon. The full first Thorn closure remains load-bearing. The
source probe owns the scalar third-root breaker; only this exact fixed-`i`
mounted lift is new here.

## Scope and physical boundary

The theorem says only that

```text
fixed standard i
+ source closure
+ first-quadrant inequalities
```

characterize the already public principal point. It does not derive or
physically select `i`, a complex embedding, positive axes, time orientation,
chirality, the Thorn closure, a vacuum state, an action carrier, `h`, `hbar`,
a decoder, an SI normalization, or any L2--L6 object. Calling the declared
quadrant an oriented positive plenum adds no theorem without a separate
higher-layer construction.

## Status ceiling

The written proof in `PREREG.md` carries the universal result. The exact
verifier audits the finite identities, the complete Galois census, the
mounted third-root control, and the type and scope guards. Subject to the
required pull-request integrity and two-architecture checks, this supports a
later public row

```text
THORN-PLENUM-QUADRANT-CHARACTERIZATION [T], L1.
```

That row is not added by this probe. A Canon fold is a separate transaction.
Until such a fold, Public Canon v68, Registry, Frontier, and every physical
status remain unchanged.

## Pin and local run

```text
public claim issue:       #637
preregistration pin:      c389fc3259081b8ecb85f85df34e91d85f6aafb9
verifier sha256:          1a0ffa8bffd180d40edbd96ed4160a6471568dbb654c329616e66fe8fdc02986
local architecture:       x86_64
local exit:               0
local stderr bytes:       0
local stdout bytes:       490
local stdout sha256:      4d2ed30606d71799c3786bb04bdd8b4cab913a4b0c4a0691f5e4b9f7a4698255
```

The local run is one architecture lane only. The pull-request workflow
remains the required independent x86_64/aarch64 reproduction and aggregate
repository-integrity audit.
