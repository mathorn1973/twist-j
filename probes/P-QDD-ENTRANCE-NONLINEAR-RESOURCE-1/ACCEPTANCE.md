# Public acceptance

Public two-architecture acceptance pending. The completed first local
run is recorded in RUN.md. This separate record will bind the tested
public result head, both architecture jobs, exact verifier/stdout hashes
and aggregate check. It does not change the immutable scientific pin.
