# Independent written review of the entrance-resource argument

**PUBLIC; NON-CANONICAL; independent mathematical review before the
scientific pin.** The reviewed claims remain candidate-T at L1. This
review does not establish physical admission, a higher action layer, or
a new Canon status.

The reviewer read `PREREG.md`, `PROOF.md`, `CONSTRUCTION.md`,
`NATIVE-ROUTING.md` and `PHYSICAL-ORIGIN.md`, together with the accepted
entrance contract. The reviewer did not read `verify.py`, import a
scientific program, or execute a numerical or symbolic scientific audit.
The checks below are separate hand derivations. No independent stdout,
architecture result or formal computational reproduction is claimed.

## 1. Autonomous-factor obstruction

Summing the five public generator formulas independently gives the
projected updates on `(z,r)`:

```text
a: (z,r)       b: (-z,-r)      c: (2-z,-r)
d: (2-z,1-r)   e: (3-z,1-r).
```

The selector depends on `z`, so it descends to this pair globally.
All four prepared endpoints share `(z,r)=(1,0)`. Equality of this
projected state therefore persists for every common native word.
The requested entrance needs different final `r` values for LOW and
HIGH, which is impossible under unchanged native waiting.

The causal-controller extension is valid because equation (14) restricts
the actuators as well as the observations. Once an entire independent
seed and auxiliary input are fixed, the projected histories and stopping
times agree on all four inputs. Almost-sure success cannot evade the
contradiction: the intersection of four probability-one success sets is
still probability one.

Two possible overextensions were explicitly excluded before pinning:

- Merely restricting observations would not exclude a supplied
  source-sensitive actuator. Such an actuator violates the stated
  projected-transition condition.
- Arbitrary coherent interference between controlled programs is not
  covered by this classical point-transition induction.

The pair `(z,r)` is not the entire port pair `(q,r)`. In particular
`q=1-h` initially already distinguishes the labels. The theorem locates
a missing influence on the projected update, not missing information in
the complete checkpoint.

## 2. Polynomial degree and independent coherent auxiliaries

An independent short lower-bound proof is obtained by subtracting the
HIGH value. If a quadratic polynomial had the required four values,
its difference from `2` would vanish at `2,3,4`. A polynomial of degree
at most two with those three distinct roots is identically zero, in
conflict with its required value at `1`.

The cubic `h^3+h^2+h+3` has the required values on the four nonzero
labels. Adding `(t-3)(1-h^4)` changes only its value at zero, giving

```text
f_t(h)=t+h+h^2+h^3+(3-t)h^4.
```

Thus degree three on the code and degree four for the declared `f(0)=0`
extension are distinct and correct statements. Their meaning is
finite-field coordinate degree, not nonlinear evolution of quantum
amplitudes or the order of a physical interaction.

The auxiliary argument survives an arbitrary independent coherent or
mixed preparation. Pulling a diagonal wrong-port projector through a
monomial unitary gives another diagonal projector. Its expectation is
a sum of nonnegative auxiliary diagonal weights. Choose one positive
weight. Exact success on all four code inputs then forces the same
fixed-auxiliary output polynomial to have the forbidden four values.
No cancellation of auxiliary coherences can alter that conclusion.

The degree hypothesis applies to the complete endpoint map. It does
not exclude a composition of quadratic primitives whose degree grows,
or affine interventions that expose the label to native selection.
The proof also does not extend without additional argument to arbitrary
nonmonomial quantum channels.

## 3. Constant shifts and the complete shear class

Direct composition confirms

```text
g_e g_d:       (p;q,r) -> (p;q+1,r),
(g_d g_b)^2:  (p;q,r) -> (p;q+2,r+2),
g_e g_b g_d g_b = T_2.
```

Powers of this last word give every constant paired shift. This is a
positive control in the commanded word algebra, not a claim that actual
native selection executes the word or conditions its power on `h`.

At fixed `b0=q+r`, commutation with every paired translation gives
`v_b0(r+delta)=v_b0(r)+delta`. Taking `r=0` proves that every admitted
two-port permutation is `(b0,r)->(b0,r+k(b0))`. The five values of `k`
are independent, so the count `5^5=3125` is exact. These structural
conditions admit the target but do not select it. This count concerns
the explicitly defined shear family, not any other previously counted
3125-element object.

## 4. Conditional arithmetic construction

The forward work table gives `(w,v,u)=(h,h^2,h^2)`. The four balanced
port blocks therefore add the four required polynomial terms. Reversing
the work table clears `v,u`; subtracting the current transported label
clears `w` even after native waiting.

An increment applied at counter `t` contributes at completion `N` with
the factor

```text
(-1)^(N-t) (-1)^(t-n) = (-1)^(N-n).
```

This proves the common final sign for all four terms, independently of
the intervening native word. The fourteen completed blocks and their
three zero-prepared work registers thus give the claimed clean identity
on the stated whole stable sheet. Exact linear extension preserves all
sixteen code matrix units and any untouched reference.

The timing restriction is substantive: no native tick may occur between
the two unbalanced halves of a paired port block. Native waiting is
permitted between completed blocks. Neither commutation of the halves
nor their endpoint conservation supplies their physical time law.

## 5. Native routing and the exact two-tick minimum

The reviewer independently substituted all four intermediate rows into
the displayed affine map `B` and then into the actual second native
generator. The resulting points are exactly

```text
(1,0,0,0,4,1), (2,0,0,0,2,2),
(3,0,0,0,1,2), (4,0,0,0,0,2).
```

The sequential inverse formulas for `B` recover all six inputs; `A` is
an invertible shear. The four points remain distinct after every listed
prefix. Consequently the point construction gives an isometry on the
four-dimensional code with a common zero phase, without an auxiliary
state or discarded fine-label record.

The elapsed-time comparison is correct. The actual bits at counters
three and four are `0,1`. Free evolution on the starting sheet selects
`b,b`, whose composition is identity. Hence the displayed routed output
at counter five is exactly `N_(3,2)C_3`, not a target with its elapsed
ticks omitted.

The one-tick obstruction was also derived independently. If the trace
is constant along the input affine line, selection is common and all
four outputs remain affine. Otherwise the three HIGH trace outputs can
have zero second difference only in the six rows of the published
table. The `d,b,e` and `e,b,d` patterns have nonzero port second
difference. The remaining `a,c,e` and `e,c,a` patterns have nonzero
second difference under `-a+b-c+d`, since only `c` contributes its
constant `3` at the middle point. These contradictions are unchanged
by an invertible affine post-map.

Zero ticks are excluded by affineness. One tick is excluded for either
driver bit, under arbitrary affine pre- and post-maps with no additional
coordinate. Together with the explicit two-tick witness, this proves
the stated minimum in that whole mathematical class. It is not a lower
bound on physical time or on apparatuses with other admitted resources.

## 6. Physical scope and review disposition

The routed construction materially strengthens the resource statement:
the existing native selector can supply the required nonlinear label
dependence when the specified affine interventions are admitted. An
extra nonlinear `f` gate is therefore unnecessary in this comparison
family. The negative endpoint-degree theorem remains consistent because
the complete routed map is not constrained to degree at most two.

The affine interventions themselves are still added operations. They
change the state, cannot be treated as passive coordinate changes, and
are not fixed native words. Their physical carrier, availability,
duration, synchronization and complete environment remain unprovided.
The routing also perturbs the source during the entrance. At completion
the source is freely evolved and the port has its prescribed entrance
displacement; the full checkpoint is not yet restored to its free port.
That later restoration belongs to the exit-and-archive protocol.

Global unitarity of native `U` is not inferred from injectivity on the
four used code points. Nor does a conditional coherent construction
establish a realized exclusive event, an occurrence law, physical
archive persistence, reset, or `feeds_U=false`.

**Written review: PASS within the declared mathematical classes.** No
unresolved mathematical counterexample was found in the stated proofs.
The physical entrance remains `STOP-DEFINITION` under the accepted
contract. Formal execution, architecture acceptance and any later Canon
fold must be recorded separately; none is certified by this review.
