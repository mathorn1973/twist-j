# Result: native nonlinearity suffices with affine entrance routing

Status: **PROOF-SURVIVES; candidate-T; L1; NON-CANONICAL**.
First pinned local audit: **PASS 12/12**, exit zero, empty stderr.
Independent written review: **PASS**, without reading the verifier.
Public architecture evidence is recorded separately in ACCEPTANCE.md.

## Strongest constructive decision

On the full four-dimensional coherent endpoint code K_3, two actual
native selector steps interposed with the explicit invertible affine
maps A,B of NATIVE-ROUTING.md give

    F_1 B F_0 A |_(K_3) = N_(3,2) C_3 |_(K_3).

The launch is counter three, the actual driver bits are theta_3=0 and
theta_4=1, and completion is counter five. The free two-step map on the
relevant sheet is b squared, hence identity. Elapsed native ticks are
therefore included in the comparison.

No auxiliary coordinate, nonlinear f gate, inverse of native U,
measurement, postselection or discarded fine-label record is used.
Every intermediate four-point image is injective. Its linear extension
preserves all sixteen matrix units and any untouched reference with a
common zero phase. This is code-level isometry, not global unitarity of U.

The minimum number of native selector steps in the entire declared
six-coordinate, no-auxiliary class with arbitrary affine interventions
is exactly **two**. Zero steps remain affine. One step is excluded for
either bit and every affine pre/post map: its three HIGH points fail
the required arithmetic-progression identity. The complete argument
reduces to forty trace arrangements, of which six pass the trace
condition and all six fail an independent r or source-functional test.
The finite audit verifies those premises; the written argument covers
every affine map without a word-length or coefficient sampling cutoff.

This is a positive reduction of the missing mechanism. The existing
native selector can supply the nonlinear label dependence. The added
source-sensitive affine interactions A,B still require physical origin
and availability. They are state interventions synthesized for the
target, not coordinate relabellings or existing native words. Source
motion during the entrance is generally perturbed. At completion the
source follows the required free endpoint while the port retains its
intended entrance displacement. The archive exit has not yet occurred.

## Exact negative and comparison boundaries

1. The global factor (z,r) is autonomous under native U. All four code
   inputs share it, so unchanged U cannot produce the required r split.
   This remains true for the complete classical causal-control class
   whose observations and actuators both factor through (z,r), with
   arbitrary independent auxiliary input, memory and finite stopping.
   Observation of q, source-sensitive actuators and coherent interference
   of control programs change that class.
2. A complete point transformation of native-coordinate degree at most
   two cannot produce the four required port values. The result survives
   any independent coherent or mixed auxiliary state when the total
   operation is monomial. It does not exclude the routed construction:
   composition with source-dependent selection raises endpoint degree.
3. The minimum response degree is three on the code. The particular
   fixed extension f(0)=0 has degree four. These are coordinate degrees
   over F5, not nonlinear quantum dynamics or a physical interaction order.
4. Every constant paired shift already lies in the commanded word
   algebra; b,d,b,e implements T_2. Native physical commandability and
   conditioning its power on the source do not follow from that identity.
5. At a known stable sheet the control label is also
   h_n=(-1)^(n-3)(z-q-r), so the entrance target has a two-port expression.
   Preserving q+r and commuting with paired translations admit exactly
   3125 shear maps. Those structural conditions select no unique response
   and are not adopted physical conservation laws.

## Separate conditional arithmetic witness

CONSTRUCTION.md gives a second, wider-domain witness when affine field
operations, multiply-add gates and three zero-prepared work registers are
supplied. Its fourteen completed blocks implement the fixed full five-value
function and return every work register to zero. Arbitrary native waiting
between completed blocks is allowed with the exact parity compensation.
The theorem holds on the whole chosen stable sheet, including h=0 and
arbitrary initial port r. The native-routing witness above is instead
confined to the four-point code at its specified launch phase.

The arithmetic witness keeps source and trace on their free paths at
completed blocks and native ticks. It permits no native tick between
the two halves of a balanced port block. Neither construction supplies
the physical finite-duration dynamics inside an added operation.

## Evidence and reproducibility

All seven scientific files were publicly pinned at
9e9dab762d618a9138f4ab293161017b8a5b0b93 and read back byte for byte before
the first execution from a clean exact checkout. They remained unchanged.
RUN.md records the neutral local platform, file hashes and exact stdout.
The independent review did not read verify.py or run a scientific program;
it is independent proof review, not an independent numerical reproduction.

The exact audit includes all 15625 native points for the quotient,
constant-word and affine-inverse checks; all 3125 commuting port laws;
12500 stable/parity inputs for the clean arithmetic circuit; 2288
interleaving schedules and 36608 code/reference matrix units; and the
two-tick witness and forty complete one-tick trace arrangements.
Negative controls preserve the errors from obsolete cleanup, absent
parity correction, omitted nonlinear terms and a tick between port halves.
No finite audit replaces the stated universal proofs.

## Physical disposition and later fold boundary

**Physical entrance origin: STOP-DEFINITION.** The active Canon supplies
neither material q/r degrees nor independent evidence for A,B, their
physical timing, code preparation, controller/environment output and
decoupling. The exact remaining question is whether those affine
interactions, or another complete independently admitted interaction,
can be physically justified under the entrance contract.

No physical apparatus, archive, event, occurrence law, reset, SI time or
layer lift is established. No architecture change is adopted, and the
interventions do not satisfy feeds_U=false. QDD-INSTRUMENT-APPARATUS and
its O1/O2 obligations remain open. PHYSICAL-ORIGIN.md records this
disposition and distinguishes an external laboratory implementation from
derivation within TWIST-J.

A later declared fold may retain the exact two-selector-step theorem,
the class-wide origin boundaries and any necessary comparison witness
at their present L1 scope. It must not close the physical entrance owner
or recast the resource minimum as physical time. Canon v90, STATUS.md
and all normative files are unchanged by this probe.
