# P-RELATIONAL-GROWTH-SATURATION-1 preregistration

**FORMAL / PROOF-FIRST / RESULT-EXPOSED.**
Author: A. M. Thorn. Date: 2026-09-07.
Public lock: [#889](https://github.com/mathorn1973/twist-j/issues/889).
Branch: `probe/P-RELATIONAL-GROWTH-SATURATION-1`.

## Authority and prior exposure

ACTIVE Public Canon v80, public base
`e6d6fcc2a23f4277345bc684b410e69aae8d45e0`; tag target
`4577448dba85c492b27773a64e5fd557abc02b30`; content
`b00171ef21ecb0d905593224f66f5e8a0f6c28e5`; Canon SHA-256
`8b076ee3d940e4a3639d3ffca06ae86e7df69dfc87f90d06ea99d4f9fca6b66c`,
541516 bytes. The hash, tag ancestry and successful public checks were
verified. Open issues/PRs, all remote heads and relevant Canon/probe names
were scanned before this fresh identifier was claimed.

The earlier pair-valuation census, canonical finite native-word class,
FIRED-COMMUTATOR-NOGO and TIME-CUT-READING are inputs. This probe does not
reopen their scopes, take over a curvature/Hodge physical owner, or share
the new native-occurrence lane #890. The mathematical constructions and
counterexamples here were known analytically before execution; there is no
blind discovery or historical priority claim. Independent analytic review
checked the oscillating-shell and triangular-ball witnesses before pinning.

Commit and push PREREG.md, PROOF.md and accepted verify.py together, then
read back and match their public bytes at that exact commit before the first
formal run. No verifier execution/import precedes that pin. Static parsing
is permitted. Never amend, rebase, squash, force-push, rename, reuse or repair
the accepted verifier after pinning.

## Field 1: targets and exact falsifiers

The full definitions and proofs are frozen in PROOF.md. Targets:

1. Exact binomial antidifferentiation and the tagged accumulation formula
   for injection-stable equality-pattern valuations. For pair weights
   a,b>=0, V_n=a n(n+1)/2+b n(n+1)(n-1)/3; b>0 gives cubic growth,
   b=0<a quadratic, and the zero valuation has no normalized exponent.
2. The strictly increasing integer V_n=n^3+(-1)^n n^2+n, V_0=0, has
   V_n~n^3 but local shell ratio one on odd n and limit five on even n.
   Polynomial counts, the stated local-error criterion and eventual shell
   monotonicity give the corrected ratio theorem. Macroscopic dilation and
   logarithmic growth estimates follow from the original asymptotic alone.
3. Finite semantic word balls saturate by at most reachable cardinality
   minus one, with exponent zero; their tagged accumulation is eventually
   linear. This applies to the literal finite native group/operator carriers,
   not to full U with its unbounded counter.
4. The registered fired translation vectors form a field basis pair plus
   their sum. Under the declared six-step word metric, native balls are
   1,7,19,25,25,... and cumulative counts are 25n-23 for n>=2. The explicitly
   chosen signed integer lift of b,d,e instead has derived lattice
   L=Zt1+Zt2, kernel 5L under reduction, triangular balls 3r(r+1)+1 and
   tagged accumulation (n+1)^3. Both carrier types and equalities are fixed.
5. The silent GROUP commutator [a,c] has displacement v(r) as displayed,
   order five on each native orbit and infinite order on each literal Z
   orbit. Its balls are min(2n+1,5) and 2n+1 respectively, with the stated
   linear and quadratic tagged accumulations. It is not K_hist or an
   additive Koopman commutator.
6. Changing radius by n=floor(m^alpha) changes the polynomial growth
   exponent by alpha; comparison within positive constant linear multiples
   preserves logarithmic growth. Thus a spatial radius cannot be implicit.

Exact admitted counterexamples falsify their target. The oscillating-shell
sequence and native cumulative count 52 instead of 64 are predicted
counterexamples to the stronger proposed inferences, not failed controls to
be discarded. No physical d=3 claim or Born probability is being tested.

## Field 2: accepted code and bounded audit

Command from repository root:

```text
python3 probes/P-RELATIONAL-GROWTH-SATURATION-1/verify.py
```

The accepted standard-library Python verifier in this pin reads no data,
repository files, network, randomness or clock. It uses integer and rational
arithmetic. Frozen finite audits:

- Binomial identities for n=0..64 and r=0..8, with empty sums and out-of-range
  binomial values. Pair weights a,b=0..7 and n=0..64, comparing direct layer
  counts/accumulation with the closed formula and complete-pair square sum;
  falling-factorial accumulation for r=1..8 and finite differences through
  fourth order check the pair family's degree and leading coefficient.
- Counterexample at n=1..256: strict positivity, exact increments, exact odd
  ratio and even ratio/error bound. All-size limits come from PROOF.md.
- Independent integer-lattice BFS through radius 16, compared with the
  triangular norm, exact ball/shell counts and cubic tagged accumulation.
  Independent quotient BFS through radius 16, compared with 1,7,19,25 and
  the native cumulative formula. Basis/reduction over coefficients [-8,8]^2.
- Native and signed integer b,d,e commutator formulas on all six-coordinate
  points in {0,1,2,3,4}^6, with all five native generators checked as
  involutions over Z and F5 on that cube. Explicit rank certificate and
  conjugation of all 289 lattice combinations with coefficients [-8,8]^2
  by b,d,e, evaluated at zero and the six standard basis points. No enumeration of Gamma,
  AGL_6(F5), all curvature operators or a physical spatial class.
- The silent a,c group-commutator displacement on the same complete finite
  coordinate cube, and native/integer iteration controls for fiber r=-3..3
  and radii zero through eight. Compare orbit balls and tagged accumulations.

The universal statements rest on the written proofs. Checking a finite
cubic-looking prefix is explicitly not an extrapolation rule.

## Field 3: carrier and data

Finite equality-only products with fixed weights, tagged disjoint unions,
integer sequences and binomial polynomials, finite semantic groups/graphs,
the inherited native affine maps on F5^6, and the SAME displayed signed affine
formulas interpreted on Z^6 as an explicit extra mathematical lift. Their
specified translation/cyclic orbits and fixed symmetric word metrics are the
counted objects. There is no experimental dataset, fitted coefficient or
physical length unit. The external monotone-density reference is contextual;
the proof is provided here and no external payload enters the verifier.

## Field 4: systematics and execution controls

Use Linux or Linux-compatible WSL with LC_ALL=C, LANG=C, TZ=UTC,
PYTHONHASHSEED=0, PYTHONDONTWRITEBYTECODE=1. Python >=3.10, standard library;
CI uses Python 3.12. Record the actual platform/interpreter. Require exit 0,
empty stderr, deterministic UTF-8 stdout and a 600-second process ceiling.

Controls separate zero from normalized counts; growth asymptotics from local
differences; ball volume from tagged repeated-ball accumulation; semantic
points from syntactic words; field equality from an integer lift; fired from
silent channel; group from additive/operator commutators; algebraic vector
dimension from geometric growth; and finite native checkpoints from complete
U. The integer lift has nonzero piston translations that vanish modulo five.
No coefficient, carrier, phase convention or radius is chosen after the run.

## Field 5: threshold and disposition

Zero exact mismatches. Preserve admitted mathematical counterexamples.
Execution/source/custody defects, timeout, nonzero exit, stderr or unmatched
bytes are integrity STOP, not physical falsification. Never repair accepted
code under this identifier. Append exact EXPECTED.txt, RUN.md and RESULT.md
after the completed first run. Both required x86_64 and aarch64 jobs must
replay the same pin and exact stdout. Passing software checks do not create
a canonical status; proofs and finite audits have distinct roles.

## Field 6: action layer and physical boundary

Action layer: **L1 exact mathematics only**. No physical spatial carrier,
radius, geometry decoder, dimension, continuum limit, metric, manifold,
curvature, J projection mechanism, Born rule or L2-L6 bridge is supplied.
The finite-native asymptotic route is rejected only at its literal scope.
A different unbounded carrier requires its own independent definition and
compatibility maps. The broad physical hypothesis remains STOP-DEFINITION.

Canon/Registry, frontier, gates, dependency files and release remain unchanged.
The accompanying NON-CANONICAL geometry note may summarize the completed
proof/evidence without promoting a status or altering any previous probe.
