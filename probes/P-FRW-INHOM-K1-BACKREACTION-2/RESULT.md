# RESULT. P-FRW-INHOM-K1-BACKREACTION-2

```text
Status: ABANDONED
Scientific run: NONE
Exact stdout: NONE
RUN.md: ABSENT BY DESIGN
EXPECTED.txt: ABSENT BY DESIGN
Owner status move: NONE
```

The successor preregistration and verifier were publicly pinned at

```text
bb224f7e916535f9480cd1d57a218e354dd43419
```

before any scientific execution.

The mandatory byte readback performed immediately after the pin found a hash
mismatch between the verifier SHA-256 declared in `PREREG.md` and the verifier
bytes actually committed through the GitHub contents transport. The public
`verify.py` Git blob is

```text
34a26da1a7ae50622258ab26b081350de5297961
```

whereas the preregistration declares the separately prepared accepted local
file SHA-256

```text
b8c418c76bb0a0e4dc909cb21204ef5003ca6815981c8a051d0de29e1461fd73.
```

Because the accepted verifier bytes and the public pinned verifier bytes are
not the same object, repository policy requires STOP. No attempt was made to
reinterpret the public bytes as the accepted verifier and no scientific
invocation occurred.

No equation, threshold, carrier, coefficient, source definition, time
placement, equality, or scientific gate was evaluated. This is a transport
failure before science, not evidence for or against the construction.

The immutable `PREREG.md` and `verify.py` remain unchanged. This identifier is
consumed and must not be reused, renamed, resumed, amended, or repaired. A
successor must use a new identifier, name both abandoned predecessors, preserve
the frozen scientific target and verdict protocol, and establish acceptance
against the exact public verifier bytes read back from its own pin before any
scientific execution.

This abandonment changes no Canon, Registry, Frontier, gate, `FRW-INHOM`,
`TT-SOURCE`, or `TT-VECTOR-STATE-NORMALIZATION` status.