# Result

Status: `ABANDONED`.

```text
probe            P-PAULI-CARRIER-ALGEBRA-1
public lock      issue #296
formal gate      never started
formal result    none
recorded         2026-08-24, Public Canon v62
```

## Why the gate never ran

The public branch contains exactly one unique commit, the commit that froze
`PREREG.md` and the accepted verifier. It was never advanced with a formal
execution or any run artifact. Consequently there is no exact stdout to pin,
no `EXPECTED.txt`, no `RUN.md`, no fired falsifier and no scientific result.

The surviving public record does not state a more specific reason for why
execution was not begun. This accounting closure records only the
demonstrable branch state and does not invent one.

## The identifier is consumed

`P-PAULI-CARRIER-ALGEBRA-1` is spent. It must not be reused, renamed,
repaired, rerun or resumed, and no part of this lane may be cited as
evidence. The pin and accepted verifier stay exactly as frozen; this record
is the only file added. No successor probe is selected by this record.

## Canon effect

None. No claim is registered, promoted, lowered or retired; no registry,
frontier, gate, dependency or Canon text changes; the live `H` and `O` count
is unchanged.
