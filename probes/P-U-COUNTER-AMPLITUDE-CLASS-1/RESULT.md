# Counter-amplitude classification

Status: **candidate-T, L1, NON-CANONICAL until a separate Canon fold**.
Verdict: **COUNTER-AMPLITUDE-CLASS PASS**. No preregistered falsifier fired.
The first pinned local verifier and independent breaker both exited zero
with empty stderr. Public architecture acceptance is recorded separately
in ACCEPTANCE.md once its exact head and jobs have passed.

## Complete reader classes

For a stipulated target set Y and bijection L, distinguish these domains:

| Domain and reader class | Complete amplitude freedom |
| --- | --- |
| Whole Omega, R(n,x)=L^n F(x) | F=g(Q(x)); arbitrary assignment on thirteen Q classes |
| Origin-zero reachable states, arbitrary counter-dependent R | R(n,x)=L^n G(ell_n(x)); arbitrary assignment on 3125 conserved chart labels |
| Current trace or selector index, arbitrary counter-dependent reader | Constant source amplitude only |
| Complete trace or selector history | Exactly the five initial-phase classes |

Q is the explicit piecewise sign quotient in PROOF.md. Its fibres are the
entire weak components of the two-control equality graph, one of size 625
and twelve of size 1250. They are not asserted to be chronological orbits
or physical superselection sectors. The independent BFS checked exact
component membership, not only these counts.

The conserved-label classification is a corollary of the already canonical
native chart/readback theorem. The new common formulation relates it to Q:

    Q=qbar ell_n,
    qbar(alpha,beta,gamma,delta,epsilon)
        =[alpha+beta,gamma+delta].

Its fibres have sizes 125 and twelve times 250. Thus the separable class
is precisely the subclass G=g qbar, strictly smaller when Y has two points.
These statements do not classify arbitrary nonseparable readers on all Omega.

## Exact common-factor boundary

The map x0 -> (ell_0(x0),z(x0)) is a bijection onto F5^5 x F5. Consequently
an amplitude recoverable both from current full-checkpoint invertible-target
prediction and from complete selector history must be constant. Nonzero
constant amplitudes are allowed. No probabilistic independence is inferred.

If G is additionally required to be additive for the declared F5^5 chart
group into a characteristic-zero vector space, then 5G(l)=G(5l)=0 forces
G=0. Arbitrary nonlinear assignments and other source algebras are outside
that additional condition.

## Scope earned and remaining selection

The proof gives the all-clock and arbitrary-target conclusions. The finite
exact verifier audits every native head, all two-control edges, the extended
chart at ticks 0..31, inverses on synchronized sheets, and the clock identity
through tick 4095. A separately pinned implementation audits graph fibres
and the chart via backward native words through tick 15. Exposed predictions
and inherited canonical results are disclosed in PREREG.md; no result-blind
discovery is claimed.

For every stipulated L, arbitrary G remains admissible. Native intertwining
therefore selects neither the J-Hodge target nor its amplitudes. A finite
image may span a six-dimensional carrier but cannot surject onto an infinite
characteristic-zero carrier. Thirteen quadratic records over six projective
directions do not identify six Hodge axes. Selecting one native generator
is not the static sum I+C^2.

No physical time, occurrence, preparation, probability or cross-layer bridge
is established. The separately owned #1069 pin is unchanged and unexecuted
by this work. Public authority remains Canon v89.
