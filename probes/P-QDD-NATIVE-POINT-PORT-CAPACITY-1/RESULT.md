# Result: nonlinear native point-port capacity

Status: `T` proposed at the exact independently reviewed L1 mathematical
scope below, subject to required public pull-request acceptance.
Outcome: `POINT-PORT-CAPACITY-CLASSIFIED; ORIGINAL-QDD-LAW-IMPOSSIBLE-IN-CLASS`.
Public reservation: issue #1042. This is evidence for a later Canon fold;
this probe changes no Canon status, physical owner or gate.

## Accepted mathematical statement

Fix one deterministic encoder `f:F5^4 -> F5^4`, one native point per
original source, common origin-zero native time, the unchanged U, and
observation through the complete `A=(q,r)` history. The result remains
valid when the observer is granted the larger complete `Q=(z,q,r)`
history. Ready and optional processor randomness have a source-independent
law, and f is independent of that randomness. The processor may use
unbounded memory, nonlinear operations, stopping, silence and memory reset
but supplies no new source-sensitive observation or intervention in U.

The complete response law factors through the encoded four-piston sum
`k(p)=sum f(p) mod 5`, so there are at most five source-conditioned
complete response laws. Pathwise equality for each common seed, integration over
its fixed law, and conditioning on the same positive-probability completion
event prove the result for all admitted history processors. Finite prefixes
are regression checks only. The underlying closed quotient and inherited
history-indistinguishability theorem are cited dependencies, not new claims.

Six exact supported original sources already require six different target
LOW values. Therefore no member of this complete specified class realizes
the original beta(p) law on every supported source. A full cycle requiring
that first event fails in this class before persistence/reset is considered.
The target is never silently changed to beta(f(p)).

The complete finite certificate gives 22 distinct target weights on the
624 supported sources; the original null has ZERO_SUPPORT and beta is
undefined there. The direct source census, independent multinomial census
and independently written coefficient-convolution census agree exactly.

For the supported-only worst-source absolute LOW-response error, the
precisely declared best comparison errors are

```text
arbitrary deterministic encoder:       9/128
permutation of all 625 source labels:  27/64
```

The free lower certificate is a six-source packing with minimum separation
9/64; the full five-block cover supplies attainment. For faithful encoders,
a beta=1 source shares a group of at least 124 supported sources, while
exactly 120 supported sources have beta>5/32. Its group therefore has
spread at least 27/32, giving the 27/64 lower bound. The self-contained
analytic high-count proof and exact census agree. Full sorted assignments
for every placement of the 124-member block and an explicit 625-image
permutation supply the upper certificate.

Both upper constructions use target-aware encoders and an explicitly
supplied common finite uniform variable to realize their rational response
centers. This establishes sharpness in the stated mathematical comparison
class, not occurrence derived from U. The arbitrary-encoder upper
certificate concerns supported beta only. The faithful subclass separately
cannot distinguish the original null's support tag from the 124 supported
labels sharing its message. No claim assigns beta(0)=0.

## Evidence, review and failure disposition

The six accepted files were frozen in public pin
`f12f5bdf6dd2c1b2259f535eaf8e5d00d8369c3c`, pushed and fetched back before
any formal scientific execution. All accepted byte identities matched
before and after both commands. RUN.md binds their hashes and the
Linux combined run; SOURCE.md binds the accepted incubation tree and
separates original custody from this new evidence.

The independent reviewer constructed and froze BREAKER.py from the exposed
preregistration without reading the author proof, author verifier or source
audit. After that code freeze the reviewer assessed the formal proof.
The standalone breaker ran first; the combined verifier then invoked the
same frozen breaker as part of its exact JSON output. Both exited zero
with empty stderr. REVIEW.md records the review scope and standalone
run; BREAKER-EXPECTED.txt preserves its exact stdout.

The combined formal output is EXPECTED.txt: 7314 bytes, one line, SHA-256
`e7f5050f75518276a6646cd5ee13e68003ce89d2deb3d57854f56aeb801df598`.
The standalone breaker output is 85 bytes, one line, SHA-256
`195587004f6c72365c3960faf4551cab171174bb439ba6bcb2650023f08d63c2`.
No assertion, hash check, timeout, zero-tolerance target or mathematical
falsifier failed. The negative exact-realization theorem is the proved
outcome, not a failed or suppressed execution.

Independent implementation and proof review support theorem-grade
acceptance at this explicit scope. The local x86_64 execution alone is not
the required two-architecture gate. Required PR jobs must replay the same
author-plus-breaker output on x86_64 and aarch64 using Python 3.12. The
acceptance check and immutable evidence history remain reviewable publicly.

## Boundaries preserved

This is not a no-go theorem for the complete physical apparatus class.
A seed-dependent encoder, independently justified source-dependent
stochastic loading, a new source-sensitive port or clock, native
intervention, different carrier/split, or coherent multipoint preparation
changes the frozen hypotheses. In particular the public Galois code is
outside the point-preparation class.

No physical preparation, complete physical class, event semantics,
occurrence/sampling law, material persistent record, native reset or
L1-to-L5/L6 gate is supplied. QDD-INSTRUMENT-APPARATUS,
QDD-TERMINAL-EVENT-SEMANTICS and QDD-INSTRUMENT-CLASS-COMPLETENESS remain O.
