# P-PHOTON-Z5-DUAL-WARD-ENGINE-QUALIFICATION-1 preregistration

Status: FORMAL ZERO-EVIDENCE ENGINEERING PREREGISTRATION / UNEXECUTED

- Owner: A. M. Thorn
- Public reservation: issue #756,
  `https://github.com/mathorn1973/twist-j/issues/756#issuecomment-5499009800`
  (2,631 UTF-8 bytes, SHA-256
  `561ceb5ae58e3565bcb75069d75aa9200a399ee7329a3d9628544bfe9b4d7df1`)
- Consumed incident:
  `https://github.com/mathorn1973/twist-j/issues/756#issuecomment-5498994728`
  (3,181 UTF-8 bytes, SHA-256
  `88e5dcb3a39ea89da1c5b32686a0b330ab53e910c39947f58d5dae2281ecd928`)
- Consumed abandoned pin:
  `b36d285b84c8339d9c0dcee08de2cb855b620aeb`
- Abandoned source-manifest SHA-256:
  `7fd9e573c00f356969c45cad4fa46ac749138647ebfe84a14fe6e3eca6efbdaa`
- Parent production experiment: issue #742
- Production firewall: issue #757, clause F3
- Branch: `probe/P-PHOTON-Z5-DUAL-WARD-ENGINE-QUALIFICATION-1`
- Directory: `probes/P-PHOTON-Z5-DUAL-WARD-ENGINE-QUALIFICATION-1/`
- Public base: `d0bc920b27117ea4a409282e3481340f50433763`
- Canon: Public Canon v74

This probe independently reproduces and removes only the exact dyadic-weight
representation ceiling that abandoned `P-PHOTON-Z5-DUAL-WARD-CROSSCHECK-2`.
It also qualifies a fail-closed synthetic two-child supervisor.  It does not
repair, rerun, resume, reinterpret or complete the abandoned pin.

The abandoned formal seeds, the four local primal replays, every hidden or
partial dual stream and every Ward value are forbidden inputs.  The consumed
commit and incident receipt above are provenance pins only: the verifier does
not read their trees or bytes at runtime.  No state generated by the abandoned
execution is admitted.

This package is deterministic and contains no scientific data-opening step.
Before the source pin, compilation and visibly non-evidential development
checks are allowed, but their output is not a run record.  The accepted
`verify.py`, this document, all executable sources, the exact fixture and the
source manifest must be committed and pushed before the first formal verifier
execution.  A post-pin failure consumes this identifier under repository
policy; it is not repaired or rerun under the same name.

## Field 1: exact carrier and reproduced incident

For one orbit heat bath, let the nonempty ordered candidate list have integer
exponents

```text
e_0,...,e_(r-1),          1 <= r <= 5,
m=min_i e_i,
q_i=e_i-m,
w_i=2^q_i,
W=sum_i w_i.
```

The exact conditional law chooses the unique index `j` satisfying

```text
sum_(i<j) w_i <= U < sum_(i<=j) w_i,
U uniform on {0,...,W-1}.
```

The inherited implementation formed every `w_i` and `W` in `uint64_t`.  It
raised exactly `orbit_integer_weight_overflow` before reading a random bit if
some `q_i >= 63`, and raised exactly `orbit_weight_sum_overflow` if an ordered
addition overflowed.  Even after both tests passed, its `bounded(W)` computed
`bit_length(W-1)` and its `bits(count)` rejected a count above 63.  Thus
`W>2^63` raised exactly `bitstream_bits_width_exceeds_63`, also before reading
a random bit.  Those three guards are reproduced as historical engineering
behavior, not treated as a scientific terminal.

The fixed high-level envelope is not an arbitrary vector.  For `L in {6,8}`
put `S=L^2`, take two disjoint translated homologous positive coordinate
two-tori `P_0,P_1`, the hard state `n=-P_0`, the generator `g=P_1`, and level
`s=S`.  Exactly `k=0,1,4` remain in the hard orbit.  In that order,

```text
k                    0       1       4
support              S       2S      2S
B=1[j!=0]+1[H_2!=0]  1       0       1
e=-support+sB        0      -2S     -S
```

Thus the exponent spread is exactly `2S`: the frozen vectors are
`(0,-72,-36)` at `L=6` and `(0,-128,-64)` at `L=8`.  The old guard must fire
with zero random bits consumed in both cases.  The exact replacement must
sample masses `(2^(2S),1,2^S)` without an overflow or approximation.

No lattice observable is evaluated.  The labels `L=6,8` identify only these
fixed synthetic integer envelopes.

## Field 2: qualified exact sampler

`orbit_qualification.cpp` uses `boost::multiprecision::cpp_int` to construct
every arbitrary-width power and total exactly.  Its bounded sampler puts

```text
b=bit_length(W-1),
```

reads exactly `b` consecutive MSB-first bits as an integer, accepts iff that
integer is below `W`, and otherwise rejects the complete draw and repeats.
For `W=1`, `b=0`, the unique draw is zero and consumes no bit.  This is exact
unbiased rejection from `{0,...,W-1}`; no floating point, modulo reduction,
saturation or host PRNG is permitted.

The SHA-256 counter stream remains the inherited stream:

```text
SHA256(ASCII domain || 16 seed bytes || 16 counter bytes),
domain = photon-z5-dual-mobility-qualification-1,
```

with unsigned 128-bit big-endian counter from zero and bits consumed
most-significant-bit first.

The replacement has two paths.  If every shift is below 63, the inherited
ordered `uint64_t` sum does not overflow, and `bit_length(W-1)<=63` (equivalently
`W<=2^63`), it must call the old bounded-u64 path without an intervening bit
read.  Otherwise it uses the `cpp_int` path.  On every old-defined table, old
and new must have the same selected index, draw, number of consumed bits, and
following stream bits.  A change to any formerly defined small-span
transition or random-bit offset is a qualification failure and would require
a fresh mobility qualification before later use.

The frozen known-answer spans are

```text
0, 1, 32, 62, 63, 64, 72, 128, 192.
```

They include the path boundary, both reproduced formal-size spreads and a
larger reserve.  A separate span-62 vector with weights
`(2^62,2^62,1)` and total `2^63+1` reproduces
`bitstream_bits_width_exceeds_63` with zero bits consumed.  Another span-62
vector whose ordered sum exceeds `2^64-1` reproduces
`orbit_weight_sum_overflow`.  Both must pass through `cpp_int`.

The prospectively fixed bounded engineering smoke is exactly one sample for
each of those nine span tables, the width-overflow table, the sum-overflow
table and the two L6/L8 two-plane tables.  Each sample starts a fresh stream
with seed bytes
`00,01,...,0f`, counter zero and the frozen domain above.  It records the
selected index, exact draw and consumed-bit count.  There is no runtime size,
seed, repetition-count or table selector.

The full frozen small-span envelope is exhaustively enumerated over every
ordered candidate vector of length one through five whose normalized shifts
are in `{0,...,8}` and include zero: exactly 28,981 ordered tables and
6,791,443 integer draws.  For each table, the audit compares exact old/new
weights and totals, compares the selected index for every integer draw
`0,...,W-1`, and compares one deterministic old/new sampling transcript:
choice, draw, consumed-bit count and the following 63 stream bits.  All these
small tables are old-defined.  This is a finite exhaustive table-and-selection
audit; it makes no claim to enumerate the infinite set of possible rejection
transcripts.

For every audited table and every ordered pair `i,j`, the verifier checks

```text
w_i/w_j = 2^(e_i-e_j)
```

by exact cross multiplication.  On one orbit the heat-bath transition is
`P(i,j)=w_j/W`, hence

```text
w_i P(i,j)=w_i w_j/W=w_j w_i/W=w_j P(j,i).
```

The algebra establishes the exact conditional target and detailed balance;
the finite tests audit its implementation.  It is not a mixing theorem.

## Field 3: synthetic supervisor qualification

`supervisor_qualification.py --fixture` opens only synthetic local children.
The fixed failure specification is named `dual_rc`.  Its engine exits 7 and
its reader exits 11 after each writes a nonempty diagnostic.  For each stderr
leg the supervisor retains at most the first 4,096 bytes while recording the
complete byte count and SHA-256, so truncation cannot erase custody.  The
record must preserve the failing specification, both return codes, both
prefixes, both complete byte counts, both full-stream hashes and the exact
engine-to-reader pipe count and hash.

A single two-worker fail-fast batch first holds one blocking
`engine+reader` sibling live, then runs the `dual_rc` trigger in the other
worker and queues four further jobs.  The trigger's nonzero returns cause one
shared cancellation decision.  The live sibling must be killed and reaped;
at least two genuine queued futures must be cancelled, and any wrapper already
dequeued in the race must observe cancellation before spawning a child.  All
futures must finish, no queued child body may start, and the final surviving
PID count must be zero.  All waits, output reads and diagnostic buffers are
bounded.  Any timeout, cap violation, collector failure or unreaped process is
a verifier failure.

Two deterministic fault injections also exercise cleanup before normal batch
coordination: reader creation fails after the engine exists, and thread start
fails after both children exist.  Each path must preserve its exact injected
error, kill and reap every created direct child under an absolute deadline,
and leave zero survivors.  Worker and collector threads are daemonized only as
a last interpreter-exit firewall; the accepted path still joins them within
the frozen bound.

These are process-supervision tests only.  The child payload contains no
dual state, Ward statistic or production input.  The fixture qualifies the
direct engine and reader children; neither synthetic child creates a process
descendant, so no claim about killing an independently spawned descendant
tree is made.  The scheduler may cancel three or four of the queued futures;
only the invariant `>=2`, zero queued child starts and deterministic Boolean
transcript are frozen.

## Field 4: source custody and independence

The manifest-covered pre-execution source inventory is exactly

```text
PREREG.md
README.md
orbit_qualification.cpp
supervisor_qualification.py
verify.py
FIXTURE_EXPECTED.txt
```

`SOURCE_SHA256SUMS` fixes these six entries in this order and does not list
itself.  These six entries plus that manifest are the complete seven-file
pre-execution package.  The manifest bytes and digest are owned by the
immutable source pin.  The
verifier rejects missing, extra-manifest, duplicate, non-lowercase or
noncanonical entries and byte-compares the listed SHA-256 values before any
build or child execution.

In the pre-run source form, where none of `EXPECTED.txt`, `RUN.md` and
`RESULT.md` exists, the verifier takes `HEAD` as the pin, requires it to be the
unique direct child of the displayed public base, requires its changed-path
set to be exactly these six files plus `SOURCE_SHA256SUMS`, and requires a
clean worktree.  On every later replay it reads the single `pin_commit` field
from `RUN.md`, requires that commit to remain an ancestor of `HEAD` and the
same direct child of the public base, and byte-compares all seven files with
`git show PIN:path`.  Thus changing a source together with its manifest after
the public pin cannot create an accepted replay.

There is deliberately no `INPUT_SHA256SUMS`: no unmanifested scientific or
data input is opened at runtime.  Pin custody reads Git commit/tree metadata,
the seven pinned package bytes and, on replay, the post-run `RUN.md` control
record.  It opens no abandoned source, log, seed, transcript, network response
or scientific data.  Git, compiler, Boost headers, Python standard library
and operating-system process primitives are execution dependencies, not data
inputs; their architecture portability is decided by the two required clean
CI replays.

`FIXTURE_EXPECTED.txt` is canonical LF-terminated ASCII and freezes the exact
combined orbit and supervisor transcript before the source pin.  The verifier
must compile with warnings as errors, require exit zero and empty stderr from
both fixtures, compare their combined stdout byte for byte with that file, and
clean its bounded build directory on every path.  The canonical public command
is

```text
python3 probes/P-PHOTON-Z5-DUAL-WARD-ENGINE-QUALIFICATION-1/verify.py
```

No network access is needed for replay.

## Field 5: thresholds and terminal

Every following condition is conjunctive:

```text
source manifest and exact fixture custody                       PASS
legacy guards at L6/L8 with zero random-bit consumption         PASS
cpp_int KAT spans, width-overflow and sum-overflow tables        PASS
full small-span table/selection audit                           PASS
old/new choice, draw, bit-count and successor-bit parity        PASS
exact weights, interval partition and detailed balance          PASS
synthetic failing spec, two return codes and pipe custody        PASS
bounded stderr prefixes, complete byte counts and SHA-256        PASS
at least two queued futures cancelled before start              PASS
no dequeued queued wrapper starts a child                        PASS
running sibling engine and reader killed and reaped             PASS
all futures completed                                            PASS
reader-spawn and thread-start fault cleanup                      PASS
surviving child PIDs                                             0
exact fixture stdout, empty stderr and exit code                 PASS
```

The only successful terminal is

```text
WARD_ENGINE_QUALIFICATION_PASS
```

Any failed condition makes `verify.py` exit nonzero, emit no success terminal
and leave the engineering obligation open.  There is no scientific FAIL,
phase decision or fallback terminal.  `EXPECTED.txt`, `RUN.md` and
`RESULT.md` may be added only after a complete pinned local execution exits
zero with empty stderr.  The pull request must then replay the unchanged
verifier byte-identically on both required `x86_64` and `aarch64` jobs before
merge.

## Field 6: action and governance

```text
action layer:        L1 exact integer/RNG and process-supervision engineering
synthetic geometry:  finite L6/L8 labels only; no sampled measure
cross-layer claim:   none
maximum status:      ZERO_ENGINEERING_ONLY
F2 / reader gate:     unchanged by this probe
F3 / production:     NOT SATISFIED; issue #742 remains forbidden
Canon movement:      forbidden; Public Canon remains v74
CROSSCHECK-3:         not reserved or authorized
```

A merged, publicly replayed `WARD_ENGINE_QUALIFICATION_PASS` is necessary but
not sufficient for a later cross-check.  Retaining the wrapper acceptance
recorded in issue #772 additionally requires exact byte parity below the old
ceiling and a separate public governance readback.  Only after this package is
merged and explicitly accepted for the sector-umbrella wrapper may a fresh
`P-PHOTON-Z5-DUAL-WARD-CROSSCHECK-3` be reserved with new paths, sources,
seeds, attempt refs, receipt and one-shot execution.

No Canon, Registry, Frontier, photon phase, Ward residual, propagator,
polarization, physical photon, Born selection, matter/light split,
contraction/expansion or cosmological statement moves here.
