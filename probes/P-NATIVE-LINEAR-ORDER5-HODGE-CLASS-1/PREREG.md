# PREREG: P-NATIVE-LINEAR-ORDER5-HODGE-CLASS-1

FORMAL PUBLIC PROBE. Action layer L1. Issue #1063.
Base: Public Canon v89 main b66266f9d604cf57fef57349e6fdad395b329cd5.

Let Gamma_lin be the complete subgroup of GL_6(F_5) generated by the exact
linear parts A_n,B_n,C_n,D_n,E_n of the five declared checkpoint generators,
with D_n=E_n=-I. The verifier constructs the group from the identity by exact
breadth-first closure under right multiplication by generators a,b,c,d,e,
with no word-length cutoff. The word convention affects only printed witnesses.

Let A_h=Lambda^2 C_4 mod 5 for the marked augmentation five-cycle. Freeze
the Hodge order-five similarity type by

    r_h(k)=rank((A_h-I)^k),  k=1,...,5.

For every exact order-five g in Gamma_lin compute the same sequence. Since
X^5-1=(X-1)^5 over F_5, this sequence determines the unipotent Jordan block
partition and hence GL_6(F_5)-similarity.

## Decisions

G1 Enumerate Gamma_lin completely and record its exact order. Check inverse
membership and closure under every generator for every enumerated element.

G2 Enumerate all exact order-five elements and partition the complete set by
Jordan rank sequence, printing exact counts and a stable shortest BFS word for
each realized type.

G3 WORD-SEAM iff at least one native word has exactly the Hodge rank sequence.
If positive, print the total Hodge-type count and a shortest witness. If
negative, print the maximum nilpotency index among all realized native
order-five types.

G4 Recheck the named canonical control P_n=B_n C_n and its prior rank sequence.

G5 Enumerate the complete fired linear subgroup generated by B_n,D_n,E_n and
record its order-five count, preserving the exact FIRED-COMMUTATOR-NOGO
boundary.

## Code and limits

verify.py is Python standard-library only with exact mod-five arithmetic.
No floating point, randomness, external package, network input, sampling or
word cutoff is admitted. Group closure terminates only when the exact BFS
frontier is empty. Scientific outcomes WORD-SEAM and WORD-DIFFER are both
accepted. Integrity/runtime failure is STOP.

A negative result excludes only direct bridges in which the LINEAR PART of
one finite native word is GL_6(F_5)-conjugate to the marked Hodge five-cycle.
Affine translations, the state-dependent selector, histories, nonlinear
readings and counter-assisted constructions remain outside scope.

No physical dimension/time, decoder, occurrence law, selector equivalence,
measure, SI or L2-L6 lift is claimed. The author explicitly authorized the
connected GitHub commit identity.
