# Independent written review

PUBLIC; NON-CANONICAL. P-QDD-SELECTED-BIPARTITE-LAW-1.
Public lock: [#1095](https://github.com/mathorn1973/twist-j/issues/1095).
Authority base: Public Canon v90, public main
`19b88a43314fa2381ba353b9b2af614bd653ae54`.

This review was written by a separate reviewing agent after reading
STATUS.md, POLICY.md, AGENTS.md, CORE.md, FRONTIER.md, the registered
BELL-CAUSAL-ACCOUNTING scope, the accepted ETH-QDD-1 model and proof, and
the selected-theory program. It then examined this probe's MODEL.md,
PROOF.md, PREREG.md and prospective fold note. It did not read the new
verify.py, import it, execute any scientific gate, or use a numerical search.
No public result or architecture acceptance is anticipated by this review.

## Decision and scope

No blocking mathematical gap or conflict among the reviewed contracts was
found. The written argument establishes the advertised conditional theorems.
The finite verifier must still satisfy its separate frozen contract, and
public acceptance must be recorded separately. This is an independent
mathematical review, not an independent computational reproduction or
physical validation.

The construction adopts the tensor product, the entangled preparation,
external setting controls, local operation boundaries and the inherited
occurrence law. Its conclusions are conditional quantum mathematics. It
does not derive these inputs from J or U, identify a material pair source,
or close the existing broader physical owners.

## 1. Metric, preparation and complete table

The endomorphism-density convention is consistent. Conjugating a local
density by F=G^(1/2) gives an ordinary positive density, and the joint
coordinate change is F tensor F. The stated partial trace is the correct
contraction for endomorphisms; no covariance is substituted for a density.

In the declared real alignment,

    (F tensor F) vec(G^-1)/2 = vec(I)/2.

Therefore the selected vector is normalized and both marginals are I4/4.
This uses the marked real chart. Invariance under an arbitrary common
complex rotation is not asserted. The tensor carrier is two supplied
source spaces, not a decomposition of one native checkpoint.

The inherited projectors obey tr(Pk Pl)=1 on equal indices and 1/16 on
unequal indices. The maximally entangled transpose identity therefore gives
p(LL|x,y)=tr(Px Py)/4. Together with the two LOW marginals 1/4 this yields
exactly

    equal:   (LL,LH,HL,HH)=(1/4,0,0,3/4),
    unequal: (LL,LH,HL,HH)=(1,15,15,33)/64.

All entries are nonnegative and normalized. The general joint instrument is
CP; local branch maps commute, and completeness proves normalization on
every complex density. A zero positive branch is the zero operator and
has no normalized successor. The written proof, not a finite collection
of positive fixtures, owns these universal statements.

## 2. The complete Bell-local class is actually decided

The explicit weights w(s,t) in PROOF section 4 are a valid reduction from
any normalized measurable setting-independent hidden-variable model with
stochastic local responses to the 1024 deterministic response pairs.
Their nonnegativity, normalization and recovery of each conditional pair
probability follow by summing the unused response coordinates. No finite
support of the original hidden-variable measure is assumed.

For a deterministic pair let a,b be the two LOW-set sizes and c their
intersection size. The proposed functional is 3c-ab. Independently of the
proof's case division, a,b>=c gives

    3c-ab <= 3c-c^2 <= 2

for integer c in {0,1,2,3,4,5}. Thus the bound is valid without equal
marginals or perfect diagonal agreement. Equal singleton or two-element
LOW sets attain two. The quantum table gives

    2*(5/4)-20/64=35/16,

which rules out the entire frozen comparison class. The twenty ordered
unequal pairs are essential. Replacing them by ten unordered pairs would
change the statistic and invalidate this conclusion.

Failure of factorization at the declared singleton source is correctly
distinguished from exclusion of every independent hidden-variable
refinement. The first observation alone would not establish the second;
the Bell bound supplies the required stronger argument. Neither conclusion
identifies a physical influence or communication mechanism.

## 3. Fixed-effect maximum, CHSH and noise

For v_k=u_k tensor u_k, the five-column Gram matrix is

    (15/16) I5 + (1/16) ones5.

Its eigenvalues are 5/4 once and 15/16 four times. The Bell operator is
3 sum_k Pk tensor Pk - (25/16) I16, so its eigenvalues and multiplicities
are exactly 35/16 once, 5/4 four times and -25/16 eleven times. Moreover
sum_k v_k=(5/2) Phi. Hence rho_Phi is the unique maximizing density for
these fixed effects. This is state optimization under known measurements,
not device-independent uniqueness or optimization over all measurements.

The CHSH proof covers repeated settings and all eight odd-parity sign
patterns. For distinct settings on both wings, the equal-setting entries
form a partial matching of size zero, one or two. The corresponding
absolute values are among 1/8, 17/16, 13/16 and 2. Repeated settings reduce
to twice one correlation. The maximum is therefore exactly two, consistent
with the five-setting Bell excess. No two-setting test is substituted for
the entire five-setting behavior.

The mixed product density I16/16 has Bell value -5/8. Consequently the
declared mixture has B(v)=(45v-10)/16 and exceeds two exactly when
v>14/15. This is a threshold for one fixed witness in one declared family;
no local decomposition below threshold or detector-efficiency threshold
follows from it.

## 4. Local records and the two communication tests

The full-state proof keeps operative memories explicit. At each finite
local protocol node the branch sum preserves trace. Induction proves that
the sum over every complete set of local leaves is trace preserving, even
with local adaptive settings and bounded stopping. Local branch compositions
then give positive normalized joint history laws and prefix consistency.
Different interleavings preserve each local order and commute between wings.

For fixed initial rho and Alice policy, every admitted Bob message changes
only Bob's trace-preserving total map. Its adjoint sends I to I, so the
entire distribution of Alice's local record is unchanged. Interchanging
the wings gives the converse. The one-shot identities also follow directly
by summing remote projectors and using cyclicity of trace. Thus both
marginal equalities and the stronger complete-record message test are
proved, rather than inferred from the special symmetric table alone.

Initial correlations are allowed because the maps act on the entire
declared joint system. The proof does not guess a reduced source CP map
for a correlated environment. It excludes remote-controlled filtering,
remote-dependent stopping, initial source-message dependence and newly
received remote controls from its hypotheses. No physical distance or
time-to-distance comparison is imported.

Selective steering respects this boundary. Bob's LOW branch prepares P_y
on Alice, HIGH prepares Q_y/3, and their weighted average is I4/4.
Recognizing one remote branch requires the remote record. Conditional
changes are not a locally controllable signal.

The explicit communicated-control witness proposed for G10 is also exact.
After Bob measures context 0 and sends his result, let Alice use context
0 on LOW and context 1 on HIGH. Her LOW probability is

    (1/4)*1 + (3/4)*tr(P1 Q0/3)
    = 1/4 + (3/4)*(5/16)
    = 31/64,

instead of the uncommunicated fixed-context value 1/4. A new remote record
has changed Alice's policy, precisely outside the no-message class. This
control does not claim faster-than-light communication. Likewise, preparing
Alice in P0 or Q0/3 according to a remote message changes a context-0 LOW
probability from one to zero, but violates the fixed-preparation premise.
These controls identify necessary hypotheses rather than contradicting the
theorem. Their exact preregistration must precede the first execution.

## 5. Measurement independence is not inferred from marginal equality

For each promised setting pair, the separable sigma_(x,y) in MODEL (6)
is positive and normalized. Its components have deterministic indicated
outcomes, so the state reproduces the complete selected table at those
settings. Its two marginals are I4/4. Thus neither marginal equality nor
knowledge of both reduced densities proves preparation-setting independence.

The alternative response description with lambda=(a,b) and
mu_(x,y)(lambda)=p_Phi(a,b|x,y) has local deterministic responses but a
setting-dependent source law. It is not one allowed refinement under (5).
It preserves the causal-premise distinction without choosing a physical
superdeterminist or retrocausal explanation.

The primary source and independent uniform controllers are explicitly
chosen. In particular 1/5 is a declared control probability, not a derived
consequence of cyclotomic order five. The setting law covers all 25 pairs;
a finite deterministic setting scan is not offered as evidence for the
stronger independence premise.

## 6. Apparatus accounting, ownership and review limits

The local records contain preparation/trial identifiers, wing and invocation
identifiers, comparison indices, settings and outcomes. Pairing uses the
prior identifiers, not a fitted coincidence window. Every supported
completed trial is retained, and the ideal primary model contains no hidden
loss channel. Physical losses would require a new outcome/error contract.

The model distinguishes fresh-pair replacement from retained-pair rereading.
Only replacement of the full operative pair and memories, together with
fresh independent setting controls, supplies the stated product law across
trials. Archives persist and fresh capacity is supplied. Open-system reset
does not become information-free reversible erasure.

The ten fields of the registered BELL-CAUSAL-ACCOUNTING requirement are
accounted for: source and measure, settings and their mechanism, instrument
and outcomes, normalized kernel, Bell/CHSH conventions, factorization,
measurement independence, both marginal tests and the separate message test,
three physical bridges, and dimensional inputs. The first eight and the
dimensional accounting are specified conditionally. The three physical
bridges are named but remain proposed. Accordingly the original owner must
remain open, as the fold proposal requires. The same is true of the QDD
apparatus, terminal-event and complete-family owners. Classification of all
Bell-local response models is not classification of all physical apparatuses.

Read-only collision inspection covered the remote pure-two-qubit relational
wedge and Pythagoras branches (#419, #422), and golden AME(4,6) branches
for two-place comparison, descent, local-unitary covariants, printed-skeleton
rigidity and the Gross--Goedicke comparison (#364, #365, #367, #369, #368).
Those scopes concern different carriers and questions. No collision with
this selected C4 tensor C4 five-context law was found; no branch was changed
or imported as a new dependency.

The algebra, finite audit and physical adoption retain distinct evidentiary
roles. A passing verifier will support the frozen exact certificates, not
convert preparation, local access, event occurrence, setting independence,
or physical isolation into deductions from native U. No novelty, laboratory
validation or empirical discrimination from ordinary quantum mechanics is
claimed by this review.
