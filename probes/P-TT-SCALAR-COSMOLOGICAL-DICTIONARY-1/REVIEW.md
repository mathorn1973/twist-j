# Independent review of the selected cosmological comparison

**PUBLIC; candidate-T assessment; NON-CANONICAL.** This review concerns
one selected classical cosmological perturbation model and its exact
conditional consequences. It does not establish empirical adequacy or
change the public Canon. The reviewer read the current normalization
owner and Canon sections 13 and 14, the accepted outgoing-state
construction, MODEL.md, PROOF.md and the competing TT/ADM notes. The calculations below
were derived analytically. The reviewer has not read or executed this
probe's verifier or performed a scientific gate execution.

## 1. A complete selected model, with a real change of dynamics

The proposed advance is substantive. A tensor preparation is accompanied
by a specified scalar preparation, both canonical momenta, a common
background and clock, an interpolation into actual spatial modes, and
one common evolution law derived from the selected reduced actions.
Together with the complete preparation measure these determine the
joint future field law and its tensor/scalar comparison.

The construction does not preserve the old graph recurrence after the
matching surface. It retains the K1 packet and first emitted tensor
profile as boundary data, then selects a continuum Einstein-scalar
perturbation model. The old rational stencil and the continuum spatial
Laplacian have different eigenvalues. Treating them as identical would
be an error; stating this new continuation as a model choice is not.

The earlier isolated tensor source also spends its declared complete
radiative channel on tensor emission. The accompanying scalar
preparation requires an additional declared preparation resource.
Using a function of the same word as its amplitude does not prove that
this additional field costs no energy or that the old source balance
already supplied it. The present construction selects the joint initial
state; it does not derive a microscopic apparatus or a common depletion
mechanism for its preparation.

## 2. Background and time normalization

Use the selected canonical Einstein-scalar action with gravitational
coefficient `1/(2 lambda)`, where `lambda=216*pi`, and a scalar with
positive canonical kinetic term. The constant-epsilon solution with
`epsilon=-dot H/H^2=3/2` is supported by

```text
F(t)=1+(3/2)H_* t,
a(t)=F(t)^(2/3),
phi(t)-phi_*=(2/sqrt(3 lambda)) log F(t),
V(phi)=(3H_*^2/(2 lambda))
       exp[-sqrt(3 lambda)(phi-phi_*)].
```

Substitution gives `dot phi^2=3H(t)^2/lambda`,
`V=3H(t)^2/(2 lambda)`,
`3H(t)^2=lambda(dot phi^2/2+V)` and
`dot H=-(lambda/2)dot phi^2`. The scalar equation follows by
differentiating these identities, or by direct substitution. This is
an explicit new scalar matter realization of the background. It is
not supplied by the dustlike K1 energy record of the earlier hybrid
dictionary.

In elapsed conformal time eta, with the matching surface at eta=0,

```text
B(eta)=1+H_* eta/2,
a(eta)=B(eta)^2,
t(eta)=[B(eta)^3-1]/(3H_*/2),
eta>=0.
```

Indeed `dt/deta=a`, and these expressions agree with the proper-time
solution above. The new sample correspondence `eta=(n-2)d` is a
declared clock dictionary, not a previously established identification
of the native counter with cosmological time.

The background is decelerating, not inflationary. Its canonical scalar
has sound speed one, although its homogeneous equation of state is
zero. Equality of the background expansion to that of pressureless
matter does not equate their perturbation theories. Also, the old Canon
phrase `rho_m=0` did not imply epsilon zero: its K1 energy still sourced
a dustlike expanding background. The source-off Minkowski case has
H=0 and does not supply this nonzero-clock scalar gauge.

`H_*`, the cell length d and the common unit scale are context inputs.
Fixing `a(0)=1` and using `c=1` does not remove the dimensionless
combination `H_* d`. Its cancellation from a particular ratio is a
proved output property, not proof that the background and box choices
were derived from J.

## 3. Reduced actions, polarization factor and scalar gauge

For `gamma_ij=sum_A e^A_ij gamma_A`, with transverse traceless
polarizations normalized by `e^A_ij e^B_ij=2 delta_AB`, the selected
quadratic Einstein-scalar actions in conformal time are

```text
S_T=(1/(4 lambda)) integral a^2
          sum_A[(gamma_A')^2-|grad gamma_A|^2],
S_zeta=(epsilon/lambda) integral a^2
          [(zeta')^2-|grad zeta|^2].
```

The scalar equation assumes the chosen canonical rolling field and
the solved lapse/shift constraints. It is not obtained merely from a
homogeneous Friedmann equation or by identifying an auxiliary graph
constraint variable with zeta. The standard quadratic reduction,
without a slow-roll assumption, is given in
[Maldacena, section 2, equation (2.12)](https://arxiv.org/pdf/astro-ph/0210603).
The physical assumptions of that action are new selected premises here.

The canonical fields are

```text
q_T,A=a gamma_A/sqrt(2 lambda),
q_S=a sqrt(2 epsilon/lambda) zeta.
```

Since epsilon is constant, their pump terms are identical:

```text
q_j''+[K_j^2-a''/a]q_j=0,
a''/a=2/(eta+2/H_*)^2.
```

The rolling clock has nonzero derivative, so comoving scalar gauge is
available. The selected nonzero spatial modes allow the scalar shift
constraint to be solved without pretending to invert the zero mode.
Neither this linear constraint solution nor the reduced quadratic
action proves a complete nonlinear Einstein solution for finite
amplitude packets.

The full metric tensor contraction contains an essential factor two:

```text
P_T=2 sum_A P_gamma_A,
P_S=P_zeta,
r=8 epsilon (sum_A P_qT,A)/P_qS.
```

A convention using half the tensor contraction would instead give
half this ratio. The present full-contraction convention must remain
fixed. Its relation to the familiar vacuum formula is a normalization
check, not an invocation of vacuum statistics for the selected state.

## 4. Boundary law, translation and continuum modes

The preparation uses the unchanged K1 word probabilities. On the
matching surface take

```text
q_T,+(0)=A Phi_interp/sqrt(2 lambda),
q_T,cross(0)=0,
q_S(0)=A (Pi_0 |Phi|)_interp/sqrt(2 lambda),
p_T(0)=q_T(0)/d,
p_S(0)=q_S(0)/d.
```

The common A is a perturbation amplitude or order-counting parameter.
It is not a new relative tensor/scalar fit. The scalar preparation,
including its coefficient, is an explicit additional choice. Its
later dynamics are linear scalar dynamics; it is not repeatedly reset
to the absolute value of the evolved tensor field.

Both canonical coordinates and momenta are now specified. The momentum
assignment uses the backward difference of the supplied zero/impulse
pair as an adopted Cauchy matching rule. It must not be misread as a
theorem that the resulting continuous solution also vanishes at
eta=-d. That past endpoint is not an additional imposed boundary
condition.

The trigonometric interpolation uses signed integer harmonics
`j=-2,-1,0,1,2` on a torus of period `5d`, with the scalar zero mode
removed and the tensor zero mode already zero. The actual continuum
wave numbers are `K_j=2*pi*j/(5d)`. Slots 1 and 4 become opposite
first harmonics; slots 2 and 3 become opposite second harmonics.
All fields are planar inside the declared three-dimensional torus.
This supplies a spatial interpretation without claiming isotropy.

After forming the seeds, translate both sectors by the same independent
uniform continuous displacement `sigma in [0,5d)`. This makes the
ensemble translation invariant: transverse translations already act
trivially and the displacement supplies longitudinal invariance. It
does not create rotational invariance. It also defines the joint law,
not just its covariance: a product of Fourier modes survives this
continuous translation average precisely when its total signed integer
harmonic is zero. Equality only modulo five would describe a different,
discretely translated ensemble and would give different higher moments.

No word-mean centering is performed before translation. After spatial
zero-mode removal, translation itself makes every retained scalar-mode
mean zero. Consequently its final connected power is the RAW word
second moment of that Fourier seed, not the connected power used by
ETH-TT-1 before translation. This is a change in preparation law, fully
declared before the comparison. Static packets still give zero in both
sectors and retain their full probability 1/3.

The law is a classical random phase-space law. A finite atomic field
and momentum prescription is not automatically a density operator for
a field satisfying canonical commutation relations. No quantum vacuum
or uncertainty-compatible quantum preparation is being inferred from
this classical ensemble or from the separate QDD measurement theory.

## 5. Independent exact power calculation

At matching, let T_j be the old tensor seed power and J_j the scalar
seed power after the translation rule just specified. For nonzero
harmonics, with the unitary five-site Fourier normalization, direct
averaging of the K1 profiles gives

| Harmonics | T_j | J_j |
|---|---|---|
| j=+/-1 | `(3+sqrt(5))/24` | `(5-sqrt(5))/120` |
| j=+/-2 | `(3-sqrt(5))/24` | `(5+sqrt(5))/120` |

The independent way to see the new scalar entry is to use its onset
law: zero with mass 1/3; two disjoint two-site half-profiles each with
mass 1/6; their four-site sum with mass 1/3. At a nonzero mode put
`t=zeta_5^j+zeta_5^(-j)`. Its raw second moment is
`(t^2+1)/60=(2-t)/60`, giving the displayed J. Spatial zero-mode
removal has no effect on these entries. Previously the squared word
mean `1/80` was subtracted; it is now part of the connected translated
ensemble because the displacement randomizes its phase.

The tensor mean was already zero, so its power is unchanged. Hence

```text
T_j/J_j=5+2sqrt(5)       for |j|=1,
T_j/J_j=5-2sqrt(5)       for |j|=2.
```

With epsilon=3/2 and the full metric contraction, the selected
cosmological ratios are therefore

```text
r_j=60+24sqrt(5)         for |j|=1,
r_j=60-24sqrt(5)         for |j|=2.
```

Both are positive and large; the smaller is still greater than six.
These values must remain as derived, with no post hoc suppression factor.
The declared theory is a finite-band anisotropic, noninflationary
preparation, not a proposed primordial isotropic CMB spectrum.

## 6. Evolution, nodes and scales

For each retained harmonic the same real transfer G_j satisfies

```text
G_j(0)=1,  G_j'(0)=1/d,
G_j''+[K_j^2-2/(eta+2/H_*)^2]G_j=0.
```

The torus Fourier coefficients of each interpolated seed are its
unitary five-point coefficients divided by sqrt(5). The tensor and
scalar canonical powers therefore acquire the same factor
`A^2 G_j(eta)^2/(10 lambda)` multiplying T_j and J_j, respectively.
The physical full-contraction powers are precisely

```text
P_T=2 A^2 G_j^2 T_j/(5 a^2),
P_S=A^2 G_j^2 J_j/(30 a^2).
```

Thus the ratio above is independent of eta,
H_*, d and A WHERE the two mode powers are nonzero.

This qualification is essential. Unlike the previous exact graph
recurrence, the new continuum transfer can have zeros. At such a
node both powers vanish and their ratio is undefined. Cancelling a
symbolic common factor does not assign a measured ratio to 0/0.
The transfer is analytic on the whole future domain and is not the
zero solution because G_j(0)=1; its zeros are isolated. At matching
the ratio is always defined.

A shared torus Fourier normalization or shared logarithmic power
weight cancels from the ratio when used consistently. It must not
create an unsupported isotropic shell interpretation. If the physical
unit is ell_0, the physical wave number is
`K_j/[a(eta) ell_0]`. No SI value of ell_0 follows from this model.
The numerical ratio does not require that additional calibration;
placing it at a measured physical pivot would.

## 7. What can be recorded as completed

The construction supplies a selected common geometry and scalar clock,
a complete initial tensor/scalar phase-space law, a new explicitly
chosen time and continuum propagation dictionary, and a numerical
directional cosmological tensor/scalar ratio in its supported band.
That is more than a composite-intensity ratio with an undefined
physical scalar interpretation. Its interpretation is now a stated
effective-theory choice, with calculable consequences.

It does not prove that the Einstein-scalar theory, its source matching,
uniform translation or relative scalar preparation follows from J.
Nor does it prove that this is Nature's preparation. The common formal
amplitude supports a controlled statement about the chosen linear
equations and quadratic spectra; no error estimate for finite-amplitude
nonlinear spacetime evolution has been supplied.

### Literal decision contract and prospective positive disposition

The registered positive clause is exactly:

> closes positively by a public vector-doublet normalization yielding a numerical r_T(k)

Its registered scope names the vector-doublet state, its two-point and
pseudo-covariance spectra, action normalization, scalar comparison and
the fourth-moment data needed by the square. The positive clause asks
for one construction. The quantifier over every admissible
normalization belongs to the separate negative clause. Those two
decision routes must not be exchanged.

Nothing in this positive clause requires inflation, rotational
isotropy, all wave numbers, a CMB fit, SI calibration, a quantum vacuum,
or a material preparation apparatus. Requiring those additional
deliverables before considering any positive decision would enlarge
the registered obligation retrospectively. They remain important
boundaries of a physical model, but are not new acceptance tests for
this particular owner.

Accordingly this review recommends a **positive D disposition in a
separate Canon fold**, conditional on accepted proof and replay,
adoption of the combined outgoing-state and cosmological dictionaries,
the explicit continuum vector law and its full two-point,
pseudo-covariance and fourth-order data, and the appropriately typed
physical dictionary gates. The continuum vector is the persistent-sign
root of the primary continuum tensor solution, not the interpolation
of the predecessor's roots. Its moment law must retain the actual
continuous translation measure; band limitation of the tensor does
not imply band limitation of its roots.

This is an existence-based dictionary adoption, not a theorem of
physical necessity. The already registered positive D adoption of
`FRW-INHOM` is the relevant precedent: one explicitly chosen
construction can meet a positive existence clause without proving
that every other reading is impossible. The selected preparation and
relative coefficient remain visible inputs, and the large resulting
ratios remain unchanged.

Until that fold and its gates occur, the present probe itself changes
no owner status. A positive fold should preserve the identifier,
history and original decision record, and state the adopted domain
literally: the selected finite-band classical linear cosmological
model, with numerical ratios on positive-power support. It must not
advertise inflationary closure, an isotropic primordial spectrum, a
continuum limit of the native graph, or experimental confirmation.
The original graph source and the separate cubic ADM, apparatus and
metrology obligations keep their own scopes. No new requirement to
solve those entire programs is attached to this owner's positive
clause.

The proof obligations in this review are analytic: the background
identities, reduced normalization, complete phase-space evolution,
translation selection rule and common-transfer cancellation. A finite
exact audit can check their algebraic ingredients and boundary data;
it cannot establish all future times by a finite sampled trajectory.
No formal execution preceded this review. The reviewed MODEL.md and
PROOF.md agree with these choices, normalization factors and output
boundaries. The proposed positive D decision remains a separate
reviewed act of Canon adoption, not an automatic consequence of this
review or a passing finite verifier.
