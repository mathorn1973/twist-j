# Result: P-QDD-V80-CLOSURE-BOUNDARIES-1

Status: CONFIRMED at the three frozen mathematical scopes.
Public issue: [#871](https://github.com/mathorn1973/twist-j/issues/871).
Preregistration pin: `9a0b42fe75b91181b8806ac618f7c54ff5b4cb85`.

The Linux x86_64 run exited zero with empty stderr and exact stdout of
1313 bytes, SHA-256
`29f7b55cca982ac4203c199fe6322d7e5a14915ecb26df0c6e25b8924adc6862`.
RUN.md records the public pin, all eight immutable file identities and
clean-tree custody. The required GitHub architecture jobs separately replay
the same bundle against the committed EXPECTED.txt. The universal statements
below rest on the written proofs; finite enumeration is their exact audit.

## Confirmed mathematical results

1. **Native common-ready recovery.** Exactly 21 of the 25 common readies
   preserve all 625 piston preparations for every finite tick. At each of
   the four exceptional readies the late image has 500 states, with 125
   double fibres and 375 singleton fibres. Fixed ready `(0,1)` gives an
   explicit all-time full-state reader for all 624 supported sources and
   the zero source, without storing a separate original head. The audit
   checked all readies at tick three, every source at ticks 0 through 64,
   40625 source readbacks, 38750 chart comparisons, and 500 conflicting
   QDD fibre pairs, with 203703 exact native checks. A regenerable history
   is a virtual readout; physical blank-cell writing is not implemented.

2. **Complete first-hit onset boundary.** The cyclic gap partition gives
   the exact ordered first-hit law for every finite word and onset vector.
   In the unchanged v79 calendar, sources `(1,0,1,0)` and `(2,-1,1,0)`
   have the same first LOW/HIGH at every one of 1024 onsets, but targets
   `1/6` and `1/26`. No common source-independent onset law can realize
   all source ratios. For `(1,0,0,0)`, uniform onset gives `23/32` instead
   of `1/16`. Chronological accepted order is cyclic `LOW^A HIGH^B` with
   the proved correlations. A separate permutation scan has uniform fine
   first-hit counts for every nonempty occupancy set; interpreting those
   counts as a law requires an additional uniform permutation premise.
   The audit covered 1092 ternary words, 6015 onsets, all 640000 native
   source-slot pairs, and 49489 permutation/subset cases. None failed.

3. **Mixed three-pass rigidity.** Every real or complex CPTP channel on
   the specified three-dimensional motor support that preserves its
   first three attenuation observables is the identity. This is equivalent
   to full unnormalized channel commutator saturation. Two passes are
   insufficient; rational pinching is an exact counterexample. General
   projective-effect repeatable branches remain arbitrary support channels;
   idempotence and full record symmetry alone do not select identity, as
   the rational 72-Kraus depolarization witness shows. The audit confirmed
   the three rank-one defects, spanning determinant -1024, scalar-commutant
   rank eight, and both negative controls exactly.

The onset nonselection and two-pass/nonselection counterexamples are
predicted conclusions of the frozen claims, not failed gates or discarded
data. No mathematical falsifier fired and no threshold was moved.

## Earned scope and unresolved physical work

The proofs support three proposed T claims at their explicit mathematical
domains. They are not three physical closures. The native reader presumes
the selected mathematical ready and full current-state/counter access.
The permutation repair changes the schedule and imports a law. The mixed
selector presumes exact attenuation preservation in a CP reduced-channel
type; neither U nor physical terminality has been shown to imply it.

No realized physical preparation, context, apparatus, fresh material archive,
reset, outcome law, full apparatus-family equality or cross-layer physical
gate is supplied. QDD-INSTRUMENT-APPARATUS, QDD-INSTRUMENT-CLASS-COMPLETENESS
and QDD-TERMINAL-EVENT-SEMANTICS remain open. The old general-head and
apparatus-only obstructions remain intact. No finite C result from v79 is
promoted to an unconditional physical theorem.

No Canon, registry or workflow changes in this probe. Any v80 intake is a
separate reviewed fold with its own evidence hashes and public release gate.
