# P-QDD-V80-CLOSURE-BOUNDARIES-1 preregistration

**FORMAL PREREGISTRATION / PROOF-FIRST / RESULT-EXPOSED.**
Author: A. M. Thorn. Date: 2026-09-06.
Public lock: [#871](https://github.com/mathorn1973/twist-j/issues/871).
Branch: `probe/P-QDD-V80-CLOSURE-BOUNDARIES-1`.

## Authority and custody

The verified public basis is ACTIVE Public Canon v79, main and tag target
`2f6384b99818280839b8219ce1ee91c9c1b278b6`, content commit
`48ede94165472e2d77896a7c20ddb3c567f3bcc1`. CANON.md has 516701 bytes and
SHA-256 `025b07fe39ec4ab857a50adf467acc640334a1ea0cb87e8f9500b90948896764`.
Tag and content ancestry, all normative checks, both architecture jobs,
aggregate check, immutable release and final publication replay were verified.
The session scanned all remote heads, open issues/PRs, probe and registry
namespaces before claiming this new identifier. Existing apparatus and
calibration issues #539, #830, #832 and #834 retain their distinct ownership.
No sealed or abandoned probe is resumed.

Every analytical prediction below was known before execution. The v79 QDD
targets and the source motor are public inputs, not blind experimental
targets. No new experimental payload or external dataset is used. Neither
the verifier nor its local modules was imported or executed before the pin.
Source inspection, written algebra, static parsing and independent review
are the only allowed preparation actions.

Commit this preregistration, the three proofs, the three audit modules and
verify.py as one complete immutable pin, push it, and read back every byte
from GitHub before execution. The module and source digests are checked by
the accepted verifier; RUN.md will record the entire pinned file inventory.
Do not amend, rebase, squash or force-push. A code/fixture defect cannot be
repaired under this identifier after pinning.

## Field 1: equations and claims

The probe examines three explicitly different mathematical interfaces of
the existing decoder. It does not assert that composing their conditional
results constructs a physical apparatus.

### A. Native preparation and full-state recovery

Keep exactly the public U on `N_0 x F_5^6` and the origin-zero driver
`theta_n=s_2(n) mod 2`. Source is the ordered four-piston vector `p in F_5^4`.
Choose common ready `(q,r)=(0,1)` before inspecting any QDD outcome.
The construction is the full native state `(p,0,1)`, not apparatus-only
access. Equality is literal checkpoint/source equality and n is a supplied
native counter. The complete 25 common-ready class and its exceptional
members are classified in NATIVE-PROOF.md.

Target: every source is recoverable from `(n,U^n(0,(p,0,1)))` for every
finite n by the fixed sheet inverse, including zero source. At any common
ready outside `(0,0),(3,0),(3,3),(1,3)`, the 625 sources have 625 distinct
late states. At the four exceptional readies only 500 distinct late states
remain. The phase distinction and inverse proof are universal in n; the
audit's bounded trajectories do not prove an infinite-time statement.

Regeneration of a deterministic source/history function from the recovered
source and n is virtual readback. No physically writable blank cells,
capture mechanism, physical readiness, reset law or change to U is supplied.
The arbitrary-head merger and apparatus-only same-sum obstruction of v79
remain true on their own larger/different domains.

### B. First-hit laws of the fixed incidence calendar

For any integer M>=1, fix a word on exactly `LOW,HIGH,SILENT`, a cyclic
inclusive onset r, and scan consecutive phases until the first occupied
slot, continuing cyclically for ordered outputs. With no occupied slot,
return NO_EVENT and form no accepted law. The predecessor gaps partition
all phases; every mathematical onset vector `mu_r>=0, sum mu_r=1` induces
the gap-mass ordered law proved in OCCURRENCE-PROOF.md.

Specialize to the unchanged v79 1024-slot incidence construction on all
625 vectors in `{-2,-1,0,1,2}^4`. Put `s=sum v_i`, `Q=sum v_i^2`,
`A=s^2`, `B=5 sum_(i<j)(v_i-v_j)^2`, `D=A+B`.
The first-label equality is coarse LOW/HIGH equality, not equality of
complete signed records, source identifiers or waiting times.

For `v=(1,0,1,0)` and `w=(2,-1,1,0)`, first LOW occurs at exactly
`{0,...,9} union {529,...,1023}` for both sources, but `A/D` is respectively
`1/6` and `1/26`. Hence no common source-independent onset distribution
matches all source QDD ratios. This includes every probability vector on
1024 phases, and any larger onset law with that residue pushforward.
Uniform onset for `(1,0,0,0)` gives `23/32`, while its accepted ratio is
`1/16`. No impossibility for other scheduling or physical classes follows.

A separate positive comparison counts all permutations of M slots. For
every nonempty occupied set of size D, each slot is first in `M!/D`
permutations; every distinct ordered k-slot list appears in
`M!/(D(D-1)...(D-k+1))` permutations. A uniform permutation law therefore
has the desired single-trial occupancy marginal, conditionally. It changes
the schedule and supplies an additional carrier and law; it is not a
consequence of the native chronological clock. Subsequent accepted outputs
within one permutation are without replacement, not independent trials.

### C. Finite mixed-channel selector

On the fixed real Hilbert space W or its complexification, use the public
Gram `H=[[2,1,1],[1,2,1],[1,1,2]]` and motor
`A=[[-1,-1,-3/4],[0,0,1/4],[1,0,1/4]]`. Write
`X^sharp=H^-1 X* H` and `B_n=(A^sharp)^n A^n`.
The complete declared mathematical class is all channels
`E(X)=sum_j K_j X K_j^sharp` with a finite real/complex Kraus family and
`sum_j K_j^sharp K_j=I`. Equality is equality of linear channels on all
operators. This does not identify complete physical apparatuses, memory
or environment implementations.

Target: `E*(B_n)=B_n` for n=1,2,3 iff E is the identity channel iff
`E o Ad_A = Ad_A o E`. Three consecutive rank-one defects, spanning W
with connected nonzero overlaps, force every Kraus operator to be scalar.
The first two B_n alone do not suffice, as an exact rational pinching
counterexample shows. Projective effects and repeatability leave arbitrary
channels on each support; mixed identity and depolarizing channels also
show that repeatability, idempotence and symmetry alone do not select the
same post-state. MIXED-PROOF.md carries the complete proof and the precise
support, field, equality, positivity and phase limits.

No physical law making completed events preserve these attenuation
observables is adopted or inferred. This conditional selector does not
close QDD-TERMINAL-EVENT-SEMANTICS or the physical class-completeness owner.

## Field 2: accepted code and exact audit domains

Command from repository root:

```text
python3 probes/P-QDD-V80-CLOSURE-BOUNDARIES-1/verify.py
```

verify.py checks immutable source and module digests and calls, in order,
native_audit.audit(), onset_audit.audit(), mixed_audit.audit(). The modules
use only Python's standard library and exact integers/Fraction arithmetic.
They have no import-time scientific execution and perform no file writes.

The native audit checks all 25 common-ready images at tick 3 and all 625
selected-ready sources at every tick 0 through 64 against the inverse.
The onset audit checks every ternary word of lengths 1 through 6, every
phase and the first three accepted labels; all 625 source calendars of
1024 slots; the two-source pointwise witness; and all permutations and
nonempty occupied subsets for M=1 through 6. The mixed audit checks the
exact Gram, adjoint, motor, defect/projector and spanning certificates,
scalar-commutant rank, and the stated countermodels. The immutable code
defines the exact iteration order and output. Universal conclusions rest
on the written proofs, not extrapolation from these finite audit domains.

## Field 3: carrier, data and provenance

No measured or third-party data are acquired. These unchanged public
documents supply the exact mathematical inputs; verify.py checks their
SHA-256 bytes before using its locally restated formulas:

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| ../P-QDD-U-NATIVE-READBACK-1/PROOF.md | 12710 | 2df18b2118d0a2e483c8089976eda8485127951efa9e2f3364d38ac2aab0d76c |
| ../P-U-PREPARATION-EVENT-RECORD-1/NATIVE-PROOF.md | 18312 | 45c93ea7a031ca0a5690ed7240d0648417d526659238cfffe52d5409925c0856 |
| ../P-U-PREPARATION-EVENT-RECORD-1/INCIDENCE-PROOF.md | 18027 | 5538718c55cabb07702a5d6d7daaecdcb4a8f5775f6863a5fc9ca39ba70910aa |
| ../P-QDD-COMMUTATOR-SATURATION-CLOSURE-1/PREREG.md | 11796 | f0d428cec1c6f0552e239be522a0f2a64decd20e538992c26f0efc18d4e25f96 |

These are inputs and acknowledged prior results. Existing verifier code,
records and old outputs are not imported as a new run.

## Field 4: systematics and controls

Use deterministic order, `LC_ALL=C LANG=C TZ=UTC PYTHONHASHSEED=0
PYTHONDONTWRITEBYTECODE=1`, 600-second ceiling, exit 0 and empty stderr.
Source coordinates, common ready, original versus moving pistons, inclusive
onset, empty acceptance, full versus coarse record, chronological versus
permuted order, H-adjoint, field and channel versus Kraus equality are fixed.
Zero and nontrivial negative controls are mandatory. No random seed, fit,
cutoff adjustment, numerical tolerance or target-selected phase is admitted.

Finite checks do not prove physical readiness or occurrence. The mixed
theorem concerns an explicit finite-dimensional Kraus class, not arbitrary
nonlinear dynamics or quantum-correlated unbounded apparatus memory.

## Field 5: thresholds and disposition

Every assertion is exact; the mismatch threshold is zero. CONFIRMED means
all frozen finite audits pass and the written universal proofs withstand
review at their declared scopes. A counterexample to a universal identity,
inverse, gap partition, onset nonselection, permutation count or Kraus
rigidity is a mathematical falsifier and must be preserved.

Pin/hash mismatch, timeout, nonzero exit or fixture defect is integrity STOP,
not physical evidence. A never-completed gate follows the repository's
abandoned-pin disposition under this spent identifier. A completed gate
retains exact stdout and RUN.md and is never relabeled as unrun. Missing
physical inputs leaves the existing physical owners O, not a new impossibility
theorem or a closed program. Required GitHub x86_64 and aarch64 jobs must
independently match one committed EXPECTED.txt byte for byte.

## Field 6: action layers and boundaries

A and B are L1 exact state, finite-word and combinatorial statements. B's
probability vectors are mathematical arguments on a fixed finite set, not
an adopted physical L6 measure. C is the L4 mathematical channel class
already supplied by the public support/motor construction. The three proofs
do not create a cross-layer edge between one another. No physical L1-to-L5,
L4-to-L5 or L6 gate is passed, no feedback into U is introduced, and no
canonical file or old probe changes in this probe pull request.

Possible v80 intake is a separate reviewed fold. No result here alone earns
complete physical apparatus realization, a terminal-event law, all-family
classification, physical fresh storage, reset, or independent outcomes.
