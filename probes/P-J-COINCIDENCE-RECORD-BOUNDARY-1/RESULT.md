# P-J-COINCIDENCE-RECORD-BOUNDARY-1 result

Status: **candidate-T / L1 / TWO MATHEMATICAL CLAIMS CONFIRMED / ONE
PHYSICAL HYPOTHESIS UNTESTED AT STOP / TWO-ARCHITECTURE COMPUTATION GATE
PENDING / PUBLIC CLAIMS UNREGISTERED / CANON UNCHANGED**

## Recorded decision

```text
J-RESIDUAL-UNIT-NORMAL-FORM:          CONFIRMED
J-COINCIDENCE-CARTESIAN-GRAM-SEAM:    CONFIRMED
COINCIDENCE-RECORD-FREQUENCY:          UNTESTED STOP
gates:                                 16/16 PASS
exit/stderr:                           0 / empty
stdout:                                byte-identical to EXPECTED.txt
SCIENTIFIC-FIRED-A/B:                  NOT SELECTED
STOP for mathematical execution:       NOT SELECTED
ABANDONED-PIN:                         NOT SELECTED
ARCHITECTURE GATE:                     PENDING
MANUAL SECURITY REVIEW:                PENDING
```

The immutable verifier was executed exactly once after public pin
`998b18f359f3d45d4fe30425cff01393fa183a6f` and byte-for-byte remote
readback. Its 23-line stdout has SHA-256
`d489a8786305d1d41b79dc8d63ba07283e6b796d5309a92303e3f06d72190c2b`.
Every mathematical and firewall gate passed. The successful computation did
not test or advance the physical H row.

## Claim A: reduced residual unit normal form

For each integer coefficient `d_k`, the candidate reduced fibre contains
fresh signed ordinal tokens

```text
U_k^+(d)={(k,+,m):1<=m<=max(d_k,0)},
U_k^-(d)={(k,-,m):1<=m<=max(-d_k,0)}.
```

Exactly one sign fibre is nonempty. The unique reduced nonnegative pair
reconstructing `d_k` is

```text
(max(d_k,0),max(-d_k,0)),
```

so the fibre cardinality is `|d_k|`. The five-cycle transports these fibres
covariantly.

This does not identify a historical path which survived cancellation. The
frozen witness with two positive labelled words and one negative word has two
different survivor choices with the same residual coefficient `+1`. Only the
sign and residual cardinality are invariant. Tokens are regenerated at a cut
and have no claimed persistence under a general linear update.

## Claim B: complete Cartesian coincidence and the Gram seam

Take two disjoint tagged copies of the reduced fibre and define

```text
C_k^x(d)=U_k^S(d) x U_k^R(d).
```

Then finite-set multiplication gives

```text
|C_k^x(d)|=|d_k|^2=d_k^2,
|C^x(d)|=sum_k d_k^2=q(d).
```

For the full-cell Cayley copy

```text
Kd=sum_k d_k e_k tensor e_k,
```

the coefficient matrix is `diag(d_k)`, so its algebraic Gram contraction is
`diag(d_k^2)`. Therefore the complete-pair cardinality vector equals that
Gram diagonal exactly.

This equality is the earned seam. The controlled copy itself has only
`|d_k|` reduced joint coefficient units in diagonal cell `(k,k)`; it does not
linearly manufacture `d_k^2` coefficient units. The square count arises only
after the complete Cartesian relation is separately defined.

Within that construction:

```text
C_k^x(d)=empty iff d_k=0,
|C_k^x(d)|/|C^x(d)|=d_k^2/q(d) for d!=0.
```

These are combinatorial emptiness and a normalized finite ratio, not yet
physical darkness, probability, or empirical frequency.

The exposed pair-count lists are

```text
d_0=(4,-1,-1,-1,-1):       (16,1,1,1,1), total 20,
Ad_0=(5,0,5,-5,-5):         (25,0,25,25,25), total 100.
```

## Complete incidence is not selected by the marginals

The same tagged fibres admit many relations. If their common size is `n`, the
empty relation has zero pairs, the ordinal diagonal has `n`, and subsets of
the complete product realize every cardinality from zero through `n^2`.
Thus equal marginal counts, the Cayley copy, and the Gram identity do not
force all pairs to be present.

This control identifies the exact physical choice instead of hiding it:

```text
available fibres  +  complete Cartesian incidence  =>  d_k^2 pairs.
```

Only the implication after the second premise is a theorem here.

## Extensive ledger

For the integral mixer `A=1+g^2-g^3-g^4`,

```text
A^T A=5I-N,
q(Ad)=5q(d) for d in V_Z,
|C^x(Ad)|=5|C^x(d)|.
```

The single-unit total `sum_k|d_k|` has no state-independent multiplier: the
two supported witnesses give ratios `5/2` and `3`.

The normalized map `U_5=A/sqrt(5)` preserves `q` on the real augmentation
sector but generally leaves the integer domain, so it does not act on literal
finite fibres without a separate unit-scale law. The raw `J=1+g^2` orbit has
the first pair totals

```text
20, 30, 70, 180,
```

and no universal quadratic-yield multiplier. The probe establishes this fork
but does not choose which map is the physical step.

## The sole physical row remains open

```text
COINCIDENCE-RECORD-FREQUENCY [candidate-H / future L5-L6 / STOP]

At a frozen calibrated read cut for a supported nonzero integral preparation,
the physically realized record population is exactly C^x(d), with every
within-cell ordered pair realized once and no other record. The ensemble is
this simultaneous finite plenum itself, not repetition in time and not modal
branches. Observed cell frequency is finite self-location in that population:

f_k(d)=|C_k^x(d)|/|C^x(d)|=d_k^2/q(d).
```

Nothing in the completed computation establishes that row. It selects the
complete relation as physically realized, names its elements records, and
identifies their cardinality ratio with an observed frequency. Those are the
remaining physical inputs.

The model-level falsifiers remain: realized counts not equal to the joint Gram
diagonal; a missing or multiply counted Cartesian pair; an off-cell record or
record without coincidence; or a nonzero realized record at `d_k=0`.
Operational use still requires the typed apparatus, background, gain,
resolution, preparation, and read cut missing from
`QDD-INSTRUMENT-APPARATUS [O]`.

If the H row were later adopted, it would provide no randomness of one run.
Every pair would be realized, and `which result do I see?` would be a finite
self-location question. This probe establishes no self-location fact.

## Earned scope and firewalls

The two claims stop at exact L1 finite-set and integer combinatorics. The words
`system`, `record`, `coincidence`, `darkness`, and `ratio` have no physical
force in the confirmed claims.

No physical apparatus, outcome, occurrence, record population, current,
frequency law, probability, sampling, observer fact, self-location fact,
collapse, temporal repetition, modal branch measure, continuous hidden
trajectory, or single-run randomness is supplied. No L2 through L6 lift is
made.

Public Canon, Registry, Frontier, gates, dependencies, dictionaries,
`QDD-INSTRUMENT-APPARATUS [O]`, and `STATUS.md` remain unchanged. Any later
registration or physical move requires a separate sealed Canon fold.
