# C-GOLDEN-AME-GALOIS-DESCENT-1-N — result

Status: **NON-CANONICAL, exact scoped negative**

Decision: **the mandatory projective baseline is the whole field of moduli**

Canon change: **none**

`PROMO.md`: **not created**

## Verdict

The printed normalized tensor has entry field `K=Q(zeta_40)`, while its
projective coordinates have the already-preregistered field
`E=Q(zeta_20)`.  Complete enumeration in the frozen labeled local-monomial
gauge gives

```text
G_mod = {1,21} = Gal(K/E),
K_mod = K^G_mod = Q(zeta_20).
```

Thus there is no Galois descent below the mandatory projective-coordinate
baseline.  This is the preregistered baseline-only negative outcome, not a
new positive bridge.

The secondary unlabeled-party audit gives exactly the same group.  Allowing
the only additional degree-compatible party permutation, exchange of parties
2 and 3, produces no support-and-amplitude transporter.

## Gate ledger

| Gate | Result | Exact scope |
|---|---|---|
| G0 source/field replay | **PASS by exact replay** | 8515 bytes, pinned blob/SHA-256, 112 entries, exact 2-unitarity and full entry field; byte-identical to the published expected transcript |
| G1 Galois action | **PASS** | all 16 units, exact `a,b,c,w` action and 16 power-basis tensor hashes |
| G2 complete transporters | **PASS / baseline only** | all 73,728 labeled degree-preserving tuples per amplitude map and every phase system mod 5 and mod 8 |
| G3 field of moduli | **BASELINE-ONLY NEGATIVE** | `G_mod={1,21}`, `K_mod=Q(zeta_20)` |
| G4 coherent descent | **COMPLETE** | the only two subgroups are `{1}` and `{1,21}`; the latter is the trivial projective cocycle represented by `A/c` |
| G5 party audit | **SAME NEGATIVE** | only identity and party swap `(2 3)` survive degree multisets; the swap has zero colored-support candidates |
| G6 output | **COMPLETE** | deterministic standard-library script, stdout, exact dual certificates, hashes |

## G1 — exact action and the mandatory baseline

The frozen generators have exact orders

```text
ord(17)=4, ord(11)=2, ord(21)=2
```

and generate all 16 units modulo 40.  Applying each automorphism in the power
basis of `Q[z]/Phi_40(z)` gives two unsigned amplitude actions:

```text
(a,b,c) -> (a,b,c),
(a,b,c) -> (b,a,c),
```

with exact signs recorded as residual exponents 0 or 20 modulo 40.

Two actual source entries give

```text
A[0,0,0,1] = c,
A[0,1,0,2] = c z^34 = c w^17,
(z^34)^13 = z^2 = w.
```

Hence the projective ratios contain `w=zeta_20`.  Conversely, division by
the common factor `c` expresses every projective coordinate using `w`, so

```text
Q(projective coordinates of A) = Q(zeta_20).
```

Also `sigma_21(a,b,c)=(-a,-b,-c)` and `sigma_21(w)=w`, whence
`sigma_21(A)=-A` entry by entry.

## G2 — complete labeled transporter census

The four support degree vectors are

```text
(20,18,16,24,16,18)
(14,14,22,20,20,22)
(20,20,18,18,18,18)
(18,18,18,18,20,20).
```

They leave exactly

```text
4 * 8 * 48 * 48 = 73,728
```

independent local permutation tuples to check.  This full Cartesian product
was enumerated for both unsigned Galois amplitude maps.

For the amplitude-exchanging map `(a,b,c)->(b,a,c)`, no tuple maps the colored
support.  Therefore the eight automorphisms

```text
3, 7, 13, 17, 23, 27, 33, 37
```

have empty transporter sets before phases are considered.

For the label-preserving map there are only two colored-support tuples:

```text
(id,id,id,id),
(id,id,t,t),  t=(0 1)(2 3)(4 5).
```

All 112 phase equations were solved exactly over `Z/5` and `Z/8`.  Only the
identity tuple for `k=1` and `k=21` lifts.  Their normalized witnesses are

```text
k=1:  d=0, h=0,
k=21: d=0, h=20.
```

Every rejected phase class has an explicit dual certificate `lambda` with
`lambda A_phase=0` but `lambda b_phase != 0` modulo 5 or modulo 8.  The
certificates use only four or six tensor rows and are independently checked
by the script.  One representative is

```text
k=11, identity tuple, modulus 8:
lambda on rows
 (0,3,4,1):7, (0,4,2,2):1, (1,0,4,1):1,
 (1,4,0,2):7, (2,0,2,4):7, (2,3,0,4):1;
lambda A_phase = 0 mod 8, lambda b_phase = 4 mod 8.
```

An independent implementation repeats the full 73,728-tuple census for every
automorphism and the party-swap audit. It obtains the same `G_mod={1,21}` and
fixed polynomial `Phi_20`; its complete machine-readable audit is
`CROSSCHECK.json`.

The identity phase kernel has order

```text
5^4 * 8^4 = 40^4 = 2,560,000.
```

It is generated by the four projectively trivial leg-scalar vectors (add one
to all six phases on one leg and subtract one from `h`).  Thus the projective
automorphism group in the frozen class is trivial.

## G3/G4 — field of moduli and coherence

The complete census gives

```text
T_k is nonempty iff k in {1,21}.
```

This set is a subgroup.  Its fixed basis is

```text
1,z^2,z^4,z^6,z^8,z^10,z^12,z^14,
```

so the fixed field is `Q(zeta_20)`, with

```text
Phi_20(x)=x^8-x^6+x^4-x^2+1.
```

The two subgroups of `G_mod` are `{1}` and `{1,21}`.  The latter has the
identity projective transporter and hence a coherent, coboundary descent
datum.  An explicit representative is `A/c` over `Q(zeta_20)`.  There is no
larger subgroup on which a cocycle question remains: individual transporters
are already empty.

## G5 — unlabeled-party audit

Comparison of degree multisets leaves only

```text
id, (2 3) in S4.
```

For each, the complete local search again has 73,728 tuples.  The nontrivial
party swap has zero colored-support candidates for both amplitude maps.  The
unlabeled result is therefore also

```text
G_mod_unlabeled={1,21}.
```

## Boundary

This result classifies exactly the preregistered projective local monomial
group with `mu_40` phases.  It does not exclude descent under arbitrary local
unitaries or `GL_6`, and it establishes no six-line, `A5`, decoder, Born-rule,
fault-tolerance, or physical write/read statement.
