# C-GOLDEN-AME-GALOIS-DESCENT-1-N

**NON-CANONICAL.** Exact classification of the Galois field of moduli and
coherent descent of the pinned golden AME(4,6) tensor in one frozen projective
local-monomial gauge.

The result is a scoped negative: the complete Galois stabilizer is the
mandatory projective subgroup `{1,21}`, so the orbit descends from
`Q(zeta_40)` to `Q(zeta_20)` through the common scalar only, and no farther.

See `PREREG.md`, `RESULT.md`, and `RUN.md`. The external source pin is in
`SOURCE.md`.

