# C-TM-SYM2-ORIENTED-BORN-JUNCTION-1-N result

```text
STATUS:       NON-CANONICAL
ISSUE LOCK:   #376
PUBLIC BASIS: Public Canon v46
RESULT:       candidate-T algebra + candidate-D conditional bridge
DECISION:     BRIDGE at incubation scope; PUBLIC GATE UNCHANGED
PUBLIC ROWS:  unchanged
```

## 1. Current-scope reconciliation

The current public target is `TM-SYM2-PHYSICAL-MEASURE [O]`. Its registered
L5-to-L6 gate requires a separately owner-approved complete L5 source with
`epsilon_read` retained as typed data, no selector representative, no enlarged
postcomposition gauge, a total exact Born bridge, one normalized L6 measure,
and coherence on all 48 selectors. The measure values must be outputs. A
construction assuming the public mathematical `1/6`, the public operator, or
the factorization `(1/2)(1/3)` is explicitly circular.

The current public dependencies relevant here are:

```text
PUBLIC T:
  GOLDEN-SIX-LINE-SYM2-FRAME
  TM-SYM2-PROJECTIVE-FOURFOLD
  TM-SYM2-SEMILINEAR-TWOFOLD
  TM-SYM2-REVERSAL-CLOSURE
  GYRON-DENSITY                         post-construction control only

PUBLIC D:
  MEASURE-BORN-VERB                     Born-square reading type
  ABELIAN-FACE-DICTIONARY               additional conditional phaseful input used below

NON-CANONICAL INPUT:
  #375 omega source and J_n=omega_n v_epsilon
```

The historical sealed `P-TM-SYM2-MEASURE-1` N2 result remains negative. No
selector is repaired or selected here.

The old v15 Born-definition documents are not authority. One construction
from them is independently re-derived below. The face weights alone do not
force a coefficient representative; the historical exact counterexample to a
weights-only uniqueness claim remains a valid boundary. The coefficient
representative used here is therefore conditional on the explicit phaseful
expression already displayed inside `ABELIAN-FACE-DICTIONARY [D]`, not on the
weights alone.

## 2. First falsifier: the direct square of omega is not a total Born map

On the three complement orbits

```text
O_A={001,110},
O_B={010,101},
O_C={011,100},
```

with

```text
omega(a,b,c)=c-a,
```

the exact squared norms are

```text
sum_(w in O_A) omega(w)^2 = 2,
sum_(w in O_B) omega(w)^2 = 0,
sum_(w in O_C) omega(w)^2 = 2.
```

Hence the naive conditional law

```text
p_O(w) proportional to omega(w)^2
```

is undefined on `O_B`. This route is exactly falsified.

[NON-CANONICAL F, bounded] The new orientation current is not itself the
physical Born amplitude. The source survives; the direct-square shortcut does
not.

## 3. Conditional coefficient-side Born carrier

Put `j=zeta_5`. Under the explicit phaseful expression

```text
Psi(k)=1+j^k
```

inside the declared `ABELIAN-FACE-DICTIONARY [D]` formula, use the exact
five-point Fourier transform. The inverse coefficient vector is uniquely

```text
v=delta_0+delta_1.
```

Indeed

```text
F(v)_k=1+j^k
```

for every `k mod 5`. With

```text
Z_pos(v)=1^2+1^2,
```

Plancherel gives

```text
sum_k star(F(v)_k) F(v)_k = 5 Z_pos(v).
```

The support of `v` is the two-point set `{0,1}`. Its coefficient-side Born
reading is derived from the two equal coefficient magnitudes. Only now the
normalized support law evaluates to

```text
Born_supp(v)=(1/2,1/2).
```

The two bijections from the support to a two-element N-orbit produce the same
probability law. Thus no support orientation is selected.

[NON-CANONICAL candidate-T algebra; candidate-D dictionary use] The algebraic
Fourier and Born statements are exact. Using this coefficient basis as the
physical sheet carrier remains conditional on the D-level phaseful dictionary.
No stronger status is claimed.

## 4. Quotient law before any physical value

The two frozen Thue-Morse child maps descend to `Q=W3/<N>`. Re-derivation gives

```text
O_A -> O_B + O_C,
O_B -> O_A + O_C,
O_C -> O_A + O_B.
```

Thus the quotient transfer matrix, with columns as parents, is

```text
T_Q = [[0,1,1],
       [1,0,1],
       [1,1,0]].
```

Solve

```text
T_Q p=2p,
sum p=1.
```

Exact RREF has one solution. Only after the system is solved it evaluates to

```text
p=(1/3,1/3,1/3).
```

No line measure, GYRON density, Born sheet value, or target `1/6` enters this
calculation.

[NON-CANONICAL candidate-T]

## 5. Complete orientation-retaining L5 source type

The candidate source is not a selector. It contains

```text
Source5=(complete projective-gauge orbit record,
         epsilon_read,
         omega stream,
         J stream),

J_n=omega_n v_epsilon,
v_epsilon=(1,-1,-1,1)^T.
```

The scalar Born map below is defined on this complete type. It does not first
replace the four projective-gauge classes by one class and it does not remove
`epsilon_read` from the domain.

The #375 character theorem gives exact coherence of the orientation field
under the frozen drive involutions. No postcomposition gauge is enlarged.

## 6. Candidate L5-to-L6 map

For each orbit `O` use the quotient probability from section 4 and the
conditional two-sheet Born law from section 3. Define first on W3

```text
mu_B(w)=p([w]) half_[w](w).
```

Both factors are already normalized on their own typed carriers, so

```text
sum_(w in W3) mu_B(w)=1
```

before any comparison with a known line measure.

Only after this map is defined and normalization proved, its six values are
read. They are equal, and each equals

```text
1/6.
```

Thus the familiar number is an output of two independently derived maps in
this candidate, not a construction input.

The independent breaker and positive verifier both enforce the order of this
construction.

## 7. All-48 coherence without selector choice

For any selector chart `s:W3->Lines` define

```text
mu_s=s_* mu_B.
```

The derivation above makes `mu_B` constant on W3. Therefore every bijection
from W3 to the six line labels gives the same line measure.

The exact audit checked all

```text
6! = 720
```

bijections, not merely the frozen 48 selectors. Every one produced the same
normalized six-line measure. Hence, a fortiori:

```text
- all 48 frozen selectors agree;
- changing representatives inside any order-12 projective-gauge orbit changes nothing;
- the four residual projective-gauge classes also give the same measure;
- no semilinear or reversal comparison action is adopted as gauge.
```

[NON-CANONICAL candidate-T at the finite carrier scope]

## 8. Orientation blindness is an output, not a quotient

The scalar measure is independent of the retained `J_n` value. This does not
mean the orientation source was absent. The candidate map has been typed on
all combinations

```text
epsilon_read in {+1,-1},
omega_n in {-1,0,+1},
```

and then proved to have a common scalar L6 image.

Hence the correct statement is:

```text
orientation is present in Source5;
the candidate Born measure is blind to it.
```

The result is therefore compatible with the current frontier requirement to
retain orientation at L5. It does not assert that L6 contains an orientation
observable.

## 9. Post-construction controls

### Golden-frame operator

Only after the measure was constructed, the exact six projectors of
`GOLDEN-SIX-LINE-SYM2-FRAME` were rebuilt over `Q(phi)`. On a basis of
`Sym2(Q(phi)^3)`, the resulting operator was checked exactly to equal

```text
M=(1/3)P1+(2/15)P5.
```

Thus the public mathematical operator is reproduced as an outcome.

### Adjacent-pair / gyron event

For

```text
pi_12(a,b,c)=(a,b),
E_00=pi_12^-1(00)={001},
```

the post-construction pushforward gives the same value as one derived line
weight. This is then compatible with the registered `GYRON-DENSITY` value.
`GYRON-DENSITY` did not enter the carrier, quotient, Born, or normalization
steps.

## 10. Dependency and status boundary

The mathematics of the candidate map closes, but two public-authority steps
are not earned by this incubation:

1. the #375 L5 orientation source is NON-CANONICAL and is not yet a separately
   owner-approved public source;
2. the coefficient-basis refinement used here is a D-level conditional
   reading of the explicit phaseful expression in `ABELIAN-FACE-DICTIONARY`.
   It is not a new free dimensionless parameter, but this exact dependency is
   not currently an authoritative dependency edge of
   `TM-SYM2-PHYSICAL-MEASURE`.

Therefore the current public gate remains OPEN/STOP. This result does not
change it.

The scientific status of the physical bridge candidate cannot exceed D under
this route even if the exact algebraic lemmas are theorem-grade.

## 11. Breaker and verifier verdict

The independent breaker was frozen before the positive verifier. Its finite
core found:

```text
B1 PASS  omega-only zero-norm orbit exhibited
B2 PASS  quotient stationary law unique
B3 PASS  coefficient support Born law orientation-independent
B4 PASS  six-word measure total and normalized
B5 PASS  representative covariance
B6 PASS  all 720 bijections coherent
B7 PASS  GYRON used only after construction
B8 PASS  no D_matter conclusion
```

The exact positive audit returned:

```text
V1  PASS  omega character; direct square non-total
V2  PASS  quotient transfer and unique stationary law
V3  PASS  conditional Fourier inverse and sheet Born law
V4  PASS  total normalized six-word measure
V5  PASS  all 720 bijective charts coherent
V6  PASS  full orientation-retaining input type; scalar blindness derived
V7  PASS  golden-frame operator reproduced after construction
V8  PASS  adjacent-pair event control
V9  PASS  conditional dictionary boundary retained
V10 PASS  no decoder / D_matter object constructed
```

## 12. Decision

```text
DECISION: BRIDGE
STATUS:   NON-CANONICAL candidate-D physical bridge
```

At the frozen incubation scope, the missing mathematical junction exists:

```text
complete fourfold L5 record + retained orientation source
  -> quotient stationary law
  -> conditional two-sheet Born square
  -> total W3 measure
  -> selector pushforward
  -> one coherent normalized six-line L6 measure.
```

The bridge produces `1/6` and the public golden-frame operator only after it is
constructed. It selects no selector and enlarges no gauge.

It is not a public closure because its source and one D-level carrier
refinement still require separate owner/public treatment.

## 13. Cleaner successor suggested by the breaker

The direct `omega^2` failure exposes a cleaner question. The N-sheet itself has
a one-dimensional odd character line on every two-element orbit. A future
fresh incubation may test whether the new orientation source selects that
projective odd line as a Born carrier directly, eliminating the conditional
phaseful coefficient refinement above. That would be a distinct carrier and
must not be retrofitted into this frozen candidate.

No Canon, Registry, frontier, dependency, gate, Born public row, decoder row,
or status changes here.
