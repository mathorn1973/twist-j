# PREREG. C-TM-SYM2-ORIENTED-BORN-JUNCTION-1-N

```text
STATUS:      NON-CANONICAL INCUBATION
ISSUE LOCK:  #376
PUBLIC BASIS: Public Canon v46
LAYER:       L5 source -> L6 measure candidate only
AUTHORITY:   none
```

This preregistration freezes the attack before any candidate verifier is run.
It creates no public claim and moves no Canon, Registry, frontier, gate, or
scheduler state.

## 1. Frozen public inputs

Use only the public scopes of:

```text
GOLDEN-SIX-LINE-SYM2-FRAME [T]
TM-SYM2-PROJECTIVE-FOURFOLD [T]
TM-SYM2-SEMILINEAR-TWOFOLD [T]
TM-SYM2-REVERSAL-CLOSURE [T]
GYRON-DENSITY [T]                   control only, after construction
SUBSTRATE-KNIT                      only at its registered scope
ABELIAN-FACE-DICTIONARY [D]         conditional phaseful dependency only
```

and the NON-CANONICAL predecessor #375:

```text
omega(a,b,c)=c-a,
J_n=omega_n v_epsilon,
v_epsilon=(1,-1,-1,1)^T.
```

No theorem status is inherited from #375. Every use is explicitly candidate
level in this incubation.

The historical v15 files

```text
notes/canon/P-TM-SYM2-MEASURE-1-OWNER-DEFINITION.md
notes/canon/P-TM-SYM2-MEASURE-1-BORN-BRIDGE-INPUT.md
```

are preparation sources only. Their definitions are not authority. Any piece
used below must be re-derived and reclassified against current Public Canon
v46.

The sealed historical probe `P-TM-SYM2-MEASURE-1` is binding as a negative
boundary: its N2 projective-linear canonical-selector route is dead and may not
be repaired or resumed.

## 2. Frozen carriers

Window carrier:

```text
W3={001,010,011,100,101,110},
N(a,b,c)=(1-a,1-b,1-c),
Q=W3/<N>,
O_A={001,110},
O_B={010,101},
O_C={011,100}.
```

The frozen quotient transfer is to be reconstructed from the registered child
maps, not assumed from a target probability.

Orientation source candidate:

```text
omega(a,b,c)=c-a,
epsilon_read=chi_Q chi_F,
J_n=omega_n v_epsilon.
```

Coefficient-side Born candidate:

```text
K_B=Q(zeta_5),
I_5=Z/5Z,
v=delta_0+delta_1,
Supp(v)={0,1}.
```

For a coefficient vector `a=(a_r)` with rational coefficients define

```text
Z_pos(a)=sum_r a_r^2,
F(a)_k=sum_r a_r zeta_5^(rk),
Born_pos(a)_r=a_r^2/Z_pos(a),
Born_spec(a)_k=star(F(a)_k)F(a)_k/(5 Z_pos(a)).
```

The literal identification `F(v)_k=1+zeta_5^k` with the phaseful registered
verb is allowed only under the exact conditional dependency already carried by
`ABELIAN-FACE-DICTIONARY [D]`. The face weights alone do not select `v`.

## 3. Frozen extended source type

The candidate domain is not a selected selector. It is the complete
projective-gauge orbit record together with the retained orientation current.
Schematic type:

```text
Source5 = (projective_orbit_record, epsilon_read, omega_stream, J_stream).
```

The concrete finite calculation may use a representative `s` only as a chart.
Every output must be proved independent of changing charts within the frozen
projective gauge, and the result across the four gauge classes must be stated
without choosing one class.

Dropping `epsilon_read` before the map is defined is a falsifier. A final
measure which happens to be independent of orientation is allowed only if that
blindness is a theorem of the map on `Source5`.

## 4. Frozen candidate construction

### Stage Q. Quotient law

Reconstruct the quotient transfer on `Q` from the two registered child maps.
Prove its unique normalized stationary law. No `1/3`, `1/6`, line frequency,
or GYRON input may enter before the linear system is solved.

### Stage B. Conditional Born sheet

Reconstruct `v=delta_0+delta_1` at its exact conditional scope. Prove the
Fourier/Plancherel identities and derive the normalized distribution on
`Supp(v)` from coefficient magnitudes. For each two-element N-orbit `O`, push
this distribution through an arbitrary bijection `Supp(v)->O` and prove
independence from the two possible support orientations.

### Stage M. Window measure

Only after Q and B are proved define

```text
mu_B(w)=pi_Q([w]) * half_[w](w).
```

Prove totality on all six words and normalization. The orientation current is
retained in the domain but is not inserted into the scalar probability by
hand.

### Stage L. Line pushforward

For every selector chart `s` in the complete 48-member class define

```text
mu_s=s_* mu_B.
```

Prove the correct equivalence/coherence statement under the frozen
projective-gauge action and decide whether all four gauge classes produce one
physical measure without enlarging postcomposition gauge.

### Stage C. Controls

Only after the measure is constructed:

1. compare with the public mathematical six-line image and operator;
2. check the exact W3-to-adjacent-pair event pushforward against
   `GYRON-DENSITY`;
3. record whether orientation is visible or blind in the L6 scalar measure.

These are controls, not selectors.

## 5. Frozen falsifiers

F1. `omega^2` totality. The direct source-square route is dead if any N-orbit
has zero total `omega^2` norm. This does not kill the source itself.

F2. Coefficient carrier. If `v` requires a stronger premise than the declared
conditional `ABELIAN-FACE-DICTIONARY` dependency, or the support Born law is
not orientation-independent, the candidate stops or fires at that scope.

F3. Target leakage. Any use of `1/6`, `1/3`, `1/2`, GYRON density, or the
known uniform line image to choose the carrier, support, normalization,
coefficient vector, or quotient law is CIRCULAR.

F4. Selector dependence. If two frozen selector charts give inequivalent L6
measures under the current allowed equivalence, the candidate is negative.
No gauge enlargement is permitted.

F5. Orientation deletion. If the map is defined only after quotienting
`epsilon_read`, it fails the current frontier type even if the numerical
measure is normalized.

F6. N3/frame conflict. Any conflict with the exact public six-line frame
operator after construction is negative at this candidate scope.

F7. Decoder overreach. Any `MatterData`, common total QCarrier, D_matter write,
SI, or decoder-completion claim is STOP.

## 6. Independent breaker order

Before any positive verifier, freeze `break.py` which must test or prove:

```text
B1  omega-only zero-norm orbit
B2  quotient stationary-law uniqueness from the transfer
B3  coefficient support equality and orientation independence
B4  all-six totality and normalization
B5  representative/gauge covariance
B6  four-class coherence without gauge enlargement
B7  GYRON used only after construction
B8  no D_matter conclusion
```

The positive verifier may not be read when the breaker is authored.

## 7. Decision

```text
BRIDGE
    all stages Q,B,M,L close exactly, the extended source remains typed,
    and one coherent normalized six-line measure results without target leakage.

PARTIAL
    source survives but one required Born, totality, coherence, normalization,
    dependency, or registered-gate premise remains open.

F
    one exact candidate falsifier fires.

STOP
    authority, collision, typing, dependency, or procedure is incomplete.
```

A BRIDGE result remains NON-CANONICAL and no stronger than its weakest
consumed dependency. It does not close `TM-SYM2-PHYSICAL-MEASURE [O]` by
itself and cannot touch `QUADRATIC-DECODER-DATA [O]`.
