# PROMO C-TM-SYM2-ORIENTED-BORN-JUNCTION-1-N

```text
STATUS:     NON-CANONICAL PROMOTION PACKAGE
AUTHORITY:  none
TARGET:     future public treatment only after separate owner decision
```

## Candidate content

The proposed successor package consists of two ordered non-canonical results:

1. `#375 C-TM-SYM2-ORIENTATION-SOURCE-1-N`

   ```text
   omega_n=theta_(n+1)-theta_(n-1),
   J_n=omega_n v_epsilon,
   ```

   the unique rational W3 source in the joint N/R odd character, selector-free
   and coherent at L5.

2. `#376 C-TM-SYM2-ORIENTED-BORN-JUNCTION-1-N`

   an exact candidate-D bridge from the complete orientation-retaining L5 type
   to one normalized six-line measure. The quotient law and sheet Born law are
   derived independently; the resulting `1/6` and golden-frame operator occur
   only after construction. All 720 bijective charts agree, so the frozen 48
   selectors are covered without a representative or gauge enlargement.

The direct `omega^2` Born shortcut is a bounded F because the palindrome orbit
has zero norm.

## Required owner decisions before any formal public probe

1. Approve or reject #375 as the successor L5 source schema. It is presently
   NON-CANONICAL.
2. Decide the exact D-level coefficient carrier dependency used by #376. The
   package consumes the explicit phaseful expression inside
   `ABELIAN-FACE-DICTIONARY [D]` to obtain `delta_0+delta_1` by Fourier
   inversion. This dependency must be explicit if retained.
3. Keep `MEASURE-BORN-VERB [D]` as the public status ceiling for the physical
   Born reading. No T promotion is proposed.
4. Do not substitute #377's intrinsic N-character carrier for #376 without a
   separate dictionary change: #377 is mathematically cleaner but does not
   satisfy the current public Born-of-the-verb dependency.

## Proposed future formal scope

If the owner accepts 1-3, open one fresh public probe with a new identifier.
The preregistration must freeze:

```text
complete L5 source type and equality,
coefficient/Born carrier and D dependency,
quotient transfer and total map,
all-48 coherence law under the existing projective gauge only,
normalization,
golden-frame operator control,
GYRON event pushforward as a post-construction control,
no-target-leakage checks,
D_matter / QUADRATIC-DECODER-DATA firewall.
```

The formal verifier must reconstruct rather than import the candidate results,
be pinned before execution, and pass the current two-architecture public
procedure. The public probe may at most earn the status justified by its D
Born dependency.

Only a later separately reviewed Canon fold may move
`TM-SYM2-PHYSICAL-MEASURE [O]`.

## Explicit nonclaims

No current public gate is closed by this file. No Canon, Registry, frontier,
dependency, gauge, selector, decoder, MatterData, SI, or physical uniqueness
claim is made. `QUADRATIC-DECODER-DATA [O]` is untouched.
