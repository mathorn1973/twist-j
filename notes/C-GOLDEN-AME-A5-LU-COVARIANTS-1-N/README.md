# C-GOLDEN-AME-A5-LU-COVARIANTS-1-N

**NON-CANONICAL.** Preregistered Schur-covariant attack on arbitrary local
unitary `A5` actions of representation type `1+5` for the pinned golden
AME(4,6) tensor.

The frozen `n<=3` test is **INCONCLUSIVE**: an exact 2-unitary cancellation
shows that every covariant in the family is automatically scalar for every
four-party perfect tensor.  The machine census and an independent
implementation confirm the complete frozen enumeration modulo 41.

See `PREREG.md`, `RESULT.md`, `AUDIT.md`, `CROSSCHECK.md`, and `RUN.md`.

