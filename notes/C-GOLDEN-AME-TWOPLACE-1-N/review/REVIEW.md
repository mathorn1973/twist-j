# C-GOLDEN-AME-TWOPLACE-1-N, independent review

Status: **NON-CANONICAL review record**

Reviewed object: result package of commit
`177f2602dc406ea4ee89f24d4fd32bd39500a5ec`

Preregistration: `494ce485e92911c107b2a171935f76d7e3f81ff5` (unchanged)

Canon writes: none. PR: none. Promotion: none. The verdict of `RESULT.md`
is not modified by this review.

## Provenance and blindness

This review comes from a second, independent session.

- `verify_g0_g1_independent.py` was written and executed against the pinned
  source before this session first fetched commit `177f2602`; its G0/G1
  certificate predates first contact with the primary package, and its
  design (Galois-stabilizer route) shares no code with the primary verifier.
- `review_g3_g4_independent.py` was written after reading `RESULT.md`, but
  re-derives every referenced number from the pinned 8515-byte source alone
  and shares no code or intermediate data with `breaker_g3_g4.py`.
- The upstream pin was re-verified by fresh clones on every machine used
  for this review: commit `1fa4a4d4d2b9a3c6d3b6c8d802116415fb679fe8`, blob
  `e0d0e171d58b3360c39595d677ffc401a466112d`, 8515 bytes, SHA-256
  `55fbc0ba2747b4e5adc5a5abd15ac7241a461ff8182268ebdae464d8a29cc9ae`.
- `sha256sum -c SHA256SUMS.txt` of the reviewed package: all ten entries OK.

## G0/G1, blind agreement

Same verdicts from a different route. The primary proves minimality of the
entry field constructively (`zeta_40 = c (1+w^5) w^-2` from two entries).
The review proves it by showing that every nontrivial `sigma_m` in
`Gal(Q(zeta_40)/Q)` moves an explicit entry (witness table in the
certificate), so the stabilizer of the entry set is trivial and
`F_U = Q(zeta_40)` by Galois correspondence. The primary checks left and
right unitarity of the three maps; the review checks the three frozen
products `U U*`, `R(U) R(U)*`, `Gamma_2(U) Gamma_2(U)*` (a one-sided
inverse of a square matrix over a field is two-sided).

Agreement on every shared quantity: support 112 = 40 a + 40 b + 32 c; all
section-5 identities of the preregistration; minimal entry field
`F_U = Q(zeta_20, sqrt2) = Q(zeta_40) = Q(zeta_5, zeta_8)` of degree 16.
The review's deterministic greedy witness search selected exactly the
primary's two generating entries, `U[1,2] = c` and `U[2,3] = c w^17` in
one-based indexing. Certificate: `CERT-G0-G1-INDEPENDENT.txt`, 38 PASS,
0 FAIL.

Review-only observation: the review verifier also parsed the
non-authoritative `AME46.py` textually. It has 112 entries and the same
letter multiset, but it matches the pinned tensor under no map in the
declared class of 24 slot permutations x conjugation x global `mu_40`
phase, not even at support level. Treating that file as textual-only in
the preregistration was necessary, not merely cautious.

## G3/G4, independent confirmation of the falsifier

Re-derived from the source alone, then compared with `RESULT.md`:

- all four per-leg support degree vectors agree;
- equal-degree classes have max sizes (2,2,4,4) and degree-preserving
  group orders (4,8,48,48);
- all 144 order-5 elements of `S6` have cycle type (5,1) (full
  enumeration), and `A5` is generated by its 24 five-cycles while 5 does
  not divide `|S4| = 24`, so every homomorphism `A5 -> S4` is trivial;
- hence the killer fires: no leg admits a degree-preserving 5-cycle, so no
  faithful six-point `A5` exists in the frozen local monomial class;
- the independent sweep of all 73728 identity-leg degree-preserving tuples
  found exactly the primary's two support+letter survivors;
- the nontrivial survivor `(id, id, (01)(23)(45), (01)(23)(45))` has no
  `mu_40` phase lift: the linear system is unsolvable mod 5 (rank 21) and
  also unsolvable mod 8, so the obstruction holds at both prime places of
  40; this slightly strengthens the primary's mod-5 certificate;
- the strict simultaneous sweep of 720 x 24 permutations leaves only the
  identity, even combinatorially.

Certificate: `CERT-G3-G4-REVIEW.txt`, exit 0. The G4 hard falsifier and
the exact scoped negative of `RESULT.md` stand.

## Not re-derived here

G2 was read for consistency only: the six frozen lines are the icosahedral
vertex axes and the printed cycle census (1, 15, 20, 24) matches the
classical rotation action of `A5` on them. This session did not recompute
the `SO(3,Q(phi))` enumeration or the marked `2I` comparison.

## Reproduction record

`RUN-LEGS.md` holds the multi-architecture record: the three primary
scripts and the two review scripts are byte-identical across x86_64 and
aarch64, which the primary `RUN.md` deliberately did not claim.

## Verdict

Independent review CONFIRMS the result package: G0 and G1 re-derived blind
with full agreement, the decisive G4 falsifier re-derived independently
with agreement in every checked number, and all certificates
architecture-stable. No canon action follows from this review.
