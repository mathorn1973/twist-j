# RESULT

**PUBLIC, NON-CANONICAL.**
Owner issue: #1165.
Author: A. M. Thorn.
Date: 26 September 2026.

## Verdict

- **candidate-T:** the written charge-faithful local projector, exact unsigned
  representation, exact exterior-completion law, and all-distance deterministic
  neutral-tube obstruction.
- **candidate-C:** the frozen exact finite audit on one Linux x86_64 lane.
- **Fired route condition:** a universal conditional continuation factor
  `q_*<1` over every admitted support/exterior conditioning is false.
- **OPEN:** any useful uniform bound on `Xi_L` or `Xi_L^(2)`, unconditional
  long-distance current control, the massless phase and P1.

## Scientific result

The local block rule

`
lambda(m,h)=1/(binom(r+h,h) r!),  r=(m-h)/2,
h in {0,5,10}
`

is an exact modulo-five projector when pairs carry opposite signs and the
single large block carries equal signs. It is charge-faithful:

`
h=5 |(sum z_t)/5|.
`

After signs are forgotten, the exact conditional law at a target edge is
weighted by

`
1{consistent} 2^k lambda(m,h),
`

not by `lambda` alone.

For the explicit all-distance neutral-tube support `n_D` from the existing
photon notes, take replicas `(n_D,-n_D)`. Every neutral degree-two edge has
two labelled pairings but both connect the same two faces. Every charged
degree-five edge has one forced ten-block. Since the occupied-face graph is
connected for every `D>=3`, the marked charged blocks remain in one auxiliary
component with conditional probability exactly one for arbitrarily large
separation.

Therefore no proof can dominate this full conditional exploration by a fixed
per-step factor `q_*<1` uniformly over all admitted conditionings.

## Audit

Frozen pin:

`fdd1662ec3f828db20c9982a6821e12628863e55`

The first successor run passed:

```text
exit_code=0
stdout_bytes=805
stderr_bytes=0
```

The exact stdout and input hashes are in `RUN.md`.

The predecessor #1164 remains separately preserved as a failed first audit.
It is not overwritten or reinterpreted.

## Boundary

The no-go is about **conditional thinning**, not unconditional decay. The
deterministically connected support has a distance-dependent small full-measure
weight, so its existence does not give a lower bound on the full covariance.

No Canon claim changes. No physical-photon conclusion is made. P1 remains
open.
