# First-cut realization and physical qualification

**NON-CANONICAL / DESIGN AND DOCUMENTATION ONLY / NO MEASUREMENT.**
This continues the existing calibration and realization lanes #830 and #832
after the selected passive family and the completed quadratic-calibration
audit. It is not a new formal probe or a fourth proposed v88 theorem.

The [compact construction](COMPACT-REALIZATION.md) specifies eight first-cut
signed outputs on the four-dimensional prepared source, four residual
channels and an explicit twelve-mode orthogonal completion. Twelve is the
minimum in that fixed complete isometry class. The ordinary sufficient full
wave buffer has 386 modes; it represents a broader state/time problem.
`explore.py` and `exploration-results.json` supply exact rational design
certificates with explicitly disclosed exploratory execution. The script
uses only the standard library and has no external input or side effects.

The [sensitivity derivation](SENSITIVITY.md) turns independently calibrated
linear transfer errors into a concrete residual allowance. No numerical
apparatus error, measured tolerance, sampling law or physical certificate
is supplied. A deliberately synthesized analogue tests implementation and
metrology; it does not independently establish autonomous TWIST-J physics.

The [source audit](SOURCE-QUALIFICATION.md) inspects only primary methods,
catalogue metadata and pinned author code. CURBy's known parameter consumer
is an entropy extractor, not a demonstrated timing/coverage certificate.
No measurement API payload, detector outcome, archive member or verifier was accessed.
SOURCE-QUALIFICATION.sources.json records the scoped evidence and gaps.

Both derivations received independent static review; a separate rational
calculation reproduced the response rows, principal minors and buffer size.
The eight-row sensitivity constants were independently recomputed in the
source-energy metric. These are design checks, not formal or empirical runs.

The [v88 proposal](../canon/PASSIVE-QDD-V88/README.md) retains only its three
previously pinned theorem candidates. All physical owners and the existing
public-archives-only acquisition scope remain unchanged.
