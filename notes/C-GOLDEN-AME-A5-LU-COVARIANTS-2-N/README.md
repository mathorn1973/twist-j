# C-GOLDEN-AME-A5-LU-COVARIANTS-2-N

Status: **NON-CANONICAL exact scoped negative**

This directory contains the publicly preregistered `n=4` continuation of the
arbitrary-local-unitary `A5` attack.  The frozen first hard witness proves
that the pinned golden AME(4,6) tensor has no diagonal local `A5` action in
which every six-dimensional local representation is unitarily equivalent to
`1+5`.

Start with:

- `PREREG.md` for the immutable pre-computation protocol;
- `RESULT.md` for the mathematical verdict and scope;
- `AUDIT.md` and `INDEPENDENT_AUDIT.md` for two implementation audits;
- `RUN.md` for reproduction;
- `SOURCE.md` for the sole external tensor pin.

The directory is a notes-only incubation record.  It changes no Canon or
Registry claim and contains no `PROMO.md`.
