# Frontier attack map at Public Canon v66 (NON-CANONICAL)

```text
STATUS:                    ANALYSIS / CLOSURE-AND-FALSIFICATION PLAN PROPOSAL
AUTHORITY:                 NOT CANON
PUBLIC CANON:              Public Canon v66 / canon-v66
PUBLIC CONTENT COMMIT:     8f11ec18825aa769308132254e8de35663006a1a
PUBLIC CANON SHA-256:      76de4fb05f7d1aed803e581a7d470e6ed8fd63923603ebe780e91990fb0be279
SCOPE:                     ALL 30 LIVE O/H ROWS OF canon/REGISTRY.tsv
METHOD:                    QUOTE-ANCHORED REDUCTION + ADVERSARIAL VERIFICATION
VERIFICATION RECORD:       28 CONFIRMED / 2 WEAK / 0 REFUTED (see section 2)
COMPANION TABLE:           REDUCTION.tsv (one row per live claim, quote-anchored)
FORMAL RUN:                NONE
PROBE / PREREGISTRATION:   NONE
CANON/TABLE/STATUS CHANGE: NONE
```

## 1. Question and method

The owner's conjecture: the 27 O rows have relations, and at the core a part
of them is really one thing. This note tests that conjecture against the
ledger and derives an attack plan whose phases are ordered by what *decides*
(closes or falsifies) rather than what merely prepares.

Method. Each of the 30 live rows was read against its full `canon/REGISTRY.tsv`
row, its `canon/FRONTIER.md` entry, its `canon/DEPENDENCIES.tsv` edges in both
directions, its gate ownership in `canon/GATES.tsv`, and the relevant
`canon/CANON.md` passages. Every blocking object attributed to a row carries a
verbatim quote from that row's own scope or falsifier text. A synthesis stage
reduced the rows to a minimal core-object set, testing three prior hypotheses
(H1: one physical event/measure law; H2: one recurring classification method;
H3: one scale-selector-to-dictionary chain) rather than assuming any. Every
entry of the resulting reduction map was then adversarially re-verified against
the registry text by independent checks instructed to refute.

All 30 quotes in `REDUCTION.tsv` were additionally re-matched verbatim
(whitespace-normalized) against the live registry before this note was filed.

## 2. Verification record

28 of 30 mappings CONFIRMED, 2 WEAK, 0 REFUTED. The two WEAK entries, recorded
so the map is not read as stronger than it is:

- `BELL-CAUSAL-ACCOUNTING` — the C1 event-law core genuinely supplies the
  instrument map, realized outcomes and the kernel `P(a,b|x,y,lambda)`, but the
  row's STOP demands one preregistered attack with further independent fields
  (setting-selection mechanism, correlation/CHSH conventions, measurement
  independence or a frozen replacement, both no-signalling equalities, the
  dimensional audit) that no core carries. Closing C1 alone does not discharge
  the row.
- `ALPHA-S-RUNNING` — the C3 scheme conventions are needed to *type* its match
  condition, but the positive close equally requires the running itself to be
  derived from the 3/4 seed; C3 makes the row scorable, not closed.

## 3. Verdict on "one thing"

The conjecture is right that a core exists and right that it is much smaller
than 30 rows — but the core is **one method, not one token object**, and the
"one thing" claim is true twice over:

**(1) Nineteen of thirty rows share one method** — the completed
admissible-class engine, in typed terms the triple `(P, Comp, Dec)`:

```text
P     a frozen typed package: carrier, whole-family equality, normalization,
      admissible-class boundary, acyclic dependency graph;
Comp  a completeness theorem: every admitted object lies in exactly one
      declared family of P;
Dec   a counted-survivor decision |S| in {0, 1, >=2} under every registered
      constraint.
```

The Canon carries roughly ten pending instances of this triple on different
carriers (apparatus families, METRO protocols, reduction arrows, certificates,
curvature operators, decoder protocols, measure lifts, dictionaries,
normalizations, source classes) and has **never completed one positively**.
The only finished run anywhere is the fired-negative
`GATE-L1-L5-TM-SYM2-SELECTOR-STREAM` (48 selectors in four free
projective-linear gauge orbits, NONCANONICAL): proof the engine executes, and
the template for what "done" looks like. Rows split by stage:
*definition-starved* rows cannot yet pose their class
(`COLOR-MEASURE-SELECTION`, `TT-SOURCE`, `TT-VECTOR-STATE-NORMALIZATION`,
`SQRT-PHI-TIME-GRAVITY`, `DE-CONFORMAL-WEIGHT`, `FRW-INHOM`,
`PROTON-RESIDUAL-IS-QCD`, `SCHEME-DICTIONARY`); *classification-starved* rows
have a posed class but no completeness proof or decision
(`QDD-INSTRUMENT-CLASS-COMPLETENESS`, `METRO-ADMISSIBILITY`,
`METRO-ADMISSIBILITY-DIM`, `METRO-REDUCTION-CALCULUS` obligations B/D/E,
`CURVATURE-OPERATOR-CANONICAL`, `TRACEKERNEL-CURVATURE-FORCING`,
`MINIMAL-READ-DERIVATION`, `ENTROPY-LAYER-BRIDGE`, the selector half of
`METRO-EDGE-SCALE`).

Because the carriers differ, one completed instance transfers as a template
plus **exactly one literal shared token**: the typed unbounded-memory
architecture disposal, named verbatim by two O rows —
`QDD-INSTRUMENT-CLASS-COMPLETENESS` ("finite and unbounded memory") and
`METRO-ADMISSIBILITY` ("unbounded-memory adaptive") — and covered by nothing
anywhere in the Canon. One freeze serves two obligations.

**(2) H1 is true for exactly four rows, not for the L6 measure wing.** The
realized-event and sampling law — O1 transducer (physical context key,
selected ready phase, persistence/reset semantics, ZERO_SUPPORT handling,
exact ordered occurrence law), a registered L1-to-L5 gate that does not exist
in `canon/GATES.tsv`, the independent terminal-saturation semantics, and the
induced outcome kernel — is genuinely one missing physical **adoption** behind
`QDD-INSTRUMENT-APPARATUS`, `QDD-TERMINAL-EVENT-SEMANTICS` and (partially,
see section 2) `BELL-CAUSAL-ACCOUNTING`. The mathematics beneath it is fully
frozen: the 22-context Euclidean carry bank, the canonical pure-record map,
the frozen effect pair. All 21 theorems `BOUNDED_BY` the apparatus row narrow
the picture; none supplies the law. `ENTROPY-LAYER-BRIDGE` and
`COLOR-MEASURE-SELECTION` do **not** wait on events — the first is a pure
existence question over a frozen lift package, the second is starved of a
target-side definitional package.

**H3 is false as stated.** `SCHEME-DICTIONARY` owns no gate, and the SI clause
sits in `METRO-EDGE-SCALE`'s registry scope. The empirical wing hangs on three
sibling tokens of one convention species (SI clause over the single `m_e`
bridge; named measurement scheme with scale/threshold and window semantics;
shadow-to-mu inference rule), not on one selector-to-dictionary chain.

**Provably outside every core (5 rows):** `GENERATIONS-L3` and
`QUADRATIC-DECODER-DATA` (nothing is missing — one is a runnable derivation
against a binary no-STOP gate, the other a verification-plus-ratification
release act), `DE-W-CONSTANT` and `NS-TILT` (external survey releases against
frozen or freezable thresholds), `LAMBDA-COCYCLE-ANGLES` (RH-equivalent,
hostage to external mathematics; the internal residual-bound test is strictly
one-sided).

## 4. The four cores

**C1 — EVENT-LAW** (blocks 3: apparatus, terminal-event-semantics, Bell in
part). A physical law to be *adopted*, not defined or computed. First
freezable piece: one candidate O1 predefinition on the already-frozen
mathematics — adopt the 22-context carry bank as the transducer, declare
reduced-p the physical context key, select one ready-phase vector with a
persistence/reset law and ZERO_SUPPORT branch, state the ordered occurrence
law, register the missing L1-to-L5 gate. Auditing that single candidate
against the row's occurrence-law and target-independence conditions is
decisive for what O1 still lacks.

**C2 — FREEZE-CLASSIFY-DECIDE** (blocks 19). The engine of section 3. First
freezable piece: the typed unbounded-memory architecture child — one carrier
type plus decision condition, registered simultaneously as
`METRO-ADMISSIBILITY`'s R3 child and as the unbounded-memory disposal clause
of the apparatus contract. The single zero-coverage class two O-row falsifiers
name verbatim.

**C3 — EXACT-TO-MEASURED** (blocks 5: `METRO-EDGE-SCALE`,
`SCHEME-DICTIONARY`, `ALPHA-S-RUNNING`, `QNM-LEAVER-MU`,
`METRO-ADMISSIBILITY` via R7). Three unwritten convention tokens of one
species. First freezable piece: `SCHEME-DICTIONARY`'s complete STOP list as
one definition-and-manifest package (seed domain `alpha* = 1/5`, strong root
`3/4`, seed ratio `15 : 4` from `reproduce/coupling-metrology`; one named
scheme with scale/threshold conventions; the total map with equality and
window semantics; pinned source manifests; the acyclic dependency graph) —
pure definition work that simultaneously types `ALPHA-S-RUNNING`'s close
condition. The Leaver continued-fraction half of `QNM-LEAVER-MU` is already
computation-ready over the closed Regge-Wheeler potential.

**C4 — SUBSTRATE-DYNAMICS** (blocks 3: `QUANT-SUBSTRATE`,
`NEUTRON-DELTA-EM`, `DRESS-CROSSCOUNT`). A second, disjoint "one thing": the
alpha-power correction machinery one floor above the closed exact seed/tree
theorems. First freezable piece: one candidate substrate-coupling
predefinition with frozen action normalization and regularization, scored
immediately by exact arithmetic against the frozen Schwinger target
`J Jbar / script-Q = 1/(2 pi)`. The row is READY; a completed failing
realization gate fires it negatively, so a finished attempt is decisive
either way.

## 5. Kill list

Eighteen of the thirty rows carry a live negative route. Grouped by what the
single firing witness needs:

**Internal, actionable now** (the witness is a finite exact computation or a
one-lemma theorem over already-pinned objects):

- `METRO-REDUCTION-CALCULUS` — one exact transport failure of obligation D at
  the smallest frozen scope (q=2, k=2 over the pinned 1024-tuple family), or
  one allowed/forbidden collision in the catalogue.
- `METRO-ADMISSIBILITY-DIM` — one exact admitted counterexample to soundness,
  completeness, coherence, total normalization or reduction invariance of
  `Cert_joint` versus direct translated-box averages over the enumerated
  q=2, a=2, r=1 families.
- `ENTROPY-LAYER-BRIDGE` — one lemma from a negative close: the mu-limit
  closure lemma over cylindrical approximants, combined with the registered
  cylinder no-go (global solution count zero over all 522 pure-word systems),
  yields the complete theorem `A_A = empty`.
- `METRO-EDGE-SCALE` — two inequivalent selectors surviving every named gate;
  the NONCANONICAL pattern has already fired once
  (`GATE-L1-L5-TM-SYM2-SELECTOR-STREAM`), so this outcome shape is proven
  reachable.
- `MINIMAL-READ-DERIVATION` — a proved-nonempty admissible class containing
  one fully compliant beta_3 realization; no uniqueness theorem needed.
- `LAMBDA-COCYCLE-ANGLES` (one-sided) — one exact finite violation of
  `0 <= M - t_n <= 2M` by certified interval arithmetic; the instrument can
  only fire the row, never confirm it.
- `QUADRATIC-DECODER-DATA` — the only negative clause not already contradicted
  by the exact factorization theorem is "an unregistered input is required";
  an acyclicity/completeness audit of the row's transitive closure decides it.

**Internal, armed by a finished derivation** (binary gates that fire only on
completion):

- `GENERATIONS-L3` — the owned gate is binary with no STOP branch: a completed
  L2-to-L3 derivation closes positively at three or fires at anything else.
- `QUANT-SUBSTRATE` — a completed substrate-coupling candidate through the
  Schwinger physical-realization gate closes or fires against the exact
  arithmetic target `1/(2 pi)`.
- `TT-VECTOR-STATE-NORMALIZATION` — every admissible normalization violating
  the registered TT identities or needing an extra free dimensionless input
  closes it negatively; one adopted fourth-moment law decides its own fate.
- `CURVATURE-OPERATOR-CANONICAL` — the classification decision itself carries
  both negative arms (NONUNIQUE, EMPTY).
- `TRACEKERNEL-CURVATURE-FORCING` — once the upstream class is public and
  nonempty, one violating member decides; the eight-of-480 `GL_2(F_5)`
  equivariance boundary is a pre-typed exact negative route.

**External data** (no internal action can fire them; freezing the arming rule
is the internal work):

- `DE-W-CONSTANT` — rule R1 is evaluable today: `P-DE-W-ARMING-1/RESULT.md`
  records R1 as PENDING against the published DESI DR2 headline constant-w
  posterior. Rules R2/R3 wait on the first primary-dataset-disjoint carrier
  pair.
- `NS-TILT` — fires against CMB-S4; its arming probe (exclusion threshold,
  carrier semantics, labeled non-firing Planck/ACT witnesses) is unwritten.
- `DRESS-CROSSCOUNT` — an exact labeled witness departing from `72 alpha^4`
  at the 0.204 ppm scale.
- `NEUTRON-DELTA-EM` — conditional on C4: a derived compression channel value
  outside the measured tier window.
- `QNM-LEAVER-MU` — conditional on the C3 inference rule: an exact spectrum
  incompatible with the preregistered mu interval.

**Conditional on C1 existing first:**

- `QDD-TERMINAL-EVENT-SEMANTICS` — a typed admissible completed physical event
  whose post-state record retains a nonzero residual commutator (a bare
  mathematical witness is explicitly not a physical falsifier).
- `QDD-INSTRUMENT-CLASS-COMPLETENESS` — one independently admissible apparatus
  outside the claimed class or violating a claimed family equality.

Not falsifiable at all: `QDD-INSTRUMENT-APPARATUS`'s failure mode is honest
permanent STOP ("SAMPLING NOT PROVIDED rather than impossible"), and
`BELL-CAUSAL-ACCOUNTING`'s partial accounts remain STOP by construction.

## 6. Attack plan

Ordered by decision density; POLICY sequencing (predefinition, owner freeze,
preregistration, verifier) applies to every formal step.

**Phase 0 — decide what is already decidable (no new objects).**
(a) Evaluate `DE-W-CONSTANT` rule R1 on the published DESI DR2 constant-w
posterior — a computation on released data that the arming probe explicitly
left PENDING. (b) The `QUADRATIC-DECODER-DATA` closing release: run the
existing verifiers, audit the dependency closure (the one live negative),
then propose the ratification identifying the quadratic `D_matter` leg with
the Route A dictionary — registering the `K_QDD = K` identity and the binding
gate, and extending the frozen domain so the dagger/transpose pair is
actually distinguished. (c) Freeze `NS-TILT`'s arming probe before the
CMB-S4 release, mirroring `P-DE-W-ARMING-1`.
*Risk:* ratification is an owner act and can be refused; the R1 evaluation
may record another labeled non-firing witness.

**Phase 1 — fire the three READY derivation gates.** `GENERATIONS-L3` (binary
gate, no STOP branch), `QUANT-SUBSTRATE` (one candidate through the Schwinger
gate against exact arithmetic), `TT-VECTOR-STATE-NORMALIZATION` (adopt the
diagonally-fixed fourth-moment law from the already-classified Z/5 family,
compute a numerical `r_T(k)`, check the TT identities).
*Risk:* an unfinished derivation decides nothing; `QUANT-SUBSTRATE`'s failure
to construct is STOP, not falsification.

**Phase 2 — the double-duty class kill.** Register the typed unbounded-memory
child once for two obligations; run the phase-gauge equality computation
against the proved inequivalent phase families (collapse is a contradiction,
distinctness forces a phase-indexed class); implement obligation D's stream
transport at the smallest frozen scope.
*Risk:* the unbounded-memory carrier may prove untypeable inside the
registered universe — informative, but not itself a registered close.

**Phase 3 — complete one classification end-to-end.** The first positive run
of the C2 engine, in the lanes closest to a decision: the
`ENTROPY-LAYER-BRIDGE` mu-limit closure lemma (one lemma from a negative
close); the `CURVATURE-OPERATOR-CANONICAL` quadruple freeze, enumeration and
decision, then the pre-typed `TRACEKERNEL` machinery over every member (two
owned gates decided in sequence); the `METRO-EDGE-SCALE` selector comparison
at frozen dimensionless scope; the `MINIMAL-READ` beta_3 hunt; `Cert_joint`
with its terminating checker versus direct averages.
*Risk:* completeness theorems are the hard step in every lane; a wave of
NONUNIQUE outcomes would be scientifically decisive but program-negative.

**Phase 4 — anchor the empirical wing, freeze the remaining packages.**
`SCHEME-DICTIONARY` as one definition-and-manifest package (types
`ALPHA-S-RUNNING`); the SI clause over the `m_e` bridge after Phase 3's
selector decision; the `QNM-LEAVER-MU` inference rule plus the
already-runnable Leaver script; the `COLOR-MEASURE-SELECTION` seven-item
target package reused as `PROTON-RESIDUAL-IS-QCD`'s schema (one freeze, two
rows); the `TT-SOURCE`, `DE-CONFORMAL-WEIGHT` (CIRCULAR-compliant) and
`SQRT-PHI-TIME-GRAVITY` packages; one candidate inhomogeneous action for
`FRW-INHOM` checked against the closed FRW-CANONICAL-FORM identities (a
single successful candidate closes the row positively).
*Risk:* adopted conventions are effectively one-shot freezes; this phase
prepares more than it closes.

**Phase 5 — the event layer, audits before adoptions.** The
`BELL-CAUSAL-ACCOUNTING` typed-attack schema instantiated with existing Canon
objects (the gap list is decisive on paper and localizes to lambda, the
instrument map, realized events, the sampling kernel); one candidate O1
adoption on the frozen carry bank with the registered L1-to-L5 gate; candidate
saturation principles audited mechanically against the forbidden-input list
and the frozen negative controls.
*Risk:* the one place a genuinely new physical postulate is required; the
audit may show the non-circular statement space is empty, and the lane can
remain honestly, permanently STOP without ever falsifying.

## 7. Boundary

This note is `NON-CANONICAL`. It creates no claim, definition, candidate ID,
probe, preregistration, verifier, evidence row, gate, dependency, owner
decision or Canon change. The reduction map describes the ledger; it does not
bind it. Every phase above that touches the Canon requires its own POLICY
sequence, and every adoption or ratification it names is an owner decision
this note does not make. The two WEAK verdicts of section 2 are part of the
record and cap what the map may be cited as proving.
