# C-GEOMETRY-RELATIONAL-ACCUMULATION-N

**NON-CANONICAL incubation.** Author: A. M. Thorn. Opened 2026-09-07.
Authority: Public Canon v80, as declared in [STATUS.md](../STATUS.md).
This note creates no Registry claim, physical gate or version change.

The hypothesis is that a geometric measure could be a cumulative integer
count of typed relations. A potential mechanism is quadratic relational
layers plus a count direction, giving cubic growth. The word "could" is
essential: neither the layer direction nor a growth exponent has yet been
identified with the physical spatial radius or dimension.

## 1. Exact mathematical content

The complete pair carrier of size k has k^2 ordered pairs. Tagged disjoint
accumulation yields exactly n(n+1)(2n+1)/6. The coefficient 1/3 is its
leading term, not an additional fitted integral constant. More generally,
Pascal's identity supplies exact discrete antidifferentiation in the binomial
basis. The pair-valuation census also shows that cubic accumulation does
not select complete incidence: every fixed positive off-diagonal weight
already gives a cubic leading term.

The [growth-boundary probe](../probes/P-RELATIONAL-GROWTH-SATURATION-1/PROOF.md)
supplies the full proofs, counterexamples, declared finite audits and scope.
It extends the existing [relational-reading program](C-RELATIONAL-READING-ARITY-N.md)
without identifying Born weights with spatial area.

An essential correction to the motivating proposal is that V_n~cn^d does
not alone imply n(V_n-V_(n-1))/V_n->d. The strictly increasing integer
sequence V_n=n^3+(-1)^n n^2+n has cubic growth, yet that ratio is one for
odd n and tends to five along even n. Polynomial counts, a controlled
local remainder, or eventually monotone shells repair the inference. A
macroscopic fixed-dilation ratio is valid under the original asymptotic.

## 2. The native carrier audit

The public definitions contain several DIFFERENT mathematical carriers.
They cannot be joined by calling each of them "commutator space".

| Registered object | What the Canon actually fixes | Growth consequence or missing bridge |
| --- | --- | --- |
| Checkpoint X=F5^6 | 15,625 distinct checkpoint states | Fixed-carrier balls eventually saturate. This is not the complete autonomous state. |
| Native word group Gamma | A subgroup of the finite AGL_6(F5), with an explicit finite bound | Semantic word balls saturate; counting unreduced words would count another object. |
| Fired derived subgroup | Exactly the 25 fiber translations, from b,d,e | Under the explicitly chosen six commutator steps, balls are 1,7,19,25 and then constant. |
| Historical curvature carrier V | Rational function-space compression with dimension 818 | Algebraic dimension is not a spatial ball-growth exponent; operator choice is not unique. |
| Trace-kernel W_5 | A separate three-dimensional finite-field vector space | The Canon expressly leaves the same-carrier bracket/curvature bridge open. Reading its dimension as the desired growth exponent would insert the result. |
| Full autonomous U | The checkpoint together with the unbounded native counter | Finite-carrier saturation cannot be applied to this whole state. A spatial filtration of its orbits is not supplied here. |

These statements use canon/CANON.md's FIRED-COMMUTATOR-NOGO,
DEF-NATIVE-WORD-CURVATURE-CLASS and TRACEKERNEL-CURVATURE-FORCING sections,
and canon/CORE.md's exact autonomous state. TIME-CUT-READING places the
spatial dictionary channel on silent a,c, not the fired commutator subgroup.

## 3. A precise positive construction and its native counterexample

Choose the displayed signed affine formulas for b,d,e over Z^6 instead of
F5^6. This is an explicit additional lift, not a consequence of native
semantic equality. Its derived group is the rank-two lattice generated by

    t1=(0,0,0,0,-2,0),
    t2=(-5,-5,-5,-5,-2,-2),

with third pair commutator t1+t2. Under reduction modulo five, its quotient
by 5L is exactly the native 25 translations. With the six signed pair
commutators as steps, its orbit balls have

    |B_r|=3r(r+1)+1=(r+1)^3-r^3.

Therefore the TAGGED cumulative count is exactly

    sum_(r=0)^n |B_r|=(n+1)^3.

This is a concrete algebraic realization of the proposed counting mechanism.
It is stronger than a numerical sphere analogy: the lift and reduction maps
are written down. It still does not derive a spatial dimension. Its balls
themselves grow quadratically, and retaining the layer tag counts a point
again at each later radius. Forgetting tags leaves the largest ball.

The native quotient exhibits the boundary immediately:

| Radius n | 0 | 1 | 2 | 3 | 4 |
| --- | --- | --- | --- | --- | --- |
| Lifted ball | 1 | 7 | 19 | 37 | 61 |
| Native ball | 1 | 7 | 19 | 25 | 25 |
| Lifted tagged accumulation | 1 | 8 | 27 | 64 | 125 |
| Native tagged accumulation | 1 | 8 | 27 | 52 | 77 |

After saturation the native tagged accumulation is 25n-23. The tempting
initial cubes do not establish an asymptotic dimension. The lifted piston
translations vanish modulo five, so changing to the lift changes meaningful
relations of the model.

The silent group commutator [a,c] supplies a distinct control. Its orbit
has five points modulo five; in the literal signed Z lift it is an infinite
cyclic orbit with balls 2n+1 and tagged accumulation (n+1)^2. This is not
the additive Koopman curvature operator K_hist. The cubic fired-lift result
does not silently transfer to the spatial dictionary's silent channel.

## 4. What the geometric hypothesis still has to supply

Before a physical d=3 claim, fix independently:

1. The geometric carrier and semantic equality: native points, operators,
   orbits, an explicitly justified lift, or a specified family of carriers.
2. The generating relations and intrinsic radius/filtration, without selecting
   three coordinates or reparametrizing the radius to force the exponent.
3. The measure's counted objects and multiplicities: distinct points, relation
   incidences, actual disjoint shells, or tagged events are different choices.
4. The limit regime and compatibility maps, particularly if fixed modulo-five
   data are to support an unbounded geometry.
5. The physical geometry/decoder bridge and which notion of dimension is
   being claimed. Growth dimension does not by itself supply a metric,
   topology, manifold, isotropy, curvature or physical length.

A time counter used as a layer index is an added geometric interpretation,
not a proof that its layers are spatial shells. Argument and modulus of J
remain the existing exact algebraic projections and declared physical
dictionary. Hat-box factorization does not establish a new map from them
to surface measure or a unique geometric realization.

## 5. Frozen scientific disposition

The elementary accumulation mechanism survives, with an exact conditional
integer lift tied to the native quotient. The unrestricted local dimension
formula is false. The literal fixed finite commutator-ball route to an
asymptotic d=3 is excluded, and an initial cubic-looking prefix has an exact
counterexample. The broad physical geometry hypothesis remains
**STOP-DEFINITION**, not empirically falsified or established.

The next eligible positive attack is an independently justified unbounded
carrier/limit and its intrinsic spatial filtration. Merely choosing the
fired integer lift, appending a count axis or adopting W_5's already known
rank would not discharge that obligation. No canonical status is promoted
by this note or its accompanying L1 audit.
