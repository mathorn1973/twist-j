# RETIRE-TM-SYM2-PHYSICAL-MEASURE

**STATUS: NON-CANONICAL OWNER DISPOSITION. NO PROBE RUN. NO CANON
CHANGE.** This note has no normative authority and changes no claim, gate,
frontier, count, hash, tag, release, or status. It records one candidate input
for a later, separately sealed Public Canon v27 fold.

```text
candidate queue       issue #199
public base           Public Canon v26 ACTIVE
base main             48213275d0ace92d8f034166179a9fee4d53d908
tag                   canon-v26
content commit        138eec5b22a823469e1fa651505815a3d5b36761
Canon SHA-256         3a62711e30b1f3e9c4ade71533354fdf669266f60f4a57ade84e31a8f2878cfd
Canon bytes           141941
claim                 TM-SYM2-PHYSICAL-MEASURE
current status        O
proposed action       RETIRE, not F
owned gate            GATE-L5-L6-TM-SYM2-BORN-MEASURE
formal execution      none
provisional release   Public Canon v27, composed with other approved inputs
```

The eventual fold must start from the then-current public `main`. The tuple
above is the currency stamp for this disposition, not permission to reuse
stale release fields. Release identifiers, dates, hashes, counts, topology,
and exact path deltas must be recomputed when the owner seals the v27
manifest.

## 1. Owner decision

Retire `TM-SYM2-PHYSICAL-MEASURE` from the live frontier. Do not close it as
`F`.

The branch has already produced its exact scientific result:

1. the frozen projective-linear postcomposition gauge has 48 selectors in
   four free orbits of size 12;
2. the preregistered `NONCANONICAL` / N2 route therefore fired;
3. the semilinear comparison image has two orbits of size 24 but was not
   adopted as physical gauge;
4. drive-word reversal joins the residual classes only by dynamic
   precomposition and was not adopted as postcomposition gauge;
5. all 48 selectors have the same mathematical pushforward and common
   operator.

The remaining live row contains no candidate bridge. It reserves a place for
a possible future owner-approved L5 source, physical Born carrier, total
map, coherence law, dependency graph, and completeness proof. None is
currently frozen, and the present architecture does not require that such a
source be invented.

An unchosen future architecture extension is not a live scientific
obligation. The correct ledger action is therefore administrative
retirement, not a new negative theorem.

## 2. Why this is not `F`

`F` would require a frozen admissible class whose registered negative route
has been decided. No complete admissible class of physical L5-to-L6 bridges
was ever frozen for this successor owner.

The fired N2 decides only the older frozen selector route:

```text
TM-SYM2-MEASURE [F]
```

It proves that the registered projective-gauge action supplies no canonical
selector stream. It does not prove that every possible future physical
L5-to-L6 construction is impossible.

The later semilinear and reversal classifications do not repair or reopen
N2. They also do not empty a frozen physical bridge class, because no such
class exists. Promoting the successor owner to `F` would therefore claim a
negative theorem outside the registered scope.

## 3. Mathematical and protocol boundary to preserve

The eventual fold must preserve without status or scope change:

```text
TM-SYM2-MEASURE                  [F]
TM-SYM2-PROJECTIVE-FOURFOLD      [T]
TM-SYM2-SEMILINEAR-TWOFOLD       [T]
TM-SYM2-REVERSAL-CLOSURE         [T]
GATE-L1-L5-TM-SYM2-SELECTOR-STREAM = FIRED_NEGATIVE
```

It must also preserve these conclusions:

- the complete selector class has size 48;
- the frozen projective-linear gauge has four free orbits of size 12;
- N2 is terminal and may not be moved, reopened, or repaired;
- the semilinear and reversal actions are comparisons, not adopted gauge;
- `epsilon_read = chi_Q chi_F` remains typed reading-orientation data;
- no representative is selected from the 48 selectors;
- every selector has the mathematical image
  `nu_s(v_i) = 1/6`;
- every selector has the common operator
  `M_s = (1/3)P1 + (2/15)P5`;
- neither equality is promoted to physical probability, Born halving, or an
  L6 measure theorem;
- `GYRON-DENSITY [T]` remains unchanged at its registered scope; its named
  density limit is `1/6`.

The retirement says only that no physical L6 bridge from this branch has
been accepted. It does not assert that every future bridge is impossible.

## 4. Reverse-order firewall

The current row starts from desired outputs and leaves a blank owner slot for
a source that might later produce them. Approving such an empty envelope
would allow the decoder or bridge definition to absorb hidden freedom.

A future attempt must reverse that order:

1. independently motivate and type one concrete L5 source;
2. before computation, freeze its carrier, retained orientation, allowed
   actions, total map, coherence law, complete dependencies, completeness
   claim, and falsifiers;
3. only then test which L6 measure, if any, it produces.

No step in this note authorizes that future work.

## 5. Re-entry rule

`TM-SYM2-PHYSICAL-MEASURE` must not be reopened or reused after retirement.
The history validator already makes `RETIRED` terminal for a claim
identifier.

Any future attempt requires:

- a new owner and new claim identifier;
- a concrete, independently motivated L5 source;
- `epsilon_read` preserved as typed data;
- no representative selected from the 48 selectors;
- no enlargement of the frozen postcomposition gauge;
- its own physical carrier and total map;
- an explanation of why the construction is more than a renaming of the
  known selector-independent mathematical image;
- a new, separately named L5-to-L6 gate.

The current gate identifier is retired with its owner. Historical references
to that identifier remain provenance and are not rewritten.

## 6. Current ledger pin

The current registry scope SHA-256 is:

```text
c794a296aec9df49ac497c536ba26559ff26d5e73faaf5bf92bf61194405ef27
```

The current evidence tuple is:

```text
evidence id            EV-TM-SYM2-PHYSICAL-MEASURE
evidence kind          INLINE_CANON
location               inline
SHA-256                c794a296aec9df49ac497c536ba26559ff26d5e73faaf5bf92bf61194405ef27
hash mode              registry-scope-sha256-v1
architecture           none
```

The declaration is history sequence 1. If this disposition is consumed
without an intervening event for the same claim, the retirement is sequence
2 and preserves the tuple above:

```tsv
CANON27-RETIRE-TM-SYM2-PHYSICAL-MEASURE	2	<RELEASE-DATE>	canon-v27	TM-SYM2-PHYSICAL-MEASURE	RETIRE	O	RETIRED	c794a296aec9df49ac497c536ba26559ff26d5e73faaf5bf92bf61194405ef27	EV-TM-SYM2-PHYSICAL-MEASURE	inline	c794a296aec9df49ac497c536ba26559ff26d5e73faaf5bf92bf61194405ef27	Public Canon v27 retires the placeholder owner because no owner-approved successor L5 source exists and the obligation is not required by the current architecture; the fired N2 boundary, the three exact classifications, and the selector-independent mathematical image remain unchanged, and no broader negative or physical theorem is claimed
```

The event identifier, release label, date, sequence, and rationale must be
checked again against the sealed v27 batch. `RETIRE` leaves this history row
as the tombstone after the claim leaves current tables.

## 7. Exact later-fold operations

When the owner seals this item into v27, append the `RETIRE` event and remove
the current claim row from:

```text
canon/REGISTRY.tsv
canon/NORMATIVE.tsv
canon/EVIDENCE.tsv
canon/FRONTIER_PROGRAMS.tsv
```

Remove its eight outgoing rows from:

```text
canon/DEPENDENCIES.tsv
```

The current public graph has no incoming dependency on the retiring claim.
Remove its sole owned gate from:

```text
canon/GATES.tsv
```

Remove the live section-18 paragraph from `canon/CANON.md` completely.
Add the short retirement boundary to the existing TM-SYM2 theorem and
falsifier block in section 13, stating:

- N2 remains fired for the frozen selector route;
- the selector-independent mathematical image remains exact;
- no physical L6 bridge from this branch was accepted;
- retirement is not `F`;
- a future construction requires a new owner, claim, source, and gate.

Preserve the section-13 theorem boundary and all exact theorem and falsified
rows. Generate `canon/FRONTIER.md` and `canon/STATUS_COUNTS.tsv`, add the
composed v27 changelog entry, update the v27 release identity in
`canon/CORE.md`, and recompute `canon/SHA256SUMS`.

Update only the current release anchors in `canon/CANON.md` from v26 to v27.
Historical v26 ledger and changelog statements remain historical and are not
rewritten. In `canon/CHANGELOG.md`, rename the old
`GENERATED CURRENT COUNTS` markers to `GENERATED CANON26 COUNTS`, then place
new current-count markers under the v27 entry.

Insert a new `The v27 ledger change is signed term by term` prose block
immediately before the existing v26 block in `canon/CANON.md`. The new block
must give the exact composed v27 totals and transaction decomposition,
including this retirement and every other sealed v27 input. Preserve the
existing v26 signed block byte-for-byte as historical release arithmetic.
Do not leave a partial item count or stale pre-composition arithmetic in the
new v27 block.

The status-separation reproduction must retain every current TM-SYM2
assertion for:

- the three exact classification rows, including statuses, scopes, layers,
  normative item types, public evidence paths, evidence kinds, bundle hashes,
  architecture requirements, and complete surviving dependency sets;
- `TM-SYM2-MEASURE [F]`, including its exact scope, public evidence,
  normative mapping, and fired selector ownership;
- `GATE-L1-L5-TM-SYM2-SELECTOR-STREAM`, including owner, gate kind,
  L1-to-L5 endpoints, and terminal decision boundary;
- the no-cycle, no theorem-to-owner, and theorem-scope firewalls.

Delete only the assertions for the active physical owner, its eight
dependencies, the open Born gate, and its scheduler row. Add checks that:

- `TM-SYM2-PHYSICAL-MEASURE` is absent from current claim, normative,
  evidence, dependency, program, and frontier views;
- `GATE-L5-L6-TM-SYM2-BORN-MEASURE` is absent from the current gate table;
- no dependency names the retired owner;
- the theorem graph remains acyclic and has no theorem-to-retired-owner edge.

Update its exact expected output and README accordingly. Update the
architecture-map test anchors for the composed release.

## 8. Standalone v26-to-v27 delta

If this were the only scientific ledger input composed directly from Public
Canon v26, the expected deltas would be:

```text
claims                       210 -> 209
T                            111 -> 111
D                             40 -> 40
C                             23 -> 23
H                              3 -> 3
O                             23 -> 22
F                             10 -> 10
live H/O                      26 -> 25

NORMATIVE rows               226 -> 225
DEPENDENCIES                 335 -> 327
EVIDENCE rows                210 -> 209
evidence none                 43 -> 42
evidence one-architecture      9 -> 9
evidence recorded-audit       31 -> 31
evidence two-architecture    127 -> 127
HISTORY events               700 -> 701
GATES                         10 -> 9
FRONTIER_PROGRAMS             26 -> 25
reproductions                 22 -> 22

direct architecture          171 -> 170
transitive architecture      185 -> 184
dependency terminals          10 -> 10
```

These are audit expectations, not frozen v27 totals. Every total must be
recomputed after all other owner-approved v27 inputs are selected.

## 9. Expected later release paths

For this item alone, the later content fold is expected to affect these 17
tracked paths:

```text
canon/CANON.md
canon/CHANGELOG.md
canon/CORE.md
canon/DEPENDENCIES.tsv
canon/EVIDENCE.tsv
canon/FRONTIER.md
canon/FRONTIER_PROGRAMS.tsv
canon/GATES.tsv
canon/HISTORY.tsv
canon/NORMATIVE.tsv
canon/REGISTRY.tsv
canon/SHA256SUMS
canon/STATUS_COUNTS.tsv
reproduce/status-separation/EXPECTED.txt
reproduce/status-separation/README.md
reproduce/status-separation/verify.py
tools/test_architecture_map_report.py
```

The separate release-form commit changes exactly:

```text
STATUS.md
README.md
CITATION.cff
```

A composed v27 may add paths required by other sealed inputs. It must not
silently omit any path required by this retirement.

## 10. Composition and release topology

Issue #199 is the non-canonical candidate queue. Each additional v27 input
must arrive as an independently reviewable owner disposition or completed
formal result. Inclusion remains provisional until the owner freezes one
exact manifest.

The final v27 release branch is created fresh from the then-current public
`main`. It is not this notes branch and not a long-lived accumulation branch.
It has exactly:

```text
B <- C <- R
```

- `B`: the public base selected at seal time;
- `C`: one complete content commit composing every selected v27 input;
- `R`: one release-form commit changing only `STATUS.md`, `README.md`, and
  `CITATION.cff`, and naming `C`.

If `main` moves after `C` or `R` is frozen, abandon and rebuild the candidate
from the new base. Do not merge `main` into it, rebase it, amend it, squash
it, or force-push it.

Before merge, audit manually that:

```text
C^ = B
R^ = C
rev-list B..R has exactly 2 commits
```

The activation merge must have parents `[B, R]`, its tree must equal the
reviewed tree of `R`, `STATUS.md` must name `C`, and annotated tag
`canon-v27` must target the verified activation merge.

## 11. Later validation

After all manual content edits, generate the derived views, exact
status-separation transcript, and normative hash manifest:

```bash
python3 tools/generate_canon_views.py --apply

LC_ALL=C LANG=C PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 TZ=UTC \
  python3 reproduce/status-separation/verify.py \
  > reproduce/status-separation/EXPECTED.txt

sha256sum canon/CANON.md canon/CORE.md canon/FRONTIER.md \
  canon/REGISTRY.tsv canon/CHANGELOG.md > canon/SHA256SUMS
```

Then run the direct pre-freeze checks, at minimum:

```text
python3 tools/generate_canon_views.py --check-dir canon
python3 tools/check_policy.py
python3 -m unittest discover -s tools -p "test_*.py"
python3 tools/check_ledger.py
python3 tools/architecture_map_report.py
git diff --check
git diff --cached --check
```

Freeze the immutable content commit `C` only after the direct checks pass.
The diff-based checks require a committed head. On `C`, run:

```text
python3 tools/check_verifier.py --base <FULL-B-SHA>
python3 tools/check_reproduce.py --base <FULL-B-SHA>
git diff --check <FULL-B-SHA>...HEAD
```

For the complete v27 fold, `check_verifier.py` must report
`NOT APPLICABLE`, and the content diff must change zero paths under
`probes/`. Any additional formal result selected for v27 must first merge
through its own issue, immutable probe branch, and reviewed probe PR before
the owner seals base `B`. This disposition neither changes nor reruns the
sealed P4 verifier.

If any post-`C` check fails, abandon that content candidate and rebuild it
from the selected public base. Do not amend, rebase, or force-push the frozen
commit.

After the post-`C` checks pass, create `R` as the exact release-form commit
changing only `STATUS.md`, `README.md`, and `CITATION.cff`. On `R`, run the
release-head checks, including:

```text
python3 tools/check_policy.py
python3 -m unittest discover -s tools -p "test_*.py"
python3 tools/check_canon.py
python3 tools/check_ledger.py
python3 tools/check_activation.py --full --content-commit <CONTENT-COMMIT>
git diff --check <FULL-B-SHA>...HEAD
```

The release identity and full activation result are judged on `R`; the
content commit `C` alone still legitimately carries the prior root release
form.

The final PR, main push, tag event, draft release assets, publication event,
and public main/tag/release readback must all pass their required checks
before v27 becomes authoritative.

## 12. Provenance and safety firewall

Do not edit or delete historical mentions in:

```text
canon/HISTORY.tsv
older canon/CHANGELOG.md entries
notes/canon/
probes/
```

Those references record the route by which the owner and gate existed.
Retirement removes them only from current normative tables and generated
live views.

This note:

- contains no executable artifact;
- runs no verifier or reproduction;
- changes no public Canon byte;
- imports no private source or log;
- contains no secret, credential, private hostname, machine nickname,
  binary, model, or personal datum;
- adopts no gauge, representative, physical probability, Born factorization,
  L6 measure, or cross-layer theorem.
