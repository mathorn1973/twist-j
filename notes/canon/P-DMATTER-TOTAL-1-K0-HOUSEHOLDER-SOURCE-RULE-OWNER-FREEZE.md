# P-DMATTER-TOTAL-1 K0 Projected-Householder Source-Rule Owner Freeze (NON-CANONICAL)

```text
STATUS:                 OWNER-ADOPTED DEFINITION RULING /
                        KERNEL-SOURCE AND SOURCE-RULE FREEZE
AUTHORITY:              NOT CANON
SCOPE:                  PROPOSAL-LOCAL / DEFINITION-ONLY /
                        ANCHORED PRE-UPDATE N0
OWNER DECISION:         K0 / PROJECTED-HOUSEHOLDER /
                        ALL OFF-READY LIFTS WITH THE SAME READY REDUCTION
SOURCE ROLE:            RULE-SELECTING INPUT, proposal-local
PHYSICAL SOURCE-INPUT DOMAIN:
                        UNRESOLVED
PHYSICAL DISTINGUISHABILITY:
                        UNRESOLVED
PUBLIC BASE:            Public Canon v23
PUBLIC CANON TAG:       canon-v23
ACTIVATION COMMIT:      4ac41b4fac3a3794a6e9d5be1e2027d324edb806
CONTENT COMMIT:         7830d852229ffc06c9d287d026c8ece290bf339b
CANON SHA-256:          f842b613d6f65fe07ddab92ddbe1fb9fec89217d52b781571b7380281c3fb2b1
CANON BYTES:            116017
PUBLIC MAIN BASE:       dcd8857c37bdeb3af10157fff4649147b6d5859a
ROUTE A OWNER DECISION:
                        8ce44e09a3967f3c160ba5db632f8e36a9ee71fbb6d62c5d7aad16e1380b2cde
GRAM-SPECTRAL DEFINITION CANDIDATE:
                        2f6d299a1df495d8894bc5d46f1dc3f642a59e16d8a196ec8a8aa0399865d5ea
ROUTE A PROPOSAL-ID PACKAGE:
                        d9e10e605e937971ea56974fb1afaecf36bfc1ebad3e1ff7ed304914f208b266
KERNEL-APPARATUS PREDEFINITION:
                        ec412acd3b4d03d17a1296d651c7c2145535b07950575bfa1cc89f410464f23c
Q2 APPARATUS OWNER FREEZE:
                        951070f9c1c2205aab5ec4309e9b0bee83c98b8ef29ce3bb8ff9908cb481d8ed
CLAIM ISSUE:            107
OWNER CONFIRMATION:     2026-07-26, current session
A11 STATUS:             PARTIAL / O-STOP, unchanged
QDD STATUS:             O / STOP, unchanged
FORMAL RUN:             NONE
CANON CHANGE:           NONE
REGISTRY CHANGE:        NONE
DEPENDENCY CHANGE:      NONE
GATE CHANGE:            NONE
```

This ruling adopts the exact anchored Route A source and one source-dependent
ready-reduced backaction rule. The selected quadratic high-sector line
controls a rational Householder reflection. The zero-line branch is the
identity on the high sector.

The physical realization relation contains every exact reversible joint
coupling whose ready reduction is the selected instrument. It does not select
one off-ready completion and does not claim one full physical joint evolution.

The projected-Householder rule is a new owner-adopted model input. Public
Canon v23, `J`, the Route A effect data, and the Q2 apparatus theorem do not
prove that nature selects it.

## 0. Falsification and freeze firewall first

The present package is inconsistent if any of the following is false:

```text
K0-SOURCE
    K0 has exactly 15625 pointed forward sequences and equality is decided by
    their n=0 entries.

N0-BRIDGE
    beta_Q is a total exact map from K0 into Veff subset H_Q and reads only
    the four frozen piston coordinates at pre-update n=0.

HOUSEHOLDER-ALGEBRA
    H_kappa is sharp-self-adjoint, H_kappa^2=d_kappa H_kappa, and the displayed
    nonzero branch squares to E_high.

ADMISSIBILITY
    K_HH(kappa) lies in Adm_comm for every kappa in K0.

Q-FIBRE-COMPATIBILITY
    equal Qcan values give equal selected ready reductions.

LEFT-TOTALITY
    every kappa in K0 has at least one related member of
    Cpl_Q2_rev_comm.

ALL-LIFTS
    every U in Cpl_Q2_rev_comm with Red(U)=K_HH(kappa) is admitted, and no U
    with another ready reduction is admitted.

DIAGONAL-CAVEAT
    for every nonzero originating vector beta_Q(kappa), the selected and
    Luders tagged event outputs agree: possible post-event rank-one
    substates coincide and impossible branches coincide. At beta_Q(kappa)=0,
    no normalized pure input exists and the Route A ZERO tag remains.
```

Failure of a displayed identity returns
`SOURCE-RULE-PACKAGE-INCONSISTENT / STOP`. This is not Canon or registry `F`.

The preliminary finite count is separately falsifiable:

```text
PRELIMINARY-COUNT
    the 625 values of Veff give 145 nonzero projective high lines and one
    zero-high branch.
```

A later exact audit that returns another count gives
`SOURCE-IMAGE-COUNT-MISMATCH / STOP`. It does not permit changing the rule.

Before a physical classification opens, a continuation returns
`FREEZE-BREACH / STOP` if it does any of the following without a new
pre-opening owner ruling:

1. replaces `K0`, pointed-sequence equality, or the pre-update `n=0` read;
2. reads `q`, `r`, a later checkpoint, the counter, environment, randomness,
   network data, or dynamic evaluation;
3. replaces `beta_Q`, `Qcan`, `E_low`, or `E_high`;
4. changes the zero-line branch from `E_high`;
5. changes the nonzero branch from the displayed Householder reflection;
6. selects one off-ready lift or adds an off-ready QND, centrality,
   self-adjointness, involutivity, or preferred-frame filter;
7. removes an exact lift with the selected ready reduction;
8. replaces exact rational equality by a tolerance;
9. calls the 146 preliminary classes alternative global decoders, model
   choices, physical sectors, or physical `NONUNIQUE(146)`;
10. identifies the rule-selecting source with the evaluated system input, or
    declares them physically independent, without freezing the joint domain;
11. changes inherited `Eq_instrument`;
12. infers public identifiers, layers, gates, sampling, history, writeback,
    dependency closure, terminality, or physical completeness;
13. opens the formal probe or moves A11 or `QUADRATIC-DECODER-DATA` from
    `O / STOP`.

After a physical classification opens, any change to the source carrier,
source equality, totality domain, read convention, bridge, source rule, zero
branch, relation, relation equality, source-selected image, instrument
equality, source-input domain, completeness method, or output meaning returns
`FIRE-POSTHOC`.

## 1. Inherited public and proposal-local boundaries

The public autonomous state is

```text
Omega = N_0 x F_5^6
```

with the public deterministic update `U`. This ruling does not replace
`Omega`, identify a checkpoint with the whole state, or introduce an action
of `U` on `H_Q`.

The owner-adopted Route A source candidate is the anchored pointed-orbit
carrier

```text
X  = F_5^6,

K0 = { kappa_x = (U^n(0,x))_(n>=0) : x in X }.
```

Equality is equality of the complete pointed forward sequences:

```text
kappa_x Eq_K0 kappa_y
    iff U^n(0,x)=U^n(0,y) for every n>=0.
```

Because the zeroth entries are `(0,x)` and `(0,y)`,

```text
kappa_x Eq_K0 kappa_y iff x=y.
```

Thus equality is an exact finite decision even though each displayed object
is an infinite forward sequence. The genesis map

```text
gen : X -> K0,
gen(x)=kappa_x
```

is a bijection and

```text
|K0|=|F_5^6|=5^6=15625.
```

`K0` is the source carrier for this anchored definition scope. It is not
declared to be every public decoder orbit, a current-time stream, a derived
log, or the complete autonomous state.

## 2. Exact source-to-amplitude bridge

Freeze the balanced section

```text
ell(0)=0,
ell(1)=1,
ell(2)=2,
ell(3)=-2,
ell(4)=-1.
```

For

```text
x=(p_1,p_4,p'_1,p'_4,q,r) in F_5^6,
```

define the rational coordinate bridge

```text
beta_Q : K0 -> Veff subset H_Q,

beta_Q(kappa_x)
  = (ell(p_1),ell(p_4),ell(p'_1),ell(p'_4))^T,

Veff=ell(F_5)^4 subset Q^4,
H_Q=(Q^4,<u,v>_G=u^T G v).
```

This is the previously owner-adopted `beta`, now typed explicitly into the
rational system amplitude carrier used by the Q2 apparatus package.

The bridge is total on `K0`. It reads only the pre-update checkpoint at
`n=0`. The coordinates `q` and `r`, every later checkpoint, the counter,
environment, randomness, network input, and dynamic evaluation are outside
its allowlist.

Every `v in Veff` has exactly 25 inverse images in `K0`, one for every
ordered pair `(q,r) in F_5^2`. Therefore

```text
|Veff|=5^4=625,
|beta_Q^(-1)(v)|=25.
```

The existing cyclotomic Route A bridge remains

```text
A_B0 = iota_B0 o beta_Q : K0 -> Q(zeta_5).
```

This ruling does not replace `A_B0`, `B0`, `iota_B0`, or the adopted
`D_scoped` dictionary. The rational bridge `beta_Q` is the coordinate stage
needed by the source rule below.

## 3. Inherited system and apparatus algebra

Retain

```text
G       = I_4 - (1/5) 1 1^T,
G^(-1)  = I_4 + 1 1^T,
A^sharp = G^(-1) A^T G,

E_low  = (1/4) 1 1^T,
E_high = I_4 - E_low.
```

The decomposition obeys

```text
G E_low  = (1/5) E_low,
G E_high = E_high,

G^(-1) E_low  = 5 E_low,
G^(-1) E_high = E_high.
```

Thus the `G` pairing restricts to the ordinary rational dot pairing on
`Image(E_high)`.

The inherited exact classes are

```text
Instr_alg
  = { (K_low,K_high) in M_4(Q)^2 :
      K_a^sharp K_a=E_a },

Adm_comm
  = { K in Instr_alg :
      [K_a,E_b]=0 for every a,b in {low,high} },

Cpl_Q2_rev_comm
  = { U in M_8(Q) :
      U^natural U=I_8 and Red(U) in Adm_comm }.
```

The Q2 apparatus, ready state, joint pairing, exact matrix equality,
`Red(U)`, and operational `Eq_instrument` are unchanged.

## 4. Projected-Householder source rule

For `kappa in K0`, put

```text
v_kappa = beta_Q(kappa),
A_kappa = v_kappa v_kappa^T,

h_kappa = E_high v_kappa,
H_kappa = E_high A_kappa E_high
        = h_kappa h_kappa^T,

d_kappa = Tr(H_kappa)
        = h_kappa^T h_kappa.
```

All displayed objects are rational. The quadratic objects `A_kappa`,
`H_kappa`, `d_kappa`, and the selected instrument below depend only on the
first component of `Qcan(v_kappa)`. The sign-sensitive representatives
`v_kappa` and `h_kappa` do not.

Because `h_kappa` lies in `Image(E_high)`,

```text
H_kappa^sharp = H_kappa,
E_high H_kappa = H_kappa E_high = H_kappa,
E_low H_kappa = H_kappa E_low = 0.
```

The rank-one identity gives

```text
H_kappa^2
  = h_kappa (h_kappa^T h_kappa) h_kappa^T
  = d_kappa H_kappa.
```

Positive definiteness gives

```text
d_kappa=0 iff h_kappa=0 iff H_kappa=0.
```

Freeze the ready-reduced operators

```text
K_low^HH(kappa)=E_low,

K_high^HH(kappa)
  = E_high                                      if d_kappa=0,

  = E_high - 2 H_kappa/d_kappa                 if d_kappa!=0.
```

The second branch is the rational reflection in the high-sector hyperplane
orthogonal to the unoriented line `Q h_kappa`. It uses no axis, angle,
orientation, measured input, or fitted parameter.

For the nonzero branch,

```text
(K_high^HH)^sharp = K_high^HH,

(K_high^HH)^2
  = E_high - 4 H_kappa/d_kappa
    + 4 H_kappa^2/d_kappa^2
  = E_high.
```

The zero branch has the same identities directly. Hence for every source

```text
(K_low^HH)^sharp K_low^HH   = E_low,
(K_high^HH)^sharp K_high^HH = E_high,

[K_a^HH,E_b]=0,

K_HH(kappa)
  = (K_low^HH(kappa),K_high^HH(kappa))
  in Adm_comm.
```

This is an exact admissibility proof. It is not a derivation that this
backaction rule is uniquely physical.

## 5. Exact Q-fibre compatibility

The Route A quadratic map is

```text
Qcan(v)=(v v^dagger,v v^T)=(A(v),A(v)).
```

If

```text
Qcan(v)=Qcan(v'),
```

then `A(v)=A(v')`. Therefore

```text
E_high A(v) E_high
  = E_high A(v') E_high,

d(v)=d(v'),
K_HH(v)=K_HH(v').
```

Thus the selected ready reduction factors through the exact `Qcan` carrier.
It is invariant under `v -> -v` and blind to the two omitted checkpoint
coordinates.

Proposal-locally there is a total map

```text
HH_Q : QCarrier -> Adm_comm
```

on the Route A image, with

```text
K_HH = HH_Q o Qcan o beta_Q.
```

This is a definition factorization of the owner-adopted rule. It is not the
independent `D_direct` factorization required by the separate QDD obligation.

## 6. Physical-realization relation at the declared scope

Freeze the exact relation

```text
PhysReal_HH
  = { (kappa,U) in K0 x Cpl_Q2_rev_comm :
      Red(U)=K_HH(kappa) }.
```

Relation membership uses:

```text
pointed-sequence equality on K0,
entrywise rational matrix equality on U,
entrywise rational matrix equality on Red(U).
```

It is a relation, not the graph of a selected joint coupling.

### 6.1 Left-totality

For every `kappa in K0`, Section 4 gives

```text
K_HH(kappa) in Adm_comm.
```

The inherited Q2 completeness result is

```text
Red(Cpl_Q2_rev_comm)=Adm_comm.
```

Therefore

```text
for every kappa in K0
there exists U in Cpl_Q2_rev_comm
with (kappa,U) in PhysReal_HH.
```

The relation is exactly left-total.

A constructive witness is

```text
X = [0 1
     1 0],

U_HH^X(kappa)
  = K_low^HH(kappa) tensor I_2
    + K_high^HH(kappa) tensor X.
```

Here both reduced operators are sharp-self-adjoint involutions on their
supports, so the controlled-X witness is itself natural-self-adjoint,
involutive, and reversible.

This witness proves existence only. It is not selected as the unique physical
joint evolution.

### 6.2 Every off-ready lift is retained

For a fixed source, define its realization fibre

```text
Lift_HH(kappa)
  = { U in Cpl_Q2_rev_comm :
      Red(U)=K_HH(kappa) }.
```

Then

```text
(kappa,U) in PhysReal_HH iff U in Lift_HH(kappa).
```

No further off-ready condition is imposed.

Every fibre has cardinality exactly `aleph_0`. The upper bound follows because
`M_8(Q)` is countable. For the lower bound, fix one witness `U_0` and any
rational `G`-orthogonal system map `P`. Put

```text
W_P
  = I_4 tensor F_low
    + P tensor F_high.
```

Then

```text
W_P^natural W_P=I_8,
W_P J_eta=J_eta,

Red(U_0 W_P)=Red(U_0).
```

The already displayed rational high-sector reflections `R_t`, for integer
`t>=2`, provide infinitely many distinct `P`. Thus the relation contains
countably infinitely many exact apparatus matrices for each source, all in
one ready-reduced `Eq_instrument` class.

Consequently this ruling does not claim a unique full joint evolution, a
unique apparatus realization, or an off-ready physical law.

## 7. Exact source-selected algebraic image

Define

```text
SourceImage_HH
  = { Red(U) :
      exists kappa in K0 with
      (kappa,U) in PhysReal_HH }.
```

By the defining equality of the relation,

```text
SourceImage_HH
  = { K_HH(kappa) : kappa in K0 }.
```

This equality is exact and complete. No unlisted ready reduction occurs, and
every listed reduction has a reversible lift.

For each fixed source,

```text
{ Red(U) : (kappa,U) in PhysReal_HH } / Eq_instrument
```

has one element.

The quotient of the union over all sources is a different object. Its
preliminary exact count is recorded below without physical interpretation.

## 8. Preliminary exact finite count

This section records an exact nonformal audit known before the owner freeze.
It is not a blind probe result.

Identify

```text
ell(F_5)={-2,-1,0,1,2}.
```

For `v in Veff`, use the integer centered vector

```text
w(v)=4 E_high v
    =4v-(sum_i v_i)1
    in Z^4,

sum_i w_i(v)=0.
```

For `w!=0`, define its canonical projective representative by:

```text
1. divide w by gcd(|w_1|,|w_2|,|w_3|,|w_4|);
2. multiply by -1 if its first nonzero coordinate is negative.
```

The Householder reflection depends exactly on this representative. The 625
vectors of `Veff` give:

```text
projective high lines with 2 Veff values:    72
projective high lines with 4 Veff values:    48
projective high lines with 8 Veff values:    18
projective high lines with 20 Veff values:    7

total nonzero projective high lines:
    72+48+18+7=145,

nonzero Veff values:
    72*2+48*4+18*8+7*20=620.
```

The remaining five values are

```text
v=c 1,
c in {-2,-1,0,1,2},
```

for which `E_high v=0`. They all select the zero-line branch
`K_high=E_high`.

Since each `v` has 25 sources in `K0`, the source fibres are:

```text
72 classes with 50 sources each,
48 classes with 100 sources each,
18 classes with 200 sources each,
7 classes with 500 sources each,
1 zero-line class with 125 sources,

72*50+48*100+18*200+7*500+125=15625.
```

Distinct nonzero projective lines give distinct reflection matrices. Under
the inherited single-operator operational equality, two rational event maps
can agree only when their nonzero operators differ by a rational sign.
Every high-sector reflection above has high-sector trace `1`, so one cannot
be the negative of another. The zero-line identity has high-sector trace `3`.

Therefore the preliminary exact algebraic output is

```text
CANDIDATE-Q2-HH-SOURCE-IMAGE-146-ALGEBRAIC:

|SourceImage_HH / Eq_instrument|=146.
```

This calculation used exact integer and rational arithmetic in independent
nonformal audits. `FORMAL RUN` remains `NONE`. A later public definition audit
must freeze its verifier before execution and must report that the expected
count was already visible.

## 9. The diagonal-domain caveat

Let

```text
v=beta_Q(kappa),
v!=0,
h=E_high v.
```

When `h!=0`,

```text
K_high^HH(kappa) h
  = (E_high-2 h h^T/(h^T h)) h
  = -h.
```

The Luders high operator gives `E_high h=h`. The two vectors differ only by a
sign, so their rank-one post-event substates are equal. The low operator is
already the Luders operator `E_low`.

If `h=0`, the high event is impossible under both operators. A low event
with zero output is likewise impossible under both because the low
operators are identical. Thus for every `v!=0`, on the normalized diagonal
pure input selected from the same source,

```text
(kappa,rho_(beta_Q(kappa))),
```

the projected-Householder and Luders tagged event outputs agree: the same
branches are possible, their probabilities agree, and their normalized
rank-one post-event substates agree.

When `v=0`, `rho_v` is undefined. Those 25 sources retain the existing Route
A `ZERO` tag and supply no diagonal pure input. The algebraic instrument
rule remains total on them through the `H=0` identity branch.

The inherited `Eq_instrument` compares event maps on the full rational state
carrier, where the 146 operators are distinct. This ruling does not change
that equality. It also does not decide which source-system input pairs are
physically admissible.

The following remain distinct and unresolved:

```text
ALGEBRAIC FULL DOMAIN
    kappa selects K_HH(kappa), which acts on every input in H_Q.

DIAGONAL PHYSICAL DOMAIN
    only a nonzero beta_Q(kappa) is evaluated under the instrument selected
    by kappa, with the zero-amplitude sources handled by the Route A ZERO
    tag.

INDEPENDENT PHYSICAL DOMAIN
    the rule-selecting source and evaluated H_Q input vary independently.
```

No physical distinguishability, sector, setting, or model-parameter claim is
made until that domain and the source role are frozen publicly.

## 10. Why the existing Born data do not select the rule

For every admitted instrument and every rational state,

```text
p_a(rho)
  = Tr(K_a rho K_a^sharp)
  = Tr(K_a^sharp K_a rho)
  = Tr(E_a rho).
```

Therefore every member of `Adm_comm`, not only `K_HH(kappa)`, reproduces the
same frozen effect and Born data.

The compatibility relation defined only by those data is

```text
CompatReal_Born
  = K0 x Cpl_Q2_rev_comm.
```

Its reduced image is all of `Adm_comm`. The existing effect/Born readout
cannot select post-event backaction.

The projected-Householder rule is therefore an additional declared
backaction principle. It is not inferred from equality of effects or from
the Route A five-field record.

## 11. Explicitly non-adopted competitors

This ruling does not adopt:

```text
CONSTANT LUDERS
    K0 x {one Luders coupling}, or all lifts of the Luders reduction.

MAXIMAL PRODUCT
    K0 x Cpl_Q2_rev_comm as the physical realization relation.

SELECTED-LETTER LINEARIZATION
    a rule reading z_6(x), q, or r and using a kernel-generator linear part.

UNIQUE OFF-READY GRAPH
    kappa maps to one selected controlled-X or cross-adjoint lift.

SOURCE-INDEPENDENT RANDOM CHOICE
    a sampled member of the apparatus class.

ALTERNATIVE HOUSEHOLDER AXIS
    an extra preferred high-sector vector or frame.

DIAGONAL-ONLY PHYSICAL DOMAIN
    the rule-selecting source is forced to equal the evaluated input.

INDEPENDENT PHYSICAL DOMAIN
    the rule-selecting source and evaluated input are separately variable.
```

The constant and maximal-product candidates leave the kernel inert. The
selected-letter candidate violates the frozen Route A read allowlist by
reading `q` and `r`. Selecting one joint lift would add an off-ready physical
principle. None is inferred.

## 12. Proposal-local schema coverage

Every identifier in this section is proposal-local and fills no public
completion-contract slot.

### 12.1 Kernel source and bridge

```text
kernel_source_id:
    CAND-QDD-KERNEL-SOURCE-K0-ANCHORED-N0

kernel_state_carrier_id:
    CAND-CARRIER-ANCHORED-ORBITS-K0

kernel_state_equality_id:
    CAND-EQ-POINTED-FORWARD-SEQUENCE

kernel_totality_domain_id:
    CAND-QDD-TOTALITY-DOMAIN-K0-ANCHORED-N0

kernel_to_amplitude_bridge_id:
    CAND-QDD-K0-TO-HQ-BALANCED-AMPLITUDE-N0

bridge_domain_id:
    CAND-CARRIER-ANCHORED-ORBITS-K0

bridge_codomain_id:
    CAND-QDD-CARRIER-HQ-G

bridge_equality_id:
    CAND-QDD-EQ-K0-HQ-BRIDGE-EXTENSIONAL

bridge_totality_certificate_id:
    CAND-QDD-CERT-K0-HQ-BRIDGE-TOTAL-N0

bridge_equality_certificate_id:
    CAND-QDD-CERT-K0-HQ-BRIDGE-EQUALITY-N0
```

The existing cyclotomic `A_B0` bridge and its identifiers remain unchanged.

### 12.2 Projected-Householder rule

```text
source_quadratic_matrix_map_id:
    CAND-QDD-MAP-K0-TO-PROJECTED-HIGH-QUADRATIC

source_quadratic_scalar_map_id:
    CAND-QDD-MAP-PROJECTED-HIGH-TRACE

source_zero_branch_id:
    CAND-QDD-HH-ZERO-BRANCH-EHIGH

source_nonzero_branch_id:
    CAND-QDD-HH-NONZERO-PROJECTED-REFLECTION

source_instrument_map_id:
    CAND-QDD-MAP-K0-TO-HH-READY-INSTRUMENT

source_instrument_map_codomain_id:
    CAND-QDD-ADM-COMMUTANT

source_instrument_map_equality_id:
    CAND-QDD-EQ-K-MATRIX

source_instrument_totality_certificate_id:
    CAND-QDD-CERT-K0-HH-INSTRUMENT-TOTAL

source_instrument_Q_fibre_certificate_id:
    CAND-QDD-CERT-K0-HH-Q-FIBRE-COMPATIBLE
```

### 12.3 Source-realization relation

```text
source_realization_relation_id:
    CAND-QDD-PHYSREAL-K0-HH-ALL-LIFTS

source_realization_domain_id:
    CAND-CARRIER-ANCHORED-ORBITS-K0

source_realization_codomain_id:
    CAND-QDD-COUPLING-CLASS-UFULL-Q2-REVERSIBLE-ADM-COMMUTANT

source_realization_membership_id:
    CAND-QDD-MEMBERSHIP-PHYSREAL-K0-HH-RED-EQUALITY

source_realization_membership_domain_id:
    CAND-QDD-DOMAIN-K0-X-CPL-Q2-REV-COMM

source_realization_membership_codomain_id:
    CAND-QDD-TRUTH-VALUES

source_realization_equality_id:
    CAND-QDD-EQ-PHYSREAL-K0-HH-EXTENSIONAL

source_realization_totality_domain_id:
    CAND-QDD-TOTALITY-DOMAIN-K0-ANCHORED-N0

source_realization_totality_certificate_id:
    CAND-QDD-CERT-PHYSREAL-K0-HH-LEFT-TOTAL

source_realization_all_lifts_certificate_id:
    CAND-QDD-CERT-PHYSREAL-K0-HH-ALL-LIFTS
```

### 12.4 Source-selected image

```text
source_selected_image_id:
    CAND-QDD-SOURCE-IMAGE-K0-HH-READY-REDUCTIONS

source_selected_image_membership_id:
    CAND-QDD-MEMBERSHIP-SOURCE-IMAGE-K0-HH

source_selected_image_equality_id:
    CAND-QDD-EQ-INSTRUMENT-OPERATIONAL

source_selected_image_completeness_statement_id:
    CAND-QDD-COMPLETENESS-SOURCE-IMAGE-EQUALS-K0-HH-IMAGE

source_selected_image_completeness_proof_id:
    CAND-QDD-PROOF-SOURCE-IMAGE-K0-HH-TWO-INCLUSIONS

source_selected_image_completeness_method_id:
    CAND-QDD-METHOD-SOURCE-IMAGE-K0-HH-EXACT-RELATION

preliminary_source_image_count_id:
    CAND-QDD-PRELIMINARY-COUNT-K0-HH-146

per_source_instrument_class_id:
    CAND-QDD-PER-SOURCE-SINGLE-INSTRUMENT-CLASS-HH

off_ready_lift_fibre_id:
    CAND-QDD-OFF-READY-LIFT-FIBRE-ALEPH0-HH
```

Supporting fields outside the strict schema are proposal-local metadata only.

## 13. Strict public fields that remain unresolved

All proposal-local identifiers above remain non-public. The strict public
identifier values remain literal `UNRESOLVED`.

The following semantic fields also remain unresolved:

```text
physical source role beyond proposal-local rule selection
physical source x evaluated-system input domain
physical distinguishability of source-selected instruments
public physical outcome IDs
public instrument, effect, and Born IDs
public MatterData outcome fields and maps
all layer assignments
all gate bindings
sampling_manifest
history_update_manifest
writeback_manifest
dependency_closure_manifest
completion_contract_binding_manifest
completion-wide terminality
formal classification readiness.
```

The strict public `output_manifest` remains:

```text
output_manifest:
    predefinition_output_id: UNRESOLVED
    candidate_rejection_output_id: UNRESOLVED
    classification_output_id: UNRESOLVED
    candidate_relation_id: UNRESOLVED
    fire_posthoc_id: UNRESOLVED.
```

No tagged `NONE` is inferred. No proposal-local name fills a strict public
identifier slot.

## 14. Frozen output semantics

```text
OWNER-KERNEL-SOURCE-FROZEN
    K0, pointed-sequence equality, totality domain, and the pre-update n=0
    rational amplitude bridge are owner-frozen at proposal scope.

OWNER-SOURCE-RULE-FROZEN
    the projected-Householder ready-reduction rule, including the E_high
    zero branch, is owner-frozen.

PHYSREAL-HH-LEFT-TOTAL
    every source has at least one exact reversible realization.

PHYSREAL-HH-ALL-LIFTS
    every exact reversible joint coupling with the selected ready reduction
    is admitted. No unique off-ready coupling is selected.

PER-SOURCE-SINGLE-INSTRUMENT-CLASS
    for each fixed source, all admitted lifts have one ready-reduced
    Eq_instrument class.

CANDIDATE-Q2-HH-SOURCE-IMAGE-146-ALGEBRAIC
    preliminary exact nonformal count of the union over all sources.
    It is not physical NONUNIQUE(146).

SOURCE-RULE-PACKAGE-INCONSISTENT
    a source, bridge, Householder, admissibility, Q-fibre, relation, or
    totality identity is false.

SOURCE-IMAGE-COUNT-MISMATCH
    a later frozen exact audit does not return the preliminary count 146.
    The source rule is not changed in response.

PHYSICAL-DOMAIN-UNRESOLVED
    the relation between the rule-selecting source and evaluated system input
    has not been physically frozen.

OWNER-INPUT-REQUIRED
    at least one public-ID, physical-domain, layer, gate, sampling, history,
    writeback, dependency, terminality, or full physical-completeness field
    remains unresolved.

STOP
    any required public type, identifier, physical domain, map, certificate,
    dependency, layer, gate, or completeness proof is missing or inexact.

FIRE-POSTHOC
    a frozen source, rule, relation, equality, domain, completeness method, or
    output meaning changes after physical classification opens.
```

The current combined output is

```text
OWNER-KERNEL-SOURCE-FROZEN
OWNER-SOURCE-RULE-FROZEN
PHYSREAL-HH-LEFT-TOTAL
PHYSREAL-HH-ALL-LIFTS
PER-SOURCE-SINGLE-INSTRUMENT-CLASS
CANDIDATE-Q2-HH-SOURCE-IMAGE-146-ALGEBRAIC
PHYSICAL-DOMAIN-UNRESOLVED
OWNER-INPUT-REQUIRED / STOP.
```

## 15. Exact status consequence

```text
K0 source carrier                        OWNER-ADOPTED, proposal-local
pointed-sequence equality                FIXED
source totality domain                   K0
beta_Q pre-update n=0 bridge             OWNER-ADOPTED, exact and total
projected-Householder rule               OWNER-ADOPTED model input
zero-high branch                         E_high, fixed
source realization relation             all exact lifts with selected Red
relation left-totality                   EXACT PROPOSAL-LOCAL CONSEQUENCE
per-source ready instrument quotient     one class
off-ready apparatus fibre per source     aleph_0 exact matrices
union source image                       146 classes, preliminary exact audit
Q-fibre compatibility                    EXACT PROPOSAL-LOCAL CONSEQUENCE

unique full joint evolution              NOT CLAIMED
physical source-input domain             UNRESOLVED
physical distinguishability              UNRESOLVED
alternative decoder count                NOT CLASSIFIED
physical NONUNIQUE(146)                  NOT EARNED
public identifiers                       UNRESOLVED
layers and gates                         UNRESOLVED
sampling, history, and writeback         UNRESOLVED
physical completeness                    UNRESOLVED
READY-FOR-CLASSIFICATION                 NO

A11                                      PARTIAL / O-STOP
QDD-PHYSICAL-EFFECT-SELECTION            O / STOP
QUADRATIC-DECODER-DATA                   O / STOP, unchanged
formal scientific run                    NONE.
```

No public theorem, physical uniqueness, physical nonuniqueness, model-choice
count, Canon claim, registry row, dependency, gate, probe, verifier, evidence,
or status move is produced by this owner ruling.

## 16. Next allowed actions

1. Freeze the physical role of `K0` and the admissible
   `source x evaluated-system-input` domain.
2. Preserve the diagonal-domain caveat when defining physical
   distinguishability.
3. Create public source, bridge, instrument, effect, Born, outcome, and
   MatterData identifiers only through a later normative action.
4. Assign every object and map an exact layer and every cross-layer map a
   public gate.
5. Freeze sampling, history-update, writeback, dependency, terminality, and
   completion-contract bindings.
6. Only after those fields close, preregister an exact finite definition audit
   of all 15625 sources, all 625 amplitudes, all 313 Qcan fibres, all 146
   preliminary source-image classes, the zero branch, totality, and negative
   controls.
7. State in that preregistration that the count 146 was already visible in
   nonformal owner-decision analysis.

No formal probe or Canon fold is authorized by this ruling.
