# P-DMATTER-TOTAL-1 owner decision input: audit and recommendation (NON-CANONICAL)

Status: `DRAFT / DECISION-INPUT / NO-AUTHORITY`

This note serves issue #107. It supplies an independent audit of the merged
branch classification and one typed recommendation for the pending owner
decision (complete dictionary/write rule versus a genuinely prior
`D_direct`). It adopts nothing, creates no row, gate, dependency, probe, or
run, and leaves `QUADRATIC-DECODER-DATA [O]` and the scheduler `STOP`
unchanged.

## Authority pin

```text
Canon:              Public Canon v14, ACTIVE, tag canon-v14
content commit:     ab4ea07d15ab1cfa0c403d3c2a74164011ffa0e7
Canon SHA-256:      f9f06af42a9b0b63f806603ddc671ebe6a9e5014b50d230da04a28722ed1a6a2
public main:        3f6500a (post #113/#114/#115 merges), SHA256SUMS OK
owner row:          QUADRATIC-DECODER-DATA [O]
controlling notes:  P-DMATTER-TOTAL-1-PREDEFINITION.md,
                    P-DMATTER-TOTAL-1-DEFINITION-CANDIDATE.md,
                    P-DMATTER-TOTAL-1-CYCLOTOMIC-REALIZATION.md,
                    P-DMATTER-TOTAL-1-BRANCH-CLASSIFICATION.md
```

## 1. Independent audit of the merged branch classification

An independent exact implementation re-derived every displayed claim of
`P-DMATTER-TOTAL-1-BRANCH-CLASSIFICATION.md` at `3f6500a`:

```text
S1  B5 completeness: the three column equations characterize the verb
    condition; full 120-injection scan equals the equation solution set
    equals the five consecutive windows.
S6  Galois covariance: sigma_a P^(m) sigma_a^(-1) = P^(a m) exactly for
    every a in F_5^* and every m; exactly one invariant projector, m = 0,
    k = 1, the rational low line.
S5  base separation: on v = (1,0,0,0) both windows give tau = 4/5 and
    normalized weights (1/16, 15/16), while rho_0(1) = 1 and
    rho_1(1) = -zeta/4 separate the fixed-basis records.
S4  scalar invariance: the scalar subrecord is identical across all five
    windows on all 625 balanced pistons.
Result: 5 of 5 exact checks; base NONUNIQUE (five full-record classes, one
scalar class) and conditional UNIQUE-ALGEBRAIC under Adm_GalSplit both
reproduce.
```

Audit grade: the classification algebra is sound. This is incubation-lane
review evidence, not authority.

## 2. The decision, typed

The issue #107 question is a three-way governance choice on a sound and
frozen algebraic surface:

```text
FILTER-ADOPTED      adopt Adm_GalSplit as a scoped dictionary clause,
                    bounded to C5. Selects c_1 inside C5 only.
                    QUADRATIC-DECODER-DATA stays [O]; D_direct may stay
                    UNRESOLVED; the amplitude bridge, record ownership,
                    manifests, and layer gate remain open debt.
ADOPTED-D           register the complete public dictionary and write rule
                    with every ownership, bridge, dependency, manifest, and
                    layer-gate identifier resolved.
CONTINUE-SEARCH     keep D_direct UNRESOLVED and keep searching for a
                    genuinely prior public readout outside C5.
```

## 3. Recommendation

```text
RECOMMEND: FILTER-ADOPTED now; ADOPTED-D deferred; CONTINUE-SEARCH kept
open in parallel at zero cost, with its expectation stated honestly.
```

Grounds, each with its label:

```text
1  (audited fact) Within C5 the residual freedom is exactly one clause.
   Base classification is NONUNIQUE with all five candidates sharing every
   scalar field; the only separating field is the fixed-basis density; and
   Adm_GalSplit reduces the class to one. The choice is invisible to every
   scalar record and no finite computation can decide it; it is a pure
   dictionary decision, which is what FILTER-ADOPTED types.
2  (candidate-grade search record) The incubation sweep behind the merged
   companions found no branch-split readout that neither collapses into a
   realization of the frozen Gram factor nor consumes one selection
   clause. This is not a nonexistence proof; it is the reason
   CONTINUE-SEARCH should stay open but should not gate the lane.
3  (cost) ADOPTED-D requires the full completion-contract surface
   (bridge, record ownership, manifests, layer gate). None of that debt is
   reduced by waiting on the filter decision, and adopting the filter does
   not prejudge it: the classification note already separates
   FILTER-ADOPTED from ADOPTED-D as distinct governance states.
4  (precedent) The clause has the same shape as the dark-energy dictionary
   decision: underdetermination recorded first, then one named clause with
   conditional uniqueness, adopted as scoped dictionary content rather
   than claimed as a derivation.
```

If FILTER-ADOPTED is taken, the adopting fold should state, verbatim from
the classification note: within C5, physical branch admissibility requires
invariance of the branch projector under the full group Gal(K/Q); this
selects c_1 only inside that bounded class; the factorization remains a
derived algebraic audit, not a blind probe; and the fold must re-freeze the
amplitude bridge, basis identifier, record equality, factor map,
dependencies, and layer scope consistently.

## 4. What this note does not do

It does not adopt Adm_GalSplit, does not close or reopen any search, does
not touch the completion contract, and does not authorize a probe. The
frontier count is unchanged.
