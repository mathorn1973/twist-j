# Concrete physics followthrough after the candidate theorems

NON-CANONICAL planning note. No empirical result, admitted source, dataset,
public task reservation or new physical dictionary is created here.

The first proposed numerical comparison is the omitted-origin mean-deposit
residual under an independently admitted common preparation and fixed cold
context. The concrete prediction, uncertainty and rejection procedure is
in [PHYSICAL-NEXT-TEST.md](PHYSICAL-NEXT-TEST.md). The present note identifies
which existing lane can supply each missing physical input.

## 1. First choose the prediction and its independent calibration

The three results make distinct comparison questions precise:

| Objective | Mathematical information now sufficient | Still missing for a physical comparison |
|---|---|---|
| Predict a mean deposit | Common preparation law, source/port/clock correspondence, three atom means plus the fixed seven independent spatial deposit means; for the displayed origin only six of those deposits enter | Independent preparation and observable identification; calibrated conductance and amplitude units; complete context; readout errors, origin error and separate geometry/conductance/sampling systematics |
| Predict counts at q=1/10 | The fixed (0,1,5) full passive law, displayed raw Sigma and one independent M4 or E[D0^2] | A physical realization of that exact source class, one-step origin context, cold input and energy threshold; independently measured fourth-moment observable; complete trial/coverage and occurrence interpretation |
| Recover original records after native evolution | Original phase class saved in one of three initialized mathematical states, or an admitted restricted preparation subset | Physical preparation, writing and retention of that tag, present-state identification, nondestructive readout and reset/persistence; cardinality alone supplies no hardware or energy cost |

The old equal-record e0/e1 pair remains an explicit calibration challenge:
every selected passive record agrees, while the fixed spatial response
differs. A measurable distinguishing procedure needs independent source
interpretation. Copying a model indicator or choosing a preparation mixture
to fit the target response would not provide that evidence.

## 2. Existing lane #830: independent port calibration

[DECODER-PORT-OBSERVABLE-CALIBRATION-1](https://github.com/mathorn1973/twist-j/issues/830)
already contains a proposed signed-port or force/velocity calibration
contract. Continue that lane rather than recreate its specification.
The next inputs are a documented physical port and units, independently
known gain/polarity, a justified cold preparation, separately owned
calibration and validation data, and bounded uncertainty. Energy-only
response retains the known g versus 4/g ambiguity. Reconstructing signed
amplitudes using the unknown impedance and then estimating that impedance
from them is circular. The candidate quadratic theorem specifies which
additional means would predict other means once those inputs are supplied.

## 3. Existing lane #832: finite optical analogue

[DECODER-PASSIVE-REALIZATION-BRIDGE-1](https://github.com/mathorn1973/twist-j/issues/832)
already specifies the three-mode component, two 7:2 mixers and two-stage
coherent state-continuation design. Its physical certificates are unissued.
That component is a g=2 single-cell design; it does not directly realize
the new gamma=1 five-source/seven-port identity. A separately specified and
independently mapped full buffered realization is needed before applying
that identity to it.
Required additions are documented components and preparation, independent
complex-transfer and pulse-energy calibration, accounting for every energy
boundary, and held-out response data under fixed uncertainties. The design
is a conditional analogue, not an identification of NIST or a physical
TWIST-J source. No new laboratory acquisition is initiated here; the
public-data-only lane recorded in #834 remains the available empirical
route unless separately changed by the owner.

## 4. Existing lane #834: archived external apparatus records

[DECODER-OPEN-DATA-BRIDGE-1](https://github.com/mathorn1973/twist-j/issues/834)
has already completed raw-record qualification and the causal
LocalObservedInterval adapter on 3,071,884 rows. Do not rebuild those
adapters or relabel their exact archival output as physical trials.

The later `notes/V81-NIST-CALIBRATION-QUALIFICATION-1.md` concludes
SOURCE_NOT_QUALIFIED_FOR_THIS_TEST for the proposed Alice run3 setting-0
single-window lag-one statistic. The inspected documentation warns that
run3 timing correction failed, and the bounded metadata search found no
independently exposed same-run timing/window/coverage locator. The note
explicitly prevents evaluating physical S11 on that qualified scope.
This is not a theorem that metadata cannot exist elsewhere.

The next admissible input is genuinely new independently applicable run3
evidence, or an explicit different documented run with its own custody and
prior-exposure disposition. Required evidence comprises same-run trial and
setting/preparation attribution, clock/PPS validity, fixed window and joint
uncertainty, registration latency/order, coverage and loss/gap ledger,
pointer meaning and multiplicity. Metadata hidden in a mixed analysis
archive requires a separately fixed semantic-access scope before inspection.
Do not choose timing alignment or windows from detector outcomes.

Even successful timing qualification leaves the U-head/source law,
Gamma/q physical interpretation, sequential response, retained state and
reset unresolved. The new passive D and proposed T rows supply no
certificate for those identifications. At present the known archive
supports exact record reconstruction; it does not support the proposed
physical threshold or lag-one comparison.

## 5. Preserve the larger owners while closing bounded questions

The calibration and moment theorems close specific information-sufficiency
questions. The memory theorem closes a mathematical current-state recovery
question. The next physical work should target a named missing input above
and compare against a frozen prediction with its uncertainty, using the
existing public lanes. It should not claim to close whole-apparatus,
event-completion, Bell, photon or L6 obligations by identifying a normalized
tuple, finite model ensemble, classical optical design or archive packet
with a physical law.
