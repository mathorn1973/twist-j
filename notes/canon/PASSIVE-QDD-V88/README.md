# Public Canon v88 candidate package

**NON-CANONICAL / NOTES-ONLY PROPOSAL / NOT A RELEASE.** This package is
intended for separate review under `notes/canon/PASSIVE-QDD-V88/`. It
proposes three new L1 T rows and applies none of them to the active Canon,
registry, authority, gate or release. Publication of this note is not a
Canon fold or activation.

Basis: immutable Public Canon v87, activation/main
`fd512f50d90382124e7c00afa926c8083fd56e06`, content
`41c8d4229b71437f09957d959975dc1772e95806`, Canon SHA-256
`a2517c6d8efb1969a7258f94d9874cca3b5b9beb1aecdfd2e39b56f33961a917`,
634207 bytes. The coordinator independently verified current public
authority and checks; this draft derives the ledger schemas and preserved
claims from the immutable release copy.

The candidates are:

- QDD-PASSIVE-QUADRATIC-CALIBRATION: complete quadratic information boundary,
  universal minimum seven additional scalar expectations, concrete cold-port
  completion and exact additive-error certificate. Public reservation #1023.
- QDD-PASSIVE-THRESHOLD-MOMENT-COMPLETION: complete sharp count-law segment
  despite full passive-law and raw-second-moment equality; smallest one-
  expectation fourth-moment repair. Public reservation #1024.
- QDD-PASSIVE-NATIVE-MEMORY: exact current-checkpoint factor criterion,
  minimum three initialized auxiliary states and five full/supported
  preparation maxima. Public reservation #1025.

Each probe has completed its pinned local run and replay with PASS, public
readback of all six files, both required architecture jobs and the aggregate
check at the exact evidence commit below. Prior exploration is disclosed.
The three evidence PRs are now merged at public main
`5c9a494de3f4b9856c292a77d35280fb086a0404`, with every evidence byte
unchanged and required main checks passing. The original receipts below
remain historical snapshots; INTEGRATED-EVIDENCE.json records integration.
The separate reviewed Canon fold and release remain outstanding. A
candidate label is not an earned status; process exit zero is not a
substitute for the scientific verdict. The proposed T status includes
mathematical conclusions only.

| Candidate | Evidence PR | Exact completed-bundle commit |
|---|---|---|
| Quadratic calibration | [#1028](https://github.com/mathorn1973/twist-j/pull/1028) | `757492055ab5a0a39e14a4089650f3f76ed9cf47` |
| Threshold moment | [#1026](https://github.com/mathorn1973/twist-j/pull/1026) | `4931bd53c8c787ff1158907ab34360e0029b1c9a` |
| Native memory | [#1027](https://github.com/mathorn1973/twist-j/pull/1027) | `c1a6341ceec61439789f0108905f0b3ea15b259f` |

## Files and integration intent

`CANON-INSERT.md` is the self-contained insertion after the selected passive
family. `REGISTRY-ADDITIONS.tsv`, `NORMATIVE-ADDITIONS.tsv` and
`DEPENDENCIES-ADDITIONS.tsv` use the exact current schema and contain only
new rows. Their scientific fields have no placeholders. All three normative
items use the existing enum THEOREM and layer L1. `GATES-ADDITIONS.tsv`
has only the exact header because no new layer gate is requested.

`PRESERVED-LIVE-REGISTRY.tsv` copies all 28 old H/O rows byte-for-byte at
the cell level, including complete scopes, evidence and decision clauses.
`LIVE-CLAIM-DISPOSITION.tsv` and `OPEN-CLAIM-DISPOSITION.md` explicitly
retain each. The whole existing registry is to be retained without row
edits, not only those 28 rows. No new live owner or frontier program row
is created. `STATUS-COUNTS-CANDIDATE.tsv` gives the expected totals after
all three accepted additions; it is not an activation claim.

`EVIDENCE-COMMITS.tsv`, `EVIDENCE-ADDITIONS.tsv` and `HISTORY-ADDITIONS.tsv`
contain actual immutable evidence commits, complete bundle hashes and exact
scope hashes; there are no pending placeholder values. Bundle identities
were computed directly from the six files at each pinned git commit with
the existing case-sensitive `bundle-manifest-sha256-v1` convention, then
matched against the public API byte-readback receipt. `EVIDENCE-RECEIPTS.json`
records that independent git-object calculation. `PUBLIC-PROBE-RECEIPTS.json`
preserves the public checks, pins and file readback. Neither receipt is an
activation or substitutes for the separately reviewed later fold.

`CHANGELOG-CANDIDATE.md` and `CORE-PARAGRAPH-CANDIDATE.md` are proposed
editorial additions. No new core-selection badge is requested. Final
SHA256SUMS, regenerated frontier/core/count artifacts and the active release
identity must be derived from the complete reviewed repository, not from
this outside draft.

`PHYSICAL-NEXT-TEST.md` gives the first concrete residual comparison and
`PHYSICAL-FOLLOWTHROUGH.md` identifies its missing inputs in existing lanes
#830/#832/#834. These notes create no dataset or admitted experiment and
do not reopen the failed run3 source-qualification target.

Before a later fold: read the three public scientific RESULT records;
verify both architecture jobs and aggregate at the recorded evidence commits;
review the combined scope; retain every pre-existing registry row; derive
the final ledgers/hashes with the repository tools; and perform the ordinary
separate reviewed Canon fold and activation. This package authorizes none
of those actions by itself.

## Completed isolated content preview and physical followthrough

`CONTENT.patch` proposes the complete fourteen-file content fold against
the integrated main above. `CONTENT-PREVIEW.json` binds every before/after
file hash. In the isolated copy, all 97 status-separation checks, the full
ledger, generated views and six architecture-map tests passed. Canon
validation reached exactly the deliberately untouched STATUS release-form
boundary; this is not full activation PASS. The patch does not change
STATUS.md, root README.md, CITATION.cff, any old claim row or any probe file.
Its only notation normalization is E[C] to equivalent E(C) in two inserted
expressions, avoiding the reserved square-bracket status syntax.

The additional [first-cut realization notes](../../QDD-FIRST-CUT-REALIZATION-1/README.md)
give a twelve-mode classical analogue design and an explicit transfer-error
allowance. They remain noncanonical design work outside these three proposed
T rows. Their source audit distinguishes cryptographic extraction parameters
from physical timing/coverage certificates; no outcome payload was opened.
