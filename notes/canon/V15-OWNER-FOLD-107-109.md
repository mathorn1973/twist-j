# V15 owner consolidation for issues #107, #108, and #109 (NON-CANONICAL)

Status: `REVIEWED-DRAFT / OWNER-DISPOSITION-PROPOSAL / NO-CANON-CHANGE / NO-PROBE`

This note consolidates the useful content of three unmerged incubation
decision inputs into one independently reviewed owner-disposition
proposal. It changes no Canon claim, status, registry row, dependency, gate, or frontier count. It
authorizes no formal probe. Public Canon v14 remains the only normative
authority.

## Authority pin and provenance

```text
Canon:              Public Canon v14, ACTIVE, tag canon-v14
activation commit:  f278855cada75691142c02ebad1a75ecc2730db3
content commit:     ab4ea07d15ab1cfa0c403d3c2a74164011ffa0e7
Canon SHA-256:      f9f06af42a9b0b63f806603ddc671ebe6a9e5014b50d230da04a28722ed1a6a2
Canon bytes:        87061
public main base:   3f6500afe1ea71fb60f7c7d81da9a2d4d05d2ba3
issues:             #107, #108, #109
```

The reviewed but unmerged inputs were commits
`57e63d45d20ff253045eba30bb7846c0f9c7ff14`,
`795d3054f97f46812cc63a64321f4f73e85a956e`, and
`87699b17cb8130110b567f2e99ff578136729b57`. They remain provenance only.
This fold incorporates the parts that survive audit and explicitly rejects their overclaims.

## 1. Consolidation rule

One owner decision must remove ambiguity without manufacturing a theorem or
creating another obligation. A conditional algebraic selector is not a
physical dictionary until adopted with its complete typed surface. A child
surface that pre-decides an outcome does not satisfy a parent reachability
contract. A certificate is a proof system for a scientific property, not a
second property renamed after a counterexample.

## 2. Issue #107: close the C5 search; defer filter adoption

Owner disposition:

```text
C5 ROOT-INJECTION SEARCH: CLOSED PROPOSAL-LOCALLY
Adm_GalSplit:           NOT ADOPTED
FILTER OWNER FOLD:      DEFERRED
D_direct:               UNRESOLVED
QUADRATIC-DECODER-DATA: O / STOP
FORMAL PROBE:           FORBIDDEN
```

The merged branch classification proves exactly:

- the five verb-compatible windows give five complete fixed-basis record
  classes and one common scalar subrecord class;
- base classification is `NONUNIQUE`;
- the optional new predicate `Adm_GalSplit` selects `c_1` inside `C5`.

It does not prove that `Adm_GalSplit` is the unique possible admissibility
clause, that finite mathematics cannot distinguish candidate predicates, or
that a public physical dictionary already supplies this choice. The
normative question is whether the owner may adopt the new predicate, not
whether its finite action can be computed.

No search restarts inside `C5`. Search outside `C5` is unscheduled and may
resume only when a genuinely prior public readout candidate is supplied; it
creates no new owner. A future filter-adoption fold, if chosen, must re-freeze
the selection-local amplitude bridge, basis identifier, record equality,
factor map, dependencies, and layer scope. Complete `MatterData` ownership,
manifests, the QDD bridge gate, and the remaining completion contract may
still remain open after that selection-local fold.

There is no dark-energy adoption precedent: `DE-CONFORMAL-WEIGHT` remains an
open dictionary problem. The C5 result therefore supplies no precedent-based
authorization.

## 3. Issue #108: reject the proposed filters by a structural no-go

Owner disposition:

```text
PRIMITIVE REYNOLDS CHILD:  ENRICHMENT ONLY
Adm_natural SURFACE:       REJECTED
Adm_recurrent SURFACE:     REJECTED
CONJUNCTION SURFACE:       REJECTED
CURVATURE-OPERATOR-CANONICAL: O / STOP
FORMAL PROBE:              FORBIDDEN
```

The raw child contains 640 syntactic rows before carrier routing. The merged
affine reduction proves `Aut_arch_aff={identity}` and exact equality acts
only on identical typed carriers.

Consider two ambient rows with the same primitive pair `{a,c}`:

```text
S_0 = empty,       dim(V_(S_0)) = 15624,
S_1 = {b,d},       dim(V_(S_1)) = 818.
```

For `S subseteq S'`, Reynolds absorption gives

```text
R_(S') R_S = R_S R_(S') = R_(S'),
P_(S') P_S = P_S P_(S') = P_(S'),
```

because the constant-removal projector commutes with every Reynolds
projector. Hence the ambient extension

```text
Ktilde_AMB(S,u,v) = P_S C_(u,v) P_S in End(Q^X)
```

is restriction-natural along the subset tower, because

```text
P_(S') Ktilde_AMB(S,u,v) P_(S')
  = P_(S') C_(u,v) P_(S')
  = Ktilde_AMB(S',u,v).
```

Both displayed rows satisfy the proposed `Adm_natural` filter. Both also satisfy the proposed
`Adm_recurrent` filter because `S_0,S_1 subseteq {b,d,e}` and the pair is
`{a,c}`. Therefore they survive each filter and their conjunction.

Their carrier dimensions differ, so they cannot be equal or affinely
identified. Every proposed surface consequently contains at least two
classes before enumeration:

```text
base, Adm_natural, Adm_recurrent,
Adm_natural AND Adm_recurrent  =>  NONUNIQUE.
```

`EMPTY` and `UNIQUE` are unreachable on these surfaces. They do not satisfy
acceptance item 8 of the controlling predefinition and cannot become a
parent candidate by declaring an unreachable outcome as a formal label. The
existing child remains useful enrichment, but the parent requires a
different pre-result surface with all frozen outcomes reachable.

## 4. Issue #109: select the joint-certificate architecture

Owner disposition:

```text
SCIENTIFIC PROPERTY:       Adm_direct
PROOF SYSTEM:              Cert_joint(P,c,d)
FACTORWISE POWERS:         FIXED-LENGTH NEGATIVE CONTROL ONLY
DEFINITION:                UNRESOLVED
PARENT CLOSURE:            UNRESOLVED
METRO-ADMISSIBILITY:       O
FORMAL PROBE:              FORBIDDEN
```

The merged two-state witness proves only that factorwise power convergence
is not necessary for joint anchored fixed-length convergence. It does not
define a padding-independent admitted `N^a` protocol, prove translated-box
uniformity, or decide complete direct-versus-factorwise predicates.

The controlling ruling and scope map are amended with one coherent design:

1. `Adm_direct(P)` is the only scientific decision map, with codomain
   `Decision(Y_r) = {INADMISSIBLE} union {ADMISSIBLE(L) : L in Y_r}`;
2. `Cert_joint(P,c,d)` is an independently specified exact proof relation:
   `d` is the semantic decision and `c` is the proof payload;
3. soundness, completeness, and decision coherence for the anchored and
   translated-box direct property are theorem content; the schema must cover
   individual digit maps, q-adic boundary/residue data, allowed-start
   evaluation, and an effective modulus, because averaged actions or a bare Krylov submodule alone are
   insufficient;
4. an exact admitted counterexample to that theorem routes `NEGATIVE`;
5. an unresolved schema or a non-reproducing checker without an exact
   mathematical counterexample routes `STOP`;
6. the fixed-length factorwise witness remains a proposal-local required
   control and creates no registry or evidence row.

The owner choice closes only the architectural fork. Digit order and
padding, certificate schema, relevant submodule, translated-box boundary
theorem, completeness or finite bounds, reductions, exact L5/L6 endpoints,
and the parent-closure gate remain open definition debt.

## 5. Residual ledger

| Issue | Closed by this fold | Still open | Next executable action |
| --- | --- | --- | --- |
| #107 | five-window root-injection search inside C5, proposal-locally | `D_direct`; optional deferred filter adoption; or a genuinely prior outside-C5 readout | none until a complete adoption package or genuinely prior typed readout exists; outside search is unscheduled |
| #108 | the three proposed filter surfaces | a different parent surface satisfying the four-way reachability contract | definition work only; no enumeration |
| #109 | factorwise-versus-joint architecture choice | certificate schema, stream semantics, translated boxes, completeness, reductions and parent closure | one reviewed definition package; no probe |

The frontier remains unchanged. No issue is closed merely because one search
surface was rejected or one architecture was selected.

## 6. Supersession and firewall

Upon merge, this note supersedes the three unmerged decision-input
recommendations as owner guidance and updates the routing sections of the
merged C5 definition candidate and branch classification. The amended
`P-METRO-CLASS-1-RULING.md` and
`P-METRO-CLASS-1-SCOPE-MAP.md` are the controlling noncanonical metrology
documents after this fold. `P-METRO-CLASS-1-FORK-WITNESS.md` remains a scoped
control and provenance record.

Nothing here adopts `Adm_GalSplit`, selects a canonical curvature operator,
narrows a Canon claim, authorizes `probes/`, or moves a public status.
