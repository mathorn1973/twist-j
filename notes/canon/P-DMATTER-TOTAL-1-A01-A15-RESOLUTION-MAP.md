# P-DMATTER-TOTAL-1 A01 to A15 Resolution Map (NON-CANONICAL)

```text
STATUS:             DEFINITION-ONLY RESOLUTION MAP
AUTHORITY:          NOT CANON
PUBLIC BASE:        Public Canon v23
PUBLIC CANON TAG:   canon-v23
ACTIVATION COMMIT:  4ac41b4fac3a3794a6e9d5be1e2027d324edb806
CONTENT COMMIT:     7830d852229ffc06c9d287d026c8ece290bf339b
CANON SHA-256:      f842b613d6f65fe07ddab92ddbe1fb9fec89217d52b781571b7380281c3fb2b1
CANON BYTES:        116017
OWNER INPUT:        P-DMATTER-TOTAL-1-ROUTE-A-OWNER-DECISION.md
QDD STATUS:         O / STOP, unchanged
FORMAL RUN:         NONE
```

This note executes the first allowed action of the Route A owner decision:
map every required A01 to A15 surface to exact public content and expose the
remaining choices. It is not the completed public manifest, does not create a
registry row or gate, and does not authorize a verifier or probe.

The sources are the active Public Canon v23 and the following merged
non-canonical notes:

- `P-DMATTER-TOTAL-1-PREDEFINITION.md`;
- `P-DMATTER-TOTAL-1-DEFINITION-CANDIDATE.md`;
- `P-DMATTER-TOTAL-1-CYCLOTOMIC-REALIZATION.md`;
- `P-DMATTER-TOTAL-1-BRANCH-CLASSIFICATION.md`;
- `P-DMATTER-TOTAL-1-ROUTE-A-OWNER-DECISION.md`.

No private snapshot, unpublished result, or machine-local source is used.

## 0. Falsification first

This draft is rejected as a complete manifest if any of the following is
treated as resolved:

1. a proposal-local `CAND-*` name is presented as a public identifier;
2. a missing physical instrument is reconstructed from its effect;
3. a missing layer endpoint or public gate is inferred from prose;
4. the hybrid label is inserted without a defined source object and total map;
5. a hidden input, partial map, dependency cycle, or untyped transport remains;
6. the definitional identity
   `D_scoped = F_Gram o Qcan o beta` is presented as an independent
   factorization result;
7. the parent `QUADRATIC-DECODER-DATA` row is moved from `O / STOP`.

Items 1 to 5 are `STOP` conditions. They are not new negative scientific
results. A later frozen candidate can earn a negative result only by firing
the already registered QDD decision condition on its exact named domain.

## 1. Two ledgers must not be conflated

`CONTENT-READY` below means that exact content can be transcribed from public
v23 and the merged notes without a new scientific choice. It does not mean
`RESOLVED` in the sense of `DEF-DECODER-COMPLETION-CONTRACT`.

| Item | Content readiness | Exact public content now available | Remaining strict contract work |
| --- | --- | --- | --- |
| A01 | `CONTENT-READY` | rational matrix ring and trivial involution; cyclotomic amplitude field and conjugation | adopt distinct public IDs and domains |
| A02 | `PARTIAL` | `K0`, `Veff`, `V_lin`, and the inclusion type | define `GyronObject`, `L_label`, label equality, `ell_gyron`, `j_hyb`, and totality domain |
| A03 | `CONTENT-READY` | public checkpoint order plus the candidate beta formula; spelling bridge proposed below | adopt the spelling bridge and public IDs |
| A04 | `CONTENT-READY` | exact `B0`, `c_0`, and `iota_B0=iota_0` | adopt public IDs |
| A05 | `CONTENT-READY` | exact `G0`, `G`, dagger, transpose, and `sharp_G` | adopt typed public IDs |
| A06 | `CONTENT-READY` | ordered `Qcan`, finite `QCarrier`, and componentwise equality | adopt public IDs |
| A07 | `CONTENT-READY` | total tagged `ZERO` or `NONZERO` codomain | adopt public type and constructor IDs |
| A08 | `PARTIAL` | five exact algebraic fields and payloads | freeze complete public field rows, roles, carriers, normalization, equality, source, emit rule, and IDs |
| A09 | `PARTIAL` | exact total composite `D_scoped` | freeze public write-map ID and one owner for every field |
| A10 | `PARTIAL` | Route A requires `D_matter` and `D_quadratic` | freeze every field role before per-field stage and leg rows |
| A11 | `PARTIAL` | exact algebraic effects, weights, and normalization identity | freeze physical `K_a`, instrument, outcome, effect, pairing, and completeness IDs |
| A12 | `PARTIAL` | five current public dependency edges and an acyclic candidate core | publish the complete dependency DAG and exact cycle check |
| A13 | `OPEN` | only the general L1 to L6 protocol and unrelated existing gates | freeze record or stream semantics, all endpoints, and scope-valid public gates |
| A14 | `PARTIAL` | five candidate write targets and `feeds_U=FALSE` | freeze terminal outputs and a public terminality basis |
| A15 | `PARTIAL` | top-level semantic allowlist and denylist | close the allowlist transitively over the complete implementation |

The content-readiness count is therefore:

```text
CONTENT-READY  6
PARTIAL        8
OPEN           1
```

In the strict completion-contract ledger, every A01 to A15 block remains
`UNRESOLVED` until a later normative action supplies the required public
identifiers and complete manifest rows.

## 2. A03 audit correction: the piston projection is available

The generic architecture first writes the checkpoint as
`psi in F_5^6`, but Canon Section 3 later fixes its coordinate order:

```text
psi = (p1, p4, p1p, p4p, q, r).
```

The definition candidate uses primed mathematical notation. This resolution
map proposes the following spelling normalization for later adoption:

```text
p_1  := p1
p_4  := p4
p'_1 := p1p
p'_4 := p4p.
```

The identification is a proposal-local notation bridge. It is not already a
separate public identifier. With that bridge, freeze the set map explicitly:

```text
X  = F_5^6
K0 = { kappa_x = (U^n(0,x))_(n>=0) : x in X }

pi_piston_n0(kappa_x)
  = (p1,p4,p1p,p4p)
    for x=(p1,p4,p1p,p4p,q,r).

ell(0)=0, ell(1)=1, ell(2)=2, ell(3)=-2, ell(4)=-1.

beta(kappa_x)
  = (ell(p1),ell(p4),ell(p1p),ell(p4p))^T.
```

`beta` is a total set map on `K0` using the pre-update checkpoint at `n=0`.
It is not additive and is not a ring-linear lift from `F_5` to `Q`. Indeed,
the displayed balanced section `ell` is explicitly not a ring homomorphism.
The coordinates `q` and `r`, every later checkpoint, the counter,
environment, randomness, network input, and dynamic evaluation are forbidden
inputs to `beta`.

Equality in `K0` remains equality of complete pointed forward sequences.
Adopting the notation bridge removes a spelling gap; it does not create a
physical selection.

## 3. Exact content-ready core

### A01. Coefficient objects and involutions

The quadratic matrix carrier uses

```text
R_mat = Q
inv_Q = identity_Q.
```

The cyclotomic realization uses the distinct amplitude field

```text
K_amp = Q(zeta),             zeta = zeta_5,
bar_K = sigma_4,
Tr_K  = Tr_(K/Q).
```

`inv_Q` and `bar_K` are different typed operations. The future rational label
module also has coefficient ring `Q`, but its object, dimension, basis, and
equality remain part of A02.

### A02. Base carriers and the unresolved hybrid extension

The exact base is

```text
X     = F_5^6
K0    = { kappa_x = (U^n(0,x))_(n>=0) : x in X }
Veff  = ell(F_5)^4 subset Q^4
V_lin = span_Q(Veff) = Q^4.
```

The natural inclusion is the recommended exact completion

```text
j_lin : Veff -> V_lin,
j_lin(v) = v.
```

It remains proposal-local until adopted. The following required objects have
no exact public definition:

```text
GyronObject
L_label
equality on L_label
ell_gyron : GyronObject -> L_label
C_hyb     = V_lin tensor_Q L_label
G_hyb     = G tensor I_L
j_hyb(v,g)=j_lin(v) tensor_Q ell_gyron(g)
complete totality domain of the hybrid map.
```

The public row `GYRON-DENSITY` supplies one exact Thue-Morse pair density. It
does not by itself define `GyronObject`, a label module, or a physical hybrid
carrier. Importing those missing objects from an unpublished source is
forbidden.

### A04. Standard basis and selected global decoder

Freeze

```text
B0 = (1,zeta,zeta^2,zeta^3)

iota_B0(v)
  = v_0 + v_1 zeta + v_2 zeta^2 + v_3 zeta^3
  = iota_0(v).

lambda_0 = -zeta^4
L_0      = Q lambda_0
H_0      = L_0^perp
Pi_0     = the trace-orthogonal projector onto L_0

c_0 = (0,B0,iota_0,L_0,H_0,Pi_0).
```

`B0` is a basis. `c_0` is the complete selected candidate whose basis
component is `B0`. The selection of `c_0` remains the explicit five-way model
choice made by the Route A owner decision. `Adm_GalSplit` remains
`NOT ADOPTED`.

### A05. Gram forms and three distinct operations

With `1` the all-ones column in `Q^4`, freeze

```text
G0 = 5 I_4 - 1 1^T
G  = G0 / 5 = I_4 - (1/5) 1 1^T.

v^dagger      = v^T
transpose(A)  = A^T
sharp_G(A)    = G^(-1) A^T G.
```

The normalized `G` is used in the Route A record. The equality
`v^dagger=v^T` over `Q` does not identify `sharp_G` with ordinary transpose,
and neither operation is identified with the cyclotomic conjugation
`bar_K`.

### A06. Ordered quadratic carrier

For `v in Veff`, let `A(v)=v v^T` and freeze

```text
Qcan(v) = (v v^dagger, v v^T) = (A(v),A(v)).

QCarrier
  = image(Qcan | Veff)
    subset M_4(Q) x M_4(Q).
```

The equality on `QCarrier` is componentwise rational equality of the ordered
pair. Equal coordinate values in this rational candidate do not collapse the
two typed slots. The proposal-local finite image has exactly 313 elements:
one zero and 312 nonzero sign-pair elements. On the 15,625 anchored
checkpoints, the zero fibre has size 25 and each nonzero fibre has size 50.

No global phase quotient is introduced on this scoped rational carrier. A
future enlarged coefficient field or phase action would require its own
pre-result action law and output covariance before any quotient.

The right-Gram numerator is derived:

```text
N_G(A)=A G.
```

It is not the definition of `Qcan`.

### A07. Total tagged codomain

Define

```text
CandidateQuadraticData =
  ZERO {
    support_state            = ZERO,
    total_weight             = 0,
    branch_weights           = (0,0),
    density_state            = ZERO_DENOMINATOR,
    normalized_weight_state  = ZERO_DENOMINATOR
  }
| NONZERO {
    support_state            = NONZERO,
    total_weight             = m(A),
    branch_weights           = (w_low(A),w_high(A)),
    density_state            = DENSITY(A G / m(A)),
    normalized_weight_state  =
      NORMALIZED((w_low(A),w_high(A))/m(A))
  }.
```

This is total. The zero branch contains explicit tags and no bare null or
division by zero.

## 4. A08 to A10: exact fields, incomplete public rows

The Route A composite is

```text
D_scoped : K0 -> CandidateQuadraticData

D_scoped
  := R_cyc o iota_B0 o beta
   = F_Gram o Qcan o beta.
```

The second equality is a derived identity of the adopted definitions. It is
not an independent factorization test.

The candidate record has exactly these five fields:

| Field | Exact carrier and equality | Exact emit rule | Recommended ownership |
| --- | --- | --- | --- |
| `support_state` | tagged set `{ZERO,NONZERO}`, literal tag equality | emit the zero test of the amplitude | `READOUT`, `D_matter`, `D_quadratic` |
| `total_weight` | `Q_(>=0)`, rational equality | emit `0` on ZERO and `m(A)` on NONZERO | `READOUT`, `D_matter`, `D_quadratic` |
| `branch_weights` | `Q_(>=0)^2`, ordered componentwise equality | emit `(0,0)` on ZERO and `(w_low,w_high)` on NONZERO | `READOUT`, `D_matter`, `D_quadratic` |
| `density_state` | tagged union `ZERO_DENOMINATOR` or `DENSITY(M_4(Q))`, tagged componentwise equality | emit the zero tag or `DENSITY(A G/m(A))` | `READOUT`, `D_matter`, `D_quadratic` |
| `normalized_weight_state` | tagged union `ZERO_DENOMINATOR` or `NORMALIZED(Q_(>=0)^2)`, tagged componentwise equality | emit the zero tag or `NORMALIZED((w_low,w_high)/m(A))` | `READOUT`, `D_matter`, `D_quadratic` |

The recommendation is that all five emitted fields are `READOUT`. No
additional auxiliary field is needed for the scoped record. This ownership
ruling is not adopted by this resolution map.

The strict field rows remain incomplete because the following identifiers do
not yet exist publicly:

```text
record_id
five field_id values
five field_type_id values
carrier_id values
normalization_id values
equality_id values
source_item_id
write_map_id
emit_rule_id values.
```

`stage_id=D_matter` and `leg_id=D_quadratic` become exact per-field entries
only after the owner adopts the five `READOUT` roles. A proposal-local file
name or `CAND-*` handle cannot fill a public identifier slot.

## 5. A11: algebraic effects are not physical instruments

The exact algebraic candidate is

```text
E_low  = (1/4) 1 1^T
E_high = I_4 - E_low

E_j^2          = E_j
sharp_G(E_j)   = E_j
E_low + E_high = I_4

m(A)           = Tr(A G)
w_j(A)         = Tr(E_j A G)
w_low(A)+w_high(A)=m(A).
```

For `v in Veff`,

```text
m      = v^T G v
w_low  = (sum_i v_i)^2/20
w_high = sum_i v_i^2 - (sum_i v_i)^2/4.
```

These are exact nonnegative rational weights. They are not thereby physical
probabilities or selected apparatus effects.

The coupling-first rule requires a separately frozen physical family

```text
instrument_id
K_a
effect_id
E_a = K_a^sharp K_a
outcome_id
Born pairing
normalization and completeness
record semantics
coarse-graining
MatterData fields that read each effect.
```

The shortcut `K_j:=E_j` would satisfy the algebraic certificate
`K_j^sharp K_j=E_j` because the projectors are sharp-self-adjoint and
idempotent. It would still not derive an apparatus selection, outcome
semantics, or post-event instrument. It is therefore not adopted here.

Consequently:

```text
algebraic effect candidates          exact
algebraic pairing Tr(E_j A G)        exact
physical effect IDs                  UNRESOLVED
instrument IDs and K_a               UNRESOLVED
physical Born-pairing ID             UNRESOLVED
physical normalization certificate  UNRESOLVED.
```

## 6. A12: dependency floor and candidate DAG

The public dependency ledger currently contains exactly these five direct
QDD edges:

```text
QUADRATIC-DECODER-DATA -> DEF-ARCHITECTURE
QUADRATIC-DECODER-DATA -> DEF-DECODER-MATTER
QUADRATIC-DECODER-DATA -> READING-SPLIT
QUADRATIC-DECODER-DATA -> COUPLINGS-DETERMINE
QUADRATIC-DECODER-DATA -> MEASURE-BORN-VERB.
```

The first mandatory delta for a completion-contract submission is

```text
QUADRATIC-DECODER-DATA -> DEF-DECODER-COMPLETION-CONTRACT.
```

This is a proposed later ledger edge, not a change made by this note.
Hybrid-carrier, instrument, and gate dependencies cannot be complete until
A02, A11, and A13 are decided.

The definition-only core has the following acyclic order:

```text
DEF-ARCHITECTURE
    -> K0
    -> beta
    -> {Qcan, iota_B0}

{Qcan,G,E_low,E_high}
    -> F_Gram

{iota_B0,trace pairing,G,E_low,E_high}
    -> R_cyc

{beta,iota_B0,R_cyc}
    -> D_scoped
    -> five CandidateQuadraticData fields.
```

There is no permitted edge from `MatterData`, an emitted outcome, a later
checkpoint, or a measured value back into `beta`, `B0`, `G`, effect
selection, or the state update `U`.

This displayed order is not yet the complete dependency manifest and has no
machine-checked public acyclicity certificate.

## 7. A13: the sole fully open block

Public v23 fixes the six layers

```text
L1 state
L2 manifold
L3 boundary
L4 support
L5 stream
L6 measure
```

and requires a named gate for every cross-layer lift. Neither
`GATE-L1-L5-LOG-PROJECTION` nor `GATE-L5-L6-BORN-READING` automatically owns
the Route A piston dictionary. The former owns the derived log projection.
The latter owns only its registered Born-square dictionary scope.

The exact current Route A map is

```text
D_scoped : K0 -> CandidateQuadraticData.
```

It returns one tagged record at the distinguished anchor. It has no output
index, shift law, or stream emit rule. `CandidateQuadraticData` therefore
cannot be called an L5 stream merely because it is a decoder output.

The exact current A13 ledger is

```text
layer of K0                    UNRESOLVED
layer of CandidateQuadraticData UNRESOLVED
beta endpoints                UNRESOLVED
iota_B0 endpoints             UNRESOLVED
write endpoints               UNRESOLVED
core gate                     UNRESOLVED
hybrid endpoints and gate     UNRESOLVED.
```

The minimal recommended owner ruling is:

1. freeze `D_scoped` first as one anchored `D_matter`, `D_quadratic` record;
2. do not label its codomain an L5 stream;
3. if an L5 output is intended, separately define a total map
   `S_scoped : K0 -> CandidateQuadraticData^(N_0)` with its exact term,
   shift law, read convention, equality, and emit rule before assigning an
   L1-to-L5 or other cross-layer gate;
4. keep the hybrid transport separate until `GyronObject`, its source layer,
   label map, equality, and totality domain are exact.

This recommendation does not define `S_scoped`. In particular, it does not
silently extend the pre-update `n=0` rule to every time index. A record-only
route still requires an exact layer assignment, and a stream route requires
the additional map above.

No gate ID is coined here. A proposal-local mnemonic cannot satisfy A13.

## 8. A14: closure is stage-local, not completion-wide

The candidate write targets are the five fields in Section 4. Canon fixes

```text
feeds_U = FALSE
```

for decoder outputs. This does not establish that each field is terminal in
the complete decoder. `D_geom` reads `MatterData`, and completion-wide
terminality remains owned by `OBSERVER-WRITE-PORT`.

Therefore the exact current closure state is

```text
write_target_ids       UNRESOLVED public IDs for the five exact fields
feeds_U                FALSE
terminal_output_ids    UNRESOLVED
terminality_basis_id   UNRESOLVED.
```

Using an empty terminal list without a public basis would be an unsupported
claim, not a harmless default.

## 9. A15: hidden-input boundary

The following is a top-level semantic allowlist, not yet the required
transitive implementation closure:

```text
the named input kappa_x in K0
the n=0 piston projection of its pointed checkpoint
the fixed balanced section ell
the exact maps beta, iota_B0, Qcan, F_Gram, and R_cyc
the exact tagged constructors of CandidateQuadraticData.
```

The internal semantic dependencies allowed inside those maps are:

```text
exact arithmetic over Q and rational matrices
K=Q(zeta), sigma_4, Tr_(K/Q), and the rational trace pairing
B0, iota_B0, G0, G, and exact matrix inversion
A(v), Qcan, N_G, E_low, E_high, m, w_low, and w_high
lambda_0, L_0, H_0, Pi_0, T_w, and the zero predicate
ordered-pair, matrix, rational, and tagged-union equalities.
```

For `beta`, even `q` and `r` are outside the read allowlist although they are
coordinates of the named checkpoint domain.

The denylist is:

```text
later checkpoints
counter or clock reads
environment variables
files not named as public inputs
randomness
network input
dynamic evaluation
measured values
post-event outcomes
MatterData feedback
unregistered label or apparatus inputs.
```

A15 remains `PARTIAL` until an implementation exists and an exact transitive
closure check covers artifact and import pins, files, environment, clock,
randomness, network, dynamic evaluation, every internal helper, and every
dependency named in A12.

## 10. Completion-contract overlay

The A01 to A15 list does not replace the public completion contract. Its
current block state is:

| Contract block | Current state | Blocking reason |
| --- | --- | --- |
| top-level candidate fields | `PARTIAL` | proposal-local values exist, but `public_pin_id` and public IDs are not frozen |
| `carrier_manifest[]` | `PARTIAL` | base carriers exact; hybrid carriers incomplete |
| `record_field_manifest[]` | `PARTIAL` | exact fields exist; public rows and owner ruling absent |
| `stage_manifest[]` | `PARTIAL` | scoped map exact; public domain, codomain, map, and dependency IDs absent |
| `leg_manifest[]` | `PARTIAL` | intended leg exact; owned public field IDs absent |
| `bridge_manifest[]` | `UNRESOLVED` | A13 record or stream semantics, endpoints, and gates not adopted |
| `quadratic_manifest` | `PARTIAL` | algebraic core exact; physical effects and Born pairing unresolved |
| `physics_manifest` | `UNRESOLVED` | no source-current-conservation-propagator-detector chain is supplied |
| `measure_manifest` | `UNRESOLVED` | no complete physical measure, metrology, or scheme IDs are supplied |
| `closure_manifest` | `PARTIAL` | `feeds_U=FALSE`; terminal fields and basis unresolved |
| `obligation_manifest[]` | `PARTIAL` | this resolution map supplies the inventory, not public owning rows |

`NOT_APPLICABLE` is not used to erase any missing identifier. Every missing
identifier remains `UNRESOLVED`.

## 11. Minimal owner decisions now exposed

The next owner action has four independent decisions.

```text
OD1 FIELD OWNERSHIP
    Recommended:
    adopt all five CandidateQuadraticData fields as READOUT fields owned by
    D_matter at leg D_quadratic.

OD2 RECORD OR STREAM
    Recommended:
    freeze D_scoped first as one anchored record. Keep its action-layer
    endpoint and gate UNRESOLVED until the owner either assigns an exact
    record layer or defines the total stream map S_scoped.

OD3 HYBRID SCOPE
    Recommended:
    keep the hybrid transport in a separate unresolved extension until
    GyronObject, L_label, ell_gyron, its source layer, equality, and totality
    domain are exact. Do not let it block transcription of the exact core,
    but do not move the parent QDD row while it is required by A02.

OD4 PHYSICAL INSTRUMENT CLASS
    Recommended:
    do not infer K_a from E_a. Prepare a separate predefinition of the
    admissible instrument class, outcome semantics, coarse-graining,
    normalization, and K-level equivalence before any enumeration.
```

OD1 and OD2 are definition decisions. OD3 preserves an explicit open bridge.
OD4 is the prerequisite for a later physical-effect-selection probe.

## 12. Status accounting and next allowed step

```text
Route A exact algebraic core          content-ready
A01 to A15 public manifest            incomplete
strict completion-contract state      UNRESOLVED
QDD scoped write dictionary           owner-adopted proposal only
QDD physical effect selection         O / STOP
QDD hybrid carrier bridge             O / STOP
QDD independent factorization         O / STOP
QUADRATIC-DECODER-DATA                 O / STOP, unchanged
```

The next allowed action is an owner decision on OD1 to OD4. After that
decision, a definition-only package may assign proposed public IDs, write the
complete field, stage, leg, bridge, quadratic, dependency, and closure
manifests, and run a static conformance audit. No formal scientific probe is
authorized by this resolution map.
