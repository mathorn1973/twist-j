# P-TM-SYM2-MEASURE-1 owner definition (NON-CANONICAL until merged)

```text
OWNER-DEFINITION-FOLD / NO-CANON-CHANGE / NO-PROBE / NO-EVALUATION
```

Rev 3. Rev 1 was submitted to an independent owner-lane break round on
2026-07-22 and took two accepted kills: the carrier forcing was stated
without naming its bijective clause as load-bearing (an exact equivariant
surjection from the length-4 language exists without it), and the
coefficient-basis forcing was presentable as resting on the face weights
alone, which do not uniquely determine a coefficient representative (an
exact full-support witness exists). Both kills are archived first-class as
checker certificates CERT-F1h and CERT-F3g. Rev 2 accepted them but a
second independent definition audit found three remaining scope defects:
the F1 carrier family did not say fixed-length and contiguous at its root,
the F2 coordinate-axis symmetry class was not itself named as an owner
adoption, and the phaseful F3 root omitted both the explicit magnetic-axiom
condition of ABELIAN-FACE-DICTIONARY and its direct dependency edge. Rev 3
accepts those defects first-class, makes every adopted root explicit,
completes the dependency graph, and retains the completed NONCANONICAL
partition record. No scientific output was read, no threshold moved, and
all evaluation routes remain live.

This document is the owner-definition fold for `TM-SYM2-MEASURE [H]` and
issue #119. On the owner's merge of this file into public `main`, the six
residual definition debts named in section 11 of
`P-TM-SYM2-MEASURE-1-BORN-BRIDGE-INPUT.md` are resolved, the definition
tuple `S_TM` of `P-TM-SYM2-MEASURE-1-PREDEFINITION.md` is complete with no
`UNRESOLVED` slot, and issue #119 can close. This file changes no canon
table, registry row, gate table, frontier state, or status; the exact
normative delta it authorizes is frozen in Appendix A and is applied only
by the separate sealed Public Canon v16 fold. No probe is authorized here;
the formal probe `P-TM-SYM2-MEASURE-1` still requires its own immutable
preregistration pin. No evaluation output is read or published here.

## Authority pin

```text
Canon:                 Public Canon v15
state:                 ACTIVE
tag:                   canon-v15
activation commit:     8f4e176c5d76f519d3493e56e438aba7856e1f01
content commit:        a850753348583e611bf7ccd5aa074030dc7e12f5
Canon SHA-256:         53237ec25b3782e833367c998c049d19459189a21c2a36638dd9e1600335976b
Canon bytes:           89288
public-main readback:  633940a0187f7bdac83eae5639844622ad955d9f
owner row:             TM-SYM2-MEASURE [H]
scheduler:             MEASURE / ROOT / STOP / FORMAL
public lock:           issue #119
consumed inputs:       P-TM-SYM2-MEASURE-1-PREDEFINITION.md (PR #120)
                       P-TM-SYM2-MEASURE-1-SELECTOR-CLASS-INPUT.md (PR #121, Rev 3)
                       P-TM-SYM2-MEASURE-1-BORN-BRIDGE-INPUT.md (PR #122)
checker (this fold):   notes/canon/P-TM-SYM2-MEASURE-1-DEFINITION-CHECKER.py
```

## 0. What this fold decides, in one block

The two merged definition inputs deliberately carried three load-bearing
declarations as unforced proposals: the radius-1 window carrier `W3`, the
line pairing `sigma_line`, and the coefficient representative
`delta_0 + delta_1` with its support-basis reading. This fold discharges
all three as PRECISELY CONDITIONED canonicity results: each ruling names
the definitional root it adopts, proves uniqueness under that root, and
archives a first-class machine witness that the root is load-bearing (the
uniqueness fails without it). It adopts both lane gates and the complete
dependency graph, pins the exact certificate checker for the full schema
B1-B7, and completes `S_TM`. What it does not and cannot do: decide the science. The
canonicality classification, gauge coherence, and every route of the
future evaluation remain exactly as frozen, with `POSITIVE`, `NEGATIVE`,
and `STOP` all live. Nothing below reads, embeds, or anticipates an
evaluation output.

## 1. Ruling F1: W3 forced in the fixed-length contiguous carrier family

ADOPTED DEFINITIONAL ROOT (the transport principle of section 1 of the
selector-class input, owner-adopted HERE IN ITS FIXED-LENGTH CONTIGUOUS
AND BIJECTIVE FORM): the candidate carrier is required to be one complete
fixed-length contiguous factor language `F_k`, for one integer `k >= 1`,
of the registered one-sided drive. The selector does not choose lines. It
is an exact transport of the drive's registered involution structure onto
the involution structure declared on the registered frame: a BIJECTION
from that `F_k` onto the six registered lines, intertwining the drive's
registered flip `N` with the frame pairing, with all weights delivered by
the exact stationary law. Fixed-length contiguity and bijectivity are
owner-adopted scope, not theorems. Noncontiguous, dilated, subsampled,
variable-length, or quotient cylinders are different candidate
definitions and are neither excluded nor classified by this ruling.

The centered selector convention
`q(n) = (theta_(n-1), theta_n, theta_(n+1))`, `n >= 1`, frozen in section 3
of the selector-class input, is separately owner-adopted here. The forcing
theorem below identifies its range carrier as `F_3 = W3` inside the adopted
family; it does not derive centering, rule out another sampling geometry,
or turn the centered convention into a theorem.

THE CARRIER FAMILY AND THE BIJECTIVE CLAUSE ARE LOAD-BEARING OWNER
CHOICES. The merged
selector-class input froze `Sel_class` on `W3` with surjectivity only;
on the six-word carrier surjectivity implies bijectivity, so on `W3` the
adopted form is conservative and changes nothing frozen. Across carriers
it is decisive: without the bijective clause the carrier is NOT forced.
The checker constructs an exact `N`-equivariant surjection from the
ten-word length-4 language onto the six lines (CERT-F1h, the archived
witness of the independent break round). The bijective form is adopted,
and justified, as the choice-free reading: an equivariant surjection from
a longer-window language factors through a six-element quotient of that
language, and which quotient is an additional unforced selection; the
transport that needs no quotient choice is the bijective one, and that
existence question is what the forcing theorem below answers.

THEOREM (carrier forcing, machine-certified as CERT-F1a to CERT-F1e).
Within the adopted carrier family the carrier is unique. Let `F_k`,
`k >= 1`, be the exact fixed-length contiguous factor language of the
registered drive `theta_n = s_2(n) mod 2` at window length `k`. Then:

```text
1.  F_k is exactly computable and complete for every k by the
    de-substitution closure of the registered doubling
    (theta_(2m), theta_(2m+1)) = (theta_m, 1 - theta_m): every position is
    an even or an odd start, so F_k is the exact image of F_(floor(k/2)+1)
    under the two expansion maps, with the k <= 2 base cases closed as
    least fixed points. An independent direct-prefix scan reproduces the
    same languages (CERT-F1a).
2.  |F_1| = 2, |F_2| = 4, |F_3| = 6, |F_4| = 10 (CERT-F1b).
3.  Right truncation maps F_(k+1) onto F_k (every factor of an infinite
    word extends on the right), so k -> |F_k| is nondecreasing
    (CERT-F1c and the one-line general lemma).
4.  Hence |F_k| = |Lines| = 6 holds for exactly one window length,
    k = 3: shorter windows are too few to surject, longer windows are too
    many to biject (CERT-F1d).
5.  W3 as frozen equals F_3, and the six centered windows q(1),...,q(6)
    already exhaust it (CERT-F1e). Cube-freeness is structural: the
    registered children theta_(2m) and theta_(2m+1) are complementary, and
    every three consecutive positions contain such a pair (CERT-F1f).
```

THEOREM SCOPE, restated exactly: within the class
`{F_k : k >= 1}` of complete fixed-length contiguous factor languages and
under the adopted bijective principle, `k = 3` is forced (the unique member
whose factor language bijects onto the frame). Under the surjective
weakening, `k = 3` is not forced and the carrier question reopens
(CERT-F1h); that weakening is rejected by the ruling above, not by a
theorem. No conclusion is made about a noncontiguous or otherwise different
cylindrical carrier.

SUPPORTING NO-GO (CERT-F1g). The increment reading
`w -> (w_2 - w_1, w_3 - w_2) mod 2` is invariant under `N` and has image
of size exactly 3 on the six windows, every fiber a full `N`-orbit. No
`N`-invariant reading separates the six lines: an admissible selector must
read the drive bits themselves. This kills the rival carrier family that
reads only changes of the drive, the exact form of predefinition false
shortcut 2 on the definitional side.

DISCHARGE OF THE DECLARED RISK. The selector-class input declared the
six-against-six match a numerological coincidence risk. The ruling is that
the count 6 enters only through the forcing theorem above: given the
fixed-length contiguous bijective transport principle, the drive selects
the unique `F_k` whose language bijects onto the registered frame. The
centered realization remains an adopted convention, not a consequence of
that count. What remains open is
exactly what the canonicality test was frozen to decide: whether the
transported structure is canonical. This ruling does not touch that test,
does not narrow `Sel_class`, and does not change the gauge.

## 2. Ruling F2: sigma_line forced under the adopted coordinate-axis class

Two exact statements, one negative and one positive, machine-certified.

NO-GO (CERT-A3 with CERT-F2i). The registered Gram data cannot select any
pairing. All fifteen pairs of registered lines have squared cosine exactly
`1/5`: the frame is pair-homogeneous. Any relation on the six lines
defined through a pairwise metric invariant is therefore empty or total,
and no pairing of the lines is metric-definable. A canonical pairing, if
any, must come from additional registered structure.

ADOPTED DEFINITIONAL ROOT (the coordinate-axis symmetry class,
owner-adopted): the registered exact coordinate presentation is read
through all signed coordinate permutations whose projective action
preserves the six-line set. This signed-permutation class is a chosen
automorphism class of the registered coordinate-axis presentation; it is
not asserted to be forced by the bare projective line set or to equal its
full projective stabilizer.

FORCING (CERT-F2ii, CERT-F2iii, with the consistency clauses CERT-I2a to
CERT-I2c). The effective signed-coordinate stabilizer in the adopted class
is a group of line permutations, computed exactly by exhaustive scan of
the 48 signed permutation matrices. Among all fifteen perfect pairings of
the six lines, exactly one is invariant under that stabilizer, and it is

```text
sigma_line = (v1 v2)(v3 v4)(v5 v6),
```

the zero-pattern pairing of the selector-class input. It coincides with
the single-sign-flip relation: negating either nonzero slot of a
registered representative lands projectively on the partner, and negating
the zero slot fixes the line (CERT-I2c). The incubation-lane break attempt
additionally found that already the involutive subgroup of the stabilizer
forces the same unique pairing, so the forcing is not sharp to the full
group; this is robustness, not load-bearing structure.

SCOPE GUARD. The group certified here is the signed-coordinate stabilizer
of the registered presentation, a definitional object. The evaluation
gauge `G = Aut(Lines, sigma_line)`, all projective-linear transformations
over `Q(phi)` preserving the line set and normalizing `sigma_line`,
remains exactly as frozen in the selector-class input, and its order and
structure remain evaluation outputs. Nothing here computes, bounds, or
anticipates them.

OWNER RULING (effective on merge): the coordinate-axis signed-permutation
class and `sigma_line` are adopted together. Within that owner-adopted
class, `sigma_line` is the unique invariant pairing. The claim is exactly
that and no more: the Gram no-go shows the pairwise metric data selects
nothing, and the signed-coordinate scan proves uniqueness under the named
class. No invariance under any larger transformation group is claimed or
implied, and whether any pairing survives larger symmetry is deliberately
left, with the gauge itself, to the evaluation.

## 3. Ruling F3: delta_0 + delta_1 under the conditional phaseful dependency

ADOPTED DEFINITIONAL ROOT (the conditional phaseful dependency,
owner-adopted): conditional on the explicitly supplied magnetic axiom pair
of `ABELIAN-FACE-DICTIONARY [D]`, the lane adopts that dictionary's exact
phaseful symbol under literal equality. Stay or twist once reads
`Psi(k) = 1 + zeta^k`, and `mu(k) = |Psi(k)|^2 / 10`
(`ABELIAN-FACE-DICTIONARY` with `MEASURE-BORN-VERB [D]` and
`BORN-FACE-WEIGHTS [T]`). The verb enters this lane through `Psi`, not only
through its Born square. This fold neither supplies nor selects the
magnetic axiom pair, and claims no uniqueness or selection theorem for
that input; it carries the dictionary's condition without strengthening
it. The Born square is a reading OF `Psi`; literal `Psi` is the prior
registered object under that condition.

THE PHASEFUL CLAUSE IS LOAD-BEARING. The face weights constrain the
normalized spectrum but do not uniquely determine a coefficient
representative: the exact full-support rational vector
`(-4, -4, 1, 1, -4)/5` reproduces the registered face weights entrywise
while its Fourier transform differs from `Psi` (CERT-F3g, the archived
witness of the independent break round). A weights-only uniqueness claim
is therefore dead, first-class. The alternative repair, a new
minimal-support axiom, was
considered and rejected: the phaseful symbol is already registered
structure, while a support axiom would be new unregistered structure
introduced only to rescue the weaker anchor.

THEOREM (basis forcing, machine-certified as CERT-B3a to CERT-B3e and
CERT-F3a to CERT-F3f).

```text
1.  Fourier inversion of the ADOPTED phaseful symbol. The exact Fourier
    map on the coefficient carrier satisfies F* F = 5 I (CERT-B3c), so it
    is a bijection, and Psi has exactly one coefficient preimage:
    F(delta_0 + delta_1)_k = 1 + zeta_5^k (CERT-B3a, CERT-F3a). Under the
    adopted root the representative is not proposed; it is the inverse
    image of a registered object. Its axiom anchor: the slot k = 2 reads
    F(delta_0 + delta_1)_2 = 1 + zeta_5^2 = J (CERT-B3b). Without the
    root, the forcing fails (CERT-F3g).
2.  Spectral agreement. Born_spec(delta_0 + delta_1) equals the registered
    face weights entrywise exactly, positive in the principal order,
    summing to 1 (CERT-B3d, CERT-B3e).
3.  Robustness on minimal support (not the forcing). Within support size
    at most 2, the registered weights force equal support coefficients
    ((alpha+beta)^2 - 2(alpha^2+beta^2) = -(alpha-beta)^2, an exact
    polynomial identity, CERT-F3d) and adjacent support (gaps d = 1, 4
    only, CERT-F3e); size-1 supports give the flat reading and are
    excluded (CERT-F3c); Born_spec is translation invariant (CERT-F3b).
    Outside that class the weights do not select a unique coefficient
    representative (CERT-F3g), which is exactly why the forcing rests on
    the conditional phaseful root, not on this certificate.
4.  The half map is pinned. Every residual minimal-support representative
    reads its two-point support with the exact uniform Born weight
    (1/2, 1/2), independent of orientation and translation (CERT-F3f).
    The value 1/2 is derived three ways that agree exactly: as the support
    Born weight of the phaseful preimage, as the sheet weight under
    COMPLEMENT-BALANCE, and as the conditional continuation weight of the
    mixed-pair factorization already public in the Born input.
```

OWNER RULING (effective on merge): the free `N`-sheet of the window
carrier is read in the coefficient support basis of the registered verb's
coefficient representative. This is the unique typed two-point bridge
between two registered two-alternative structures: the verb's stay-or-twist
support `{0, 1}` and the `N`-sheet `{w, wbar}` of an orbit, with
orientation provably irrelevant (CERT-F3f). The dictionary choice named
open in section 4 of the Born input is hereby adopted, at dictionary
grade, with its content now a rigidity theorem rather than a preference.

## 4. Owner adoption: both lane gates

OWNER RULING (effective on merge; table edits ride the v16 fold): the two
proposal-local gate records of section 8 of the Born input are adopted
verbatim as lane gates of `TM-SYM2-MEASURE`:

```text
gate_id:             GATE-L1-L5-TM-SYM2-SELECTOR-STREAM
owner_item_id:       TM-SYM2-MEASURE
from_layer:          L1
to_layer:            L5
gate_kind:           OPEN_LIFT
decision_condition:  emits the exact typed gauge-orbit record R_[s]^5, never
                     a representative, only when the frozen carrier/class
                     audit and canonicality classification are complete;
                     EMPTY or NONCANONICAL routes scientific NEGATIVE,
                     incompleteness or inexactness routes STOP

gate_id:             GATE-L5-L6-TM-SYM2-BORN-MEASURE
owner_item_id:       TM-SYM2-MEASURE
from_layer:          L5
to_layer:            L6
gate_kind:           OPEN_LIFT
decision_condition:  closes this candidate block only when the Born
                     carrier, coefficient and spectral readings, support
                     pushforwards, structural compatibility implications,
                     gauge-coherence record, original N3 record, GYRON
                     bridge, normalization, and exact checker are complete;
                     an exact scientific N3 or gauge disagreement routes
                     NEGATIVE, while missing, incomplete, or inexact
                     typing or proof routes STOP
```

Both are lifts in the registered six-layer protocol (`DEF-ACTION-LAYERS`);
the second rests on the generic `GATE-L5-L6-BORN-READING` dictionary
without replacing or strengthening it. The layer endpoints of section 7 of
the Born input (the `L1` source record, the `L5` representative and orbit
records, the guarded `L6` evaluation orbit and measure endpoint) are
adopted with the gates as the lane's layer surface. Well-formedness is
machine-certified (CERT-B7f).

## 5. Owner adoption: the complete dependency graph

OWNER RULING (effective on merge; table edits ride the v16 fold): the
direct parent set of `TM-SYM2-MEASURE` is exactly

```text
existing floor (unchanged):
  TM-SYM2-MEASURE -> DEF-ARCHITECTURE
  TM-SYM2-MEASURE -> DEF-AUTONOMOUS-STATE
  TM-SYM2-MEASURE -> GOLDEN-SIX-LINE-SYM2-FRAME
  TM-SYM2-MEASURE -> GYRON-DENSITY
  TM-SYM2-MEASURE -> MEASURE-BORN-VERB
adopted delta (from section 9 of the Born input):
  TM-SYM2-MEASURE -> DEF-ACTION-LAYERS
  TM-SYM2-MEASURE -> RAMIFIED-TM-LIFT
  TM-SYM2-MEASURE -> SUBSTRATE-KNIT
Rev 3 phaseful-root completion:
  TM-SYM2-MEASURE -> ABELIAN-FACE-DICTIONARY
```

with the explicit ruling that there is NO edge to `DEF-LOG-STREAM`: the
lane's shifted selector records are not the registered total Log, and the
generic log-projection gate does not cover them. The folded graph is
acyclic and every endpoint is a registered normative item
(machine-certified, CERT-B7b to CERT-B7e, nine direct parents and 291
edges audited). The `ABELIAN-FACE-DICTIONARY` edge is load-bearing: it
carries the literal phaseful `Psi` together with the dictionary's explicit
magnetic-axiom condition and no uniqueness or selection claim for that
input. `MEASURE-BORN-VERB` and its transitive `BORN-FACE-WEIGHTS` parent
carry the Born-square reading, not this phaseful dependency by themselves.

## 6. The certificate checker, pinned

```text
file:      notes/canon/P-TM-SYM2-MEASURE-1-DEFINITION-CHECKER.py
sha256:    f0499d1cd42ae199edff048277841bcea3074e28c9b73c8c12895b731d35100c
bytes:     69159
language:  Python standard library only; exact arithmetic only (int,
           Fraction, exact Q(sqrt5) pairs, exact cyclotomic 5-tuples);
           no floats in any assertion
modes:     --definition   structural, forcing, and spectral certificates;
                          emits no classification, no gauge order, no
                          orbit data, no B5 record
           --evaluate     the complete B1-B7 evaluation surface including
                          the B5 tagged branch record, all-representative
                          GAUGE-COHERENCE and N3 records, and the
                          deterministic routing; pinned probe use only.
                          On a NONCANONICAL classification the record
                          carries the exact orbit invariants required by
                          the frozen contract: the complete gauge
                          partition, every orbit with its size, minimal
                          representative, and full membership.
run:       definition mode, from the repository root, environment
           LC_ALL=C LANG=C PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 TZ=UTC
stdout:    sha256 0c2062adc23de902b1106dcaafcb37296d40b0c9244039a9489437c8d7a1dd8a
           7834 bytes, 63 LF lines and zero CR bytes, 60 certificates,
           all PASS, exit 0, empty stderr (Microsoft Windows NT
           10.0.26200.0, x64, Python 3.12.10; engineering readout)
evaluate:  NEVER EXECUTED in this fold or its sessions, by construction of
           the fold's result neutrality
```

Certificate coverage against the frozen schema:

```text
B1  CERT-B1a-d      language, free N, three orbits, conditional typing
                    theorem over the complete function space
B2  CERT-B2a-f      child identities, carrier closure, quotient descent,
                    COMPLEMENT-BALANCE, QUOTIENT-STREAM-COMPATIBILITY
B3  CERT-B3a-e      Fourier map, verb symbol, Plancherel, registered face
                    weights, principal-order positivity
B4  CERT-B4a-c      normalization, BORN-STREAM-COMPATIBILITY, guarded
                    SCALAR-LINK
B5  evaluate mode   tagged branch record with all-representative records;
                    no expected class count, orbit count, invariant list,
                    or weight vector is embedded anywhere in the file
B6  CERT-B6a-d      finite census-index identity, event fiber, registered
                    pair-law pushforward, GYRON-PUSHFORWARD
B7  CERT-B7a-f      gates, complete nine-parent dependency graph with 291
                    edges, phaseful dictionary edge, acyclicity, absent
                    Log edge
S1-S4 CERT-S1,S2a,S2b,S3a,S3b,S4   exact characteristic polynomial and its
                    integer factorization, minimal polynomial on the
                    sum-zero subspace with strict subdominance rho < 2 and
                    the exact growth pair (C_J, m), stationary uniqueness
                    by exact rank, contiguous recursion audit
F1-F3 CERT-F1a-h, CERT-F2i-iii, CERT-F3a-g   the three conditioned forcing
                    rulings, including the two load-bearing-root witnesses
                    CERT-F1h and CERT-F3g from the independent break round
I2  CERT-I2a-c      sigma_line consistency clauses
I3  CERT-I3a-b      exact projective-frame and full-rank realization-system
                    completeness method; I3c-d remain evaluate-only
```

The checker honors the result-neutrality requirement of the selector-class
input: audit integrity and the scientific route are separated, computed
values route decisions, and no legitimate scientific outcome is a checker
failure. The future probe's `verify.py` embeds or wraps this checker in
evaluate mode; that file receives its own immutable preregistration pin.

BREAK ROUND 1 (builder's own attempt, incubation lane, files not public):
seven attacks by an independent code path (literal-substitution drive with
no popcount, Q(phi) pairs with no sqrt5 pairs, reduced degree-3
cyclotomics, explicit integer matrices; extended factor range k <= 12,
rational coefficient grid over all supports and gaps, independent sum-zero
basis, recursion range N <= 20000). Zero kills; the extended factor census
(2, 4, 6, 10, 12, 16, 20, 22, 24, 28, 32, 36) confirms k = 3 unique in
the extended range. Pins: breaker sha256
aad18050dc26a53ea7f8ad5019e1ff0b74916ba5a7354eba77e8baa1ccfc1eca, stdout
sha256 6973a14df197f781435d6f9ddf6b5af3244426953447d5fd02fd5ced25ac9558,
713 bytes, exit 0, empty stderr.

BREAK ROUND 2 (independent owner-lane breaker, 2026-07-22, report held in
the owner lane; the decisive round). Reproduced the Rev 1 definition run
byte-for-byte (same 56 PASS, exit 0, empty stderr), then killed two
overreaches and found one record defect:

```text
KILL 1 (accepted)   the carrier forcing holds only under the bijective
                    transport clause; an exact length-4 equivariant
                    surjection exists without it. Disposition: the clause
                    is now a named owner adoption, and the witness is a
                    permanent certificate (CERT-F1h).
KILL 2 (accepted)   the face weights alone do not force the coefficient
                    representative; the exact full-support witness
                    (-4, -4, 1, 1, -4)/5 has the registered spectrum.
                    Disposition: the phaseful dependency, conditional on
                    the explicitly supplied magnetic axiom pair of
                    ABELIAN-FACE-DICTIONARY, is now a named owner adoption
                    with a direct dependency edge, and the witness is a
                    permanent certificate (CERT-F3g). A weights-only
                    uniqueness claim is archived as dead.
CONFIRMED           the signed-coordinate uniqueness of sigma_line (the
                    group of order 12, exactly one invariant pairing).
                    The breaker's stronger no-pairing statement about the
                    full projective stabilizer stays in the owner lane:
                    that object is an evaluation output and this fold does
                    not import it.
DEFECT (fixed)      the Rev 1 evaluate mode emitted an orbit count without
                    the exact orbit invariants the frozen contract
                    demands on NONCANONICAL. The record now carries the
                    complete partition certificate. Evaluate mode remains
                    never executed in this fold.
STALE BASE (fixed)  the Rev 1 package was built on a superseded main; the
                    Rev 3 package is rebuilt on the current head named in
                    the authority pin.
```

## 7. S_TM is complete

Every field of the predefinition tuple, with its freezing source:

```text
C_sel, Dom_sel, Eq_sel        class input 2, 3 (W3; Dom(Sel) = {n >= 1};
                              literal equality); W3 forced by ruling F1
                              within the owner-adopted fixed-length
                              contiguous F_k family
update_convention             class input 3 (registered selection-law
                              indexing; centered q window separately
                              owner-adopted, not derived by F1)
starts                        class input 3 (all n >= 1; none discarded)
seed_semantics                class input 3 (drive reads the counter;
                              psi-independence by construction)
gauge_quotient                class input 5 (G = Aut(Lines, sigma_line),
                              postcomposition; order an evaluation output)
sign_quotient                 class input 5 (window-side N is a structure
                              map, not a gauge; projective signs absorbed
                              by the zero-pattern invariance, CERT-I2)
Lines, line_labels            class input 4 (registered order and
                              coordinates); coordinate-axis symmetry class
                              owner-adopted and pairing forced within it by
                              ruling F2
Sel                           class input 3 (Sel(n, psi) = s(q(n)))
selector_class                class input 5 (equivariant surjections;
                              frozen, not narrowed by this fold)
selector_equivalence          class input 5 (s ~ g o s, g in G)
canonicality_test             class input 6 (CANONICAL, NONCANONICAL,
                              EMPTY, STOP; all live)
Measure, convergence,         class input 7 (nu_N; Lemma 1 and Lemma 2
normalization                 all-N proof; certificates S1-S4; unique
                              normalized stationary law)
completeness_method           complete-function-space enumeration (B1d
                              method); exhaustive signed-matrix and
                              projective-realizability scans (methods
                              frozen; gauge executed at evaluation);
                              de-substitution closure (F1); S1-S4
C_Born, Eq_Born               Born input 4 (C_pos, both codomains, star,
                              principal order)
Born_map                      Born input 4 (Born_pos, Born_spec,
                              Born_supp); basis forced by ruling F3 under
                              the conditional phaseful dependency from
                              ABELIAN-FACE-DICTIONARY
half_map                      Born input 4 (half_O); pinned by CERT-F3f
third_map                     Born input 3 (third_Q = third_f, the
                              quotient stationary law)
layer_endpoints, layer_gates  Born input 7, 8; adopted in section 4 above
dependencies                  Born input 9; adopted in section 5 above
order, decision_algorithm     Born input 10 (fixed evaluation order) with
                              class input 9 (I1-I5, N1-N3); unchanged
certificate_schema            B1-B7 plus S1-S4, I1-I5, F1-F3, I2
certificate_checker           the pinned file of section 6
```

Acceptance test of predefinition section 5, item by item: (1) no
`UNRESOLVED` slot remains; (2) the selector reads the declared autonomous
dynamics through the counter, not the checkpoint alone; (3) class,
equality, and canonicality test were frozen in the merged inputs before
any output and are not modified here; (4) conventions, starts, seeds,
gauge, sign, and labels are explicit; (5) the limiting measure has a
complete all-N convergence proof with exact certificates; (6) the Born
carrier and the meanings of 1/2 and 1/3 are typed and are forced only
within the precisely conditioned owner-adopted roots of rulings F1-F3;
(7) the density bridge is a commuting map-and-event square,
not a numerical equality; (8) every lift is gated by a named gate and the
dependency graph is complete and acyclic with nine direct parents,
including the load-bearing conditional phaseful dictionary edge; (9) the
certificate schema and
checker embed no result-dependent expectation; (10) positive, negative,
and STOP routes all remain available. The lane state may therefore move
from `STOP` to `READY` in the v16 fold.

## 8. What this fold does not do

No science is decided. The canonicality classification, the gauge group
and its order, the orbit structure, gauge coherence, and the line weights
remain unread evaluation outputs; the checker's evaluate mode was never
executed in this fold. `Sel_class` is not narrowed, no threshold moves, no
clause is rewritten, and a future `NEGATIVE` on any frozen route is
first-class and will be folded, not repaired. The rulings F1-F3 are
conditioned canonicity results about the definition surface, adopted
before any evaluation output exists in the public line, exactly so that
whatever the probe returns will bind: with the roots named and their
load-bearing status machine-witnessed, no it-was-just-one-ansatz escape
survives in either direction. No value computed in an incubation lane
is imported into any public claim.

## Appendix A. The exact Public Canon v16 fold delta (frozen text)

The following delta is authorized by this fold and applied only in the
separate sealed release branch per POLICY sections 5 to 7 (two frozen
commits: the content fold, then the release form). It changes no status
and no claim or status counts. The Canon witness below is fixed against the
exact v15 input surface named in the authority pin; any mismatch invalidates
the fold and requires a rebuild from that input surface.

```text
A1  canon/REGISTRY.tsv, row TM-SYM2-MEASURE, falsifier field only:
    replace the leading clause
      "STOP until the selector carrier, pre-update versus post-update
       convention, seed versus ensemble, gauge and sign quotients, line
       labels, limiting measure, and meanings of 1/2 and 1/3 are frozen;"
    with
      "the definition tuple S_TM is frozen by the v16 owner-definition
       fold;"
    the remainder of the field is unchanged. Scope, status, evidence
    unchanged (so no HISTORY.tsv event is required; the scope hash chain
    is untouched).
A2  canon/FRONTIER_PROGRAMS.tsv, row TM-SYM2-MEASURE:
    work_state STOP -> READY. All other fields unchanged.
A3  canon/GATES.tsv: append the two rows of section 4, exactly:
      GATE-L1-L5-TM-SYM2-SELECTOR-STREAM<TAB>TM-SYM2-MEASURE<TAB>L1<TAB>L5<TAB>OPEN_LIFT<TAB>emits the exact typed gauge-orbit record R_[s]^5, never a representative, only when the frozen carrier/class audit and canonicality classification are complete; EMPTY or NONCANONICAL routes scientific NEGATIVE, incompleteness or inexactness routes STOP
      GATE-L5-L6-TM-SYM2-BORN-MEASURE<TAB>TM-SYM2-MEASURE<TAB>L5<TAB>L6<TAB>OPEN_LIFT<TAB>closes this candidate block only when the Born carrier, coefficient and spectral readings, support pushforwards, structural compatibility implications, gauge-coherence record, original N3 record, GYRON bridge, normalization, and exact checker are complete; an exact scientific N3 or gauge disagreement routes NEGATIVE, while missing, incomplete, or inexact typing or proof routes STOP
A4  canon/NORMATIVE.tsv, row TM-SYM2-MEASURE, gate_ids field:
      GATE-L1-L5-TM-SYM2-SELECTOR-STREAM;GATE-L5-L6-TM-SYM2-BORN-MEASURE
A5  canon/DEPENDENCIES.tsv: append the four rows, exactly:
      TM-SYM2-MEASURE<TAB>DEF-ACTION-LAYERS<TAB>REQUIRES<TAB>the lane's layer endpoints and both lane gates use the registered six-layer protocol
      TM-SYM2-MEASURE<TAB>RAMIFIED-TM-LIFT<TAB>REQUIRES<TAB>the window carrier reads the registered drive cut and its sign-quotient realization theta_n = s_2(n) mod 2
      TM-SYM2-MEASURE<TAB>SUBSTRATE-KNIT<TAB>REQUIRES<TAB>the coefficient-side Born carrier rests on the registered position/Fourier overlap and Plancherel mass statement
      TM-SYM2-MEASURE<TAB>ABELIAN-FACE-DICTIONARY<TAB>REQUIRES<TAB>literal Fourier inversion of the phaseful verb Psi(k) = 1 + zeta_5^k is conditional on the dictionary's explicitly supplied magnetic axiom pair, for which no uniqueness or selection theorem is claimed
A6  canon/CANON.md, section 18, the MEASURE block line:
      "TM-SYM2-MEASURE            the Thue-Morse selection and physical measure;"
      "                           residual Born phase halving 1/6 = (1/2)(1/3)"
    gains the trailing clause "; S_TM definition frozen (v16), scheduler
    READY". In the same content commit the release identity moves from v15
    to v16: the CANON.md title line and its five in-text version
    references, and the CORE.md release-identity line and its one in-text
    version reference.
A7  canon/CHANGELOG.md: the generated CURRENT COUNTS markers of the v15
    entry are renamed to CANON15 COUNTS, and this new v16 entry with fresh
    CURRENT COUNTS markers is inserted above it, verbatim:
      "Public Canon v16 is the owner-definition fold of the TM-SYM2-MEASURE
       lane (issue #119). The definition tuple S_TM of the v15 predefinition
       is complete: the selector-class and Born-bridge inputs are adopted;
       the window carrier, the line pairing, and the coefficient
       representative are discharged as canonicity theorems relative to the
       owner-adopted fixed-length contiguous bijective transport principle,
       the owner-adopted coordinate-axis signed-permutation class, and the
       registered phaseful verb conditional on the explicitly supplied
       magnetic axiom pair of ABELIAN-FACE-DICTIONARY, for which no
       uniqueness or selection theorem is claimed (rulings F1-F3 of
       notes/canon/P-TM-SYM2-MEASURE-1-OWNER-DEFINITION.md, with the pinned
       exact checker green on 60 definition-mode certificates). The two
       lane gates GATE-L1-L5-TM-SYM2-SELECTOR-STREAM and
       GATE-L5-L6-TM-SYM2-BORN-MEASURE are registered with four dependency
       edges (DEF-ACTION-LAYERS, RAMIFIED-TM-LIFT, SUBSTRATE-KNIT,
       ABELIAN-FACE-DICTIONARY), completing the nine-parent graph, and an
       explicit no-edge ruling for DEF-LOG-STREAM; the scheduler state moves
       from STOP to READY. No claim enters, leaves, or changes status; the
       registry counts are unchanged. No probe is authorized: the formal
       probe P-TM-SYM2-MEASURE-1 still requires its own immutable
       preregistration pin, and POSITIVE, NEGATIVE, and STOP all remain
       live routes."
A8  regenerate canon/FRONTIER.md and the counts tables with
    tools/generate_canon_views.py --apply; regenerate canon/SHA256SUMS
    over the five normative files. With the delta applied on the v15
    content base, the resulting canon/CANON.md is
    sha256 836b8d642f5209d46a5b833a3a1e7a1acc14a249e83066af1adb21845242d4a9,
    89364 bytes; a different base invalidates this witness and the fold
    must be rebuilt, not patched.
A9  release form (second frozen commit): STATUS.md to Public Canon v16
    with tag canon-v16, the content-fold commit SHA as CONTENT_COMMIT, and
    the new CANON_SHA256 and byte count; README.md release-version text
    with v15 added to the immutable predecessor list and the successor
    sentence moved to v17 and v18; CITATION.cff version 16. The release
    branch carries exactly these two frozen commits. Tag the merge
    canon-v16 only after readback; release assets per POLICY section 7.
    The full ceremony was validated in the preparation lane: policy,
    canon, ledger, and unit checks green, every reproduction replayed, and
    check_activation --full --post-activation green on a simulated merge
    and tag (engineering witness, not authority).
```

## Appendix B. Closure of issue #119

Closing comment (posted by the owner together with the merge of this
fold):

```text
Definition debt resolved by the owner-definition fold
(notes/canon/P-TM-SYM2-MEASURE-1-OWNER-DEFINITION.md, Rev 3): the three
load-bearing choices are discharged as precisely conditioned canonicity
results (F1 carrier forcing only within the owner-adopted fixed-length
contiguous bijective F_k family, with centered q separately owner-adopted;
F2 pairing forcing under the owner-adopted coordinate-axis
signed-permutation class with the metric no-go; F3 coefficient-basis
forcing by literal Fourier inversion of the registered phaseful verb,
conditional on the explicitly supplied magnetic axiom pair of
ABELIAN-FACE-DICTIONARY and claiming no uniqueness or selection for that
input), each with a machine-archived witness that its adopted root is
load-bearing (CERT-F1h, CERT-F3g, both from the independent break round of
2026-07-22, whose two kills are accepted and folded first-class). Both
lane gates and the nine-parent dependency graph are adopted; the B1-B7
certificate schema has a pinned exact checker (f0499d1c..., definition
mode 60/60 PASS, evaluate mode never executed, NONCANONICAL record
completed to the frozen contract and CANONICAL all-representative records
with exact negative witnesses); S_TM is complete with no UNRESOLVED slot.
The scheduler flip STOP -> READY and the table edits ride the sealed
Public Canon v16 fold per Appendix A. The formal probe
P-TM-SYM2-MEASURE-1 remains forbidden until its own immutable PREREG pin;
POSITIVE, NEGATIVE, and STOP all remain live. Closing.
```

## Appendix C. Firewall

This file and the checker are the only public artifacts of this fold. The
incubation-lane candidate records, run records, and break scripts stay in
the incubation lane; their hashes above are provenance pins, not evidence.
No public status is claimed by this fold; `TM-SYM2-MEASURE` remains `[H]`
with its registered falsifier, and nothing in this fold weakens it.
