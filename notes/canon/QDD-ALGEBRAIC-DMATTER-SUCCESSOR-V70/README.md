# QDD algebraic `D_matter` successor manifest for v70 (NON-CANONICAL)

```text
STATUS:                  OWNER-CHOICE CONFORMANCE MANIFEST / NOT CANON
PUBLIC BASE:             6e3d54576d9348eadac7fedfcc87adbe6c3e3811
PUBLIC CANON:            Public Canon v69 / canon-v69
CLAIM ISSUE:             #107
OWNER-CHOICE PR:         #648
FORMAL RUN:              NONE
PROBE / PREREGISTRATION: NONE
SCIENTIFIC RESULT:       NONE
V69 NORMATIVE CHANGE:    NONE
NEXT TRANSACTION:        SEPARATE PUBLIC CANON v70 RELEASE
```

## Purpose

This four-file package freezes the accounting and conformance contract for a
later Public Canon v70 transaction. It does not itself define, register,
retire, close, falsify, or promote a public claim. Its checker reads the exact
public baseline from the local Git object database with
`git show 6e3d545...:<path>`, so the audit remains stable after a later release
changes the live Canon.

Conformance here is governance and dictionary conformance, not a scientific
result. The owner selected `ALGEBRAIC-DMATTER`; no theorem is said to force,
uniquely select, or canonize that architecture. `PHYSICAL-DMATTER` remains an
unadopted route label, not a public claim, and is neither falsified nor shown
complete.

This is a scoped `DICTIONARY / D / L1` transaction contract, not a submitted
whole-decoder completion candidate. It claims no full completion-contract
conformance and submits no full carrier, stage, leg, bridge, physics, measure,
closure, or obligation candidate outside the exact `K_QDD` / `D_quadratic`
binding below; no cross-leg or state reconstruction is inherited.

## Frozen lifecycle

The later atomic content fold has three distinct accounting effects:

| public item | v69 | v70 operation | target state |
|---|---|---|---|
| `QUADRATIC-DECODER-DATA` | `OBLIGATION / O / MULTI`, `STOP` | retire as a split | historical `RETIRED`; no fired falsifier and no positive closure |
| `ALGEBRAIC-DMATTER` | absent | declare a new owner-selected dictionary | `DICTIONARY / D / L1`, ceiling `D`, no gate and no frontier program |
| `QDD-INSTRUMENT-APPARATUS` | `OBLIGATION / O / MULTI`, `STOP` | strengthen scope and take physical debt | remains `O / STOP`; debt is `TRANSFERRED_NOT_SATISFIED` |

The old composite scope and evidence tuple are retained in its terminal history
event. The old v47 manifests remain byte-exact. The Route A and independent
Hermitian-pair evidence bundles remain unchanged. `P-DMATTER-TOTAL-1` is not
resumed, reused, or executed.

## Algebraic successor boundary

The only adopted binding is

```text
D_matter|_(K_QDD,D_quadratic)
    := D_QDD_direct : K_QDD -> MatterData_QDD.
```

Its codomain has exactly five L1 algebraic record fields:

```text
support_state
total_weight
branch_weights
density_state
normalized_weight_state
```

The successor schema uses the ordered algebraic
`projector_ids = (DEF-QDD-PROJECTOR-LOW, DEF-QDD-PROJECTOR-HIGH)` and
`branch_weight_pairing_id = DEF-QDD-BRANCH-WEIGHT-PAIRING`. It does not use
the legacy physical slots `effect_ids` or `born_pairing_id`. The independent
identity

```text
D_QDD_direct = F_QDD o Q_QDD o beta_QDD
```

continues to audit equality of the two algebraic expressions. It is not the
definition source and does not select the route.

Nothing in this package supplies a physical effect, instrument, apparatus
carrier or completeness class, ready phase, realized event, occurrence or
frequency law, sampling, randomness, independence, post-state content or
uniqueness, or L6 measure. It also creates no CM, Psi, primary-carrier, seam,
or writeback consequence. The corresponding machine-readable physical fields
occur only inside `apparatus_manifest` and remain `UNRESOLVED`; its two
resolved `projector_target_ids` are algebraic targets, not physical identifier
aliases.

## Exact ledger contract

The v70 content fold removes all 19 outgoing dependencies of the retired
composite, gives `ALGEBRAIC-DMATTER` the exact 19-edge algebraic dependency set
in `MANIFEST.json`, and adds only these two ownership edges:

```text
READING-SPLIT -> ALGEBRAIC-DMATTER
QDD-INSTRUMENT-APPARATUS -> DEF-DECODER-COMPLETION-CONTRACT
```

`MEASURE-BORN-VERB` is not inherited by the algebraic successor. The resulting
graph must remain acyclic and the direct-write closure must remain disjoint
from the factor-side objects.

The existing `reproduce/status-separation` verifier, expected output, and
README change atomically so the current-status witness checks retirement, the
new `D` dictionary, and the strengthened apparatus row. This is a conformance
update only: it changes no scientific evidence, adds no reproduction, and does
not modify the pinned QDD Route A or QPAIR bundles.

Exactly six lifecycle rows are planned: retirement of the old composite,
declaration of the new dictionary, and scope-only events for `READING-SPLIT`,
`QDD-ALGEBRAIC-FACTORIZATION`, `QPAIR-HERM-INTEGER-NONDESCENT`, and
`QDD-INSTRUMENT-APPARATUS`.

The exact target counts are:

```text
claims 342              T 219   D 44   C 33   H 2   O 27   F 17
live H/O 29             normative items 388   dependencies 632
evidence rows 342       history rows 871      gates 11
frontier rows 29        program IDs 7         CORE_SELECTION rows 30
reproductions 23        two-architecture evidence 257
```

## Deterministic check

From the repository root:

```sh
python3 notes/canon/QDD-ALGEBRAIC-DMATTER-SUCCESSOR-V70/verify.py
```

Exact stdout is committed as `EXPECTED.txt`. The checker uses only the Python
standard library and local Git. It validates canonical JSON, authority and
ancestry, immutable artifacts, the frozen v69 ledger, owner-choice epistemics,
the type wall, exact field and dependency sets, the direct-write firewall,
lifecycle sequences, simulated v70 counts, and the separate release contract.
It is deliberately non-formal and earns no evidence or status.

## Separate v70 release

This notes-only PR must merge and pass public readback before the release
branch is cut. The later `release/canon-v70` branch starts from that exact
public `main` and contains exactly two commits:

1. `C1`, the complete atomic content fold, which does not change `STATUS.md`,
   root `README.md`, `CITATION.cff`, or any byte of this frozen successor
   package;
2. `C2`, the release-form commit changing exactly those three files and naming
   the full C1 content commit.

The release is merged without squash or rebase. `canon-v70` targets the public
release-PR merge commit. Both architecture jobs and aggregate `check` must be
green, and the public merge tree must read back byte-identical to C2 before an
annotated tag is created. Its message names C1. Publication uses a draft with
exactly the successful tag job's `activation-manifest.json` and the tagged
`canon/SHA256SUMS`; both are downloaded and validated before publication. The
published release is complete only after its release-event readback succeeds.

Any partial adoption, physical-input leak, identifier type alias, route choice
presented as theorem, alternative erasure, debt erasure, cross-layer gate,
mutation of old manifests, non-atomic fold, or third release-branch commit is a
stop condition.
