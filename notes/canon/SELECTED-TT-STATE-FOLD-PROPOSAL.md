# Selected outgoing TT state: prospective Canon disposition

PUBLIC; NON-CANONICAL. Proposed intake of
P-TT-SELECTED-OUTGOING-STATE-1; lock #1097. Public Canon v90 remains
the authority. This frozen proposal anticipates no successful run or
public acceptance. Consult the probe's RESULT and ACCEPTANCE records
when available. It is not a release, registry edit or owner closure.

## 1. The actual advance

TT-SOURCE already selects an isolated rational outgoing tensor history h
from the ten-word native K1 law. It explicitly left its outgoing vector
state unspecified. ETH-TT-1 fills that missing construction with a full
joint law over every labelled site and counter:

    v=epsilon_+ sqrt(h_positive)+i epsilon_- sqrt(h_negative),

where the same two fair signs are used throughout a packet. The whole
law has 40 source/sign labels and 25 distinct histories. Its two-point,
pseudo-covariance and fourth-moment data follow from one common finite
atomic law, so there is no hidden temporal coupling or Gaussian closure.

No previous source or propagator changes. The h recurrence remains the
primary equation, including at zero field. The construction explicitly
does not derive that equation by varying the singular action S[h=v^2].

## 2. Candidate conditional theorem rows

Subject to complete proof review and accepted exact replay, propose:

**TT-SELECTED-OUTGOING-STATE [T], L1.** Under the unchanged isolated K1
source/propagator and the explicitly selected persistent sign-coherent
lift class with conditional reflection and halfturn invariance, the
uniform four-lift law is uniquely fixed for every active source. The
resulting 40-label law gives 25 histories, literal prefix consistency,
v^2=h, zero vector mean and pseudo-covariance, the complete displayed
unequal-time covariance and sign-parity moment rule, and the exact
rank-two tensor fourth-moment contraction. The rational transfer is
invertible on the mean-zero subspace for every emitted counter m>=2,
so the seven tensor and 25 vector slice supports persist. Uniqueness
is conditional within the declared class, not among all physical states.

**TT-SELECTED-COMPOSITE-POWER [T], L1.** Under the separately declared
finite intensity readout s=|v|^2=|h|, coefficient one, unitary Fourier
normalization, connected ensemble comparison and common scale, the
position kernels are rational and exactly computable for all m>=2;
their unscaled diagonal Fourier powers and ratios lie in Q(sqrt(5)).
Full off-diagonal Fourier kernels can lie in Q(zeta_5), and canonical
powers include the inherited factor 1/(2 lambda). Every intensity
denominator is strictly positive,
the zero-mode tensor power vanishes and every other tensor mode is
positive. At m=2 the table is exactly that in MODEL/PROOF, with total
connected powers 1/2 and 1/4. Counters 0,1 have undefined ratios.
This is R_TI on a finite carrier, not cosmological r_T.

Each candidate row needs its own exact public evidence, falsifier,
semantic type and L1 placement. The proof supplies the universal
quantifiers; the finite verifier is an audit. Acceptance earns no
physical cross-layer bridge by itself.

## 3. Selected dictionary, with its predictive cost

If the owner adopts ETH-TT-1 in a later fold, describe its use as a
selected L1 effective state dictionary, at most D. The declaration must
retain the inherited K1 amplitude/source/transfer/frame/onset choices
and name these new selections:

- CH-TT-ROOT-COHERENCE: one sign for every positive entry and one for
  every negative entry throughout the isolated history.
- CH-TT-DECK-LAW: conditional reflection/halfturn invariance and
  independence from the source word. This forces the four equal masses
  in the selected class, but supplies no physical randomizer.
- CH-TT-INTENSITY-COMPARISON: unit composite scalar coefficient and
  the same canonical scale, with all modes and connected means retained.

The state has no fitted continuous coefficient. Its discrete class,
law and comparison remain real choices and count as theoretical inputs.
Neither numerical proximity to a measurement nor a desired tensor ratio
motivated these choices. The potentially large derived finite ratios
must be printed unchanged. They carry no novel cosmological prediction.

The common-line O(2)-covariant family must not be described as an
isotropic ensemble or a globally single-valued equivariant root.
The selected ensemble is not spatially translation invariant, so full
off-diagonal Fourier covariance belongs to the state definition.

## 4. What remains under the existing owner

Retain TT-VECTOR-STATE-NORMALIZATION [O], its identifier, history and
FULL registered decision condition. Its positive clause asks for a
physical vector normalization yielding numerical r_T(k). The present
probe resolves the selected outgoing state, temporal moments and
tensor action convention, but the invariant intensity s is not yet
the physical scalar mode consumed by that owner.

The remaining target is concrete: specify and independently justify or
explicitly select the physical scalar carrier and its dynamics/state,
its relation to the tensor units, and the physical coordinate, epoch
and mode correspondence. If a later proposal adopts this composite as
that scalar, the adoption, domain and consequences must be separately
typed and tested; it cannot be supplied by changing R_TI's name.

Do not invent new open owners for the same debt. Conversely do not
record F: a failure of one selected lift or scalar comparison is not
the owner's universal negative condition over every admissible
normalization. The earlier TT-VECTOR-MOMENT-UNDERDETERMINATION theorem
and TT-SOURCE's physical limitations remain intact.

Three prospective gates are named in MODEL:
GATE-L1-L4-SELECTED-TT-STATE,
GATE-L4-L5-SELECTED-TT-READOUT,
GATE-L5-L6-SELECTED-TT-COMPARISON. None is passed here or added to the
registry. A future physical H claim also needs a declared domain,
error model and independent empirical consequence, not just a theorem
about finite sums.

## 5. Fold procedure and bounded completion

Any actual fold is separate: confirm current authority and collisions,
review accepted pin/records and final/main CI, bind exact theorem
scopes and dependencies, regenerate Canon core/frontier/hashes and
run the complete required Canon and architecture checks. Retain the
old IDs and evidence, and do not silently replace physical questions
with finite algebraic ones.

After a supported intake the selected program may mark **complete
outgoing TT state and moments** finished. Its next TT task is the
physical scalar comparison and coordinate/readout contract, not
another choice of fourth moments for this already specified state.
