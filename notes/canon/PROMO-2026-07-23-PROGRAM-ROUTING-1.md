# PROMO-2026-07-23-PROGRAM-ROUTING-1

**STATUS: NON-CANONICAL PROGRAM PROMOTION DOSSIER.** This note carries no
authority, changes no public status, adopts no equivalence, edits no Canon
ledger, and authorizes no probe or formal execution. It inventories the public
work completed on 2026-07-23, separates three independent promotion lanes, and
freezes the order and firewalls for later owner decisions and sealed folds.

## 0. Decision in one block

```text
dossier id             PROMO-2026-07-23-PROGRAM-ROUTING-1
public anchor          main 381f5b06ff5c7353396633f582f0adfd93332f0a
active authority       Public Canon v16, tag canon-v16
normative delta today  none after the v16 activation
public result lanes    C20 arithmetic; TM-SYM2 classification
reviewed proposal      Quant-Schwinger arithmetic, merged PR #140
recommended order      Quant -> C20 -> TM owner disposition
combined v17 fold      not authorized by this dossier
formal execution       none
```

The program moved substantially, but the movement has three different logical
types:

1. the Quant-Schwinger scalar is closed arithmetic with an open physical
   realization;
2. the C20 result is a theorem-grade L1 ring and carrier classification with
   no time or decoder reading;
3. the TM-SYM2 sequence is a chain of exact L5 classifications whose use as a
   normative equivalence still requires an owner decision.

They may be reviewed together as a daily program record. They must not be
silently combined into one scientific claim or one automatic Canon promotion.

## 1. Public authority and completed work

The currency tuple for this dossier is:

```text
STATE             ACTIVE
CANON             Public Canon v16
tag               canon-v16
activation        ffed1ff536972113cbc3d8f74830172206b3a489
content commit    a96f6c7a8ed63c2234977cb1c7a3432fd315bd7a
CANON SHA-256     836b8d642f5209d46a5b833a3a1e7a1acc14a249e83066af1adb21845242d4a9
CANON bytes       89364
current main      381f5b06ff5c7353396633f582f0adfd93332f0a
```

Public v16 contains 194 claims: `T98 D40 C22 H5 O20 F9`, with 210 normative
items, 291 dependency edges, 194 evidence rows, 671 history events, 9 gates,
and 8 frontier programs.

The complete public sequence for the local calendar day is:

| PR | public role | merge commit | disposition |
|---|---|---|---|
| #127 | TM-SYM2 owner definition, Rev 3 | `476f12dd386df28ec4b37bdb64ac3d63f7e94b82` | consumed by v16 |
| #128 | Public Canon v16 owner-definition fold | `ffed1ff536972113cbc3d8f74830172206b3a489` | active authority |
| #129 | C20 Teichmuller split protocol-repair result | `390c1d254b0c3939ff7fc9e6b900eac8bd1b877a` | public evidence, unregistered |
| #131 | TM-SYM2 frozen-selector N2 result | `284b3e28cba9daa80ec757592656e2ecbbfddfe0` | public evidence, parent unchanged |
| #133 | semilinear successor definition | `87c1a0a42a23ad68612cabcddd1c91fd784c9150` | result-neutral definition |
| #135 | semilinear twofold result | `d78f6a91a6bbaf97d16e4c189fc538d9c6ad838c` | public evidence, no gauge adoption |
| #137 | reversal-closure successor definition | `d8f91078760ed904ae748883dfe2b8fd98c97af9` | result-neutral definition |
| #139 | reversal-toggle closure result | `537b56ee1881021a942d46cb7b1216712f482c29` | public evidence, no gauge adoption |
| #140 | Quant-Schwinger arithmetic proposal | `381f5b06ff5c7353396633f582f0adfd93332f0a` | public notes-only review material |

PR #140, head `48ff7f13d3edb163178d36acb420c57d5d39a6a7`, is different in
kind. It merged as notes-only promotion material and is now part of `main`,
but it is not probe evidence and changes no authority.

No commit after the v16 activation changes `canon/`, `STATUS.md`, `README.md`,
or `CITATION.cff`. Therefore no result listed above has been registered merely
by being merged.

## 2. Promotion routing matrix

| lane | exact result now available | maximum proposed move | still blocked |
|---|---|---|---|
| Quant | `J Jbar / script-Q = 1/(2 pi)` from two registered T identities | new arithmetic T row | substrate coupling, first-order electron coefficient, action, normalization, regularization, and L5-to-L6 realization |
| C20 | exact local-ring, cycle, split, root, depth, and reduced-operator theorem | new L1 T row after a dedicated promo note | every time, clock, decoder, physical-carrier, uniqueness, and L2-to-L6 reading |
| TM-SYM2 | four projective-linear orbits, two semilinear orbits, and one reversal-extended orbit | closed classification facts may become scoped T rows after owner mapping | normative gauge adoption, treatment of the fired N2 threshold, quotient-versus-datum choice, Born halving, and physical measure |

The existing open owners remain authoritative until later folds:

```text
QUANT-SUBSTRATE [O]   unchanged
TM-SYM2-MEASURE [H]   unchanged pending an owner-disposition fold
TIME-QUANTUM-TOWER [C] unchanged
```

## 3. Lane A: Quant-Schwinger arithmetic

The complete proposed Canon patch for this lane is intentionally kept in the
separate sibling document:

```text
notes/canon/PROMO-C-QUANT-SUBSTRATE-SCHWINGER-1.md
merged PR #140 at 381f5b06ff5c7353396633f582f0adfd93332f0a
```

Its narrow proposal is:

```text
QUANT-SCHWINGER-TARGET [T]:
J Jbar / script-Q = 1/(2 pi).

QUANT-SUBSTRATE [O]:
physical production of this scalar as
[alpha^1]((g_e(alpha)-2)/2) remains open.
```

The theorem uses only:

```text
J-MODULUS-CHORD [T]: J Jbar = phi^-2
BRIDGE-DEFECT [T]:   script-Q phi^2 = 2 pi
```

This dossier does not duplicate the exact registry, normative, dependency,
evidence, history, count, or release templates in PR #140. Its merge published
proposal material only. The recommended first normative move is a later
independent Quant fold consuming the byte-reviewed proposal.

No `T-LOCK`, physical child split, named physical gate, substrate coupling,
measured prediction, or promotion of `QUANT-SUBSTRATE` is part of this lane.

## 4. Lane B: C20 Teichmuller split

### 4.1 Earned exact result

`P-C20-TEICHMULLER-SPLIT-2` is public, proof-first, and passes the
two-architecture computation gate. At its frozen L1 scope it proves:

1. for `O = Z[zeta_5]`, `lambda = 1-zeta_5`, and
   `A_m = O/(lambda^m)`, the ring `A_m` is local with residue field `F_5`
   and `|A_m| = 5^m`;
2. in `R = A_4 = O/(5)`, `J = 1+zeta_5^2` has exact order 20;
3. with `t = J^5` and `u = J^16`, one has
   `<J> = <t> x <u> ~= C_4 x C_5`;
4. `mu_8(R) = mu_4(R) = F_5^*`, hence `R` has no element of order 8;
5. for every `m >= 1`, the two-primary subgroup of `A_m^*` is `C_4`, so
   no `A_m` contains an element of order 8;
6. the independently reconstructed reduction of the public `J-STEP [T]`
   operator agrees with multiplication by `J` and satisfies the frozen exact
   matrix identities.

The all-`m` clause is theorem-grade. The finite scan audits the proof; it does
not extrapolate from a finite prefix.

### 4.2 Candidate promotion boundary

The working future claim label is:

```text
C20-TEICHMULLER-SPLIT [T], L1 arithmetic only
evidence: probes/P-C20-TEICHMULLER-SPLIT-2
direct claim premise: J-STEP [T]
```

This is a routing label, not yet a registry row. A dedicated C20 promo note
must freeze the final claim id, exact registry scope, Canon section, dependency
edges, evidence bundle hash, history event, count delta, reproduction impact,
and release ceremony before any fold.

The protocol-invalid `P-C20-TEICHMULLER-SPLIT-1` predecessor is not evidence.
Only the repaired `-2` branch may support the future row.

### 4.3 C20 firewall

This result does not establish:

- the all-`k` order of `M_J` modulo `5^k`;
- a time quantum, tick, clock, chronology, phase, or metrology scale;
- a decoder or physical carrier;
- a unique interpretation of the `C_4 x C_5` split;
- any claim about carriers outside the declared lambda-adic tower;
- any lift beyond L1.

In particular, `TIME-QUANTUM-TOWER [C]` remains exactly at its current
finite-depth scope. The untracked pre-freeze `C-TIME-QUANTUM-TOWER-ALL-K-1`
material is outside this dossier, is not evidence, and must not be staged into
either promo PR.

## 5. Lane C: TM-SYM2 classification and reversal

### 5.1 Three exact public classifications

The public sequence establishes three distinct facts.

First, under the frozen projective-linear gauge:

```text
selector class                 48
gauge order                    12
action                         free
orbit decomposition            12 + 12 + 12 + 12
route                          NEGATIVE / N2
```

Second, for the candidate semilinear image:

```text
Gamma_sl order                 24
exponent-one coset             12 elements, character (1,1)
residual invariant             chi_Q chi_F
orbit decomposition            24 + 24
route                          SEMILINEAR-DOUBLE
```

Third, after adding drive-word reversal as precomposition:

```text
t_N / t_R / t_NR               (0,1) / (1,0) / (1,1)
N / R transport                NONREALIZABLE / NONREALIZABLE
NR transport                   REALIZABLE-E1
reversal action                toggles chi_Q chi_F
extended orbit decomposition   48
route                          REVERSAL-TOGGLE
```

All three are exact two-architecture classifications. The following are
working theorem labels for a future owner note, not registry rows:

```text
TM-SYM2-PROJECTIVE-FOURFOLD
TM-SYM2-SEMILINEAR-TWOFOLD
TM-SYM2-REVERSAL-CLOSURE
```

The closed facts can be proposed at T with scopes conditional on their exact
frozen objects and actions. A theorem row saying what an action does is not the
same as a normative decision to use that action as physical gauge.

### 5.2 What is not decided

The result records explicitly defer:

- adoption of `Gamma_sl` as the public gauge;
- adoption of `R`, `N`, or `NR` as gauge;
- quotienting the reversal bit rather than retaining it as a physical datum;
- rewriting the frozen `S_TM` projective-linear threshold;
- a unique microscopic selector;
- Born halving or a physical probability;
- the L5-to-L6 measure lift.

The exact N2 failure of the frozen selector must be preserved. Later
semilinear and reversal classifications do not retroactively turn the original
projective-linear route positive.

The parallel incubation candidate `C-TM-SYM2-TWOFOLD-NOGO-1` is provenance,
not public evidence. Its nullity and `Q(zeta_5)` robustness statements are
outside the public result and may not enter a fold through this dossier.

### 5.3 Required owner disposition

Before any TM Canon status or scope edit, a separate notes-only owner document
must freeze:

1. which of the projective, semilinear, and reversal-extended relations are
   mathematical comparison actions and which, if any, are adopted
   equivalences;
2. whether reading orientation is quotiented or retained as typed physical
   data;
3. how the fired N2 threshold is recorded without moving or repairing it;
4. whether the existing compound `TM-SYM2-MEASURE` row is preserved, scoped,
   retired, or accompanied by a new successor owner;
5. stable claim ids and statuses for the three closed classifications;
6. exact dependency directions, avoiding a cycle through the existing owner;
7. the fate of the L1-to-L5 selector gate;
8. the still-open L5-to-L6 Born and measure gate.

Until that document is reviewed and merged, `TM-SYM2-MEASURE [H]`, its two
registered gates, and its scheduler row remain unchanged.

## 6. Public evidence pins

| lane | pin | two-architecture evidence head | verifier SHA-256 | stdout SHA-256 |
|---|---|---|---|---|
| C20 | `338a90c6b82a8934a0f7f28090dee51bce6c791c` | `df394f370de4eab91636652dd56a9ded018668b5` | `5ba3680f2cca840ab458e72e8a2f0febb99c732fcacaeadd68c71c020baacd41` | `4cbf934b2220c5a5c3de8169a448baac1396236ebc37c286536c3886bb8d86e8` |
| TM projective | `69ff42438154bc165a74d03e914988efda9bccf9` | `826e6e1dd94daa3b683d34876cf9e54d6fb1e59e` | `30b581a2b84e0ac8b3b333e5731f7ecdcfa9bfb975585da0603f02ab3e8ed6eb` | `395209f15d0943f38b1d8af4f6d20769c5e113c65bac9455944613ab3b40726f` |
| TM semilinear | `3d3521fe1ed557cc0b2c271baea9a67787aa7951` | `c3cffac4f6e297ef977295bc60517f062911d2b7` | `f526af796e0fd2951d5b136b17f786045a932214744040dfd0b86e47c50b3590` | `47e04141e0ee0b290a4ca91b1b0b977ad2674d555ab296aaa371177f662bfd19` |
| TM reversal | `00235474b2b826e3f2ddd08bcfde1f42a2a3e6b1` | `36b7ca9d337ce638030a6cf5cf11c9fa1463bd39` | `03f5cf7e49b5a03ccb9ee2a0d7383737ab0f84d0d1da7c04a6c4dce481ba77ad` | `687174ea019ba5aa66ad119f581e227ac863e3dbc078312e3d6aa254dab2ea1a` |

Each row passed one clean Linux/aarch64 formal leg and a byte-identical GitHub
Linux/x86_64 replay. These pins are evidence for their exact scopes only.
They are not evidence for the Quant lane and they do not decide a normative
TM equivalence.

## 7. Recommended promotion queue

### Stage 1: publish review material

1. PR #140 is merged at `381f5b06ff5c7353396633f582f0adfd93332f0a`
   and its public note has been byte-read back;
2. review and merge this routing dossier;
3. create no tag and change no Canon status at this stage.

### Stage 2: Quant fold

Consume the exact templates from the reviewed Quant proposal in a separate
sealed content fold. Register only the arithmetic target and keep the physical
parent open.

### Stage 3: C20 fold

First publish a dedicated exact C20 promotion note. Only then prepare a
separate sealed fold for the scoped L1 theorem. Do not attach a time or decoder
interpretation.

### Stage 4: TM owner disposition and fold

Publish the owner-disposition document described in section 5.3. After review,
prepare a separate fold for the closed classification facts and the explicit
disposition of the frozen N2 result. The Born and physical-measure question
remains open unless independently closed by its registered gate.

If another Canon release lands first, all proposed version numbers, history
sequences, counts, hashes, and base commits must be recomputed from the new
public `main`.

## 8. Fold atomicity and release ceremony

The default is three independent scientific folds in this order:

```text
Quant arithmetic -> C20 arithmetic -> TM owner-mapped classification
```

A single theorem-harvest release is not forbidden in principle, but this
dossier does not authorize it. Such a fold would require an explicit owner
decision plus complete independent registry scopes, dependencies, evidence,
history, count deltas, and status-separation checks for every lane. It must
still keep `QUANT-SUBSTRATE [O]`, `TIME-QUANTUM-TOWER [C]`, and, absent the
owner disposition, `TM-SYM2-MEASURE [H]`.

Every future normative fold must:

1. start from then-current public `main`;
2. change no frozen probe bytes;
3. contain one immutable content commit;
4. run generated-view, policy, unit, Canon, ledger, verifier, reproduction,
   whitespace, and named-file security checks;
5. create the separate release-form commit changing only `STATUS.md`,
   `README.md`, and `CITATION.cff`;
6. run full activation on that release-form head, naming the immutable content
   commit;
7. merge by merge commit, create the next integer Canon tag, and complete
   public byte and release-asset readback.

No squash, rebase, force-push, threshold repair, or silent composition with an
unrelated scientific change is part of this route.

## 9. Scope firewall

This dossier does not:

- modify or activate Canon v17;
- register a claim or move a status;
- rerun any formal verifier;
- import old or quarantined probe output;
- promote `TIME-QUANTUM-TOWER`;
- interpret the C20 split as time or metrology;
- adopt `Gamma_sl`, reversal, or another TM action as gauge;
- erase the projective-linear N2 failure;
- close `TM-SYM2-MEASURE`;
- derive Born halving or a physical measure;
- close or split `QUANT-SUBSTRATE`;
- identify the arithmetic Schwinger target with a physical coefficient;
- use the incubation-only nullity or `Q(zeta_5)` no-go as evidence;
- authorize a combined release.

The public registry and the latest active integer-tagged Canon remain the sole
authority.

## 10. Validation and security

This proposal is markdown only. It contains no executable artifact, secret,
credential, private hostname, private path, machine nickname, personal datum,
binary, model file, or private log.

The notes-only PR for this dossier must change exactly this file and run the
ordinary repository policy, unit, Canon, ledger, and whitespace checks. Review
must confirm every cited commit and evidence hash against public `main`.
