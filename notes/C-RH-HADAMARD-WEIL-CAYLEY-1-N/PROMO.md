# PROMO-C-RH-HADAMARD-WEIL-CAYLEY-1-N

```text
STATUS: NON-CANONICAL promotion package only
PUBLIC PROMOTION REQUESTED NOW: no
ISSUE: #371
```

## Candidate theorem package

The following statements are suitable for later review as mathematics-only candidates if they remain useful:

1. **Cayley functional-pair theorem.** For `rho*=1-conjugate(rho)` and `xi=1-1/rho`, `xi*=1/conjugate(xi)` and `|xi|=1 iff Re(rho)=1/2`.
2. **Hadamard radial-defect theorem.** The normalized `H2` antisymmetric channel has exact energy

   ```text
   E = (1-2 beta)^2 /(2 |rho|^2 |1-rho|^2),
   ```

   and the positive channel matrix has eigenvalues `E+2,E`, so its rank drops exactly on the critical line.
3. **Radial power-collapse theorem.** `E_n=T_n(1+E_1)-1`; all power-level radial Hadamard data are generated by one scalar.
4. **Finite Li/Cayley no-linearization theorem.** `E` is strictly subharmonic, with an explicit everywhere-positive Laplacian certificate, so `E` cannot be represented by a finite real linear combination of holomorphic single-zero Cayley powers and conjugates.

## Negative route to preserve

The frozen finite-Li/source-side advantage is dead. Do not reopen it by increasing precision, taking a larger finite Li prefix, or adding another finite strict Toeplitz block. Issue #363 already blocks the latter grid-discrimination shortcut.

## Successor, only if separately locked

A meaningful successor would have to enlarge the source layer rather than the finite Li layer. The narrow question is whether an **unconditional two-zero/pair-correlation observable** can access a positive weighted sum of the radial defects without importing individual zero locations or RH.

That successor is not authorized by this package and needs its own collision search, lock, and preregistration.

## No movement

No RH, `LAMBDA-COCYCLE-ANGLES`, Canon, Registry, frontier, decoder, Born, physical, or SI status movement follows from this package.
