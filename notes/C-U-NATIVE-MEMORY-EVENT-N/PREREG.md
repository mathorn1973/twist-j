# C-U-NATIVE-MEMORY-EVENT-N: preregistration

Status: NON-CANONICAL incubation; no authority; L1 only.
Owner: ChatGPT-U-closure-20260906. Author: A. M. Thorn.
Claim lock: issue #861. Date: 2026-09-06.

## Basis and exposure

Public main 5de2e71f4000e02599919bf3dcb18c7671b67445; Public Canon v78;
content 767b136713ae12f5f30869642852dcb8a3f671b0; tag canon-v78.
CANON.md SHA256 82b29c75eb007c71d73dc33e63270d0e0895fdf41886499fcf79b928826855b5,
473631 bytes. Five normative hashes, ancestry, required two-architecture main
checks and repository policy, Canon, ledger and gate-contract checks passed.
The sealed native-readback PROOF.md is read and inherited, not independently
confirmed. Imported C-CENSUS-ERGODIC notes distinguish 313 coarse supports
from 625 oriented hull cells; their measure proofs are not assumed.
Unmerged U-INDUCED-2 and NULL-ANATOMY branches are not resumed.

The prior conversation drafted this work but saved no preregistration or code.
No new scientific computation in this incubation precedes this file. The
algebraic lead below is result-exposed analytical work, not a blind prediction.
This is a new incubation pin, not a formal public probe. Results remain
candidate-T, candidate-D or candidate-C until a separate public promotion.

## 1. Equation, carrier and reading classes

Use exactly the five generators and selector of the sealed native-readback
PROOF.md, with x=(p1,p4,p1p,p4p,q,r) in F5^6, theta_n=popcount(n) mod 2,
z=sum(x) mod 5, U(n,x)=(n+1,g_(z+2theta_n mod 5)(x)). No changes to U.

Let X14={x:z in {1,4}}. Set chi(1)=1 and chi(4)=-1. Define

    V(x)=(p1+p1p,p4+p4p,chi*(p1-p1p-2),chi*(p4-p4p-1)).

All arithmetic is in F5. The proposed protected record is the unordered sign
class {V,-V}; lexicographic least representative is an implementation only.
It is not the original balanced-piston QDD record and not a scaling quotient.

The fixed checkpoint reader class is ALL maps f:X14->A for a finite alphabet.
The fixed decorated reader class is ALL maps
f:X14 x {0,1}->{LOW,HIGH,SILENT}, evaluated at (x_n,theta_n).
No linearity restriction. SILENT allows fixed present-state acceptance.
Absolute time, longer clock windows, changing contexts, extra memory, a reset
or intervention, additional copies and changed dynamics are outside this class.
The class is mathematical, not claimed to be the complete physical class.

## 2. Exact clock construction

Use S(0)=0 and S(m)=m-S(floor(m/2)), from the inherited chart. Define

    c_m=(m mod 5,S(m) mod 5,theta_m,theta_(m+1)),
    T0(r,s,u,v)=(2r,2r-s,u,1-u),
    T1(r,s,u,v)=(2r+1,2r+1-s,1-u,v),
    c_0=(0,0,0,1).

Reduce the first two coordinates modulo five. Take the COMPLETE reachable
subset of the 100 nominal states. Determine SCCs, period, rational invariant
vector and a finite primitive/contraction certificate if one exists.
Stationarity alone is NOT a proof of frequencies along the fixed-origin word.
A written proof must transport exact block counts to arbitrary prefix lengths.

For a fixed chart label l=(alpha,beta,gamma,delta,epsilon), read odd and even
checkpoints from c_m by the inherited formulas:

    odd n=2m+1: t=1,h=(-1)^(1+u),N=s-1,z=4-3u,theta_n=1-u;
    even n=2m+2: t=-1,h=(-1)^(1+u),N=s-1,z=1+3u,theta_n=v;
    A=t*alpha,B=t*beta,C=2+h*gamma,D=1+h*delta,
    r=t*(epsilon+N), p1=3(A+C),p1p=3(A-C),
    p4=3(B+D),p4p=3(B-D),q=z-A-B-r.

These are chart functions, not new physical registers.

## 3. Frozen work packages

A. Exhaust 6250 stable checkpoints and both bits. Test V(next)=-V and record
invariance; classify fibres and full directed reachability. Predicted counts:
313 classes, 312 fibres of size 20 and one of size 10. Retain any disagreement.
Classify ALL fixed invariant readers, not just polynomial readers.

B. Build the complete clock automaton and verify its recursion against literal
S and the Thue-Morse word on 0<=m<=65535. Derive the limiting letter frequencies
by an all-length proof with integer certificates. The finite sample is audit,
not proof. Failure of an expected primitive certificate is retained.

C. Push the exact clock frequencies to ALL 3125 chart labels; record atom-weight
multisets and support counts without assuming them. Verify native/chart update
agreement on all labels at times 3 through 34, and on the first nine labels in
lexicographic order at 2^127 and 10^100+123.

D. Classify fixed-reader frequency sets by finite subset sums of checkpoint
atoms and by two-bin subset sums of decorated atoms. Every atom is allocated
whole to exactly one of LOW,HIGH,SILENT. Empty acceptance is UNDEFINED.
Only afterwards compare the attainable sets with all supported QDD head
weights. Known targets include 1/256 and 9/14. Distinguish unconditional,
Snap-only, Flow-only and accepted-event ratios. No target chooses a reader.

E. Where every checkpoint in a component has positive limiting frequency,
prove that any eventually constant fixed checkpoint record was constant on
that whole component. This excludes permanent blank-to-written transitions
AFTER synchronization in this class only, not general physical memory.

F. Give a target-independent encoding/readback of the protected record and
show what it does not preserve about the original QDD head. No preparation
is called an endogenous write. No independent system/apparatus preparation
is claimed without its own proof. Record a precise proof-level disposition
of the proposed protected-register route and the fixed-event-reader route.

## 4. Execution and failure rules

Use only integers and Fraction, zero tolerance. Commit and push this file
before the first new computation. Freeze the verifier by Git before its
recorded execution; compilation and static checks may precede that code pin.
Exploratory implementation defects may be corrected in notes, preserving the
scientific scope and documenting every correction. A mathematical falsifier
is retained, never repaired by changing its claim, class or threshold.

A rerun is reproduction. A structurally different same-session audit is NOT
blind independent confirmation. Local x86_64 alone is not a two-architecture
gate. No formal verifier is invoked outside repository procedures.

## 5. Deliverables and boundaries

Package proof, exact verifier, stdout, run record, SHA256SUMS and
PROMO-C-U-NATIVE-MEMORY-EVENT-N.md. No canon/ changes, no sealed probe edits.
No physical event is adopted and no L1-to-L5 or L6 lift is asserted. The
first-three-tick loss and three-state original-record repair of #860 remain.
Native static storage is not writable apparatus; rate realization is not
selection; a clock-word density is not an adopted physical measure. No O1/O2,
Bell, SI, full apparatus, decoder or complete physics closure is claimed.
Failure of this fixed-reader class is not SAMPLING IMPOSSIBLE for the complete
physical class. A subsequent larger class needs a separate new preregistration.
