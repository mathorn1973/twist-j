# Review record: public PRs #500 and #501

Date: 2026-08-21. Reviewer: independent session (Cowork), review only; not the probe owner.
Target line: public, mathorn1973/twist-j main.

## Currency gate

STATUS.md read from a fresh clone of public main: STATE ACTIVE, Public Canon v59,
tag canon-v59 (activation 7820173b), CONTENT_COMMIT 5da6b883, CANON_SHA256
7fdea700...87f641, CANON_BYTES 314310. Tag and content commit verified ancestors
of main. canon/SHA256SUMS 5 of 5 OK, re-verified after main advanced to 47fa9ddd
(merge PR #504) during the review. POLICY.md, AGENTS.md, CORE.md, FRONTIER.md read.

## PR #500, probe/P-J-BINARY-NORM-DESCENT-1 (issue 499)

Verdict: PASS, merge approved. Earned grade at merge: probe result recorded as
CONFIRMED; proof-first target row J-BINARY-NORM-DESCENT [T] is a later fold, not
this merge.

- Scope: exactly one new probe directory, 5 files, no canon/ edits. No collision:
  identifiers absent from probes/ and registry; CARRY-PENTAD named as collision
  guard, not re-proved.
- Pin discipline: 846f116b = PREREG.md + verify.py together, results commit
  bf149b3b adds only EXPECTED/RUN/RESULT; pinned files byte-identical across the
  two commits. Prereg sha256 a023d6c8..., verifier sha256 6f10ad20... match RUN.md.
- Exact arithmetic: integer and F2/F16 bit-polynomial only, stdlib only, no float
  anywhere, deterministic, no I/O.
- Independent rerun (this session, x86_64, CPython 3.11.15, LC_ALL=C etc.):
  exit 0, empty stderr, stdout 1604 bytes, sha256 bdeb9a20... byte-identical to
  EXPECTED.txt. Same architecture as the recorded local leg, so this is
  reproduction; the two-architecture gate is carried by the PR workflow.
- Public workflow (policy.yml, x86_64 + aarch64 + aggregate check) on the PR
  branch: Success (read from the public Actions page).
- CI-equivalent checks rerun locally on the PR tree: POLICY PASS, CANON PASS v59,
  LEDGER PASS, 102 unit tests OK, VERIFY PASS for the probe.
- Proof audit (second reading): Phi_5 irreducible mod 2 hence O/(2)=F16 and
  O_K+/(2)=F4; u reduces to Frobenius, c to Frobenius^2; motor D_J is translation
  k->k+2 vs Frobenius dilation k->2k, generating AGL_1(F5) of order 20; q_+ mod 2
  = Tr_(F4/F2) o N_(F16/F4); singular locus = mu_5 via norm-kernel count 5 and
  trace-kernel {0,1}; A4 bridge q_+(Px) = (5/2) sum x_r^2 with index-5 (odd)
  reduction isomorphism. All steps check out.
- Breaker attempt (independent code path: GF(16) built on X^4+X+1 with a root
  search for Phi_5, 5-coordinate convolution ring, all four embeddings, larger
  integral lifts): BREAKER EXHAUSTED, no discrepancy. Script and result retained
  in the review session.

## PR #501, probe/P-CARRY-QUADRATIC-SYMMETRY-1 (issue 316)

Verdict: PASS, merge approved. Earned grade at merge: probe result recorded as
CONFIRMED; proof-first target CARRY-QUADRATIC-SYMMETRY [T] is a later fold.

- Scope: exactly one new probe directory, 5 files, no canon/ edits. Predecessor
  #314/#315 invalidation and the 5=2^2+1 / ord_5(2)=4 collisions are declared;
  the only scientific dependency, CARRY-PENTAD [T], is still live in v59.
- Pin discipline: PREREG pinned 2346374f (2026-08-09), verify.py completed the
  prospective pin 47 s later in 6229acde, both pushed before any execution;
  pinned files unchanged through the result commit 2fc46205 (2026-08-21).
  Two-commit pin instead of a single commit is a cosmetic deviation from the
  PREREG's own "together" wording; POLICY's requirement (both pinned and pushed
  before first formal execution) is met.
- Stale-base note: PREREG cites Public Canon v39 (main 4d855835), accurate at pin
  time; content is self-contained L1 mathematics and its one dependency survives
  in v59, so the age does not invalidate the probe. Not blocking.
- Disclosed infrastructure incident: first post-pin invocation through a ChatGPT
  Python wrapper wrote 754 unrelated stderr bytes and was rejected as a formal
  leg; accepted leg used system CPython, empty stderr. Honest, first-class
  disclosure; no pinned byte changed.
- Independent rerun (x86_64): exit 0, empty stderr, stdout 535 bytes, sha256
  001d554f... byte-identical to EXPECTED.txt. Verifier sha256 b03ae613... matches
  RUN.md.
- Public workflow on the PR branch: Success.
- CI-equivalent checks on the PR tree (v39 base): POLICY PASS, CANON PASS v39,
  LEDGER PASS, unit tests OK, VERIFY PASS.
- Proof audit (second reading): S_n-transitivity on pairs forces the invariant
  pure quadratic space to {0, e_2}; C(w,2) mod 2 has least period 4 with block
  0,0,1,1; P_4 = {e_1..e_4, 1111}, |P_4| = 5, XOR-sum 0; full GL(4,2) enumeration
  gives Aut(q_4) of order 120 inducing all of Sym(5); order obstructions
  |GL(n,2)| < |P_n|! at n=5,6,7 exact and for n>=8 via C(n,4) >= n^2+2 with the
  C(n,3) >= 2n+1 induction step and m! >= 2^(m-1) > 2^(n^2) > |GL(n,2)|.
  All steps check out.
- Breaker attempt (independent code path: subset-expansion e_r, full-generator
  invariance solve, basis-image GL(4,2) enumeration with kernel check, weight-
  formula singular counts, symbolic inequality chain to n=39 and step check to
  n=199): BREAKER EXHAUSTED, no discrepancy.

## Merge state

Both merges were prepared locally as true merge commits (no squash, no rebase),
committer A. M. Thorn <thorn@twistj.com>, on top of 47fa9ddd (post-#504 main):

- 0476c1544ae885303f3b9dcd24814fece20e1114  Merge PR #500: J binary norm descent
- cae25c103123f292155e4bcd91f51994b8344c47  Merge PR #501: carry quadratic symmetry

The fully merged tree passes POLICY, CANON v59, LEDGER, all 102 unit tests, and
check_verifier for each merge against its own push base. Note for whoever pushes:
the two merges must land as two separate push or merge events; a single push
spanning both fails the workflow's one-probe-per-diff check (verified locally).

Push from the review session was refused: the session's git proxy does not carry
credentials for mathorn1973/twist-j (read-only clone worked; push 403). The
merge therefore needs either the GitHub UI merge button ("Create a merge
commit", never squash/rebase; #500 first, then #501) or a session with push
access. Review is complete; nothing else blocks the merges.
