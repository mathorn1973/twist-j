# PLAN: the photon sector after the kappa F and the point-group STOP

```text
STATUS   NON-CANONICAL working plan. No authority, no claim, no probe.
         Scheduling and scoping only; every open decision named here is
         the owner's.
BASIS    Public Canon v36 (photon frontier empty; PHOTON-WINDOW-PROOF and
         PHOTON-KAPPA-LEMMA at F). Candidate record
         notes/c-photon-point-group-1 (56674bb2, two-architecture,
         unaffected by the STOP). Sealed probe P-A3-FCC-POINT-GROUP-1
         (STOP, #275 NOT_PLANNED). Proposal of fresh identities
         claude/PROPOSAL-FRESH-IDENTITIES-POINT-GROUP_2026-08-05.md.
         Owner verdict of 2026-07-27 on the spatial-symbol lane: the
         successor C-PHOTON-TIME-CHARACTERISTIC-1 opens only after
         rulings R1 (canonical lift) and R3 (weights).
```

## 1. Where the sector stands, one paragraph

The photon's identity floor is the densest in the registry and just got
denser at candidate grade (point group full octahedral, icosahedral
non-lift, invariant dimension 2, shell anisotropies, the cone). The
window route is honestly dead at its kappa conjunct. The public frontier
carries no live photon row. Exactly one scientific unknown remains
between here and a finished photon: THE DERIVED NULL CHARACTERISTIC,
det C(omega, k) = 0, and the one input it consumes, the transfer data on
the carrier (weights, and per the registered cell dictionary also Z_5
edge phases). Everything else in the sector is either floor, F, or
bookkeeping.

## 2. The critical path runs through three owner keys

```text
K1  ruling R1 (canonical lift). Held since 2026-07-27. Without it the
    time-characteristic lane stays closed by the verdict's own order.
K2  ruling R3 (weights). Same verdict, same hold.
K3  the fresh-identity option for the public floor (proposal of
    2026-08-05: recommended chain P-FCC-AXES-FORCED-1,
    P-ICOSAHEDRAL-NONLIFT-1, P-A3-FCC-ISOMETRY-1,
    P-FCC-FOURTH-ORDER-CONE-1; or the narrow or zero-identity options).
```

These three keys have been the sector's actual blocker; no computation
substitutes for them. K1 and K2 gate the science, K3 gates only the
public floor and can wait without cost to understanding.

## 3. Session-executable now, no ruling needed

**Lane A, weights-and-phases recon (the R3 feeder).** Before anyone
freezes weights, understand what the machine actually deposits. Three
branches, each against registered material, output a recon doc, not a
probe:

```text
A1  phase-free shell weights. Frozen fact already in hand: the
    single-shell nearest-neighbor system fails fourth order (deficit
    -4); the cone -4 w1 + 32 w2 - 72 w3 = 0 admits exactly the frozen
    witnesses. Question: does ANY registered principle (step census of
    the five generators projected to the kernel, Born/quadratic
    amplitudes, uniform-per-orbit) select a point on the cone, or is
    the weight system a genuine moduli slot?
A2  Z_5-phased transfer. The registered cell dictionary carries edge
    phases with flux quantum zeta_5 and unimodular amplitudes. The cone
    analysis so far is phase-free; a phased symbol can have different
    isotropy behavior, and this is where the gauge/holonomy obligation
    of the isotropy-6 list enters. Compute the phased symbol on the
    carrier at fourth order and place it against the cone.
A3  verdict map. For each branch: FORCED (a selection principle lands
    on the cone with zero fit), NONUNIQUE (classified orbit, a moduli
    coordinate, honest), or EMPTY (nothing registered selects; the
    weights are owner input and R3 knows it). Every outcome feeds R3
    with facts instead of taste.
```

**Lane B, the closing condition of the photon program.** Write and
freeze, as a candidate formulation with a falsifier, the convergence
test that defines "the photon is finished":

```text
ONE CONE, TWO ROUTES. Route 1: L2 spatial symbol plus L5 time transfer
give det C(omega, k) = 0 on the canonical carrier. Route 2: the
quadratic carrier reading gives the causal cone as the determinant
degeneration of Herm_2 (the Born-positivity boundary). The photon is
understood when both routes exist and yield THE SAME cone.
Falsifier: two derived cones that differ is an F for the
identification, first-class, and would be the most informative single
result the sector can produce.
```

This turns "skoro hotový foton" from a feeling into a decidable state.

## 4. What not to do

```text
1  Do not resurrect the kappa bound. The threshold does not move.
2  Do not open the roughening import (#201) before its public criterion
   is written, and preferably not before the operator route says the
   window still matters. The window was a Coulomb-phase certificate,
   not the photon's identity.
3  Do not open the time characteristic before R1 and R3. The verdict's
   order stands.
4  Do not open new candidate lanes beyond lane A and lane B while open
   decision 3 (five parked lanes) is unresolved.
5  Any new probe obeys the STOP lessons: one ambient per identity,
   every consumed object displayed in the prereg, every mutation
   determined, pre-pin blind review by the other model family.
```

## 5. Order

```text
1  Owner: rule R1 and R3; pick the identity option (K3 may wait).
2  Session, in parallel, on a word: lane A recon (one session), lane B
   formulation (small, same or next session).
3  Then the time-characteristic chain as small probes under the STOP
   discipline.
4  Then the convergence test of lane B against the quadratic carrier.
5  Anytime, cheap and adjacent: the Larmor fold
   (PROMO-C-LARMOR-TREE-GATE-1 is packaged), which narrows
   QUANT-SUBSTRATE to the Schwinger clause.
```

## 6. Endgame definition

The photon part is DONE when: the public floor carries the group and
cone rows (K3), the null characteristic is derived under a preregistered
threshold (post R1/R3), and the two cones of lane B coincide. At that
point c is a theorem of the reading, not a convention, and the massive
sector inherits a derived light cone, which is the unlock the nadhled of
2026-08-04 named as the biggest single gain available.
