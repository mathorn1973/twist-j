# FORMULATION: one cone, two routes. The closing condition of the photon program

```text
STATUS   NON-CANONICAL candidate formulation (lane B of the post-STOP
         photon plan). No claim, no status, no registry intent, no
         physics asserted. Frozen by SHA-256 on delivery so that later
         work is measured against this text, not against memory.
BASIS    Public Canon v36. Inputs: the C-PHOTON-POINT-GROUP-1 candidate
         record (carrier point group, cone), the lane A recon
         RECON-PHOTON-TRANSFER-DATA_2026-08-05 (three named missing
         objects; alphabet asymmetry F6; flux ansatz F7), the v31-line
         quadratic carrier rows [T] with their declared remaining gap
         (QDD [O]), DIRAC-STEP-THEOREMS [T] (photon determinant 1,
         null branch), FORCE-WEYL-HOLONOMY [T].
PURPOSE  turn "the photon is almost done" into a decidable state: a
         single frozen convergence test whose every outcome is
         first-class.
```

## The two routes

**Route 1, the operator route (L1 to L2 to L5).** Given the three
frozen inputs named by the lane A recon:

```text
I1  the cell-to-carrier lift Lambda (the R1 object),
I2  a transfer weight system over the machine's deposited spatial
    alphabet, or over a DECLARED completion of it (the first R3
    object),
I3  a flux assignment on the carrier (the second R3 object; in the
    fully symmetric ansatz one Z_5 number),
```

the transfer operator W on the canonical carrier is determined, and its
temporal transfer gives the characteristic

```text
K_op = { (omega, k) : det C(omega, k) = 0 },
```

the derived null cone. Preconditions: rulings R1 and R3, and the
characteristic derivation run under the post-STOP identity discipline
(one ambient per probe, every consumed object displayed).

**Route 2, the quadratic route (L4).** The quadratic carrier line
carries, at its current labels, the cone of Hermitian positivity with
determinant in Minkowski form:

```text
K_quad = boundary of the Born-positivity cone of Herm_2,
         det = t^2 - x^2 - y^2 - z^2,
```

the causal cone as the degeneration locus of the quadratic reading.
Precondition: the typed chain from kernel orbit to the quadratic slots
far enough that K_quad carries a declared label (today the gap is
exactly QDD [O]; until it closes, K_quad enters at its algebraic
label, and the test's verdict is bounded by that label).

**The identification.** The two cones live in different spaces: K_op in
the momentum space of the carrier, K_quad in the determinant space of
the quadratic slots. The test requires a TYPED identification map iota
between them, itself frozen before comparison. iota is not innocent
plumbing; it is the photon dictionary, and freezing it is part of the
test, exactly as the Z3/Z4 lesson of the sealed probe demands.

## The claim schema

```text
CONE-CONVERGENCE:  iota(K_op) = K_quad, as exact algebraic equality of
                   null sets under the frozen identification.
```

## Outcomes, all first-class

```text
AGREE    the photon identification closes. c becomes a theorem of the
         reading (the conversion of counter to commutator is carried by
         a DERIVED cone), and the massive sector inherits a derived
         light cone: every massive dispersion is a perturbation of it
         (DIRAC-STEP: the coin is an off-diagonal insertion into the
         massless shift).
DIFFER   F for the identification, first-class and maximally
         informative: the mismatch localizes into I1, I2, I3 or iota,
         each of which is frozen separately and can be indicted
         separately.
```

**The symmetry referee (lane A input, run BEFORE the comparison).**
Test K_op for invariance under the carrier point group (the 48, a
candidate-T fact):

```text
symmetric    the measure or decoder RESTORES the isotropy that the raw
             deposited alphabet breaks (recon F6); the restoration
             mechanism becomes a named lemma of route 1.
asymmetric   either the transfer data were mis-frozen, or the carrier's
             octahedral symmetry is physically broken by the dynamics.
             The second reading would be a falsifiable physical
             statement (direction dependence of propagation) and MUST
             NOT be adopted by rhetoric; it would need its own
             preregistered lane. [reading, open]
```

## Well-posedness gates

The test can run only when every input exists as a frozen object:

```text
GB1  Lambda frozen (R1).                        owner ruling
GB2  weight system frozen (R3, first object).   owner ruling or public
                                                derivation
GB3  flux assignment frozen (R3, second).       owner ruling or public
                                                derivation
GB4  characteristic derived under the identity  session work, post
     discipline.                                R1/R3
GB5  iota typed and frozen.                     part of the test
GB6  K_quad at a declared label.                bounded by QDD [O]
```

## Falsifier of this formulation

This formulation is wrong and must be repaired or discarded if: any
gate GB1 to GB6 is proved impossible to freeze (for instance an EMPTY
admissible class for I2); if the two routes are shown to be logically
dependent (a shared premise that makes agreement automatic, which
would void the test's value; the adversarial check of iota must look
for exactly this); or if a registered row supersedes either route's
cone by a stronger object. A closing condition that can never run, or
that cannot fail, is not a closing condition.

## Non-claims

No cone is computed here, no route is executed, no status moves, no
identity is opened. This document defines the finish line; crossing it
is separate, gated work.
