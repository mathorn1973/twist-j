# PREREG-C-QDD-IDEMPOTENCE-DOMINATES-FORK-1

```text
CANDIDATE: C-QDD-IDEMPOTENCE-DOMINATES-FORK-1
KIND:      incubation-lane candidate. Project side. No authority, no repo
           edit, no registry motion, no canon change, no fold. NON-CANONICAL.
SESSION:   C-QDD-IDEMPOTENCE-DOMINATES-FORK (one named session, this
           candidate only).
TARGET:    public line, mathorn1973/twist-j, as a later probe
           P-QDD-IDEMPOTENCE-DOMINATES-FORK-1 if it survives.
BASIS:     Public Canon v57 ACTIVE. Gate passed this session: clone HEAD
           d44645a239df764c630984765a9fdd458b090a31, STATE ACTIVE,
           AUTHORITY mathorn1973/twist-j main, TAG canon-v57 and
           CONTENT_COMMIT 8e8b04ab ancestors of main, canon/SHA256SUMS 5 of
           5 OK, canon/CANON.md sha256 c96a2ef5.., 295013 bytes.
SOURCES:   sealed public probes P-QDD-J-CENTRALIZER-TERMINALITY-1 (PR #462),
           P-QDD-RECORD-COMPLETE-STABILIZER-1, P-QDD-RECORD-NATURALITY-FORK-1
           (PR #478, merge d44645a2).
LAYER:     L4 apparatus and support only. No lift. O1 untouched, SAMPLING NOT
           PROVIDED. L5 and L6 untouched.
DATE:      2026-08-20
```

## Falsifiers first. Each is a first class outcome if it fires.

```text
CF1  MAIN dead: some member T = eps rho(h) Q_k of the record normalizer with
     h != e satisfies T^2 = +T or T^2 = -T exactly, at any token.
CF2  CEILING dead: some non-affine permutation of the five simplex vertices
     is found inside the image of the twenty affine maps, or the fixed-label
     certificate fails (a non-identity affine map with two or more fixed
     labels, or a transposition with other than three).
CF3  INDEX dead: at any token the complete record stabilizer intersected with
     the affine group is not exactly the order four affine stabilizer, or the
     index is not six.
CF4  DOMINATION dead: in the enlarged family {rho(h) X : h in S_4, X in the
     C_4 centralizer with X^sharp X = Q_k} some member with
     (h, X) not in {(e, +Q_k), (e, -Q_k)} satisfies the effect equation and
     class level idempotence T^2 = +-T exactly.
CF5  DISAGREEMENT: the independent recomputation contradicts any sealed
     identity of the three source probes (centralizer dimension one under
     S_4 and three under C_4 at every token; 48 normalizer members and 24
     sign classes; rho(tau) Q_k involutive with T^2 = Q_k; tau commuting
     with exactly 4 of 24 stabilizer elements), or a sealed verifier fails
     to reproduce byte-identically from repository root under the frozen
     environment.
CF6  INTEGRITY: any float in any assertion, any nondeterministic output, any
     dependency outside the Python standard library, any runtime above 120
     seconds, or any comparison against the target effects before the last
     gate.
```

## Field 1. Equation (the exact claims and their grades)

```text
CA1 carrier      independent reconstruction from the axiom step
                 (a,b,c,d) -> (a-c+d, b-c, a, b-c+d): D = M_J - I, D^5 = I,
                 G = I - (1/5) one one^T, D^T G D = G, simplex u_x with
                 sum zero and u_2 = -one.                       [candidate-T]

CA2 ceiling      The dynamically realized relabeling group of the five
                 J-orbit labels is the affine group of order 20 generated by
                 D and rho(2,0). All 120 vertex permutations are
                 G-orthogonal, so S_5 is a symmetry of the carrier geometry,
                 but only 20 of them are J-motions. Certificate: every
                 non-identity affine map fixes at most one label, every
                 transposition fixes three, hence no transposition is
                 affine. In particular the fork breaker rho(tau) is not a
                 J-motion.                                      [candidate-T]

CA3 index        At every token, complete record stabilizer S_k of order 24,
                 affine stabilizer H_k of order 4,
                 S_k intersect affine = H_k, index six.         [candidate-T]

CA4 centralizers dim End_{S_k}(W_k) = 1 with basis Q_k;
                 dim End_{H_k}(W_k) = 3 with basis R_k, C_k, J_k.
                 Independent recomputation of the sealed pair. [candidate-T]

CA5 MAIN         Class level idempotence dominates the fork. Over the
                 complete rational G-orthogonal record normalizer
                 N_k = {+-rho(h) Q_k : h in S_k}, 48 members and 24 sign
                 classes at every token, the equation T^2 = +-T holds
                 exactly at T = +-Q_k and fails at the other 46 members.
                 Exhaustive, no sampling. Reason, verified elementwise:
                 T^2 = rho(h^2) Q_k, so T^2 = +-T forces
                 rho(h)|W_k = +-identity, and the standard three
                 dimensional module is faithful with no -identity in the
                 image of S_4, hence h = e.                     [candidate-T]

CA6 DOMINATION   In the enlarged family {rho(h) X} spanning both fork
                 branches, the effect equation together with class level
                 idempotence holds exactly at (h, X) = (e, +-Q_k) on the
                 frozen circle list.                            [candidate-C]

CA7 breaker      An independent second code path, label permutations and
                 {R, C, J} coordinate algebra instead of matrix products,
                 reproduces CA5 and CA6.                        [candidate-T]

CA8 reproduction the three sealed verifiers reproduce byte-identically from
                 repository root under the frozen environment.  [reproduction]

CA9 target       Comparison LAST: at k = 2, P_2 = E_low and Q_2 = E_high, so
                 the surviving class is the Lueder class in both branches.
                                                                [candidate-T]
```

Consequence to be stated, not computed: the fork of
P-QDD-RECORD-NATURALITY-FORK-1 is a fork about the symmetry premise and not
about the post state law. One group free equation cuts both branches to the
same class. O2 is therefore not a choice between strict naturality and gauge
enlargement; it is a single remaining obligation to derive or falsify class
level idempotence. This consequence carries no grade of its own and is not a
promotion.

## Field 2. Code

Two programs, Python standard library only, integers and Fraction only, no
float anywhere, deterministic output, exit nonzero on any FAIL, each under
120 seconds, run from repository root under
LC_ALL=C LANG=C PYTHONDONTWRITEBYTECODE=1 PYTHONHASHSEED=0 TZ=UTC.

```text
verify_qdd_idem_dominates_1.py   own Fraction matrix kernel, no import from
                                 any probe directory, gates CA1 to CA9 in
                                 that order, target gate last.
breaker_qdd_idem_dominates_1.py  independent path: permutations as tuples,
                                 the {R, C, J} coordinate algebra, and a
                                 search for any surviving member; declares
                                 explicitly what was attempted.
```

## Field 3. Carrier

(Q^4, G) with G = I - (1/5) one one^T and the five simplex vertices u_x built
from D powers. No external dataset. Frozen lists, fixed before execution:

```text
tokens          k = 0, 1, 2, 3, 4, all five, exhaustive.
permutations    all 120 of the five labels, exhaustive.
normalizer      all 48 members at every token, exhaustive.
circle list     rational points (r, s) with r^2 + s^2 = 1 from
                t in 0, 1, -1, 1/2, -2, 3, 1/3, -1/5, 7/2 by
                r = (1 - t^2)/(1 + t^2), s = 2t/(1 + t^2), each with
                e = +1 and e = -1.
enlarged family h ranges over all 24 elements of S_k, X over the circle list.
```

## Field 4. Systematics

```text
R1 accidental reuse of sealed code            -> own kernel, no import, the
                                                 reproduction leg is a
                                                 subprocess only.
R2 confirmation by target proximity           -> target gate is last, CA9.
R3 sampling mistaken for exhaustion           -> CA5 is exhaustive over a
                                                 finite set; CA6 is labeled
                                                 candidate-C, finite range,
                                                 and says so.
R4 group presented in the wrong action        -> group law verified on all
                                                 400 affine pairs and on the
                                                 permutation composition
                                                 table.
R5 a claim about Nature smuggled in           -> the consequence paragraph
                                                 carries no grade and no row.
R6 threshold movement after a fired falsifier -> forbidden; a fired CF is
                                                 archived and published as
                                                 the result.
```

## Field 5. Failure threshold

Exact, byte level, no tolerance, no retry. PASS only if every declared gate
passes exactly. Any FAIL fires the matching CF, is archived, and becomes the
result of this candidate. No threshold moves after execution. A fired
falsifier is reported, not repaired.

## Field 6. Action layer

L4 apparatus and support. No lift to L5 or L6. No motion on O1. No registry,
frontier, or canon edit from this project. The only permitted output on
success is PROMO-C-QDD-IDEMPOTENCE-DOMINATES-FORK-1, a promotion proposal
that a later public probe and sealed fold may consume.
