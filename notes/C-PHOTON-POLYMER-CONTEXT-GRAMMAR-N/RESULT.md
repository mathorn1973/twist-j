# RESULT

**PUBLIC, NON-CANONICAL. FAILED FIRST AUDIT.**
Owner: #1172.
Author: A. M. Thorn.
Date: 26 September 2026.

Frozen pin:

`b7ccd0f945752ce34f35b1873ce19724d1d09af2`

The exact verifier and preregistration were read back from that pin and
statically compiled before execution.

The first scientific execution produced no stdout and no stderr within the
45-second execution window. The process was then explicitly terminated. No
scientific assertion completed and no candidate-C result is earned.

This is an algorithmic execution failure, not evidence for or against the
context-resolved tree grammar. The identifier is consumed. The verifier is not
edited and rerun under this pin.

A successor, if used, must freeze a mathematically equivalent but more
efficient implementation before execution. The state definition, transition
law, dyadic grid, y-list, state cap, iteration cap and coordinate cap must not
be relaxed merely to obtain a result.

`Xi_L`, `Xi_L^(2)`, the massless phase and P1 remain open.
