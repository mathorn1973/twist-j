# C-TT-SOURCE-CONSERVATIVE-EXCHANGE-N

Status: NON-CANONICAL incubation. No public scientific promotion.
Owner: ChatGPT-TT-SOURCE-20260910. Object lock: issue #929.
Authority: Public Canon v83, main 51dee9705628e0c9f7bfc7082df46af46ed314b3.
Content: 528e868abaa368403ac3c7e81ebd326a3d81b588.
Canon SHA-256: aaaa9773390b3283f6ea72df1ef47fb5d29f80db2197b2b6419e32b65757bca6.
Action layer: L1 only. No L2-L6 lift is claimed or executed.

## 1. Frozen question

Does the following explicitly selected reciprocal source-field system supply a nonzero retarded output from an initially zero trial radiation coordinate, an action-derived local work balance, and the v83 scalar/vector constraint completion?

The trial output is x. It is NOT identified with the registered TT square h=v^2. Passing this mathematical question does not close TT-SOURCE. The complete square-reader bridge, registered spin-dependent propagation, physical source reading, preparation and any SI or measure claim are separate unresolved obligations.

## 2. Source, carrier, action and equality

Use exactly the v83 graph X=(Z/5)^3, edges (z,a,k), a=1,2,3, k=1,2, Bf=f(head)-f(tail), weights (29/324,65/324), L=B^T W B, and the vertex and edge inner products with common factor 1/25. V0=5. No spatial mean or harmonic current is discarded. Pi0 subtracts the mean only for the mean-zero auxiliary constraint.

The independent rational vertex fields x_n,y_n have integer times n>=0. Fix g=1/5. This is an explicit new architecture choice, not a derived coefficient or fitted parameter. Use the same x3-planar embedding iota and precisely the ten words and two initial squares of DEF-K1-LINEAR-METRIC. For each available word w, prepare x_0=x_1=0 and y_t=iota[(delta_(r,u_t)+delta_(r,u_t+1))/2], u_t=w_(t+2)-w_t, t=0,1. No later native windows, law of occurrence or word averaging are inserted.

Freeze the free exchange action

    A0 = (1/4) sum_n [||Delta x_n||^2 + ||Delta y_n||^2
         - <x_n,L x_n> - <y_n,L y_n> - g||x_n-y_n||^2].

Its intended independent Euler equations are

    R_L x_n = g(y_n-x_n),     R_L y_n = g(x_n-y_n),
    R_L z_n = z_(n+1)-2z_n+z_(n-1)+L z_n.

The factors 1/4 and g are frozen before computation. The synchronized restriction x=y=h, not the reservoir-zero restriction, is intended to recover the original v83 free action literally. Do not relabel synchronization as removal of a reservoir.

For any field z, define e[z] and j[z] exactly as in DEF-K1-HYBRID-FRW-CELL-METRIC. Put d=x-y and define

    e_int,n+1/2 = (g/2) d_(n+1) d_n,
    e_tot = (e[x]+e[y]+e_int)/2,
    j_tot = (j[x]+j[y])/2,     E_tot=<1,e_tot>.

Products in e_int are pointwise. The interaction energy is the polarized quadratic potential of this action. Its local sign is not presumed positive. Test the complete energy, not positivity of each local term.

Use exactly the v83 time cells and homogeneous variables, and freeze

    A = A0 + sum_n[-(eta_n-1) E_tot,n
          +2<ell,L tau>-<ell,e_tot>]
          +sum_m[2<N_m,WB Delta tau+p_m>+<N_m,j_tot,m>],
    S = S_FRW[rho_m] + A/(2 lambda),   lambda=216 pi.

On the proposed representative nu=1, ell=N=0, choose tau=(L|mean-zero)^(-1)Pi0 e_tot/2 and p=-j_tot/2-WB Delta tau. The full independent variables remain present until variation. This is an exact restricted hybrid functional, not an asserted discretization of full GR or an all-order gauge completion.

Equality means literal equality of every field, word, site, time and fixed coefficient. Prefixes have the inherited endpoint/finite-support convention. Do not quotient away a failed equation.

## 3. Claims to attack and immutable failure conditions

FA: A0 does not yield both displayed reciprocal equations, or the interaction term is not the frozen action's polarized potential.
FB: Delta e_tot+B^T j_tot is nonzero on any admitted solution, or conservation requires an unexplained compensator.
FC: the stated auxiliary construction fails 2L tau=Pi0 e_tot, 2P+j_tot=0, or B^T p=0; the spatial zero mode must also be retained in the homogeneous lapse equation.
FD: the rational recurrence is not total, unique and prefix-compatible, or x_2=g y_1 is zero for an admitted nonzero word preparation.
FE: the synchronized action/source restriction fails to recover the v83 model literally.
FF: the selected joint finite system has an unstable eigenvalue (outside [0,4] for its spatial block operator), or the claimed positive total energy fails for a selected word. A stationary common zero mode is admitted and must be treated explicitly.

These are zero-tolerance exact failures. A failure is retained, not rescued by changing g, the K1 words, an amplitude or a threshold. No universal metric-signature bound is preregistered for x and no existing K1 signature bound is transferred to x.

## 4. Mandatory negative and scope controls

NC1: delete e_int from the energy and exhibit the resulting nonzero balance defect on an exact witness.
NC2: show explicitly whether y=0 is dynamically invariant in the presence of x. It must not be confused with synchronization x=y.
NC3: remove reciprocal response while retaining forcing and check that the closed-action conservation proof no longer applies.
NC4: inspect, without promotion, whether the construction supplies the registered native vector doublet and square decoder, spin-dependent propagation and physical source identification. An absent map is STOP for TT-SOURCE, not an impossibility theorem and not a successful bridge.

## 5. Audit and proof separation

Freeze verify.py in Git before execution. It will use only Python standard-library integers and Fraction. Planned audits: symbolic polynomial identities for the local edge balance, kinetic difference and interaction cancellation; exact planar recurrence for all ten words over 32 links; all 125 vertices and 750 edges on three-dimensional nonplanar rational fixtures; rational solution of the planar mean-zero auxiliary equation; synchronized restriction; first-emission witnesses; negative controls; and the exact rational spectral upper bound via sqrt(5)<9/4.

Universal statements require the written algebraic proof, not these finite fixtures. Rational spectral bounds and a symmetric block-operator proof may establish stability. Source energies are evaluated only after this preregistration is committed. No external target data or floating-point arithmetic are consumed.

One author/session and one x86_64 local lane: reproduction/audit only, not blind independent confirmation or the public two-architecture gate. No formal public probe is created. Scientific results may be recorded only as candidate-T, candidate-D or candidate-C under NON-CANONICAL. Package PROOF.md, RESULT.md and PROMO.md for public review. A later public fold, not this incubation, owns any Canon status change.
