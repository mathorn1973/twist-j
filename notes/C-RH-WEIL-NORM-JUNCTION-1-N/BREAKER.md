# BREAKER C-RH-WEIL-NORM-JUNCTION-1-N

```text
STATUS:     NON-CANONICAL independent breaker design
ISSUE LOCK: #374
FROZEN:     before any verifier or RESULT
```

## B1 Fourier-sign control

Starting only from `rho=1/2-i z`, rederive what Suzuki's `bar z` pairing means in the usual zero coordinates. Reject any swap between `1-rho`, `1-conjugate(rho)`, and `conjugate(rho)`.

## B2 holomorphic-scope control

If a universal single-Weil-test no-go is claimed, prove only an open-domain identity obstruction. Do not infer impossibility of accidental equality on the discrete zeta zero set.

## B3 cross-Gram expansion

Expand `G_T(x,y)` directly from its L2 definition. Reindex the second zero only after the conjugation map is explicit. Check that the `x=y` specialization returns the published `mathcal F` exactly.

## B4 identifiability

Try to construct two distinct finite zero multisets with the same proposed reduced bivariate data. If the complete analytic `G_T` uniquely determines the full multiset only by generic exponential-sum uniqueness, label the outcome reconstruction, not compression.

## B5 prime-side polarization

The source paper supplies the diagonal quadratic quantity. A cross term obtained merely by algebraically polarizing an asymptotic formula is not automatically an asymptotic theorem with uniform errors. Require either polarization of the underlying exact source expression or a fresh error proof.

## B6 Cauchy weight

Reject any `1/rho` factor produced from an integral outside its convergence range, from `theta=-1` in a theorem requiring positive short intervals, or by a regularization chosen after seeing the desired denominator.

## B7 anti-diagonal selection

A functional-reflection projector must be a fixed operation on the source family. Reconstructing every `(beta,gamma)` and then selecting `beta+beta'=1` is `RECONSTRUCTION` by preregistration.

## B8 factor and counting

Every full-zero versus upper-zero factor two must be explicit. The target is `S2-lambda_1` in the upper convention from #373.
