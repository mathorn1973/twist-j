# RESULT

**PUBLIC, NON-CANONICAL.**
Owner: #1170.

- candidate-C: exact five-type uncle-memory context census.
- negative: the frozen family `q_u=q r^u` has no certificate.
- not negative: the unrestricted five-vector supersolution problem.
- exact structural finding: `B0=B1`, so a forced strict decay
  `q1<q0` is not naturally compatible with the worst-case grammar.
- OPEN: tree activity under a general vector certificate, cyclic surfaces,
  full current covariance, `Xi_L`, `Xi_L^(2)`, massless phase and P1.
