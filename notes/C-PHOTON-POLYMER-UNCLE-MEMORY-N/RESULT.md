# RESULT

**PUBLIC, NON-CANONICAL.**
Owner: #1170.

- candidate-C: exact five-type uncle-memory context census.
- negative: the frozen family `q_u=q r^u` has no certificate.
- not negative: the unrestricted five-vector supersolution problem.
- exact structural finding: `B0=B1`, so a forced strict decay
  `q1<q0` is not naturally compatible with the worst-case grammar.
- OPEN: tree activity under a general vector certificate, cyclic surfaces,
  full current covariance, `Xi_L`, `Xi_L^(2)`, massless phase and P1.


## Post-audit candidate-T strengthening

The exact equality `B0=B1` implies that the complete coefficientwise-worst
five-type map has no finite nonnegative componentwise supersolution at
`x=1/16`, not merely that the preregistered `q r^u` family failed.

For any proposed supersolution put `t=min(q0,q1)`. The common value
`F0=F1=S` obeys
`S>=(1/16)(1+15t+74t^2)>t`, while supersolution requires `S<=t`.
Contradiction.

Scope: this closes the **uncle-count coefficientwise-max majorant**. It does
not close the full context-resolved grammar or the actual embedded tree sum.
