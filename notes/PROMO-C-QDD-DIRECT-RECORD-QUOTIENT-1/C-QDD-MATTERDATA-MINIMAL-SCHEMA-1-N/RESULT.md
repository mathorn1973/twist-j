# Result

Status: `candidate-T / NON-CANONICAL`.

Exactly 12 of the 32 top-level MatterData field subsets preserve the full direct-record equality. The criterion is exact: density is required, together with either total weight or the raw ordered branch-weight pair. The only inclusion-minimal complete schemas are `{M,R}` and `{B,R}`.

The five-field schema is therefore redundant as an equality carrier, but this result does not authorize schema deletion or move decoder ownership.

This is one x86_64 local lane and is not public evidence or independent confirmation.
