# PROMO-C-QDD-DIRECT-RECORD-QUOTIENT-1 publication wrapper

Status: **NON-CANONICAL NOTE PACKAGE. NO PUBLIC SCIENTIFIC AUTHORITY.**

```text
owner lane:          issue #107
publication session: chatgpt-gpt56pro-2026-08-23-qdd-direct-record-promo
branch:              notes/PROMO-C-QDD-DIRECT-RECORD-QUOTIENT-1
path:                notes/PROMO-C-QDD-DIRECT-RECORD-QUOTIENT-1/
current public main: b938dbbd5499108f8b18122d2c5f2e2065513358
Public Canon:        v61
content commit:      76b405033b41397cd62217bf3998ac9c26111964
Canon SHA-256:       e9ee0781e489e1c3951b978be567a19c5c7370708095631f966561efe03b6cb5
Canon bytes:         334100
```

The inner incubation package was frozen on the same v61 content basis when
public `main` was `bbfaec744ab635b75a195be46b6799f9eaf07dbd`. That historical
basis is intentionally preserved in the preregistrations and promotion memo.
It is an ancestor of the current publication base. The intervening public
changes are the separate affine-character probe and odd-motor hardening probe;
neither alters the QDD Route A definitions consumed here.

## Byte preservation

The source archive was

```text
PROMO-C-QDD-DIRECT-RECORD-QUOTIENT-1.zip
SHA-256 423f2624224f3678ff38a9b86ce67c7641e35108e79c29516437b5d548fbfd7c
bytes   32121
```

Every inner file is published byte unchanged. From this directory,

```text
sha256sum -c QDD_DIRECT_RECORD_PROMO_SHA256SUMS
```

must pass. The outer `README.md` and the manifest itself are publication
metadata and are not listed inside that historical manifest.

A local packaging audit on one x86_64 lane reproduced every committed
`VERIFY.out`, every `BREAK.out`, and `BREAK_METRIC_ONLY.out` byte for byte with
empty stderr. This is reproduction only. It is not a public formal run, a
two-architecture gate, or independent confirmation.

## Contents

The package contains three preregistered incubation candidates:

```text
C-QDD-DIRECT-QUADRATIC-LINEARIZATION-1-N
C-QDD-DIRECT-RECORD-QUOTIENT-1-N
C-QDD-MATTERDATA-MINIMAL-SCHEMA-1-N
```

The combined proposed result is stated in
`PROMO-C-QDD-DIRECT-RECORD-QUOTIENT-1.md`.

At candidate scope only, the package argues that:

1. the frozen direct cyclotomic raw write has a unique rational-linear
   extension in `A=vv^T` and recovers the registered Gram and ordered effect
   pair without using the Born factor map as a construction premise;
2. complete direct-record equality is exactly `Q_QDD` equality, giving the
   canonical 313-class quotient and a unique minimal reduced exact carrier;
3. among the five frozen top-level `MatterData_QDD` fields, complete record
   equality is preserved exactly by subsets containing `density_state` and at
   least one of `total_weight` or `branch_weights`.

All three candidates retain their breakers and nonselection boundaries.

## Ownership and STOP boundary

Issue #107 owns the future `P-DMATTER-TOTAL-1` lane and remains
`QUADRATIC-DECODER-DATA [O] / STOP`. This note package does not create or
impersonate that probe. It fills no completion-contract slot and changes no
Canon, Registry, Frontier, dependency, gate, status, tag, release or workflow.

No Born measure, physical effect ownership, instrument, realized event,
sampling law, decoder completion, or L2-L6 lift is claimed. A formal public
child probe may begin only after an explicit owner ruling under #107 and a new
collision check against the then-current public basis.
