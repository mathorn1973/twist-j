# Result

Status: `candidate-T / NON-CANONICAL`.

The preregistered targets T1-T6 pass exactly. The direct cyclotomic raw write has a unique rational-linear extension from `Sym_4(Q)`, determined by ten independent piston heads. The extension recovers `G`, the ordered projector pair, the raw weights and the density numerator without taking the Born factor map as a premise.

Expected scope breakers also fired:

- uniqueness fails when rational linearity is removed;
- density alone loses scale;
- the metric alone does not select the LOW line.

This is one x86_64 local lane and is not public evidence or independent confirmation.
