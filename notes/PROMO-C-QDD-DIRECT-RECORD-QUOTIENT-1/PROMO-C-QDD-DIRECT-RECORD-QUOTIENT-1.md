# PROMO-C-QDD-DIRECT-RECORD-QUOTIENT-1

Status: NON-CANONICAL promotion package. No public authority.
Basis: Public Canon v61, public `main` read back at `bbfaec744ab635b75a195be46b6799f9eaf07dbd`.
Content commit: `76b405033b41397cd62217bf3998ac9c26111964`.
Canon: `e9ee0781e489e1c3951b978be567a19c5c7370708095631f966561efe03b6cb5`, 334100 bytes.
Local execution: one x86_64 lane only. It is reproduction/audit, not a two-architecture gate and not independent confirmation.

## Scientific result

Three preregistered incubations close one coherent L1 statement.

### A. Direct quadratic linearization

The frozen direct cyclotomic raw write is rational-linear in `A=vv^T`. Since the ten matrices

```text
q(e_i), q(e_i+e_j)
```

span `Sym_4(Q)`, there is one and only one rational-linear extension. It is

```text
m(A)      = Tr(A G),
w_low(A)  = Tr(E_low A G),
w_high(A) = Tr(E_high A G),
N(A)      = A G,

G      = I-(1/5)11^T,
E_low  = (1/4)11^T,
E_high = I-E_low.
```

The metric and ordered effect pair are recovered from the direct raw fields. The factor-side formulas are therefore forced by the frozen direct dictionary plus rational linearity. The Born factor map is not a construction premise.

### B. Canonical direct-record quotient

For the complete tagged direct record `D` and public quadratic map `q`,

```text
D(x)=D(y) iff q(x)=q(y).
```

The common quotient has 313 classes, one of size 25 and 312 of size 50. `QCarrier_QDD` is terminal among reduced exact factor carriers of the fixed direct write. Every such carrier maps uniquely onto it and has at least 313 elements. Equality occurs exactly for a uniquely isomorphic minimal carrier.

### C. Minimal equality schema

Among the five top-level `MatterData_QDD` fields, exactly 12 of 32 subsets preserve full direct-record equality. The exact criterion is

```text
contains density_state
and contains at least one of
    total_weight
    branch_weights.
```

The only inclusion-minimal complete subsets are

```text
{total_weight, density_state},
{branch_weights, density_state}.
```

This is an equality classification only. It does not authorize deletion of fields or change field ownership.

## Expected scientific status

`candidate-T` by complete exact derivations. The local verifiers audit all 625 pistons or all 15625 pointed heads as applicable. Public status remains unearned until the repository protocol is followed.

## Negative controls retained

1. Remove rational linearity: uniqueness fails. The cubic `A_00(A_00-1)(A_00-4)` vanishes on the finite quadratic image but not on all `Sym_4(Q)`.
2. Keep only density: scale is lost, witnessed by `e_0` and `2e_0`.
3. Keep total metric but omit the direct LOW subrecord: the projector pair is nonselected; the rational line `Q e_0` gives another `G`-self-adjoint rank-one projector.
4. Omit density: `(1,0,0,1)` and `(1,1,0,0)` have the same support, total weight, branch weights and normalized weights but different quadratic records.

## Public collision and sequencing

Open issue #107 owns the future `P-DMATTER-TOTAL-1` lane and remains `QUADRATIC-DECODER-DATA [O] / STOP`. This package must not create or impersonate that probe. It is a proposed theorem-grade child input to that owner lane.

Recommended public sequence:

1. Owner review under #107 decides whether these results are accepted as a scoped child theorem package.
2. Only after that ruling, claim a noncolliding probe such as `P-QDD-DIRECT-RECORD-QUOTIENT-1`.
3. Freeze one public preregistration with the three exact theorem blocks and all negative controls.
4. Use a fresh standard-library verifier and the required x86_64/aarch64 byte-identity gate.
5. Fold no physical Born, measure, apparatus, event, decoder-completion or higher-layer claim.

## Proposed public rows

### QDD-DIRECT-RECORD-QUOTIENT-CANONICAL [candidate-T]

At exact L1 scope on the frozen finite Route A domain, the complete direct-record equality is exactly the `Q_QDD` equality; the common quotient has 313 classes with fibres 25 and 50; `QCarrier_QDD` is terminal among reduced exact factor carriers and is the unique minimal carrier up to unique factor isomorphism. The direct raw map has the unique rational-linear extension displayed above. No direct dictionary, physical effect, Born interpretation, apparatus, event, decoder ownership or layer lift is derived.

### QDD-MATTERDATA-EQUALITY-MINIMALITY [candidate-T]

At exact L1 scope for the five frozen direct `MatterData_QDD` fields, a projected field subset preserves complete record equality iff it contains `density_state` and at least one of `total_weight` or `branch_weights`; exactly 12 subsets qualify and the two displayed pairs are the only inclusion-minimal ones. No schema deletion or physical interpretation is authorized.

## Relation to existing public rows

- `QDD-ALGEBRAIC-FACTORIZATION [T]` supplies the exact direct/factor equality and finite fibres. The proposed result adds the rational-linear forcing theorem and the universal quotient property.
- `QDD-PROJECTOR-PAIR-TR4 [T]` supplies projector uniqueness inside the frozen `G`-self-adjoint kernel class. The proposed linearization derives the same pair from the frozen direct raw fields.
- `QDD-TR4-OCCURRENCE-WEIGHT-WEDGE-BLIND [T]` already supplies the decisive density reconstruction and no-density witness. The proposed schema theorem classifies all 32 top-level field subsets.
- `QUADRATIC-DECODER-DATA [O]` remains open. Physical ownership, the Born-square dictionary, completion-contract conformance and every cross-layer gate remain untouched.
