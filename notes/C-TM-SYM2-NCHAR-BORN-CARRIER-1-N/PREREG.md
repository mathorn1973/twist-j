# PREREG. C-TM-SYM2-NCHAR-BORN-CARRIER-1-N

```text
STATUS:      NON-CANONICAL INCUBATION
ISSUE LOCK:  #377
PUBLIC BASIS: Public Canon v46
LAYER:       L5 source -> L6 carrier candidate
```

Freeze before computation.

## Carrier

For each free complement orbit `O={w,Nw}` in the public six-word carrier:

```text
H_O=Q^O,
S_N(a)(w)=a(Nw),
H_O^-=ker(S_N+I).
```

The candidate physical sheet is the projective line `P(H_O^-)`, not the
instantaneous restricted vector `omega|_O`.

## Gates

1. Prove `H_O^-` one-dimensional and projectively independent of sheet order.
2. Match its N-character to the #375 source character without changing the
   fact that `omega=0` on the palindrome orbit.
3. Define the standard rational Gram and coordinate effects on `H_O` and prove
   every nonzero representative of `P(H_O^-)` yields one Born law independent
   of scale, sign and sheet orientation.
4. Prove this carrier and law exist on all three N-orbits.
5. Compose only after 1-4 with the quotient stationary law derived from the
   public W3 transfer; derive the six-word and six-line measure and all-chart
   coherence without a target value.
6. Decide the current `MEASURE-BORN-VERB [D]` boundary literally. A generic
   quadratic reading is insufficient if the public dependency requires the
   specific J/verb Born amplitude.

## Outcomes

```text
INTRINSIC-BRIDGE      mathematics closes and public Born dependency accepts it
MATHEMATICAL-CARRIER  mathematics closes but public Born dependency is narrower
F                     exact carrier/Born/coherence failure
STOP                  unresolved typing or authority
```

## Falsifiers

- sheet-order dependence;
- scale/sign dependent Born probabilities;
- failure on the palindrome orbit;
- replacing the zero instantaneous source by a nonzero state without keeping
  carrier/state types distinct;
- use of `1/2`, `1/3`, `1/6` as construction inputs;
- selector choice or gauge enlargement;
- claiming compatibility with `MEASURE-BORN-VERB` by the word "Born" alone.

No Canon, Registry, frontier, decoder, QCarrier, D_matter, physical SI, or
public status change is authorized.
