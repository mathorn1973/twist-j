# PROMO C-RH-PYTHAGORAS-HALFANGLE-2-N

```text
STATUS: NON-CANONICAL PROMOTION PACKAGE
AUTHORITY: none
ISSUE: #355
PROMOTION: not executed
```

## What survived

A narrow exact result survives incubation and is separable from the speculative `zeta_8` bridge.

### Candidate public theorem A: signed Pythagorean factorization

For Suzuki's exact prime-side function `Psi(t)` from arXiv:2206.03682v4 equation (1.1), define the amplitudes in `RESULT.md`. Then for every `t>=0`,

```text
Psi(t) = ||U_t||^2 - ||V_t||^2
```

in one fixed explicit Krein carrier `H_plus (+) H_minus`, with

```text
prime weights at amplitude exponent 1/4,
4(e^(t/2)+e^(-t/2)-2) = [4 sinh(t/4)]^2,
Gamma/Lerch correction = positive L2 square-sum - one L2 square.
```

The identity uses no zeta zeros and no RH assumption. The RH statement enters only by importing Suzuki Theorem 1.7 after the factorization.

Potential public status if independently reviewed: `T` for the exact factorization at the declared source scope; no RH status movement.

### Candidate public theorem B: balanced conjugate polarization

For a unit phase `omega=c+is`, the conjugate quadratic norm-difference pair reconstructs both real and imaginary parts of a complex cross term whenever `cs != 0`; hence reconstruction alone does not select an eighth root. If equal sensitivity of the two quadratures is separately required, `|c|=|s|` and therefore

```text
omega^2 = +/- i.
```

Potential public status if independently reviewed: `T` as a pure complex-Hilbert polarization lemma. No claim that the zeta problem supplies the balancing premise.

## Falsifications to carry

1. Independent delayed prime-power screw legs are indefinite: exact `2 x 2` determinant `-L^2/4`.
2. Bilinear reconstruction alone does not uniquely select `zeta_8`; exact rational phase witness `(3+4i)/5`.
3. The predecessor `C-RH-PYTHAGORAS-HALFANGLE-N` is STOP because its frozen source equation was incomplete.

## What does not promote

No RH proof, RH evidence, Weil positivity, fixed contraction, stationary-increment screw-line construction, J-native carrier, Born/decoder relation, physical interpretation, or role for `zeta_8` in zeta is established.

## Preferred next attack

Do **not** open a public theorem probe merely to repackage the scalar identity unless independent review finds it useful enough for Canon. The scientifically valuable next target is a new construction problem:

```text
Find an independently defined single contraction / partial isometry / block Gram completion
on a frozen amplitude carrier that transports the positive-side curve U_t to the negative-side
curve V_t, or yields Suzuki's stationary-increment screw kernel, without using RH, zero data,
or positivity of the Weil form.
```

This must be a fresh scoped object. A pointwise `T_t` defined from `U_t,V_t` is circular and is not a candidate.

## Repository action requested by this package

None automatically. Promotion, if desired, occurs only through a separate public claim lock and fold after collision review and independent mathematical audit.
