# PROMO-C-TM-SYM2-HADAMARD-CHARACTER-1-N

```text
STATUS: NON-CANONICAL promotion package only
PUBLIC PROMOTION REQUESTED NOW: no
ISSUE: #372
```

## Candidate mathematics package

A later review may consider the following bounded L5 corollaries if they are useful enough to earn public surface area.

1. **Quotient character theorem.** From the registered `W`, `chi_Q`, `chi_F`, and `G`, one has `W/G ~= C2 x C2`.
2. **Walsh character decomposition.** The frozen `H4` is the exact quotient character table for `1, chi_Q, chi_F, epsilon_read` and yields four rational orthogonal projectors.
3. **Representative-free orientation projector.** `P_epsilon=(1/4)v_epsilon v_epsilon^T` is invariant under all within-`G`-orbit selector permutations, relative to the frozen characters.
4. **Current-output orientation no-go.** Any linear construction using only selector-independent common outputs remains in the trivial H4 component and cannot generate a nonzero `epsilon_read` component.

## Scientific decision

Do not claim a physical-measure advance. The public open row already required `epsilon_read` as retained L5 data. H4 makes that requirement algebraically transparent but supplies none of the missing bridge objects.

The result should be summarized, if cited at all, as:

```text
DIAGNOSTIC: the missing information is an orientation-carrying successor L5 source,
not a missing change of basis.
```

## Successor boundary

A meaningful successor must begin with a separately frozen L5 source carrying nonzero `epsilon_read` content and must then prove coherence across all 48 selectors without choosing a representative. Only after that may an L5-to-L6 Born/measure gate be opened.

H4 may be used as the exact diagnostic/projector inside such a future probe, but it is not the source, the Born carrier, the normalization, or the measure.

## No movement

No Canon, Registry, frontier, `TM-SYM2-PHYSICAL-MEASURE`, Born, decoder, physical-probability, or L6 status movement follows from this package.
