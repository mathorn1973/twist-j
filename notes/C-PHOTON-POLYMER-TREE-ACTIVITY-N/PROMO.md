# PROMO-C-PHOTON-POLYMER-TREE-ACTIVITY-N

**NON-CANONICAL review package. No Canon promotion here.**
Owner: #1168.
Basis: Public Canon v92.

## Review candidate

The exact one-generation local majorant for all face-simple neutral cube-tree
boundaries is supercritical at the physical face weight.

The local planted child polynomial is

`
P5(u)=1+15u+74u^2+154u^3+143u^4+49u^5.
`

At cube activity `x=1/16`,

`
P5(u)-16u>0
`

for all `u>=0`. Therefore the recursive upper description series
`T=xP5(T)` cannot have a finite value at `x=1/16`.

This closes only the **sibling-only local-majorant route**. The actual embedded
tree sum may still converge because cousin and longer-range collisions were
discarded.

## Evidence

- `PREREG.md`: frozen scope and grid
- `verify.py`: exact cubical incidence and independent-set enumeration
- `RUN.md`: first run and hashes
- `README.md`: written proof and scope
- `RESULT.md`: scientific disposition
- pin: `ea6d059ed3f7bf64b9cb05e1606583235df69856`

## Next target

Do not enlarge the same local branching estimate. The algebra proves that no
choice of scalar fixed-point certificate can save it.

The next admissible absolute route is a **two-generation geometric grammar**:
retain enough state to forbid plaquette collisions between children of
adjacent parents before recursively forgetting longer-range collisions.

A positive result would need a finite state transfer matrix whose weighted
spectral radius is strictly below one at cube activity `1/16`. A negative
result would show that even parent-child memory is insufficient, pushing the
program toward signed axial cancellation rather than absolute polymer
summation.

No `Xi`, massless-phase or P1 claim is proposed.
