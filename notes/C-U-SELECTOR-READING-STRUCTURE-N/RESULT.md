# Result: explicit native quotient and selector amplitude boundary

**NON-CANONICAL. L1. Candidate-T for the exact proofs; candidate-C for
the independent finite local audits. No public Canon status is changed.**

The exposed thirteen-class conjecture has an explicit solution. PROOF.md
derives the piecewise-affine map kappa and proves that its sign classes
are exactly the full equality graph's weak components. Connectivity is
proved by native transient paths and the two independent translations
they generate, not by assuming the reported component count.

All frozen validation targets survived the direct audit. An independently
written breadth-first graph traversal reproduced exactly the same vertex
partition and checked the path construction. Both source hashes were frozen
and publicly read back before either run. Each run exited zero with empty
stderr on the same x86_64 architecture. No fired scientific falsifier or
runtime failure occurred. This is not a formal two-architecture probe gate.

## Exact positive content

```text
Q = F5^2/{+/-1}, via the explicit sheet-dependent kappa
weak components = 13
sizes = one 625, twelve 1250
every component meets all five trace sheets
faithful symmetric record = kappa kappa^T
record values = 13; nonzero projective directions = 6, two values each
```

The universal property classifies all total fixed checkpoint readings
preserved by both selector bits. Applied to R(n,x)=L^n F(x), it identifies
the admissible remainder as F=G o Q. The function G remains arbitrary.

## Exact restricted negative content

For all origin-zero heads, z_3=i_3=1. Hence any exact reading from only
the current counter and current trace/index into an invertible target
orbit L^n v(x0) forces v to be constant. Arbitrary nonlinear readers
and an unbounded counter do not remove this restriction.

Complete index/trace history remembers exactly the initial trace. Since
each Q fibre meets every initial trace sheet, a source amplitude which
factors both through Q and through this complete history is constant.

The selector's branch choice C^(2t)e_z differs from the group-algebra
sum (I+C^2)e_z. Their static augmentation-lattice correspondence with J
does not identify the actual native transition with the Hodge operator.

## What remains open

No characteristic-zero Hodge amplitude map is selected. The full checkpoint
contains the invariant source label Q and, with the counter on the
synchronized domain, the previously registered five-coordinate orbit chart.
This is more information than the current selector port, but neither its
availability nor 13>=6 chooses source amplitudes or the target dynamics.

The thirteen classes are invariant subsets for this total fixed reader.
They are not physical superselection sectors and are not the 313 terminal
components or the set of complete chronological trajectories. The six
finite projective directions are not identified with Hodge axes by count.

## Custody

Contract commit: c083513a8f00ea1f66a72c54e7e1c8e24bb3342e.
Source freeze: b13fb785c43143115087b01de9c9495b5b264b76.
Execution records: RUN.md and BREAKER.md.
Builder stdout SHA256:
68f9e15af77382350e52440a70ba242d96e2451928c054b6471a16f0af1660ae.
Breaker stdout SHA256:
6e83664aa0536bf21c0d373b9780e347b03e00199702a5d0590c836683351b5b.

The user-supplied numerical result and F5^2/sign suggestion were exposed.
No blind-discovery claim is made. The frozen #1069 probe and all Canon
files are unchanged. PROMO-C-U-SELECTOR-READING-STRUCTURE-N.md proposes
the narrow later promotion scope and preserves these qualifications.
