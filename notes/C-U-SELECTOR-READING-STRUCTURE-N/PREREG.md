# C-U-SELECTOR-READING-STRUCTURE-N: frozen validation contract

Status: NON-CANONICAL incubation. L1 only. No authority or Canon promotion.
Owner: A. M. Thorn / selector reading structure session 2026-09-19.
Public lock: #1074. Base: 911481e735b57cbf66cbf3ba47e2e21adcfc6d94,
Public Canon v89. Branch: notes/c-u-selector-reading-structure-n.

## Exposure and scope

The author supplied an unpinned independent component count 13, sizes
625 and twelve times 1250, and all five trace values in every component.
The author also proposed F_5^2 modulo sign. These conclusions were read
before this contract. This is not blind discovery or a formal probe run.
The author's code, full output and full hash are not available here.
The closed formula below was derived symbolically before any computation
in this session. This contract precedes its first computational validation.

Probe P-U-J-HODGE-SEPARABLE-COUNTER-READ-1, lock #1069, retains its owner
and pin 51672be0f697ce3d977fde20e93572382d9cf7ab. Its verifier is neither
read nor executed for this work. Its census/classification is not taken
over. This separate candidate explains the quotient structurally and
tests the selector-only route to state-dependent target amplitudes.

## Equations and carrier

All checkpoint arithmetic is modulo five. X=F_5^6 with
x=(p1,p4,p1p,p4p,q,r), z=sum(x). The five declared maps are

    a(x)=(p4,p1,p4p,p1p,q,r)
    b(x)=(-p1p,-p4p,-p1,-p4,-q,-r)
    c(x)=(-p1p+2,-p4p+1+r,-p1+2,-p4+1-r,1-q,-r)
    d(x)=(2-p1,1-p4,3-p1p,4-p4p,1-q,1-r)
    e(x)=(2-p1,1-p4,3-p1p,4-p4p,2-q,1-r).

Put f_t(x)=g_(z(x)+2t)(x), t=0,1, (g_0,...,g_4)=(a,b,c,d,e).
The equivalence relation is generated by undirected pairs {x,f_t(x)}.
It is not the graph of all five unrestricted generators, a single
chronological orbit, or a physical superselection relation.

Set S=p1+p4+p1p+p4p, T=p1+p4-p1p-p4p-3 and

    kappa(x)=(S-1,-T-2r)   if z=0,
             (S,T)         if z=1 or 2,
             (S,-T)        if z=3 or 4.
    Q(x)={kappa(x),-kappa(x)}.

## Frozen exact targets

G1. Q(f_t(x))=Q(x) for every x and both t. Each oriented kappa value
has 125 preimages on each trace sheet, hence 625 in X. The Q fibres
have sizes 625 (zero) and 1250 (twelve nonzero sign classes), and meet
every trace sheet with 125 or 250 states.

G2. These fibres are exactly the connected components of the equality
graph. The proof must explain connectivity, not infer it from a count.
Use the registered terminal invariant on X14:

    V=(alpha,beta,gamma,delta)
     =(p1+p1p,p4+p4p,chi(z)(p1-p1p-2),chi(z)(p4-p4p-1)),
    chi(1)=1, chi(4)=-1, with equality V~ -V.

Its 313 connected components are registered in
U-NATIVE-INVARIANT-AND-NOWRITE. Transient undirected paths induce

    H(V)=(beta,alpha,delta-1,gamma+1),
    G_k(V)=(beta+3,alpha-3,delta+k,gamma-k), k in F_5.

In particular G_k H translates by (3,-3,k+1,-k-1), generating the
kernel of (alpha,beta,gamma,delta)->(alpha+beta,gamma+delta).
Every transient state must have a path into X14. Audit these path
formulas and their available fibre coordinates, or reject the proof.
A separately written graph traversal may independently test completeness.

G3. The map [(u,v)] -> (u^2,uv,v^2) is injective on F_5^2/{+/-1}.
It supplies 13 symmetric rank-one-or-zero matrices, whose 12 nonzero
members project to six directions, with two members per direction.
This is finite algebra only. It does not identify these coordinates
with the Hodge output or supply a characteristic-zero amplitude lift.

G4. Trace laws of (a,b,c,d,e) are (z,-z,2-z,2-z,3-z). Consequently
the trace maps T_0,T_1 have tables (0,4,0,4,4) and (2,1,1,3,1).
Since theta_0,theta_1,theta_2=(0,1,1), all origin-zero heads have
z_3=1. The registered all-n law is

    z_n=4-3 theta_(n-1), n>=3,
    i_n=z_n+2 theta_n.

For any invertible target L, every exact read
R(n,i_n(x0))=L^n v(x0) on all heads forces v to be constant, by n=3.
The same holds for current trace instead of index. Complete index/trace
histories depend only on z_0, hence distinguish at most five head
classes. If v is both a function of Q and recoverable from that
complete history, it is constant because every Q class meets each
initial trace sheet. The target may be a set; linearity is unnecessary.

G5. Distinguish the actual selector e_(z+2t)=C^(2t)e_z on the free
five-label module from the sum (I+C^2)e_z=e_z+e_(z+2). Their
augmentations are respectively 1 and 2 over Z and F_5. On the
augmentation lattice A4, I+C^2 is the existing algebraic J action,
and its exterior square is the existing Hodge L. This is a static
representation comparison, not a native transition law. The native
affine-word source torsor already considered in #1000 is a distinct
regular C5 candidate; the selector alphabet is not asserted unique.

G6. Thirteen labels permit arbitrary source assignments F=G o Q and
therefore reads L^n G(Q(x)) in the separable class. Nonconstant and
six-spanning assignments exist but are choices. At fixed n the output
has at most thirteen values; it is not all of an infinite rational
six-space. No preferred amplitudes, characteristic-zero lift, source
preparation or target-independent selection of L is asserted.

## Code, systematics, thresholds

The builder audit uses standard-library exact integer arithmetic over
all 15625 states, both selector bits, all five generator trace laws,
and all 25 two-coordinate values. No random sampling, floats, external
packages, tolerance, measurements or post-result threshold changes.
It checks the explicit formula, affine path formulas, fibre sizes,
quadratic injection and trace/reset identities. All-n statements are
proved symbolically; finite time prefixes do not establish them.

A separate breaker is written from this contract and canonical formulas,
without opening the builder verify.py. Freeze both source hashes before
execution/comparison. Prior numerical and conjectural exposure is disclosed;
this is implementation-independent adversarial checking, not blind discovery.
Only candidate-C follows from finite one-architecture checks. Independently
proved statements may be reported candidate-T, never registered public T.

One exact violation rejects its corresponding assertion. A code/integrity
failure is STOP for that validation attempt and is preserved. No failed
assertion is erased, no fired scientific target is repaired in place.
This is incubation and does not satisfy a formal two-architecture probe gate.

## Boundaries and deliverable

Deliver a proof, exact audit, independent breaker, execution records and
PROMO-C-U-SELECTOR-READING-STRUCTURE-N.md proposing narrowly scoped public
work. Existing local RUN records on other probes remain unchanged; their
CI acceptance is separate evidence. No Canon edit, physical time,
physical superselection, decoder, occurrence, probability, SI or L2-L6 lift.
