# Promotion proposal: native quotient structure and selector boundary

**NON-CANONICAL. Proposal only. No formal probe or Canon promotion.**
Candidate: C-U-SELECTOR-READING-STRUCTURE-N. Owner lock: #1074.

## Candidate atomic statements

1. **Explicit global fixed-reader quotient, L1, candidate-T.** The two-edge
   equality graph on the complete checkpoint set has the piecewise-affine
   sign invariant Q of PREREG G1-G2 as its complete weak-component quotient.
   Its 13 components and exact fibre sizes follow from constructive
   connectivity and affine-rank arguments. Its faithful quadratic record
   has 13 values and six nonzero projective directions.
2. **Counter-selector amplitude obstruction, L1, candidate-T.** For the
   actual origin-zero native trajectories and any invertible target L, a
   reader of only the current counter and current trace or fired index
   cannot realize L^n v(x0) with nonconstant v. Complete history retains
   exactly the initial trace; a simultaneous Q factor is constant.

Both statements have exact proofs. Any public computational promotion must
use the repository's normal prospective probe pin and two-architecture
runner, with unchanged source and expected stdout. Existing note validation
is one local architecture and is not a formal probe gate.

## Nonduplication and dependencies

The census and universal separable-read classification are already reserved
under #1069, P-U-J-HODGE-SEPARABLE-COUNTER-READ-1. Its owner decides that
probe's run and disposition. Do not execute or expand its frozen verifier.
The new content here is the closed formula, completeness mechanism and
precisely typed selector-port obstruction. Coordinate any eventual fold
with that probe rather than adding two competing census claims.

Logical native inputs are DEF-ARCHITECTURE,
KERNEL-Z6-SYNCHRONIZATION and U-NATIVE-INVARIANT-AND-NOWRITE. The proofs
also provide their required trace identities and terminal connectivity
directly. The existing Hodge construction and #1000 source torsor are
comparisons; neither is required to prove the quotient or general target
obstruction. No physical or cross-layer owner is closed.

## Hodge and carry wording to retain

For a later fold of the existing semilinear-memory probe, the normalized
block's invariant wording is trace 3, determinant 1, with diagonal 3/2
and off-diagonal product 5/4 in the conjugate Hodge basis. The individual
off-diagonal values depend on the preregistered basis normalization.
The fourth coordinate is required for F-linear prediction, not new
information absent from exact current F^3 data when conjugation is allowed.

The counter/carry identity remains a typed exponent comparison. This note
does not upgrade it to a native amplitude or physical time theorem. The
new explicit finite quotient supplies source labels, not a chosen lift
into characteristic-zero Hodge amplitudes.

## Acceptance evidence must retain its type

The historical RUN.md in a probe correctly records its local architecture.
It must not be rewritten to imply that a second local architecture ran.
A separate final acceptance record may pin the reviewed PR head, verifier
and expected-output hashes, both architecture job URLs and aggregate check.
A later Canon fold should consume that exact public evidence, not the stale
phrase 'public acceptance pending' as if it were the final disposition.

For this incubation note, passing repository PR jobs establishes repository
policy checks. The workflow does not automatically execute arbitrary note
audits on both architectures. RUN.md remains the authority for the actual
scientific executions reported here.

## Next positive obligation

Any proposed Hodge amplitude map must name a checkpoint port beyond the
current selector, specify the complete admitted initial states and equality,
derive its characteristic-zero source assignment independently of the
target, and prove the exact update. The Q quotient completely describes
the total separable class; it leaves arbitrary G:Q->target. A broader
counter-dependent reading has the registered synchronized chart available,
but no canonical G or Hodge action is selected by that availability.
