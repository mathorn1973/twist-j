# TWIST-J core

One axiom. Reality is the closed integer J-Cayley plenum, the
canonical integer Cayley system generated by

```
J = 1 + zeta_5^2
```

with no external boundary, no external clock, and only internal
readout. Zero free dimensionless parameters; one SI calibration
anchor, the electron mass. Time is a counter. Space is a commutator.
J is the verb; phi and pi are projections of J, not primitives. Two
forces are two J-projections: the modulus gives gravity and scale, the
argument gives electromagnetism and phase. Plenum, not vacuum. If it
cannot be calculated in integers, it is not physics.

The five stones:

```
1. J = 1 + zeta_5^2                       the axiom
2. L = Re Li_2(J) = pi^2 / (2p)^2         the L-value, the action
3. The Klein-100 typology                 the foam structure
4. The decoder, clock then geometry then matter
5. The plenum T = s + i phi = 2i(1 - J)   structured space itself
```

Strongest results, stated in full in the Canon: the unit facts of J
(J-UNIT, J-GOLDEN-BRIDGE, J-STEP); the exact coupling seed alpha* =
1/p on the trace direction (ALPHA-SEED); the abelian face measure
exact in Q(sqrt5) (BORN-FACE-WEIGHTS); the fine structure constant
formula matching measurement below one part per billion, labeled as a
measured comparison; gravity closed at 27 = d^3 with the Kahler
capacity 64 pi^3 phi^2; the muon and tau mass ladder on the single
anchor; g = 2 as vertex flux over spinor half angle; relativity as
counting with rapidity ln phi; the 313 attractor census, machine verified end to end;
the qubit from F_5 with the magic boundary at F_25; the color door
through the binary icosahedral group to affine E8; the FRW law
H^2 = 72 pi rho with every coefficient derived.

Statuses run in the rigid order: locked theorem, theorem, derived,
computed at finite range, hypothesis with an explicit falsifier, open
obligation, falsified. Every claim carries a label; no summary is
stronger than the label it summarizes. Falsification is first class
progress.

Where to read: canon/CANON.md is the complete current Canon;
canon/FRONTIER.md lists the live obligations and hypotheses;
canon/REGISTRY.tsv is the machine readable claim registry. To verify,
run the reproductions under reproduce/ from the repository root; each
must exit 0 with byte identical stdout.
